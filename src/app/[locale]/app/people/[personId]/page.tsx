import Link from"next/link";import{ArrowLeft,UserRound}from"lucide-react";import{notFound}from"next/navigation";import{requireUser}from"@/lib/auth/require-user";import{isLocale}from"@/lib/preferences";
export default async function PersonDetailPage({params}:{params:Promise<{locale:string;personId:string}>}){const{locale:rawLocale,personId}=await params;if(!isLocale(rawLocale))notFound();const locale=rawLocale,pt=locale==="pt-BR";const{supabase}=await requireUser(locale);const[{data:person},{data:taskLinks},{data:projectLinks},{data:entryLinks},{data:memories}]=await Promise.all([supabase.from("people").select("id,name,notes,created_at,updated_at").eq("id",personId).maybeSingle(),supabase.from("task_people").select("task_id,role").eq("person_id",personId),supabase.from("person_projects").select("project_id,role,valid_from,valid_until").eq("person_id",personId).is("valid_until",null),supabase.from("entry_entities").select("entry_id").eq("entity_type","person").eq("entity_id",personId),supabase.from("memories").select("id,content,kind,important").eq("person_id",personId).order("important",{ascending:false})]);if(!person)notFound();const taskIds=taskLinks?.map(item=>item.task_id)??[],projectIds=projectLinks?.map(item=>item.project_id)??[],entryIds=entryLinks?.map(item=>item.entry_id)??[];const[{data:tasks},{data:projects},{data:entries}]=await Promise.all([taskIds.length?supabase.from("tasks").select("id,title,status,due_at").in("id",taskIds).order("updated_at",{ascending:false}):Promise.resolve({data:[]}),projectIds.length?supabase.from("projects").select("id,name,status").in("id",projectIds):Promise.resolve({data:[]}),entryIds.length?supabase.from("entries").select("id,original_content,occurred_at,is_retroactive").in("id",entryIds).order("occurred_at",{ascending:false}):Promise.resolve({data:[]})]);return <div className="content-page entity-detail"><Link className="back-link" href={`/${locale}/app/people`}><ArrowLeft size={16}/>{pt?"Pessoas":"People"}</Link><header className="entity-hero"><UserRound size={28}/><div><p className="eyebrow">{pt?"PESSOA":"PERSON"}</p><h1>{person.name}</h1><p>{person.notes??(pt?"Contexto construído a partir das interações registradas.":"Context built from recorded interactions.")}</p></div></header><div className="entity-columns"><section><h2>{pt?"Pendências e tarefas":"Open work and tasks"}</h2>{tasks?.length?<div className="mini-list">{tasks.map(task=><article key={task.id}><strong>{task.title}</strong><span>{task.status}</span></article>)}</div>:<p className="quiet-state">{pt?"Nenhuma tarefa vinculada.":"No linked tasks."}</p>}</section><section><h2>{pt?"Projetos em comum":"Shared projects"}</h2>{projects?.length?<div className="mini-list">{projects.map(project=><Link href={`/${locale}/app/projects/${project.id}`} key={project.id}><strong>{project.name}</strong><span>{project.status}</span></Link>)}</div>:<p className="quiet-state">{pt?"Nenhum projeto vinculado.":"No linked projects."}</p>}</section></div>{(memories?.length??0)>0&&<section className="entity-memory"><h2>{pt?"Memórias":"Memories"}</h2>{(memories??[]).map(memory=><article key={memory.id}><strong>{memory.content}</strong><span>{memory.kind}</span></article>)}</section>}<section className="entity-timeline"><h2>{pt?"Linha do tempo":"Timeline"}</h2>{entries?.length?<div className="timeline-list">{entries.map(entry=><article key={entry.id}><span className="timeline-dot"/><div><Link href={`/${locale}/app/inbox/${entry.id}`}><strong>{entry.original_content}</strong></Link><small>{new Intl.DateTimeFormat(locale,{dateStyle:"long",timeStyle:"short"}).format(new Date(entry.occurred_at))}{entry.is_retroactive?` · ${pt?"adicionado depois":"added later"}`:""}</small></div></article>)}</div>:<p className="quiet-state">{pt?"A linha do tempo começa na próxima menção.":"The timeline starts with the next mention."}</p>}</section></div>}
