import Link from "next/link";import{FolderKanban}from"lucide-react";import{notFound}from"next/navigation";import{createRecord}from"@/features/operations/actions";import{InlineCreateForm}from"@/features/operations/inline-create-form";import{requireUser}from"@/lib/auth/require-user";import{isLocale}from"@/lib/preferences";
export default async function ProjectsPage({params}:{params:Promise<{locale:string}>}){const{locale:rawLocale}=await params;if(!isLocale(rawLocale))notFound();const locale=rawLocale,pt=locale==="pt-BR";const{supabase}=await requireUser(locale);const{data}=await supabase.from("projects").select("id,name,description,status,updated_at").neq("status","archived").order("updated_at",{ascending:false});return <div className="content-page"><header className="list-header"><div><p className="eyebrow">{pt?"CONTEXTO":"CONTEXT"}</p><h1>{pt?"Projetos":"Projects"}</h1><p>{pt?"Frentes ativas reconhecidas nas suas entradas ou criadas por você.":"Active workstreams recognized in your entries or created by you."}</p></div><InlineCreateForm action={createRecord} kind="project" locale={locale}/></header>{data?.length?<div className="list-stack">{data.map(project=><Link href={`/${locale}/app/projects/${project.id}`} className="list-row" key={project.id}><div className="list-row-main"><strong>{project.name}</strong><p>{project.description??(pt?"Projeto ativo":"Active project")}</p></div><div className="list-meta"><span className={`status-badge ${project.status}`}>{project.status}</span></div></Link>)}</div>:<div className="empty-list"><FolderKanban size={30}/><strong>{pt?"Nenhum projeto ainda":"No projects yet"}</strong><p>{pt?"Projetos citados nas capturas aparecem aqui automaticamente.":"Projects mentioned in captures appear here automatically."}</p></div>}</div>}
