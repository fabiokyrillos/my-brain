import Link from"next/link";import{ArrowLeft,FolderKanban}from"lucide-react";import{notFound}from"next/navigation";import{requireUser}from"@/lib/auth/require-user";import{isLocale}from"@/lib/preferences";
export default async function ProjectDetailPage({params}:{params:Promise<{locale:string;projectId:string}>}){const{locale:rawLocale,projectId}=await params;if(!isLocale(rawLocale))notFound();const locale=rawLocale,pt=locale==="pt-BR";const{supabase}=await requireUser(locale);const[{data:project},{data:taskLinks},{data:personLinks},{data:entryLinks}]=await Promise.all([supabase.from("projects").select("id,name,description,status,created_at,updated_at").eq("id",projectId).maybeSingle(),supabase.from("task_projects").select("task_id").eq("project_id",projectId),supabase.from("person_projects").select("person_id,role,valid_from,valid_until").eq("project_id",projectId).is("valid_until",null),supabase.from("entry_entities").select("entry_id").eq("entity_type","project").eq("entity_id",projectId)]);if(!project)notFound();const taskIds=taskLinks?.map(item=>item.task_id)??[],personIds=personLinks?.map(item=>item.person_id)??[],entryIds=entryLinks?.map(item=>item.entry_id)??[];const[{data:tasks},{data:people},{data:entries}]=await Promise.all([taskIds.length?supabase.from("tasks").select("id,title,status,due_at").in("id",taskIds).order("updated_at",{ascending:false}):Promise.resolve({data:[]}),personIds.length?supabase.from("people").select("id,name").in("id",personIds):Promise.resolve({data:[]}),entryIds.length?supabase.from("entries").select("id,original_content,occurred_at,is_retroactive").in("id",entryIds).order("occurred_at",{ascending:false}):Promise.resolve({data:[]})]);return <div className="content-page entity-detail"><Link className="back-link" href={`/${locale}/app/projects`}><ArrowLeft size={16}/>{pt?"Projetos":"Projects"}</Link><header className="entity-hero"><FolderKanban size={28}/><div><p className="eyebrow">{project.status.toUpperCase()}</p><h1>{project.name}</h1><p>{project.description??(pt?"Contexto construído a partir dos seus registros.":"Context built from your entries.")}</p></div></header><div className="entity-columns"><section><h2>{pt?"Tarefas":"Tasks"}</h2>{tasks?.length?<div className="mini-list">{tasks.map(task=><article key={task.id}><strong>{task.title}</strong><span>{task.status}</span></article>)}</div>:<p className="quiet-state">{pt?"Nenhuma tarefa vinculada.":"No linked tasks."}</p>}</section><section><h2>{pt?"Pessoas":"People"}</h2>{people?.length?<div className="mini-list">{people.map(person=><Link href={`/${locale}/app/people/${person.id}`} key={person.id}><strong>{person.name}</strong></Link>)}</div>:<p className="quiet-state">{pt?"Nenhuma pessoa vinculada.":"No linked people."}</p>}</section></div><section className="entity-timeline"><h2>{pt?"Linha do tempo":"Timeline"}</h2>{entries?.length?<div className="timeline-list">{entries.map(entry=><article key={entry.id}><span className="timeline-dot"/><div><Link href={`/${locale}/app/inbox/${entry.id}`}><strong>{entry.original_content}</strong></Link><small>{new Intl.DateTimeFormat(locale,{dateStyle:"long",timeStyle:"short"}).format(new Date(entry.occurred_at))}{entry.is_retroactive?` · ${pt?"adicionado depois":"added later"}`:""}</small></div></article>)}</div>:<p className="quiet-state">{pt?"A linha do tempo começa na próxima menção.":"The timeline starts with the next mention."}</p>}</section></div>}
