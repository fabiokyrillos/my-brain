# Phase 2D requirement traceability matrix

Generated from `docs/PHASE_2D_PRD.md` by `scripts/generate-phase-2d-traceability.mjs` during Slice 2D.6 closeout. Do not edit by hand — run `npm run docs:phase-2d:traceability` to regenerate.

Inventory: 58 functional/non-functional requirement IDs across 15 families, 6 per-epic acceptance criteria, and 5 global gates: 69 individually mapped rows. Evidence is referenced by durable artifact and executable gate rather than copied test output.

The generator fails closed if the PRD inventory is not exactly 58 functional IDs with the expected per-family counts, 6 epic-acceptance bullets, and 5 global gates, or if any family lacks an evidence mapping. Every Phase 2D requirement is delivered and verified by closeout: the phasing "unavailable-before-slice-N" constraints (2D-DISPOSITION-001, 2D-SUGGEST-001, 2D-ACTION-001) held at their slice boundary and their gated capability then shipped; the deterministic suggested-answer path fully satisfies 2D-SUGGEST-002 / 2D-OPERATIONS-003 (the additive AI extraction-schema field they name is an explicitly deferred, separately-authorized fallback that was not needed, never an unmet obligation); and the 2C-UNDO-004 hard gate behind 2D-ACTION-006 / 2D-UNDO-003 is resolved (migration 202607230050 forward-fixes undo_operation's own SQLSTATE 40001 to 55P03).

## Functional and non-functional requirements

| ID | Required behavior | Owning slice(s) | Delivered artifacts | Local / browser evidence | Remote / linked evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `2D-ANSWER-001` | An open, current pending question can be answered with trimmed free text (1–4000 chars) through a versioned `SECURITY DEFINER` RPC, not a plain table write. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-ANSWER-002` | A successful answer sets `status='answered'`, persists the answer and `answered_at`, and records one audit row and one undo operation in the same transaction. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-ANSWER-003` | Answering a question whose interpretation is no longer the entry's current interpretation is rejected as stale with no write. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-ANSWER-004` | Answering a question not owned by the caller, or that is not `open`, is rejected without disclosing whether another owner's question exists. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-ANSWER-005` | The command is a closed shape (question id, answer, operation key, optional consequence/suggestion provenance in later slices); unknown keys are rejected. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-ANSWER-006` | An empty, whitespace-only, or overlong answer is rejected before any mutation and identifies the answer field. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-ANSWER-007` | The resolution never edits `entry_interpretations.pending_questions` or the immutable question/reason. | 2D.1; extended by 2D.2/2D.4 | resolve_pending_question_v1–v3 (answer kind); question-resolution-contract; resolvePendingQuestion Action | question-resolution-contract and answer-pending-question Action tests; pending-question E2E | question-resolution smoke answer/stale/not-open/replay cases | complete |
| `2D-DISPOSITION-001` | Disposition controls (defer / dismiss / not relevant) are unavailable in Slice 2D.1. | 2D.2 | resolve_pending_question_v2 (deferred/dismissed/not_relevant); actionablePendingQuestionFilter; list_needs_attention snooze reactivation | question-resolution-contract disposition tests; question-visibility tests; disposition form tests | question-resolution smoke defer/dismiss/not-relevant/reactivation/undo cases | complete |
| `2D-DISPOSITION-002` | Slice 2D.2 supports exactly these non-answer resolutions: `deferred` (snoozed), `dismissed`, `not_relevant`; each is owner-scoped, atomic, audited, and undoable. | 2D.2 | resolve_pending_question_v2 (deferred/dismissed/not_relevant); actionablePendingQuestionFilter; list_needs_attention snooze reactivation | question-resolution-contract disposition tests; question-visibility tests; disposition form tests | question-resolution smoke defer/dismiss/not-relevant/reactivation/undo cases | complete |
| `2D-DISPOSITION-003` | `deferred` sets `status='snoozed'` and a validated future `snoozed_until`; the question leaves actionable projections until that instant, then returns to `open`. | 2D.2 | resolve_pending_question_v2 (deferred/dismissed/not_relevant); actionablePendingQuestionFilter; list_needs_attention snooze reactivation | question-resolution-contract disposition tests; question-visibility tests; disposition form tests | question-resolution smoke defer/dismiss/not-relevant/reactivation/undo cases | complete |
| `2D-DISPOSITION-004` | `dismissed` and `not_relevant` are terminal (until undo), create no task or other domain object, and are labeled distinctly in history. | 2D.2 | resolve_pending_question_v2 (deferred/dismissed/not_relevant); actionablePendingQuestionFilter; list_needs_attention snooze reactivation | question-resolution-contract disposition tests; question-visibility tests; disposition form tests | question-resolution smoke defer/dismiss/not-relevant/reactivation/undo cases | complete |
| `2D-DISPOSITION-005` | Only `open → answered\|snoozed\|dismissed\|not_relevant` and the automatic `snoozed → open` are permitted; direct terminal-to-terminal transitions are rejected. | 2D.2 | resolve_pending_question_v2 (deferred/dismissed/not_relevant); actionablePendingQuestionFilter; list_needs_attention snooze reactivation | question-resolution-contract disposition tests; question-visibility tests; disposition form tests | question-resolution smoke defer/dismiss/not-relevant/reactivation/undo cases | complete |
| `2D-DISPOSITION-006` | A resolution stores only the narrow decision/provenance needed for lifecycle; it never duplicates question or entry content. | 2D.2 | resolve_pending_question_v2 (deferred/dismissed/not_relevant); actionablePendingQuestionFilter; list_needs_attention snooze reactivation | question-resolution-contract disposition tests; question-visibility tests; disposition form tests | question-resolution smoke defer/dismiss/not-relevant/reactivation/undo cases | complete |
| `2D-SUGGEST-001` | Suggested answers and preview are unavailable before Slice 2D.3. | 2D.3 | question-suggestions (deterministic, no AI schema); question-preview-projection (read-only source/effect DTO) | question-suggestions, question-preview-projection, and suggestion-UI tests | question-preview smoke source/effect and event-allowlist cases | complete |
| `2D-SUGGEST-002` | Suggested answer options are bounded and closed, generated deterministically by default from the question type and existing owned domain context (never free-form model output injected as instructions); an additive validated extraction-schema field is a later fallback only if deterministic suggestions prove insufficient. | 2D.3 | question-suggestions (deterministic, no AI schema); question-preview-projection (read-only source/effect DTO) | question-suggestions, question-preview-projection, and suggestion-UI tests | question-preview smoke source/effect and event-allowlist cases | complete |
| `2D-SUGGEST-003` | Accepting a suggestion populates the editable answer and records that the answer originated from a suggestion; it never auto-submits. | 2D.3 | question-suggestions (deterministic, no AI schema); question-preview-projection (read-only source/effect DTO) | question-suggestions, question-preview-projection, and suggestion-UI tests | question-preview smoke source/effect and event-allowlist cases | complete |
| `2D-SUGGEST-004` | The source view is read-only and shows the originating entry/interpretation/candidate without exposing raw database rows to the component. | 2D.3 | question-suggestions (deterministic, no AI schema); question-preview-projection (read-only source/effect DTO) | question-suggestions, question-preview-projection, and suggestion-UI tests | question-preview smoke source/effect and event-allowlist cases | complete |
| `2D-SUGGEST-005` | The predicted-effect preview is read-only, performs no write/enqueue, and accurately names the consequence that a commit would apply. | 2D.3 | question-suggestions (deterministic, no AI schema); question-preview-projection (read-only source/effect DTO) | question-suggestions, question-preview-projection, and suggestion-UI tests | question-preview smoke source/effect and event-allowlist cases | complete |
| `2D-ACTION-001` | Permitted consequences are unavailable before Slice 2D.4. | 2D.4 | resolve_pending_question_v3 closed consequence enum (none/reinterpret); reused enqueue_entry_reprocessing/worker path | question-resolution-contract consequence tests; reinterpret confirm-flow form tests | question-reinterpretation smoke consequence/idempotency/undo-compensation cases | complete |
| `2D-ACTION-002` | A resolution may carry at most one consequence from a closed database-validated enum (initially `none`, `reinterpret`). | 2D.4 | resolve_pending_question_v3 closed consequence enum (none/reinterpret); reused enqueue_entry_reprocessing/worker path | question-resolution-contract consequence tests; reinterpret confirm-flow form tests | question-reinterpretation smoke consequence/idempotency/undo-compensation cases | complete |
| `2D-ACTION-003` | `reinterpret` applies through the existing owner-scoped `enqueue_entry_reprocessing`/correction path atomically with the resolution; Phase 2D adds no new interpretation engine, queue, or worker. | 2D.4 | resolve_pending_question_v3 closed consequence enum (none/reinterpret); reused enqueue_entry_reprocessing/worker path | question-resolution-contract consequence tests; reinterpret confirm-flow form tests | question-reinterpretation smoke consequence/idempotency/undo-compensation cases | complete |
| `2D-ACTION-004` | The consequence is idempotent per operation key and never double-applies on replay or concurrency. | 2D.4 | resolve_pending_question_v3 closed consequence enum (none/reinterpret); reused enqueue_entry_reprocessing/worker path | question-resolution-contract consequence tests; reinterpret confirm-flow form tests | question-reinterpretation smoke consequence/idempotency/undo-compensation cases | complete |
| `2D-ACTION-005` | The committed result is surfaced to the user (e.g. "re-interpretation queued", "no change applied") without exposing internal job/interpretation identifiers as instructions. | 2D.4 | resolve_pending_question_v3 closed consequence enum (none/reinterpret); reused enqueue_entry_reprocessing/worker path | question-resolution-contract consequence tests; reinterpret confirm-flow form tests | question-reinterpretation smoke consequence/idempotency/undo-compensation cases | complete |
| `2D-ACTION-006` | Undo reverses the resolution and compensates the consequence; the residual `undo_operation` `40001` risk is resolved or proven safe before remote acceptance. | 2D.4 | resolve_pending_question_v3 closed consequence enum (none/reinterpret); reused enqueue_entry_reprocessing/worker path | question-resolution-contract consequence tests; reinterpret confirm-flow form tests | question-reinterpretation smoke consequence/idempotency/undo-compensation cases | complete |
| `2D-SURFACE-001` | Chat renders open pending questions as interactive elements built from untrusted data; question/answer text is never treated as a model instruction. | 2D.5 | conversational-questions region panel (Chat + queue); shared resolvePendingQuestion contract; untrusted-data rendering | conversational-questions and surfacing tests; Chat/queue convergence E2E | authenticated Chat↔queue↔/questions convergence journey (desktop + Pixel 7) | complete |
| `2D-SURFACE-002` | Answering/deferring from Chat and from the queue use the identical resolution contract and converge on the same state. | 2D.5 | conversational-questions region panel (Chat + queue); shared resolvePendingQuestion contract; untrusted-data rendering | conversational-questions and surfacing tests; Chat/queue convergence E2E | authenticated Chat↔queue↔/questions convergence journey (desktop + Pixel 7) | complete |
| `2D-SURFACE-003` | The "Precisa de você" queue and the questions page agree on which questions are actionable after any resolution. | 2D.5 | conversational-questions region panel (Chat + queue); shared resolvePendingQuestion contract; untrusted-data rendering | conversational-questions and surfacing tests; Chat/queue convergence E2E | authenticated Chat↔queue↔/questions convergence journey (desktop + Pixel 7) | complete |
| `2D-COOLDOWN-001` | Any proactive surfacing is deterministic (no LLM decides timing) and respects the user's local quiet hours, `max_followups_per_day` cap, and rolling cooldown, honoring `important_reminder_override`. | 2D.5 | decideQuestionSurfacing (quiet hours/cap/rolling cooldown/override); question-surfacing-data (notifications-ledger budget, fail-closed) | question-surfacing and question-surfacing-data tests (heartbeat-aligned discipline) | surfacing gating verified through the authenticated convergence journey | complete |
| `2D-COOLDOWN-002` | Cooldown/quiet-hours evaluation is per-user, timezone-aware, and one user's failure never blocks the batch, matching heartbeat guarantees. | 2D.5 | decideQuestionSurfacing (quiet hours/cap/rolling cooldown/override); question-surfacing-data (notifications-ledger budget, fail-closed) | question-surfacing and question-surfacing-data tests (heartbeat-aligned discipline) | surfacing gating verified through the authenticated convergence journey | complete |
| `2D-PROVENANCE-001` | Every resolution audit row identifies the question id, interpretation id, resolution kind, consequence, and effective-value fingerprint without becoming state. | 2D.1; suggestion origin in 2D.3 | append-only audit rows (question/interpretation/kind/fingerprint); server-authenticated origin: typed\|suggested | question-resolution-contract provenance/origin tests | question-resolution and question-preview smoke audit/origin assertions | complete |
| `2D-PROVENANCE-002` | A suggestion-originated answer records the suggestion provenance distinctly from a typed answer. | 2D.1; suggestion origin in 2D.3 | append-only audit rows (question/interpretation/kind/fingerprint); server-authenticated origin: typed\|suggested | question-resolution-contract provenance/origin tests | question-resolution and question-preview smoke audit/origin assertions | complete |
| `2D-PROVENANCE-003` | The immutable interpretation remains the sole provenance of the question text; resolution rows never copy it beyond the minimum needed for support. | 2D.1; suggestion origin in 2D.3 | append-only audit rows (question/interpretation/kind/fingerprint); server-authenticated origin: typed\|suggested | question-resolution-contract provenance/origin tests | question-resolution and question-preview smoke audit/origin assertions | complete |
| `2D-IDEMPOTENCY-001` | The server and database derive a canonical, owner-independent request fingerprint (reusing the Phase 2C fingerprint pattern). | 2D.1; extended by 2D.2/2D.4 | canonical request fingerprint on undo_operations; resolve-v1/v2/v3 operation-key namespaces | question-resolution-contract canonicalization tests | question-resolution/reinterpretation smoke replay and concurrency single-winner cases | complete |
| `2D-IDEMPOTENCY-002` | Replaying the same operation key + canonical payload returns the original result and undo id. | 2D.1; extended by 2D.2/2D.4 | canonical request fingerprint on undo_operations; resolve-v1/v2/v3 operation-key namespaces | question-resolution-contract canonicalization tests | question-resolution/reinterpretation smoke replay and concurrency single-winner cases | complete |
| `2D-IDEMPOTENCY-003` | Reusing the operation key with a different question, resolution, answer, or consequence fails deterministically. | 2D.1; extended by 2D.2/2D.4 | canonical request fingerprint on undo_operations; resolve-v1/v2/v3 operation-key namespaces | question-resolution-contract canonicalization tests | question-resolution/reinterpretation smoke replay and concurrency single-winner cases | complete |
| `2D-IDEMPOTENCY-004` | Two concurrent resolutions of the same question cannot both apply; exactly one wins and the other reports a deterministic conflict. | 2D.1; extended by 2D.2/2D.4 | canonical request fingerprint on undo_operations; resolve-v1/v2/v3 operation-key namespaces | question-resolution-contract canonicalization tests | question-resolution/reinterpretation smoke replay and concurrency single-winner cases | complete |
| `2D-OWNERSHIP-001` | The authenticated owner is derived from `auth.uid()`; no client-controlled owner id is accepted. | 2D.1; every database-bearing 2D slice | SECURITY DEFINER resolve RPCs with search_path=''; auth.uid() identity; closed p_resolution JSON; RLS on pending_questions | resolvePendingQuestion Action ownership tests (no client-supplied owner id) | question-resolution smoke anonymous/cross-owner denial (indistinguishable from not-found) | complete |
| `2D-OWNERSHIP-002` | Question, interpretation, entry, consequence target, audit row, and undo row all belong to the same owner. | 2D.1; every database-bearing 2D slice | SECURITY DEFINER resolve RPCs with search_path=''; auth.uid() identity; closed p_resolution JSON; RLS on pending_questions | resolvePendingQuestion Action ownership tests (no client-supplied owner id) | question-resolution smoke anonymous/cross-owner denial (indistinguishable from not-found) | complete |
| `2D-OWNERSHIP-003` | Anonymous and cross-owner calls are denied without revealing whether another owner's question or entry exists. | 2D.1; every database-bearing 2D slice | SECURITY DEFINER resolve RPCs with search_path=''; auth.uid() identity; closed p_resolution JSON; RLS on pending_questions | resolvePendingQuestion Action ownership tests (no client-supplied owner id) | question-resolution smoke anonymous/cross-owner denial (indistinguishable from not-found) | complete |
| `2D-OWNERSHIP-004` | The RPC accepts a closed JSON shape, rejects unknown keys at every level, uses a safe explicit `search_path`, least-privilege grants (execute to `authenticated` only; revoke `public`/`anon`), qualified references, and no dynamic SQL. | 2D.1; every database-bearing 2D slice | SECURITY DEFINER resolve RPCs with search_path=''; auth.uid() identity; closed p_resolution JSON; RLS on pending_questions | resolvePendingQuestion Action ownership tests (no client-supplied owner id) | question-resolution smoke anonymous/cross-owner denial (indistinguishable from not-found) | complete |
| `2D-UNDO-001` | A successful resolution creates one available undo operation in the same transaction. | 2D.1; reinterpret compensation in 2D.4 | single-transaction undo_operation restore-to-open; guarded against clobbering a newer resolution; reprocess-job compensation | resolvePendingQuestion/undoQuestionResolution Action tests | question-resolution/reinterpretation smoke undo idempotency and reprocess-compensation cases | complete |
| `2D-UNDO-002` | Undo restores the question to its exact prior state (`open`, cleared answer/snooze as applicable), records immutable audit evidence, and is idempotent. | 2D.1; reinterpret compensation in 2D.4 | single-transaction undo_operation restore-to-open; guarded against clobbering a newer resolution; reprocess-job compensation | resolvePendingQuestion/undoQuestionResolution Action tests | question-resolution/reinterpretation smoke undo idempotency and reprocess-compensation cases | complete |
| `2D-UNDO-003` | Undo of a resolution with a `reinterpret` consequence compensates the reinterpretation through the existing interpretation-undo semantics and never resurrects or edits a later interpretation revision arbitrarily. | 2D.1; reinterpret compensation in 2D.4 | single-transaction undo_operation restore-to-open; guarded against clobbering a newer resolution; reprocess-job compensation | resolvePendingQuestion/undoQuestionResolution Action tests | question-resolution/reinterpretation smoke undo idempotency and reprocess-compensation cases | complete |
| `2D-UNDO-004` | Undo never rewrites the immutable question and never silently changes an unrelated question or entry. | 2D.1; reinterpret compensation in 2D.4 | single-transaction undo_operation restore-to-open; guarded against clobbering a newer resolution; reprocess-job compensation | resolvePendingQuestion/undoQuestionResolution Action tests | question-resolution/reinterpretation smoke undo idempotency and reprocess-compensation cases | complete |
| `2D-UX-001` | Open, editing, pending, success, deferred, terminal, conflict, and retryable-failure states each have distinct visible states. | every slice; aggregated in 2D.6 | distinct open/editing/pending/success/deferred/terminal/conflict/failure states; no raw confidence/state name in primary flow | question-answer-form and suggestion-UI component tests | desktop/Pixel-7 authenticated question journey | complete |
| `2D-UX-002` | The primary answering flow never renders a raw confidence score or internal state name. | every slice; aggregated in 2D.6 | distinct open/editing/pending/success/deferred/terminal/conflict/failure states; no raw confidence/state name in primary flow | question-answer-form and suggestion-UI component tests | desktop/Pixel-7 authenticated question journey | complete |
| `2D-UX-003` | The flow is usable without horizontal scrolling at the Pixel 7 viewport. | every slice; aggregated in 2D.6 | distinct open/editing/pending/success/deferred/terminal/conflict/failure states; no raw confidence/state name in primary flow | question-answer-form and suggestion-UI component tests | desktop/Pixel-7 authenticated question journey | complete |
| `2D-I18N-001` | Every new label, hint, error, status, suggested-answer chrome, and action has reviewed PT-BR and English copy. | every slice; aggregated in 2D.6 | next-intl PT-BR/en copy for every label/hint/error/status/suggestion/action; locale + profile-timezone deferral copy | form copy and defer-time rendering tests | PT-BR/en authenticated browser matrix | complete |
| `2D-I18N-002` | Deferral times and any dates render with locale plus the persisted profile timezone. | every slice; aggregated in 2D.6 | next-intl PT-BR/en copy for every label/hint/error/status/suggestion/action; locale + profile-timezone deferral copy | form copy and defer-time rendering tests | PT-BR/en authenticated browser matrix | complete |
| `2D-A11Y-001` | Every control has a programmatic label; errors associate with fields; aggregate failure focuses an error summary. | every slice; aggregated in 2D.6 | programmatic labels, field-associated errors, live regions, focus-return, ≥44px targets, keyboard flow, region landmark | question-answer-form accessibility assertions | desktop/Pixel-7 keyboard/focus/live-region/target Playwright assertions across Chat and queue | complete |
| `2D-A11Y-002` | Pending and successful results use appropriate live regions; focus returns predictably after answer, defer, conflict, and undo. | every slice; aggregated in 2D.6 | programmatic labels, field-associated errors, live regions, focus-return, ≥44px targets, keyboard flow, region landmark | question-answer-form accessibility assertions | desktop/Pixel-7 keyboard/focus/live-region/target Playwright assertions across Chat and queue | complete |
| `2D-A11Y-003` | Pointer targets remain at least 44×44 CSS px; the full flow is keyboard-operable, including suggested-answer chips and the chat surface. | every slice; aggregated in 2D.6 | programmatic labels, field-associated errors, live regions, focus-return, ≥44px targets, keyboard flow, region landmark | question-answer-form accessibility assertions | desktop/Pixel-7 keyboard/focus/live-region/target Playwright assertions across Chat and queue | complete |
| `2D-ANALYTICS-001` | Product events contain no question text, answer text, reason, entity names, or free text. | 2D.1–2D.5; verified in 2D.6 | content-free question_answered_basic (bounded origin), question_resolved (bounded kind), question_effect_previewed, question_reinterpret_applied; needs_attention_viewed reuse on questions surface | product-analytics allowlist and fail-open Action tests | product-events remote persistence smoke (allowlist/privacy/idempotency/ownership) | complete |
| `2D-ANALYTICS-002` | New events are closed, narrow, allowlisted, and fail-open; they observe a persisted outcome or confirmed interaction only. | 2D.1–2D.5; verified in 2D.6 | content-free question_answered_basic (bounded origin), question_resolved (bounded kind), question_effect_previewed, question_reinterpret_applied; needs_attention_viewed reuse on questions surface | product-analytics allowlist and fail-open Action tests | product-events remote persistence smoke (allowlist/privacy/idempotency/ownership) | complete |
| `2D-ANALYTICS-003` | Resolution-kind granularity is permitted only where it cannot reveal content (e.g. a bounded enum of `answered\|deferred\|dismissed\|not_relevant`), decided as a privacy boundary per Slice 2D.2/2D.6; consequence application may record a boolean/count only. | 2D.1–2D.5; verified in 2D.6 | content-free question_answered_basic (bounded origin), question_resolved (bounded kind), question_effect_previewed, question_reinterpret_applied; needs_attention_viewed reuse on questions surface | product-analytics allowlist and fail-open Action tests | product-events remote persistence smoke (allowlist/privacy/idempotency/ownership) | complete |
| `2D-OPERATIONS-001` | Every schema change is append-only; generated types are refreshed from the linked schema; local/remote migration parity is proved. | every slice; closeout in 2D.6 | append-only migrations 046–051; regenerated linked types; deterministic suggestions with no AI schema/worker change | generated-type parity and full local gate | migration list parity; linked db lint; cleanup verifier; fail-fast test:remote:2d aggregate | complete |
| `2D-OPERATIONS-002` | Reinterpretation reuses the deployed worker/dispatch; any Edge Function, cron, secret, Auth, email, or provider change requires explicit separate authorization and is out of scope for Slices 2D.1–2D.2. | every slice; closeout in 2D.6 | append-only migrations 046–051; regenerated linked types; deterministic suggestions with no AI schema/worker change | generated-type parity and full local gate | migration list parity; linked db lint; cleanup verifier; fail-fast test:remote:2d aggregate | complete |
| `2D-OPERATIONS-003` | Slice 2D.3 generates suggested answers deterministically with no extraction-schema or worker change; if a later fallback introduces an AI schema field, it is additive, validated with Zod and Structured Outputs, and never breaks existing interpretation persistence. | every slice; closeout in 2D.6 | append-only migrations 046–051; regenerated linked types; deterministic suggestions with no AI schema/worker change | generated-type parity and full local gate | migration list parity; linked db lint; cleanup verifier; fail-fast test:remote:2d aggregate | complete |

## Per-epic acceptance (PRD 19.1)

| Epic | Acceptance criterion | Closeout evidence |
| --- | --- | --- |
| `Epic 2D-A` | All `2D-ANSWER`, `2D-PROVENANCE`, `2D-IDEMPOTENCY`, `2D-OWNERSHIP`, and `2D-UNDO` requirements pass locally, in database contracts, in disposable remote smoke, and in authenticated desktop/mobile journeys; the plain-`UPDATE` answer path is fully replaced by the versioned RPC with the legacy behavior preserved only until cutover. | Slice 2D.1 traceable-answer report; resolve_pending_question_v1 pgTAP; question-resolution remote smoke; desktop/mobile E2E. |
| `Epic 2D-B` | Every open question can move atomically to exactly one of `deferred\|dismissed\|not_relevant`, with deterministic snooze reactivation, terminal semantics, truthful history, queue convergence, undo to `open`, and no content duplication or content analytics. | Slice 2D.2 dispositions report; resolve_pending_question_v2 and shared actionablePendingQuestionFilter/list_needs_attention reactivation; disposition remote smoke; queue convergence. |
| `Epic 2D-C` | Suggested answers are bounded/closed and never auto-apply; source and effect previews are read-only and accurate; any extraction-schema extension is additive and validated with no regression to interpretation persistence. | Slice 2D.3 suggested-answers/preview report; deterministic question-suggestions and read-only question-preview-projection; question-preview remote smoke; non-mutating E2E. |
| `Epic 2D-D` | A resolution can apply exactly one bounded consequence (`reinterpret`) atomically and idempotently through the existing path, show a truthful result, and be fully undone — with the `undo_operation` `40001` risk resolved/proven before remote acceptance. | Slice 2D.4 reinterpretation report; resolve_pending_question_v3 closed consequence enum reusing the deployed reprocessing path; 2C-UNDO-004 40001→55P03 resolved; reinterpretation remote smoke. |
| `Epic 2D-E` | Questions render and resolve identically in Chat and the queue as untrusted-data elements; proactive surfacing is deterministic and honors quiet hours, cap, cooldown, and override, per-user and failure-isolated. | Slice 2D.5 conversational-surfacing report; conversational-questions region panel and deterministic decideQuestionSurfacing; Chat↔queue↔/questions convergence journey (desktop + Pixel 7). |
| `Epic 2D-F` | The questions page, Chat, Needs Attention, review, analytics, accessibility, localization, remote gates, cleanup, reports, traceability, and permanent documentation agree on the completed behavior. | Slice 2D.6 closeout: convergence audit (single shared predicate/resolution/undo, no product fix required), this traceability matrix, cleanup verifier, test:remote:2d aggregate, and permanent-doc reconciliation. |

## Global gates (PRD 19.2)

| ID | Gate | Required behavior | Evidence |
| --- | --- | --- | --- |
| `GATE-01` | Security | authenticated ownership, RLS/grants, cross-owner denial, closed JSON, stale/replay/concurrency safety, untrusted-data prompt boundary, and no content telemetry pass | SECURITY DEFINER + search_path='' + auth.uid() across resolve_pending_question_v1–v3; RLS/grants/cross-owner denial; stale/replay/concurrency safety; untrusted-data Chat boundary; content-free telemetry. |
| `GATE-02` | Accessibility/localization | WCAG-oriented labels, focus, live regions, target size, responsive layout, PT-BR/English, and timezone copy pass, including the chat surface and suggested-answer chips | question-answer-form/conversational-questions a11y assertions; PT-BR/en + profile-timezone copy tests; desktop/Pixel-7 keyboard/focus/live-region/target Playwright including Chat and suggestion chips. |
| `GATE-03` | Rollback | the legacy answer path remains callable during rollout; UI can be restored without reverting an additive migration | resolve_pending_question_v1/v2 remain byte-identical and callable; legacy answerPendingQuestion wrapper preserved; additive-only migrations; UI restorable without reversing a migration. |
| `GATE-04` | Observability | content-free resolution events are allowlisted, idempotent as specified, and fail-open | content-free resolution/preview/consequence events allowlisted, idempotent, and fail-open; product-events remote persistence smoke. |
| `GATE-05` | Documentation/traceability | each requirement maps to a slice, test/evidence owner, and closeout report | this traceability matrix maps every requirement to a slice, evidence owner, and closeout report. |

## Regeneration check

Run `node scripts/generate-phase-2d-traceability.mjs`. The generator throws if the PRD requirement inventory, family counts, epic-acceptance count, or global-gate count drift from the closed expectation, guaranteeing this matrix stays synchronized with the PRD.
