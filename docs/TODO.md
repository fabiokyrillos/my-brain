# Project Backlog

Last updated: 2026-08-07 (Phase 2H authorized for **execution** of slices 2H.1–2H.6 by the owner, 2026-08-07; **slices 2H.1 and 2H.2 closed and deployed**; migration budget 5 allocated · 2 spent; hosted parity `202608070080`)
Active milestone: **Phase 2H — Deploy and Operate** (authorized by ADR-085, 2026-08-06 for planning; **execution of slices 2H.1–2H.6 authorized by the owner on 2026-08-07**). Phase 2G — Conversational Creation — and every phase before it are complete; Signup Hardening is closed at SH.7 and public signup remains closed.

## Post-2H rollout readiness — IN EXECUTION (2026-08-07, owner instruction). **NOT Phase 2I.**

Governing artifact: `docs/initiatives/post-2h-rollout/POST_2H_ROLLOUT_READINESS.md`. Records: `docs/reports/post-2h-rollout/`. **No successor phase is authorized, and this effort does not authorize one.** It carries no requirement families and no slices.

- [x] **A0 — the owner retired green ×3 (ADR-090).** Docs/planning PRs: **one** complete green PR-head run + a clean full-diff review → merge, **no merge-SHA run**. Code/migration/runtime PRs: one green PR-head run → full-diff review → merge → **one** green exact-merge-SHA run → **then** deploy. The runbook's §9 order was corrected to match (merge-SHA green now precedes the migration push, where it used to follow it) and the old rule is struck rather than deleted. **A flake is a defect with an owner**; repeated CI attempts are not an acceptance mechanism. Handoff §39 exists so a future session does not resurrect ×3 from a Phase 2H acceptance document — all of which correctly still say ×3 about the past.
- [x] **A0.1 — the first flake under the new policy, diagnosed and fixed.** `src/lib/byok/project-key-guard.test.ts` passed 19/19 in isolation and failed roughly one full-suite run in three, always on `Test timed out in 5000ms`. The assertion is instantaneous; the `await import("@/lib/ai/openai-provider")` inside the test body pulls the whole OpenAI SDK graph, which under parallel load on a cold transform cache exceeds vitest's 5 s default. Moved to `beforeAll` with a 60 s budget, so the test keeps the default 5 s and still measures only the constructor — raising the *test's* timeout would have hidden a slow constructor behind a budget sized for a bundler.
- [x] **A1 — `202608050077`'s fresh-database scheduling defect is CORRECTED (ADR-091, migration `202608070084`).** F-2H.5-7 closed. The five user-content sweeps are unscheduled by name; the migration **schedules nothing, executes no sweep, deletes no user row, and drops/alters/grants nothing** — all eight destructive mechanisms stay built and unscheduled. Idempotent (membership checked before `cron.unschedule`, which raises on an absent name). **Four transaction-local postconditions**: none of the five survives by name; none of the eight is scheduled under *any* name (asserted by **command**, since a job name is an artifact of which environment wrote the catalog); every pre-existing job that is not one of the five survived (this is what protects the two authorized attempt prunes and every operational job — an absence-only check passes for a migration that emptied the catalog); nothing was added. **Not charged to Phase 2H's budget**, which stays 5 allocated · 5 spent; the file is named `post_2h_` so no phase-filtered guard absorbs it. Regression proof: `supabase/tests/post_2h_retention_schedule_correction.sql`, which runs after CI's `supabase db reset` — **a hosted readback cannot see a chain-only defect**, and would have passed before the migration existed. Repository guard: `src/lib/closeout/post-2h-retention-correction.test.ts`.
- [x] **A1 deploy — DONE. `202608070084` is applied; hosted parity is exactly `202608070084`, local = remote.** PR #127 merged at `a20505f`; PR-head CI green on `50003c2`, **exact-merge-SHA CI green on `a20505f` attempt 1, all three jobs** — one run each under ADR-090, and the merge-SHA run **preceded** the deploy. Deploy NOTICE, the idempotency proof on the live project: *"5 cron jobs before, 5 after, five user-content sweeps forbidden and absent"* — nothing removed (hosted was already clean), nothing added, all four postconditions still executed. Cron catalog byte-identical at five jobs; both authorized attempt prunes intact; Edge parity untouched and green; rollout gate re-read **25 · 3 · 2**, unchanged.
- [x] **FOUND, and the worst of the three — the defect also made `RG-QUO-3` pass without an authorized purge.** `verify-signup-rollout.mjs` computes `retentionSweepsScheduled` as *"all five user-content sweeps are in `cron.job`"*, and `202608050077` scheduled all five at apply time — so **in any chain-built database the gate whose whole purpose is to require an authorized retention activation would have read PASS**, satisfied by the very defect ADR-082 was written about. Hosted never reached that state only because an operator removed the five the same day, so **the gate has been honest by accident rather than by construction**. After `202608070084` the only way it can pass is `sh6-retention-schedule --enable`, an operator act. **No code change** — the gate logic was right; the chain was wrong. This one is the mirror of the other two findings: there the probe was wrong, here the probe was correct and the product lied to it, which no amount of probe scrutiny would have caught. It surfaced from asking *what else references these five names?*
- [x] **FOUND by CI — a pgTAP fixture borrowed a live catalog entry.** `phase_2h_error_sink_and_deadman.sql` test 46 needed a never-reported job and used `sh-prune-notifications`, which existed **only because `202608050077` scheduled it**; its own comment called the hosted divergence "harmless here". Unscheduling the five made the subject vanish and the assertion read `NULL`. **The test was right to break — it measured the environment, not the product.** No surviving job could be borrowed (everything else reports since 2H.4; the BYOK prune's *name* differs by environment), so the suite now schedules its own subject inside its transaction, as the T-2H-14 probe fifteen lines below already did. `plan(52)` unchanged.
- [x] **A2 — backup readiness is BUILT.** Verdict: **a bounded owner-run `pg_dump` schedule satisfies the restore-readiness contract without a provider upgrade** — the upgrade would not have covered Storage or hosted Auth configuration either, so it buys convenience, not coverage. `backup:check` / `backup:census` / `backup:run` / `backup:verify` / `backup:restore-disposable`, guarded by 23 assertions in `src/lib/closeout/backup-toolchain.test.ts`. Encryption default-on (AES-256-GCM), checksum over the **plaintext** (ciphertext checksums prove the file survived, not the content), a failed dump deletes its partials and writes no manifest, the connection string is env-only and every error is redacted, and `--out` is refused inside the repository. Record: `docs/reports/post-2h-rollout/POST_2H_BACKUP_READINESS.md`.
- [ ] **OWNER — three actions unblock the backup, and nothing else does.** (1) Install the PostgreSQL **17** client tools (Windows: EDB installer, *Command Line Tools only* — Docker Desktop is the heavier alternative and is not required); (2) `$env:SUPABASE_DB_URL` from the Supabase dashboard — **never** into `.env.local`, this repository, or a command argument; (3) mint `$env:BACKUP_ENCRYPTION_KEY` (32 base64 bytes) and store it where `BYOK_MASTER_KEY` lives, **never beside the backups it opens**. Then `npm run backup:check` reads READY.
- [ ] **A3 / `RG-DEP-3` — INCOMPLETE, and deliberately not faked.** The gate passes on the mere *existence* of `docs/reports/signup-hardening/SIGNUP_HARDENING_BACKUP_RESTORE.md`. **That file was not created.** It gets written when a restore actually succeeds. The restore path is: migration chain from git → data dump → drill. Three refusals, no override: target ≠ production ref; `RESTORE_DB_URL` must not contain the production ref **and must** contain the target ref (the check `--target` cannot make — the writes follow the connection string, not the flag); target must hold zero rows.
- [x] **FOUND — the shipped restore drill counted `public.entities`, which does not exist.** The first census ever taken returned `42P01`. Nothing had caught it because **the drill had never been run**, so nothing had asked the database whether the list was true. It would have reported `entities is readable — FAIL` on a *perfectly good restore* — check 1 being the only check that measures whether the data came back. Fixed at the source: one list in `scripts/backup-shared.mjs`, imported by both the census and the drill, with the four real entity tables (`people`, `organizations`, `projects`, `contexts`) replacing the phantom, and asserted against the generated `database.types.ts`. *Suspect the probe before the product — eight times now.*
- [x] **A4 — the SMTP path is prepared end to end.** Every `smtp_*` field is `null`. **Resend** is repository truth and is not re-opened. Zero application changes and zero migrations required; `uri_allow_list` is already correct. Exact dashboard settings and seven verification steps written. **`npx supabase config push` is forbidden** — it is all-or-nothing and would open signup as a side effect of configuring email. Record: `docs/reports/post-2h-rollout/POST_2H_SMTP_ROLLOUT_PATH.md`.
- [ ] **OWNER — SMTP needs a sending domain you control, its DNS records, and a Resend credential.** `my-brain-dusky.vercel.app` cannot be the sender: Vercel owns that DNS. `rate_limit_email_sent = 2/hour` is a default-SMTP artifact and **must** move with the cutover or public signup is non-functional. **The new value is one compact owner decision with four costed options in `POST_2H_SMTP_ROLLOUT_PATH.md` §9 — it is deliberately NOT presented as a single recommendation.** An earlier draft offered 30/hour, which is exactly how a report's suggestion becomes a product value nobody decided; a ceiling is an owner signature. Everything in the SMTP path that does not need a secret, a domain or a DNS record is finished.
- [x] **A5 — the monitoring adequacy packet is written.** Everything is observable; **nothing is proactively alerted**, and detection latency is therefore "whenever someone next runs the command". ADR-089 holds — no destination was invented. Record: `docs/reports/post-2h-rollout/POST_2H_MONITORING_ADEQUACY.md`.
- [ ] **GAP — the rate limiter has no operator read.** 2H.3 built `rate_limit_events`; none of 2H.4's five operator reads covers it, so "is anyone being refused?" needs raw `service_role` SQL. Enforcement is proven (80-vs-60, 30-vs-20 in CI); **visibility** is missing. This is the ADR-084 shape again — a producer whose refusals nobody reads. **No migration needed.** It is the one gap that widens *because* signup opened. **Recommended before signup opens**, not before the `RG-DEP-4` signature.
- [ ] **OWNER — `RG-DEP-4`: the attestation is DRAFTED and unsigned.** `docs/reports/post-2h-rollout/POST_2H_RG_DEP_4_ATTESTATION.md` carries the exact signable text with every number filled in: **eleven inspections** named with the command that performs each and what it uniquely catches; a **proposed cadence per inspection**; and the **maximum silent-failure duration each cadence implies — approximately 48 hours** for a silently broken scheduled job (the daily interval plus the staleness window of the least frequent job, the 04:xx prunes) and **up to 7 days** for anything weekly. For comparison, the incident that motivated Phase 2H sat visible for **two days** with nobody looking. **The block refuses to be signable without a cadence**, because an adequacy signature with no cadence is a signature on nothing. **Nothing signs it, and no alert destination was invented** (ADR-089). If a day is too long, the smallest closing action is one destination for one exit code — a scheduled workflow running `npm run ops:health` and opening an issue on non-zero, ~1 hour, no migration.
- [x] **A6 — the legal packet is written and the machinery is internally consistent** (56 tests green; version constant pinned to SQL in both directions; `record_policy_acceptance` takes no version argument; acceptances append-only by *absence of policy*). **The material fact a reviewer must be told first: eight sweeps are built, zero scheduled, no purge has ever run — so every retention window either document describes is implemented and not enforced.** Record: `docs/reports/post-2h-rollout/POST_2H_LEGAL_REVIEW_PACKET.md`.
- [x] **A6 extended — the legal packet now discloses the two things a reviewer would not find in an older data map.** §4.1: the **four operational data classes Phase 2H introduced** (`error_events`, `scheduled_job_health`, `rate_limit_events`, `account_deletion_attempts`), each with whether it holds user content, its deletion behaviour and its retention state — including that `error_events` **has nowhere to put content**, asserted against the catalog by the migration itself, and that `account_deletion_attempts` deliberately has **no sweep** because a stalled row is the only evidence the deletion stalled. §4.2: **BYOK**, because it changes the controller/processor analysis — the user's own key, the user is the payer, no project key exists on any deployed path, and deleting the account destroys the envelope but **nothing the provider holds**.
- [ ] **OWNER — `RG-LEG-4` needs a professional review, a recorded attestation, and only then the SH-LEGAL-013 banner removal** (two lines in `documents.ts` plus its assertion, in the same commit as the attestation, never before). Fold F-2H.5-5's `error_events`/`rate_limit_events` question into that same version bump so one re-acceptance event carries both.
- [x] **A7 — the rollout gate was re-run and is UNCHANGED: 25 pass · 3 fail · 2 owner-signature, "SIGNUP MUST NOT OPEN".** Correctly so: not one of the four remaining items is closable without an owner act. `RG-QUO-3` needs a retention schedule enabled, **which is the authorization of the first live purge of user content** (`npm run sh6:retention-dry-run` first). **Opening signup remains a separate explicit owner action after the gate is satisfied.**

## Phase 2I — Foundation and Findability — **COMPLETE** (2026-08-07)

**61 declared · 61 classified · 0 undelivered** — 53 built, 4 baseline, 1 rename, 2 evidenced negatives, 1 partial. **Budget 1 allocated · 0 spent.** Report: `docs/reports/phase-2i/PHASE_2I_REPORT.md`. Matrix: `PHASE_2I_TRACEABILITY_MATRIX.md`. PRs #131 `e2779a5`, #132 `1660bad`, #133 `4295200`, #134 — each with one green PR-head run and one green merge-SHA run under ADR-090.

- [x] **The migration was NOT spent, and the measurement is why.** G-2I.2 ran before any search code: sequential `UNION` 338–669 ms (**FAIL**), parallel slowest-domain 121–244 ms (**PASS**). The parallel shape was not chosen to fit the budget — `2I-SEARCH-006` and `2I-SEARCH-007` already required per-domain queries, so wall clock is the slowest domain. Revisit threshold recorded: ~10k entries or ~1k attachments with `extracted_text`. ADR-093.
- [x] **The benchmark's first design could have committed to production.** It seeded thousands of fixture rows inside `begin;` and relied on the Management API not committing. It became a single `DO` block whose **only exit is a raise**, which Postgres rolls back regardless of the caller, with the rollback verified by reading the table back on every run.
- [x] **The phase's own audit was wrong three times**, all caught by reading the source: `chat: "Conversar"` had already shipped; `Mais` already rendered grouped destinations with labels and `role="group"` on both breakpoints; Library's membership already existed as `group: "context"` data. `2I-SHELL-003` reclassified **built → baseline**. This is why the matrix carries `baseline` as a distinct class.
- [x] **Two requirements delivered by NOT being built**, both re-derived rather than asserted: `2I-LIB-004` (**zero** pin/favourite columns anywhere in the schema — the guard re-checks `database.types.ts` every run, so a future migration breaks it) and `2I-PALETTE-009` (recents need state `2I-PALETTE-010` forbids).
- [x] **T-2I-01 proved in pgTAP with two accounts, in both directions** — two owners, rows matching the *same* needle, each seeing only their own. Both directions, because `user_id = auth.uid()` and `user_id = '<A>'` are indistinguishable from one side. The guard's first assertion is **no service-role client**, the failure RLS cannot save you from.
- [x] **T-2I-02 closed three ways** — no client in the directory; **an action has no invocable field**; and a hostile query cannot invent one. *User text selects; it never constructs.*
- [ ] **`2I-CLOSE-002` is PARTIAL, and the remainder is named.** Component accessibility is asserted by executed test; **no axe pass, no screen-reader session, no Playwright journey** over palette, search and Library at the four viewports. **Destination: a Playwright accessibility lane.** Classifying it built on component tests alone would be the over-claim the `baseline` class exists to prevent.
- [ ] **Locale debt unchanged: 263 before · 263 after**, ceiling 266. The ratchet held but did not fall — this phase created new surfaces rather than rewriting old ones. The sweep is owed by the parent PRD's later etapas, which do touch existing surfaces.
- [ ] **The rate limiter still has no operator read** (carried from post-2H). Enforcement proven, visibility missing, no migration needed — the one gap that widens when signup opens.
- [ ] **OWNER — authorize the next phase, or not.** The parent PRD's Etapa 2 (Hoje cockpit, *Precisa de você*, unified capture, **voice** — the audio-discard contract is already signed and recorded in `PHASE_2I_PRD.md` §13) is the natural successor. **Phase 2J is unstarted and A13 guards it; no successor scope is invented here.**

## Phase 2I — planning record, retained (2026-08-07, ADR-092)

**Implementation is NOT authorized.** Governing pair: `docs/initiatives/phase-2i/`. Records: `docs/reports/phase-2i/`. Scope: the parent mobile-first PRD's **Etapa 0 + Etapa 1 only** — the rest stays a roadmap, decomposed one phase per etapa, because a seven-etapa phase cannot close and a phase that cannot close cannot be traced, budgeted or gated.

- [x] **ADR-092 accepted; A13 retargeted 2I → 2J in the same commit** — fourth application of the precedent ADR-083 and ADR-085 set. The heading says *"retargets to the roadmap successor"*, never naming 2J, **because the detector treats an accepted ADR naming the next phase in its heading as a start signal** — the first draft hit exactly that and failed the guard it was moving.
- [x] **Planning package written:** PRD (8 requirement families, per-slice UX contracts, privacy-safe metrics), implementation plan (slices 2I.0–2I.7, six gates, budget rules), current-experience audit, threat model (T-2I-01…10), traceability contract, gaps and opportunities.
- [x] **Signed owner decisions recorded:** signup **not** a prerequisite (cohort-separated instrumentation); **dark mode out**, not partially implemented; **palette → search → Library**; **audio discarded** after confirmed transcription (recorded for the future Etapa 2 phase; **voice is not in 2I**); budget **max 1**; implementation unauthorized.
- [x] **The audit corrected the parent PRD in three load-bearing places.** The four primary destinations, capture as the global centre action and the five-slot mobile bar are **already delivered**; **`chat: "Conversar"` is already shipped** (the earlier study wrongly listed it as pending); and Library's six members **already share `group: "context"` in `capabilities.ts`** — the grouping exists as data and is not rendered. **So the navigation slice is one label (`home` → Hoje) plus one grouping, not a redesign.** Any 2I requirement claiming a rename must cite the exact constant it changes.
- [x] **Search surface verified against the schema, not assumed.** Seven existing tables, no new data model: `tasks`(title, description) · `entries`(original_content) · `memories`(content) · `people`(name, notes) · `projects`(name, description) · **`organizations`**(name, description) — displayed **Empresas/Companies** · **`attachments`**(original_name, description, **extracted_text**) — the route is `files`, the table is not.
- [ ] **OWNER OD-1 — do `private` and `highly_sensitive` records appear in global search results?** Three domains carry `sensitivity in ('normal','private','highly_sensitive')`. Nothing declares search behaviour for them because there has never been a global result list. Not a confidentiality question — the user owns all of it — but an **expectation** one: a `highly_sensitive` record in a result list on a phone in public breaks what the classification implies. **A search feature that silently surfaces every class has taken this decision by omission.** Blocks slice 2I.5 at gate G-2I.5.
- [ ] **OWNER OD-2 — is `attachments.extracted_text` searchable?** It is document *content*, not text the user typed. Including it is what "search inside files" means; it also means a query can surface text the user never wrote. Blocks 2I.5 at G-2I.5.
- [ ] **OWNER — authorize implementation, or not.** The planning package is complete. **The next step after it merges is a separate owner authorization**, in the shape of the Phase 2H execution authorization. Until then Phase 2I is planned and unstarted.
- [ ] **Migration budget: MAXIMUM ONE, to slice 2I.5 only, and zero is the preferred close.** Investigation established the input: **no `tsvector`, no GIN index, no `pg_trgm` anywhere in the 84-migration chain** — lexical search is greenfield at the database layer. Gate **G-2I.2** takes the decision **by measurement before any search code** and records the numbers. `1 allocated · 0 spent` is a success, not a shortfall. A migration must not be created to justify the budget; a second is an owner amendment.
- [ ] **`2I-LIB-004` is expected to close as "not supported, not built".** No pin/favourite column exists in any of the six Library domains, and adding one would be the data model `2I-LIB-002` forbids. The traceability contract accepts an evidenced negative as *delivered*.
- [ ] **ADR-055 expires 2026-10-27** — the nearest dated commitment. `2I-SEARCH-010` forbids embeddings, vector retrieval and generated answers, and the slice must state at close that ADR-055 is **neither satisfied nor superseded**. Lexical search is what would generate the evidence ADR-055 has been waiting for; it must not quietly resolve it.

## Superseded — the neutral-named planning study (2026-08-07)

`docs/initiatives/next-experience/` produced the proposal that became Phase 2I. **Superseded by `docs/initiatives/phase-2i/` and retained as the study that produced it.** Its one factual error is corrected above: it listed the `chat` → Conversar rename as pending when it had already shipped. Original entries retained below.

## Next UX initiative — PLANNING ONLY (2026-08-07). **Superseded by the Phase 2I section above.**

Workspace: `docs/initiatives/next-experience/` — a **neutral name**, held until scope is settled. The owner's PRD is `docs/initiatives/product-ux/MY_BRAIN_MOBILE_FIRST_EXPERIENCE_PRD.md` (commit `5b60705`). **Planning is authorized; implementation is not**, and beginning planning does not authorize a Phase 2I.

- [x] **Current experience audited against the code**, not against recollection. 21 authenticated routes; the navigation model is data in `src/features/shell/capabilities.ts`. `CURRENT_EXPERIENCE_AUDIT.md`.
- [x] **The PRD reviewed adversarially, product intent preserved.** Ten findings. `UX_PRD_REVIEW.md`.
- [x] **Proposed changes mapped onto existing contracts**, split by change class (UI / app / schema / AI / privileged / external). `UX_GAPS_AND_OPPORTUNITIES.md`.
- [x] **Initiative definition proposed** — name, objective, requirement families, slices, migration budget, threat impact, acceptance strategy, out-of-scope. `INITIATIVE_DEFINITION_PROPOSAL.md`.
- [ ] **F-1 (high) — the PRD's Etapa 1 navigation is largely ALREADY SHIPPED.** `home`/`inbox`(Registros)/`work`(absorbing `today`,`tasks`,`waiting`)/`chat` are already primary, capture is already the global centre action, and the five-slot mobile bar exists. **What is missing is `Biblioteca`** — and its six members are *already tagged* `group: "context"` in `capabilities.ts`. The grouping exists as data and is not rendered. Slice 1.1 should be re-priced as copy plus rendering.
- [ ] **F-2 (high) — the real problem is discoverability, not navigation size.** Twelve flat items behind `Mais` is a *retrieval* problem, so the command palette and global search are the highest-leverage items in Etapa 1. **Recommendation: promote 1.3/1.4 ahead of Biblioteca.** This is the one substantive change proposed to the owner's ordering.
- [ ] **F-3 (high) — voice (slice 2.5) is priced like a known and is the largest unknown.** Entirely greenfield, and it lands on **five** contracts at once: BYOK (whose key pays Whisper), the rate limiter (does a transcription consume an AI slot — §14.2 V-6 permits no exemptions), storage quota, the job pipeline, and retention (**audio is a new retained class and §14.1's signed windows do not name it**). Re-estimate 4–6 weeks or split spike-then-flow.
- [ ] **F-5 (high) — slice 3.6 collides with ADR-055, which EXPIRES 2026-10-27** — the nearest dated commitment in the repository, ~11 weeks out, with an empty funnel. Shipping 1.4's lexical search early is what would generate the evidence ADR-055 has been waiting for. **Letting the date pass silently is the one unacceptable outcome.**
- [ ] **F-6 (medium) — the PRD never mentions cost, and this product is BYOK.** The daily planner and contextual suggestions imply proactive model calls that spend the user's money. Recommend a §4 principle — *proactive AI work is opt-in and its cost is visible* — plus a §7 spend metric.
- [ ] **OWNER Q7 — does this initiative assume public signup opens? ANSWER THIS FIRST.** It decides what every §7.1 activation metric means; today they would be measured over three hand-created accounts. **Signup and this initiative must not be quietly coupled.**
- [ ] **OWNER Q1 — is the original audio kept or discarded?** Blocks the voice slice entirely (retention class + storage quota + privacy surface). **Recommendation: discard by default** — it removes all three without removing a single step of §2.5's eight-step flow.
- [ ] **OWNER — four smaller decisions:** accept "Phase 2I — Foundation and Findability" for Etapa 0+1 only, leaving the rest a roadmap (a seven-etapa phase cannot close, and a phase that cannot close cannot be traced or gated); accept the search-before-Biblioteca re-ordering; accept **ONE** migration allocated to the search slice and possibly unspent; dark mode in or out (**recommend out** — it doubles the visual QA surface of every later slice, and §0.1 already says "only if complete").

## Phase 2H — Deploy and Operate — ACTIVE, PLANNING (2026-08-06, ADR-085)

- [x] **Planning package written (2026-08-06).** `docs/initiatives/phase-2h/PHASE_2H_PRD.md` (44 requirements, nine families), `..._IMPLEMENTATION_PLAN.md` (slices 2H.0–2H.6, gates G-2H.1…G-2H.6, migration budget **FIVE** allocated per slice and non-transferable), `docs/reports/phase-2h/PHASE_2H_THREAT_MODEL.md` (T-2H-01…T-2H-24) and `..._TRACEABILITY_CONTRACT.md`. The A13 phase-start guard retargeted from Phase 2H to Phase 2I in the same commit as ADR-085.
- [x] **GATE G-2H.1 — merge-SHA CI for `508cf6c` green ×3 — CLEARED.** Run `31116254874` **attempt 6** went green on all three jobs during the Actions recovery, so the authorized rerun was never needed. The paragraph below is the terminal state of attempt 5, retained: ~~**NOT met; the run is terminal.**~~ `database and journey` ✅ and `edge worker` ✅; the **`application` job was cancelled** at `2026-08-06T16:10:47Z` after ~15 minutes queued, having executed **zero steps** — it never acquired a hosted runner and never reached `Set up job`, so it tested nothing. Run `31116254874` concluded `failure`. This is GitHub Actions runner availability, not a code failure, and it is the fourth such occurrence on this lineage. **No Phase 2H implementation PR may merge and no Phase 2H migration may deploy until all three are green on this SHA.**
- [x] **OWNER — decide when to re-run the `application` job on `508cf6c` — DISCHARGED.** Attempt 6 went green on its own during the Actions recovery; the rerun was never spent. Retained struck: ~~`gh run rerun 31116254874 --failed`. No code change is needed; the job never reached the repository. Held rather than fired automatically because the runner incident is ongoing and three prior attempts on this lineage failed the same way — a fourth immediate attempt spends queue capacity without new information.~~
- [x] **2H.0 — pre-code gates — CLOSED (2026-08-06).** All six gates executed and green; **0 migrations** (phase total unchanged at 5 allocated · 0 spent). Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_00_ACCEPTANCE.md`. G-2H.1 baseline green ×3 on `508cf6c`; G-2H.2 census — migration parity **exact**, **one** parity gap (`process-jobs`), and the finding that a merge auto-deploys Production; G-2H.3 census — **5 active cron jobs, 0 failures, 0 stale, 0 duplicated**, plus 5 retention sweeps built-and-unscheduled per ADR-082; G-2H.4 — the `42501 permission denied for table user_ai_credentials` cause reproduced **live and read-only** with a passing control, zero residue, reaper not built; G-2H.5 signed; G-2H.6 recorded as **ADR-087**. Threat model reviewed and **T-2H-25 added** by the census.
- [x] **2H.1 — deletion recovery — CLOSED AND DEPLOYED (2026-08-07).** PR #114 merged at `d7d5091`, merge-SHA CI green ×3 (run `31148068318`); **hosted parity `202608070079`**, 79 migrations, local = remote; hosted acceptance 29/30 + 7/7 on a corrected control, zero fixture residue. (1 migration, `202608070079`; budget 5 allocated · **1 spent**). All six `2H-RECOVER` requirements delivered and cited: `docs/reports/phase-2h/PHASE_2H_SLICE_01_ACCEPTANCE.md`. ADR-088 records the reap door. **Open and named:** the deployed `delete-account` build now differs from the repository and its deployment is a separate recorded operation; the reaper is built and **not armed** (no cron job, neither Vault secret set). `2H-RECOVER-001…006`. **First, because it is the only family here addressing harm that already occurred.** The bound is built before the retry loop, deliberately: a reaper written before its ceiling is an unbounded retry against the most destructive operation in the product.
- [x] **2H.2 — error sink and cron dead-man switch — CLOSED AND DEPLOYED (2026-08-07).** PR #116 merged at `88c9e3b`, merge-SHA CI green ×3 (run `31158204968`); **hosted parity `202608070080`**, 80 migrations; hosted acceptance **33/33**. One permanent calibration row in `error_events` by design (append-only, sweep executable by no role). (1 migration, `202608070080`; budget 5 allocated · **2 spent**). Seven requirements delivered and cited: `docs/reports/phase-2h/PHASE_2H_SLICE_02_ACCEPTANCE.md`. **Open and named:** no consumer until 2H.4, and no scheduled job reports yet, so every job classifies `never_reported`. Both sweeps unscheduled and executable by no role. `2H-SINK-001…004`, `2H-DEADMAN-001…003`. Their consumers land in 2H.4 — claiming them here would repeat ADR-084's producer-with-no-consumer.
- [x] **2H.3 — distributed rate limiting, C1 — CLOSED AND DEPLOYED (2026-08-07).** Merged at `46f7244` (PR #119), exact merge-SHA CI green ×3, `202608070081` applied, hosted acceptance **28/28**, and the concurrency proof executed in CI (80 vs 60 and 30 vs 20, stored counts exact). **Both stale Edge Functions deployed as separate gated operations**: `delete-account` v2→v3 (19/19) and `process-jobs` v20→v21 (20/20, ADR-086 executed). `verify:edge-parity` now reads *every deployed function is at or ahead of its source*. Budget **5 allocated · 3 spent · 2 remaining**.
- [x] ~~**2H.3 — distributed rate limiting, C1 — IMPLEMENTED (2026-08-07).**~~ `2H-RATE-001…006`, all six delivered. Migration `202608070081` — the slice's whole allocation; phase budget now **5 allocated · 3 spent · 2 remaining**. G-2H.5 was cleared first, so no placeholder ceiling was ever written. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_03_ACCEPTANCE.md`. Concurrency: `npm run test:local:2h3:rate-race` (80 racers vs 60, 30 vs 20, both doors, stored count read from the table). **Not yet applied to the hosted project.**
- [x] **2H.4 — operator surfaces — CLOSED AND DEPLOYED (2026-08-07).** PR #121 merged at `70d26a5`, exact merge-SHA CI green ×3; `202608070082` applied, **hosted parity `202608070082`**, 82 migrations, local = remote; `process-jobs` v21 → **v22**; `verify:edge-parity` fully green; hosted acceptance **39/39**. Both per-minute cron jobs reporting `success_empty` on the deployed system within a minute. Budget **5 allocated · 4 spent · 1 remaining**. Record: `docs/reports/phase-2h/PHASE_2H_SLICE_04_DEPLOYMENT.md`. **Open and named:** three jobs read `never_reported` until their next scheduled tick (hourly and 04:xx daily), which is the honest classification and not a gap.
- [x] ~~**2H.4 — operator surfaces — IMPLEMENTED (2026-08-07), not yet merged or deployed.**~~ `2H-OPS-001…005` plus `2H-SINK-005`/`2H-DEADMAN-004`, all eight delivered and cited: `docs/reports/phase-2h/PHASE_2H_SLICE_04_ACCEPTANCE.md`. Migration `202608070082` — the slice's whole allocation; budget now **5 allocated · 4 spent · 1 remaining**. Five `service_role`-only `stable` reads plus `record_scheduled_job_failure`; the dead-man mechanism moved to `private.apply_scheduled_job_*` because `pg_cron` runs as `postgres` with a **null** `auth.role()`. All five scheduled paths now report their own runs. ADR-089 records alerting as defined-and-not-built. Bound by ADR-075 — operator CLI over `service_role` SQL, **no product admin UI, no service-role HTTP endpoint**, mechanised by `operator-surface-boundary.test.ts`. **Ordering for deployment: merge first (the deployed application depends on nothing new), then apply `202608070082`, then redeploy `process-jobs`** — the drain's report is the only new consumer and it must not run before the RPC exists.
- [x] ~~**2H.5 — deploy, retention and backup — IMPLEMENTED (2026-08-07).**~~ All twelve of this slice's requirements delivered and cited: `docs/reports/phase-2h/PHASE_2H_SLICE_05_ACCEPTANCE.md`. Migration `202608070083` — **the fifth and last of the budget; 5 allocated · 5 spent · 0 remaining**, and `AUTHORIZED_MIGRATION_HEAD` moved in the same commit. Three windows seeded into SH.6's `private.retention_windows` at PRD §14.1's **signed 90 days**; `rate_limit_events` gains the predicate, twin and sweep it never had and **reuses** that signed window rather than minting a fourth number; the 2H.2 sweeps gain zero-argument forms so a cron statement carries no number. **The migration schedules nothing and asserts that about itself against `cron.job`.** 31 boundary assertions, pre-flighted 31/31 against the real hosted database and rolled back. Runbook, env/secret contract (27 variables, machine-checked both ways), backup posture and restore drill all written; **the drill's production refusal is executed, the drill itself is not.** Nothing here executed: eight sweeps built, **zero scheduled**, no purge.
- [ ] **`202608050077` still schedules five user-content sweeps at apply time (F-2H.5-7, high, latent).** Those five were removed from the **hosted** project the same day by `scripts/sh6-retention-schedule.mjs` (ADR-082), but the migration was not and could not be edited afterwards. So the hosted project has 5 cron jobs and no user-content sweep — while **any database built from the chain** (CI's `db reset`, a restored disposable project, a future environment) schedules all five, and they begin deleting user content at 04:11 UTC the next morning. This is the failure ADR-082 was written about, still live in the chain and reachable by anyone standing up a new environment. **Not fixable by 2H.5:** migrations are append-only, so unscheduling them takes a **sixth** Phase 2H migration and the budget is five, non-transferable — **an owner amendment, or the first migration of the next phase.** `phase-2h-restore-drill.mjs`'s check 4 asserts the destructive posture of a restored copy, so a drill following the documented procedure **would fail that check correctly** and surface the five schedules before they ran.
- [ ] **OWNER — there is no operator-restorable backup of this project (F-2H.5-1, high).** `pitr_enabled: false`, `backups: []`, organization plan **free**. The schema is recoverable from git; **the rows are not**, Storage is uncovered on any plan, hosted Auth configuration is reconstructable but not restorable, and a restore would arrive without `BYOK_MASTER_KEY` and so recover every credential envelope as unreadable ciphertext. **This blocks `RG-DEP-3`** — a drill needs something to restore. Smallest closing action: upgrade the organization to a plan with daily backups; the alternative is an owner-run `pg_dump` schedule, which is more work and puts a credential in more places. Full posture: `docs/reports/phase-2h/PHASE_2H_BACKUP_AND_RETENTION.md` Part I.
- [ ] **OWNER question — should the Privacy Policy's retention table enumerate `error_events` and `rate_limit_events`? (F-2H.5-5, low.)** Both are per-user *operational* records with a 90-day window; both are handled by account deletion (`error_events` de-identified, `rate_limit_events` cascaded) and neither carries user content, so the Policy makes no false claim today. Adding a class there changes a **published legal document and its version**, forcing re-acceptance by every user — an owner and legal act, which is why Phase 2H routed it rather than deciding it. Take it with the next policy version bump.
- [ ] **Deployment observability — the application exposes no deployed-commit identifier (F-2H.5-4, low).** `verify:edge-parity` can assert "the deployed function matches its source"; nothing can assert "the deployed application is the merge SHA". The response carries `server: Vercel` and a request trace, neither of which names a commit, so the authoritative reading is the Vercel dashboard. An HTTP-readable build identifier would close it.
- [x] ~~**2H.6 — closeout — DONE (2026-08-07). PHASE 2H IS COMPLETE.**~~ `2H-CLOSE-001…005` delivered and cited: `docs/reports/phase-2h/PHASE_2H_SLICE_06_ACCEPTANCE.md`. **44 declared · 44 delivered · 0 partial · 0 undelivered**; budget **5 of 5 spent, each by its allocated slice**; hosted parity `202608070083`. The fail-closed generator refused on its first run against the real repository, naming exactly the five `2H-CLOSE-*` it had not yet evidenced. Thirteen mutation fixtures plus the real repository as the positive control. Final report: `docs/reports/phase-2h/PHASE_2H_REPORT.md`. **No successor phase is authorized.**
- [x] **`2H-DEPLOY-007` — the `process-jobs` audit is DONE (2026-08-06).** `docs/reports/phase-2h/PHASE_2H_PROCESS_JOBS_AUDIT.md`. Deployed build = tree at `7be25f0`; three deployable commits since (`715dc15`, `7d84a2b`, `8982d74`). **No migration changed a signature or grant on any RPC the deployed build calls** — the `delete-account` failure mode is specifically absent. Reachable and idle: 4 jobs, all completed, newest `2026-08-02T13:06:59Z` (after the deploy). **Recommendation: deploy, as its own change, after baseline CI is green, never bundled into a slice.** Nothing was deployed.
- [x] **ADR-086 is ACCEPTED (2026-08-06, owner).** The audit and its recommendation are accepted in principle, on four binding conditions: `process-jobs` is deployed as a **separate explicit operation**; **never bundled into a Phase 2H slice merge**; **only after `508cf6c` is green ×3** and the audit's §7 verification lane and §8 rollback are ready; and **not during the current GitHub Actions incident**. **Acceptance is not execution** — condition 3 is unmet and condition 4 is active, and nothing has been deployed.
- [ ] **OWNER (later, when conditions 3 and 4 clear) — execute the `process-jobs` deployment.** `npx supabase functions deploy process-jobs`, preceded by `npm run byok:verify-runtime` and followed by `npm run verify:edge-parity` reading `ok`. Record the pre-deploy `updated_at` first, or the rollback target is a guess.
- [x] **G-2H.5 is CLEARED — the `2H-RATE` ceilings are SIGNED (2026-08-06, owner).** V-1 **60 AI operations/user/rolling hour**; V-2 **20 accepted uploads/user/rolling hour**; V-3 **rolling window, not fixed clock-hour**; V-4 **bounded worker retries consume no slot, user-initiated retries do**; V-5 **background provider-reaching work consumes the owner's AI slot, but drain-admitted work is not double-refused**; V-6 **no exemptions, including the owner**. Recorded in `PHASE_2H_PRD.md` §14.2 (authoritative) with the argument retained in `docs/reports/phase-2h/PHASE_2H_RATE_LIMIT_DECISION_REQUEST.md`. **Clearing G-2H.5 does not authorize implementing, migrating, merging or deploying slice 2H.3.**

## Phase 2G — Conversational Creation — COMPLETE (2026-08-05, ADR-083)

- [x] **Phase 2G — COMPLETE (2026-08-06).** All four slices closed with merge-SHA CI green; 29 requirements, 27 delivered, 2 named as not delivered with a destination; 1 of 1 migrations spent and deployed (parity `202608060078`). `2G-ROUTE-008`/`2G-CLOSE-003` are the two, both on the CAPTCHA journey blocker. The funnel is measured and empty — ADR-055 expires 2026-10-27. Final report: `docs/reports/phase-2g/PHASE_2G_REPORT.md`.
- [ ] **Phase 2G implementation record (retained), per `docs/initiatives/phase-2g/PHASE_2G_IMPLEMENTATION_PLAN.md`.** Migration budget: **ONE**, allocated to 2G.3 only; Signup Hardening's spent budget cannot be reused and nothing is borrowed from Phase 2H.
  - [x] **2G.1 — the create-intent contract — CLOSED.** PR #100 merged at `ad0b56c`, exact merge-SHA CI green on all three jobs (the planning PR #99 at `7516569` likewise); branches preserved. G-2G.1 executed: zero migrations proven by reading every cited SQL object. Policy version `2026-08-05.1` with the four policy-lock digests unmoved and the fingerprint invalidation exercised. Evidence: `docs/reports/phase-2g/PHASE_2G_SLICE_01_ACCEPTANCE.md`.
  - [x] **2G.3 — capture routing — CLOSED AND DEPLOYED.** PR #102 merged at `e2c3718`, merge-SHA CI green ×3; **hosted parity `202608060078`**, verified behaviourally 5/5 with both controls refused. `docs/reports/phase-2g/PHASE_2G_MIGRATION_078_DEPLOYMENT.md`.
  - [~] **2G.4 — convergence and closeout** (0 migrations). **Its first question is settled with evidence: hosted CAPTCHA blocks the entire authenticated online journey suite** — 28 spec files sign in through the login form and every one lands on `?error=captcha-failed` since SH.5 enabled it on 2026-08-05. Three approaches tried, three hypotheses eliminated, helper in the tree with its guard's positive case honestly `test.fixme`. Evidence: `docs/reports/phase-2g/PHASE_2G_ONLINE_JOURNEY_BLOCKER.md`. **Repository half remains and is not blocked:** fail-closed traceability generator over every `2G-*` id, funnel measurement for ADR-055, final report, documentation reconciliation re-raising every Phase 2H deferral by name.
- [x] **CLOSED (2026-08-06): account deletion completes end to end. Two fixes were required, and only one was code.** The deployed `delete-account` was at v1 (2026-08-04) and never redeployed; migration `202608050077` had revoked the grant that build depended on, so the executor answered `credential_not_erased` for **every** account and nothing re-ran it. The owner deployed the function — no config push, no migration, no Auth change — and terminal deletion is now proven on the hosted project: `completed`, Auth user **404**, access token **403** and refresh **400**, every owned row and the `account_lifecycle` row gone with the executor's own census returning `{}`, storage zero, fixture residue zero, project accounts 3 → 2 with both survivors real, CAPTCHA still enabled, signup still disabled, retention still 0/5. The `test.fixme` is removed and `e2e/online-account-deletion.spec.ts` runs **4/4**, automating everything the challenge stands in front of; the single form submit carrying a valid Turnstile token was performed once interactively and is not automatable without defeating the control. `npm run verify:edge-parity` (PR #110) is what makes the deploy half visible — a green repository proved nothing about it. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_DELETION_EXECUTOR_STALL.md` §8b.
- [ ] **Separate decision, deliberately not taken here: `process-jobs` is undeployed** (`8982d74 feat(bounds): one home for the file limits, the input bounds and the body bound`). Nothing observed depends on that change, and it was explicitly excluded from the deletion closeout. `npm run verify:edge-parity` reports it on every run, so it stays visible rather than forgotten. Deploying it is an owner decision on its own terms.
- [ ] **Follow-up, after the deploy above: `re-runnable` is a property, not a mechanism.** SH-DELETE-005 documents the executor as re-runnable and the invocation as best-effort in `after()`, which is correct on its own terms — but nothing re-runs it, so a single transport or provider failure strands an account in `deleting` with every write refused. Destination: Phase 2H (deploy-and-operate), alongside the error sink and dead-man switch already deferred there. A stalled-deletion sweep is the natural shape; it needs a migration and is **not** taken opportunistically.
- [x] **PRODUCT DEFECT — FIXED as a standalone hotfix (2026-08-06): account deletion refused a correct password.** `requestAccountDeletion` (`src/features/account/actions.ts`) re-authenticated with `signInWithPassword` and forwarded **no `captchaToken`**, unlike all four surfaces in `src/features/auth/actions.ts`. Since SH.5 enabled hosted CAPTCHA on 2026-08-05, `POST /auth/v1/token?grant_type=password` answers `400 captcha_failed` for **every** client, so the surface reported `A senha não confere.` to people typing the right password and the account could not be deleted at all. Fixed by rendering the **existing** `TurnstileWidget` on the deletion page (server-side, handed to the client surface as a node), permitting the widget origin in the CSP for that **exact** route, forwarding the token in `options.captchaToken`, distinguishing `captcha-missing` / `captcha-failed` / `password` / `throttled` / `unavailable` / `lifecycle`, and admitting the grant against the existing `signin_failure` ceiling — **zero migrations, no second CAPTCHA implementation, and hosted CAPTCHA untouched**. The guard that missed it listed four call sites by name; it is now stated over every `signInWithPassword` in `src/`. **One owner step remains and is not claimed:** the successful deletion needs an interactive Turnstile solve, so `both correct: the account is destroyed` stays `test.fixme`. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_DELETION_CAPTCHA_HOTFIX.md`.
- [x] **Repository maintenance: the authenticated online journey suite runs again (2026-08-06).** Three blockers, not one: hosted CAPTCHA on the login form; SH.4's consent gate interposing on every admin-created fixture account since it shipped, independently of Turnstile; and the password grant being refused for every client. The fixture is `e2e/support/online-session.ts`, proven in both directions by `e2e/online-session-fixture.spec.ts` (absent / malformed / wrong project ref / expired all refused; the real session admitted, and the account it resolves proven to be the disposable one). The service-role boundary is guarded at runtime and in source by `src/lib/closeout/online-session-boundary.test.ts`. **Nothing about CAPTCHA, GoTrue or product authentication was weakened.** Evidence: `docs/reports/phase-2g/PHASE_2G_ONLINE_HARNESS_ACCEPTANCE.md`.
- [ ] **Repository maintenance, phase-independent — SUPERSEDED by the entry above, kept as the record of what was true: the authenticated online journey suite cannot run.** Not a Phase 2G defect and not a product defect — the product reports `?error=captcha-failed` with its own declared code, correctly. It is a harness that has not caught up with a control enabled on 2026-08-05, and it affects all 28 `e2e/online-*.spec.ts` files, so every initiative that plans a hosted lane inherits it. **Disabling hosted CAPTCHA is not the fix**; the fix is `e2e/support/online-session.ts` working.
  - [ ] **2G.3's repository record (retained).** Work was completed on `codex/2g-slice-3`. **Spends the phase's ONE migration (`202608060078`); the budget is now exhausted and any further DDL is an owner amendment.** `captureSource` gains `'composer'`; the inventory gate found and fixed the `failureKind: 'quota'` defect at zero extra migration cost (ADR-084 — SH.6's quota refusals have recorded nothing since deployment, and do not backfill). ~~**`202608060078` has not yet been applied to the hosted project.** Hosted parity therefore remains one behind the chain head, at `202608050077`.~~ **Superseded** by the deployed line above: parity is `202608060078` and was verified behaviourally. Retained struck rather than deleted. Evidence: `docs/reports/phase-2g/PHASE_2G_SLICE_03_ACCEPTANCE.md`.
  - [x] **2G.2 — creation from the composer — CLOSED.** PR #101 merged at `e63e103`, exact merge-SHA CI green on all three jobs; branch preserved. Zero migrations.
  - [ ] **2G.2's record (retained).** Repository work was completed on `codex/2g-slice-2`. Routing to the deployed creation family with preview/confirm, one-validator qualifier resolution, refusal narrowing in both locales, undo through the registered handler. **The authenticated journey is WRITTEN, NOT EXECUTED** (BYOK provider calls need the deployed app + a disposable product credential) — destination: 2G.4's hosted verification. Evidence: `docs/reports/phase-2g/PHASE_2G_SLICE_02_ACCEPTANCE.md`.
  - [ ] **2G.3 — capture routing** (the ONE migration: `captureSource` allowlist widening). Separately gated; drops cleanly if declined.
  - [ ] **2G.4 — convergence and closeout** (0 migrations). Fail-closed traceability, hosted non-destructive verification on disposable fixtures, measured funnel statement for ADR-055.
- [ ] **OWNER — Task group A, retention activation (optional, NOT authorized by anything above):** `npm run sh6:retention-dry-run`, review the counts, and only under an explicit later authorization `npm run sh6:retention-schedule -- --enable`. **Enabling IS the authorization of the first live production purge; deleted rows are irreversible.** No destructive sweep may execute during Phase 2G work.
- [ ] **OWNER — Task group B, the public-signup rollout gates (in order):** (1) configure custom SMTP via Resend with a verified sending domain — RG-DEP-1; (2) restore a production backup into a disposable project and record the evidence — RG-DEP-3; (3) obtain and record professional legal review approval — RG-LEG-4; (4) obtain and record monitoring adequacy approval — RG-DEP-4; (5) run `npm run rollout:verify` and require a fully green result; (6) only after a separate owner authorization, flip `disable_signup`; (7) run `rollout:verify` again against the open state (SH-ROLLOUT-005). **Documenting these tasks is not their completion**; none of them is performed by Phase 2G, and none of them blocks its repository-safe work.
Previous milestone record, retained: **Phase 2F — One Write Path is complete.** (Its closing line "No successor phase is authorized or started" was true when written and is superseded by the Phase 2G section above.) All six slices (2F.1–2F.6) are accepted and merged; the two that carried a migration are deployed, and remote parity is `202607300063`. The box below is checked under the same merged-and-deployed-and-released rule Phases 2D and 2E used: Slice 2F.6 merged as PR #33 → `7e3e5f0` with merge-SHA CI run `30520514810` green on all three jobs, and every closeout gate was re-executed from merged `main`. Phases 2A, 2B, 2X, 2C, 2D and 2E are complete before it — Phase 2C through Slice 2C.6, Phase 2D through Slice 2D.6 (both merged and closed), and Phase 2E **released and tagged `phase-2e-complete`**. The split/merge sub-epic (`2C-STRUCTURE-004`, GitHub issue #8) remains deferred and non-blocking. No successor phase is authorized or started; the three open items below carry their own destinations.

Items are ordered by execution priority. Completed work moves to `CHANGELOG.md`; decisions move to `DECISIONS.md`; the current snapshot stays in `STATE.md`.

## Where a new document goes (2026-08-05)

- [x] **`docs/` separates canon from initiative material, and the rule is guarded.** The `docs/` root holds only the living canon; a phase's or initiative's PRD, plan, proposal or closing report goes in `docs/initiatives/<name>/`, using the same directory name as `docs/reports/<name>/`. Rule: `ENGINEERING_STANDARDS.md` §"Documentation placement". Guard: `src/lib/closeout/docs-taxonomy-guard.test.ts`. Index: `docs/README.md`.
- [x] **`docs/reports/` is filed by initiative and the placement rule is guarded.** A new phase or initiative creates `docs/reports/<name>/` — lowercase kebab-case — before its first report is committed; slices file into the owning directory; acceptance, deployment, adversarial review and evidence stay grouped with their initiative. Nothing new goes at the reports root. Rule: `ENGINEERING_STANDARDS.md` §"Reports and evidence placement". Guard: `src/lib/closeout/reports-taxonomy-guard.test.ts`. Index: `docs/reports/README.md`.

## Authorization state (2026-07-31)

- [x] **G-0.5 — hosted signup closed and verified.** `disable_signup: true`; a signup attempt with a provider-accepted address returns `422 signup_disabled`. Evidence: `docs/reports/byok/G05_HOSTED_SIGNUP_CLOSURE_EVIDENCE.md`.
- [x] **Abandoned production test account removed**, owner-authorized, full cascade verified across 16 readable tables. Two real accounts remain. Evidence: `docs/reports/byok/GENERATED_ACCOUNT_CLEANUP_EVIDENCE.md`.
- [x] **Entity Graph Completion — COMPLETE.** All three slices merged with green merge-SHA CI. **53 requirements, 53 delivered, 0 deferred.** Zero migrations across the initiative; parity still `202607310064`. Final accounting: `docs/reports/entity-graph/EGC_REPORT.md`; per-requirement disposition: `docs/reports/entity-graph/EGC_TRACEABILITY_MATRIX.md`.
  - Two permanent guards now run in CI beyond the initiative: the **locale-ternary non-increase guard** (ceiling 266, current 262) and the **reachability assertion** over the whole authenticated route tree.
  - The **UX-22 localization sweep stays deferred** to localization maintenance, unchanged — the guard stops the count growing, it does not do the sweep.
  - The **CRLF test fragility** Slice H diagnosed stays deferred to repository maintenance: `sql-reachability.test.ts` loses 2 of 46 assertions on a Windows checkout and is green in CI. EGC's own traceability generator hit the identical fragility and was fixed, because that one is this initiative's instrument.
- [ ] **BYOK — BYOK.1 through BYOK.4 are COMPLETE; BYOK.5 and BYOK.6 remain.** `docs/initiatives/byok/BYOK_PRD.md` + `..._IMPLEMENTATION_PLAN.md`, on `docs/reports/byok/BYOK_SECURITY_DEFINITION.md`. Five migrations budgeted (`ADR-070`), five written: `202608010065`…`202608010069`. **No Node or Deno user path can reach a project key**; what remains is the deployed secret itself and the convergence evidence.
  - [x] **Pre-code gates G-0.1, G-0.2, G-0.3 (procedure + `local`/`test`), G-0.5** — satisfied and evidenced.
  - [x] **G-0.4 — dedicated low-limit OpenAI validation key.** Satisfied (`ADR-070`): dedicated project and key as `BYOK_VALIDATION_OPENAI_API_KEY`, USD 2 budget alert, restricted models, lowest practical limits, acceptance-lane-only.
  - [x] **Validation key reachable, and the live lane EXECUTED.** `npm run test:byok:validation` — 4 passed, three real round trips. Evidence: `BYOK_SLICE_03_ACCEPTANCE.md` §2.
  - [x] **Per-IP throttle conflict** — resolved by `ADR-070`: `ip_hash` column, third independent secret `BYOK_RATE_LIMIT_PEPPER`, migration budget 4→5.
  - [ ] **`BYOK_RATE_LIMIT_PEPPER` for `preview` / `production`.** **OWNER ACTION, deferred to point of use** alongside the other two secrets (Amendment A-1.2). Three secrets per environment now, not two.
  - [ ] **G-0.3 `preview` / `production` secrets.** **OWNER ACTION, deferred to point of use.** Preview pair before the first BYOK preview deployment; production pair before BYOK.5's cutover. Both runtimes — hosting platform *and* Supabase Edge Function secrets — per environment, all values distinct.
  - [x] **BYOK.1** — credential store and crypto core. Migration `202608010065`; acceptance record `docs/reports/byok/BYOK_SLICE_01_ACCEPTANCE.md`.
  - [x] **The per-IP validation throttle conflict** — raised by BYOK.1, **resolved by the owner** as `ADR-070`: an `ip_hash` column in a BYOK.3 migration, a third independent secret, budget 4→5. Shipped in `202608010067`.
  - [x] **BYOK.2** — resolvers. Migration `202608010066`; acceptance record `docs/reports/byok/BYOK_SLICE_02_ACCEPTANCE.md`.
  - [x] **BYOK.3** — complete. Migration `202608010067`; the project-key fallback is deleted; acceptance record `docs/reports/byok/BYOK_SLICE_03_ACCEPTANCE.md`.
  - [ ] **Deferred acceptance evidence, available at the first BYOK deployment:** matrix cases 1–3, concurrent rotation (C10), and the desktop/Pixel 7 Settings journeys (C11). All three need a shared database carrying the BYOK tables. Not deferred quietly — `BYOK_SLICE_03_ACCEPTANCE.md` §4.
  - [x] **BYOK.4** — complete. Migrations `202608010068` and `202608010069`; the worker's `Deno.env.get("OPENAI_API_KEY")` read, its 503 branch and the allowlist entry that permitted it went in **one commit**. Acceptance record `docs/reports/byok/BYOK_SLICE_04_ACCEPTANCE.md`; `ADR-071` records the second function in the second migration and narrows *"consumes no retry"*.
  - [ ] **`attachments.processing_error` is Portuguese-only**, including the configuration message BYOK.4 added. A pre-existing gap — `attachment.ts` has never had access to a locale, unlike `entry.ts` — that BYOK.4 matched rather than widened. Moving all three strings behind the i18n contract needs the worker to know the attachment's locale, which it currently does not read.
  - [ ] **`fail_job` still accepts free text at the database level.** Safe today only because `_shared/job-failure.ts` is its sole caller and passes a declared code. `fail_job_terminal` is constrained; `fail_job` is not, and constraining it would change a contract callers outside BYOK depend on. `BYOK_SLICE_04_ACCEPTANCE.md` R1.
  - [ ] **`configure_ai_credential` cannot appear in the needs-attention queue.** `list_needs_attention` is an RPC BYOK.4 does not change, and `needs_attention_item_opened` validates its reason against a five-member enum **inside Postgres**. The awaiting state reaches the user through the Inbox and the entry detail instead. Widening either would cost a migration nobody has budgeted; `TrackedAttentionReason` makes the boundary a compile error meanwhile.
  - [~] **BYOK.5** — every repository task done; **STOPPED at four owner actions**. Gate E4 executed. `docs/reports/byok/BYOK_SLICE_05_ACCEPTANCE.md` §3 carries the exact commands, in the order that matters.
  - [x] **OWNER ACTION 1** — the three BYOK secrets are provisioned in the Supabase Edge Function store. Verified by read-back. (There is no hosting platform: no shared Next.js environment exists.)
  - [x] **OWNER ACTION 2** — migrations `202608010065`…`202608010069` applied. Remote parity `202608010069`, verified against local.
  - [x] **OWNER ACTION 3** — `process-jobs` deployed (version 20, 16/16 files byte-identical to `abef6e4`) and `OPENAI_API_KEY` unset. Gate **E2 passes** on the read-back.
  - [x] **OWNER ACTION 4** — the owner configured a credential through Settings. The row exists, `active`, with a fingerprint and a `validated_at`. **It is unreadable by the deployed worker** — see the blocker below.
  - [x] **Unblocked and executed:** BYOK.3's matrix cases 1–3, BYOK.4's D2 and the deployed-bundle half of D10, gate E2, and eight further deployed-worker cases. **Twelve previously-blocked gates now pass.** `docs/reports/byok/BYOK_DEPLOYED_ACCEPTANCE.md` §1 and §3.
  - [x] **BLOCKER CLEARED — the owner cutover SUCCEEDED on the second attempt.** Both steps were performed by the owner and **verified rather than believed**: `npm run byok:verify-runtime` prints `IN PARITY`, and the owner's stored credential now opens under the Node runtime's master key. `docs/reports/byok/BYOK_DEPLOYED_ACCEPTANCE.md` §10.
    - [x] **STEP 1 — the two runtimes agree.** Confirmed by the outside check, which prints no value and no digest.
    - [x] **STEP 2 — the credential was re-entered through Settings.** Replaced, not repaired; not seeded from an environment variable, not by SQL, not by script.
  - [x] **Unblocked and executed by that remediation:** **E1** (the owner's credential opens in Node; **OWNER-ASYNC passes on the deployed worker** — job completed, one interpretation, `ai_usage_events` 8 → 10), **E5** (all five remote smokes green), and the **capture lifecycle both halves** (uncredentialed capture stores `awaiting_ai_configuration` with no job; an unopenable credential fails closed with `credential_unreadable` and bills nothing).
  - [x] **Two remote smokes were asserting pre-BYOK behaviour and are now inverted, not weakened.** `remote-supabase-smoke.mjs` and `remote-entry-processing-smoke.mjs` required a disposable account to receive AI, which is the project-key fallback BYOK.3 deleted. They now assert the refusal, the declared code, the single attempt and the **absent** ledger row. Do not restore the old assertions.
  - [x] **UNBLOCKED and EXECUTED.** The owner provisioned two disposable low-limit product credentials, and every gate that needed one ran against the deployment: the two-user isolation matrix (sync and async, evidenced cryptographically because a provider-side distinguisher was measured **absent**), concurrent rotation (C10, genuine contention on one witness), the Settings journeys (C11, **4/4** desktop and Pixel 7 in both locales), the removal / queued-job / capture lifecycle, and bounded pending processing. `docs/reports/byok/BYOK_DEPLOYED_ACCEPTANCE.md` §11.
  - [x] **BYOK.6's last code deliverable SHIPPED:** the bounded two-key master-key rotation window. Zero migrations. `key_version` selects **one** key — a fallback would be a decryption oracle — with a 30-day maximum refused rather than truncated, four environment names pinned by the guard in both runtimes, a Node/Deno parity lock, and a completion counter that will not report "done" while any row is unreadable. Drilled on disposable material.
  - [x] **Three product defects found by the acceptance lane and fixed with regression tests:** `formatFingerprint` had no production consumer so its parse-don't-trust guarantee was not in force; the panel reported the previous action's outcome after a removal; and a save following a removal announced itself as a replacement.
  - [x] **E3 — proven on disposable accounts with real credentials; still REFUSED ON PURPOSE on the owner's.** Removal is easy; restoring requires the owner to re-enter a key this loop must never see or request. The full remove/reconfigure cycle now runs end to end on disposable accounts holding genuine provider-accepted keys, which is the same property without the risk.
- [ ] **BYOK residual items — named, NOT counted as delivered.** BYOK is closed; these are not.
  - [ ] **`BYOK-OPERATIONS` (6 requirements) is not built** — no operator dashboard, no alerting, no admin view of credential health. Unchanged from `2F-OPERATIONS-002`.
  - [ ] **The production master-key rotation has never been run.** Owner-authorised key change; **no undo** — a rotation begun with a wrong current key strands every row it touches, so `npm run byok:verify-runtime` must print `IN PARITY` first and `--limit` exists so a first run can be checked. Procedure and rollback limits: `BYOK_INCIDENT_RUNBOOK.md` §2a.
  - [ ] **Six orphaned storage objects from 2026-07-16** — `user-files/<uuid>/…` owned by users deleted long since. Database rows cascade on account deletion; **storage objects do not**. Not caused by BYOK and not deleted here; it is precisely the gap **Signup Hardening's account-deletion** requirement must close.
  - [ ] **Three OpenAI keys should be revoked** at platform.openai.com now their lanes have run: `BYOK_VALIDATION_OPENAI_API_KEY` and the two disposable product credentials. Outside the implementer's administrative boundary.
  - [ ] **47 of 131 requirement ids are untraced**, dispositioned family by family in `docs/reports/byok/BYOK_TRACEABILITY_MATRIX.md`. Most are implemented and were executed; `BYOK-OPERATIONS` genuinely is not.
  - [~] **BYOK.6** — **PARTIAL and unable to close.** Executed: the zero-secret residue verifier (`supabase/tests/byok_residue.sql`, run in CI, enumerating `public` rather than a fixed table list) and the incident runbook (`docs/reports/byok/BYOK_INCIDENT_RUNBOOK.md`, marked *written, not drilled*).
  - [x] **BYOK.6 — queued-job removal and recovery, EXECUTED against the deployed worker.** Removal blocks queued work; reconfiguring makes the same job claimable again; a "removal" that does not erase key material is refused by `user_ai_credentials_status_shape_check`, so removal is a real erasure and not a flag.
  - [x] **BYOK.6 — the master-key loss drill, EXECUTED.** It did not need a disposable project: it happened for real against the deployed one. Ciphertext unreadable, application failed safely, no plaintext recovery claimed, affected user must re-enter. `BYOK_DEPLOYED_ACCEPTANCE.md` §5.
  - [ ] **BYOK.6 remaining, blocked on the two remediation steps:** the two-user isolation matrix with **real** credentials, concurrent rotation, the desktop/Pixel 7 Settings journeys in both locales, validation-key revocation evidence, the remote smoke, and the final BYOK report. Each needs a provider-accepted key used as a **product credential**, and `BYOK_VALIDATION_OPENAI_API_KEY` is explicitly not one.
  - [ ] **BYOK.6's one remaining code deliverable:** the bounded two-key master-key rotation window — a previous-key read, a per-row `key_version` bump, a completion counter, and a 30-day expiry rule. Still not attempted, and now for a sharper reason: building an unproven recovery mechanism on top of a deployment whose credential path is currently broken is the failure `ADR-069` exists to prevent. Until it exists the only rotation available is invalidate-and-ask — right for a compromise, wrong for hygiene.
  - [x] **Inherited open question — CLOSED** (`ADR-072`). The census found zero runtime consumers of `OPENAI_API_KEY`, so the name left `.env.example` and `README.md`, a guard now fails the build if either offers it again, and the allowlist was re-pinned at four entries with a new `operator-verification-script` classification.
  - [ ] **`BYOK_VALIDATION_OPENAI_API_KEY` — owner revocation is RECOMMENDED.** G-0.4's lane has been executed and no remaining acceptance item needs it. It is still referenced by `npm run test:byok:validation`, an opt-in lane that stays, so the **name** is kept; the **key** should be revoked at platform.openai.com unless the lane is to be re-run. Revoking an external key is outside the implementer's administrative boundary and is not attempted here.
- [ ] **Repository maintenance — a CI flake, recorded rather than re-run silently.** `task-candidate-form.test.tsx > "keeps an unselected pending candidate after partial success and reconciles removed state"` failed once on PR #71's first CI run with *"Received element is checked"*, then passed on re-run and passes 3/3 locally. The branch it failed on touches nothing under `src/features/tasks/`. This is the **third** recorded timing failure in this one file (two earlier race fixes are in its history), which is enough to treat the reconciliation assertion as genuinely racy rather than unlucky: it asserts checkbox state immediately after a `rerender` that shrinks the candidate list, and the effect that filters `selected` reconciles on a later commit. Worth an `await waitFor` on the assertion, or a deterministic reconciliation, before it fails on a branch where somebody believes it.

- [x] **Signup Hardening — PLANNED, and the package is APPROVED (2026-08-04, `ADR-077`)**, per `ADR-068`, which BYOK's close unblocks. The planning package was merged approved-pending-owner on 2026-08-02; the owner approved it in full two days later — the five documents, ADR-073…ADR-076 (now Accepted), the eight-migration budget, the SH.0–SH.7 sequence, the PRD §20 value sheet as proposed, and the §7 retention schedule as proposed. Package: `docs/initiatives/signup-hardening/SIGNUP_HARDENING_PRD.md`, `docs/initiatives/signup-hardening/SIGNUP_HARDENING_IMPLEMENTATION_PLAN.md`, `docs/reports/signup-hardening/SIGNUP_HARDENING_FINDINGS.md`, `docs/reports/signup-hardening/SIGNUP_HARDENING_THREAT_MODEL.md`, `docs/reports/signup-hardening/SIGNUP_ROLLOUT_GATE_DEFINITION.md`.
  - **Scope measured, not assumed** (four-agent census): row-level cascade is already complete; the deletion gaps are **storage objects** (the six orphans below) and the **untested** 43 composite `NO ACTION` FKs; **no lifecycle state exists**, so deletion and suspension share one foundation; `service_role` still holds DML on the two BYOK tables; retention is absent except BYOK's prune.
  - **`SH-*` requirement namespace** (not a phase prefix — would trip `ADR-067`); 16 families, ~120 requirements, 8-migration budget, slices SH.0–SH.7; deletion/suspension/rollout never share a PR.
  - **Tiered pre-code gates** (`ADR-073`): all-implementation (cascade drill green in CI, census, boundary inventory, owner-signed quota/retention values), deployment-only (hosted-config readback, backup verified, disposable accounts, hosting/SMTP decision), public-signup-only (the rollout gate). **Owner actions needed**: sign the PRD §20 quota/retention value sheet; verify a restorable backup; provision disposable accounts; the hosted-config readbacks and CAPTCHA/SMTP decisions.
  - **The six orphaned 2026-07-16 storage objects** (recorded above) are `SH-DELETE-015` — manifested by the scanner, then deleted as a separate owner-authorized irreversible step.
  - **`disable_signup` stays `true`;** the initiative delivers the rollout gate, not the flip. Phase 2G stays unauthorised until it closes.
- [~] **Signup Hardening implementation — IN PROGRESS since 2026-08-04** (`ADR-077` supplied the owner approval the previous line named as the precondition). The three original prerequisites — **account deletion**, **admin suspension**, **terms and privacy policy** — remain absent until their slices (SH.2, SH.3, SH.4) deliver them.
  - [~] **SH.0 — census, contracts, pre-code gates (0 migrations).** The cascade drill (`supabase/tests/signup_hardening_cascade_drill.sql`, SH-DELETE-001: row-complete two-account fixture over every runtime-enumerated user-owned table, `delete from auth.users`, zero residue, row-complete negative control) and the grant-census skeleton (`supabase/tests/signup_hardening_grant_census.sql`, SH-EXPOSURE-002: anon-zero, the service_role revoke carve-out pinned both directions on grant presence, forced-RLS census, the F-18/F-19 exposures pinned by name; its first cut pinned the hosted platform-default posture and CI refused it — recorded in the acceptance report). Owner approval recorded append-only (`ADR-077`, PRD `P-1`, plan `A-1`). Gate SH-G0.2 is proven by this slice's own CI `database` job.
  - [~] **SH.1 — account lifecycle foundation** (2 migrations: `202608040070`/`202608040071`, head moves to `202608040071`). `account_lifecycle` + state machine + unconditional audit + seed/backfill; the predicate in capture, reprocessing, all three claim paths and the heartbeat; the `requireUser`/`assertActiveAccount` gate over every page and all 19 inline-auth action sites; the `/account-state` surface; SH-EXPOSURE-004 executed and the census F-19 pin flipped. In PR; acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_01_ACCEPTANCE.md`. **Deployment note:** apply both migrations to the hosted project before running updated app code against it — the fail-closed gate otherwise closes the product for everyone (recorded, SH-GD.1-gated).
  - [~] **SH.2 — account deletion** (1 migration: `202608040072`, head `202608040072`). Deletion log (de-identified, RPC-only), self-only request RPC, run-time owned-row census, the Edge Function executor (outside `src/`, resumable, stop-on-unknown, exact-prefix storage), the request surface with provider-validated re-auth and typed confirmation, the storage-orphan scanner, the capability guard. In PR; acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_02_ACCEPTANCE.md`.
  - [x] **SH-DELETE-015 — CLOSED 2026-08-04.** Owner authorized; the six orphans were re-verified immediately before the act (structural re-classification, no live owner, no referencing row, measured set equal to the committed manifest in both directions) and removed. Scanner now reports **zero objects of every class**; both protected accounts verified untouched. The tool is single-use by construction. **No restore drill was performed** — the owner recorded the backup as an attestation and declined fabricated evidence; that stays a rollout-gate limitation for SH.7 residual risk. `docs/reports/signup-hardening/SH_DELETE_015_ORPHAN_MANIFEST.md` §6.
  - [~] **SH.3 — suspension and the administrative boundary** (1 migration: `202608040073`, head `202608040073`; four of eight spent). `service_role`-only DEFINER transitions (`suspend_account`, `reactivate_account`, `begin_account_deletion_admin`) with per-verb closed reason sets and the unconditional audit write; the `admin_account_lifecycle_status` readback; `defer_job_for_inactive_owner` (a deferral, not a failure); one shared worker gate wired into both handlers, closing SH-WORKER-001 and SH-WORKER-002; the no-retroactive-reminder-burst predicate (ADR-078); the operator CLI (`npm run account:lifecycle`, dry-run default) pinned as the only executable caller in both directions; the suspended surface's public reason labels. In PR; acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_03_ACCEPTANCE.md`; runbook: `docs/reports/signup-hardening/SIGNUP_HARDENING_ADMIN_RUNBOOK.md` (**written, not drilled**).
  - [x] **SH.3's deployed half — EXECUTED 2026-08-04.** Hosted parity reached `202608040074` and `e2e/online-account-suspension.spec.ts` ran green: SH-WORKER-004 (two ticks skipped, job stays `pending` with no error and **no attempt spent**, then claimable after reactivation with no re-enqueue), SH-WORKER-005 (no heartbeat run, no notification), SH-ADMIN-005 (`banned_until` read back, fresh sign-in refused, ban lifted as its own step), and the authenticated suspended-surface journey over four routes. The runbook is now **3 of 4 drilled**; administrative deletion-start is left undrilled deliberately. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_DEPLOYED_ACCEPTANCE.md` §3.
  - [ ] **The lifecycle predicate does not cover every `authenticated`-executable RPC** — found by SH.3's adversarial review, verified rather than assumed. SH.1 wired exactly the set the PRD names (capture, reprocessing, the three claim paths, the heartbeat); `confirm_entry_task_candidates_v6`, `resolve_pending_question_v3`, `create_task_command`, `apply_reminder_command_v1`, `correct_entry_interpretation` and `undo_operation` carry none, so a suspended account holding a valid JWT could reach them directly through PostgREST. Partial mitigation today: such an account can neither capture nor have any job executed, so nothing reachable this way causes AI spend or queued work. **Destination: SH-EXPOSURE-007's re-census (SH.7) and SH-EXPOSURE-002's full matrix (SH.6)** — recorded here so the re-census meets a written expectation rather than discovering it. Not fixed inside SH.3's migration because extending the predicate to six-plus further functions is exactly the silent scope growth `ADR-071` exists to stop. `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_03_ACCEPTANCE.md` §3.9.
  - [x] **SH.4 — Terms, Privacy and versioned consent — CLOSED** (PR #78, merge `66b2648`, merge-SHA CI green on all three; 1 migration: `202608040074`, head `202608040074`; five of eight spent). `policy_acceptances` (forced RLS, select-own and insert-own only, no UPDATE/DELETE policy at all); the current version stated in SQL as well as TypeScript and pinned both ways (ADR-079) because the client INSERT that SH-LEGAL-005 requires is a real door; `record_policy_acceptance` takes no version parameter; both drafts in both locales with the review banner and four unfilled owner placeholders; the retention section generated from `retention.ts` with an honest warning while any window has no active sweep; public legal routes; a schema-enforced signup consent control; one interposition covering first-session and version-bump alike; a real decline path. Acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_04_ACCEPTANCE.md`.
  - [ ] **SH.4's owner gates — values, not blockers.** `OPERATOR_ENTITY`, `OPERATOR_CONTACT`, `GOVERNING_LAW` and `JURISDICTION` render as visibly unfilled markers in both documents; supplying them is an owner action, and removing the professional-legal-review banner is a named rollout gate (SH-LEGAL-013).
  - [ ] **The retention warning is load-bearing until SH.6 DEPLOYS.** SH.6 built all seven sweeps and their twins; the flags stay `false` until the migration reaches the hosted project, because a flag that says "enforced" must mean enforced for the people reading the policy, and announcing it at merge would be false for exactly the window in which nobody is looking. A new assertion pins that every unenforced window nonetheless has a sweep *and* a twin built for it, so "not implemented" and "implemented, not yet deployed" stay distinguishable. Original note: The Privacy Policy states the declared windows *and* says plainly that the automated sweeps are not running yet. SH.6 flips `sweepActive` in `src/features/legal/retention.ts` per class and the warning disappears by construction; a test asserts it is present exactly while some window is unenforced.
  - [ ] **SH.5 — signup/CAPTCHA/throttle** (1 migration, **SPENT 2026-08-04 as `202608040075`; six of eight**), **SH.6 — quotas/retention/exposure** (2), **SH.7 — rollout gates** (0). The two remaining migrations are SH.6's; SH.5 has none left, so its outstanding wiring must fit the mechanism already shipped.
  - [~] **SH.5 — application half DELIVERED, database half BLOCKED.** Built with no migration: the app-level signup gate defaulting closed, honest `signup-disabled` copy in both locales, the configured application origin replacing request-`Origin` trust, and a `config.toml` that can no longer open signup (it carried `enable_signup = true`, and `config push` sends the file wholesale — the standard command for any hosted Auth change would have opened public signup as a side effect). **Not built:** database-locked throttles + migration `202608040075`, `auth_event_attempts` + retention, concurrency boundary tests, confirmation resend, enumeration-uniform outcomes, Turnstile widget/token plumbing, session-fixation evidence, deployed probes.
  - [x] **SH-GD.1 hosted readback — CLOSED 2026-08-04 (PR #83).** Full 242-field readback with the owner-supplied PAT. Provider ceilings recorded: `rate_limit_email_sent = 2/hour` (binding; the *default* Supabase SMTP value, so it changes when Resend lands), `rate_limit_verify` 30/5min, `rate_limit_otp` 30/5min, `rate_limit_token_refresh` 150/5min. Applied `site_url` + a 12-URL enumerated allow list by **Management API PATCH** (2 changed, 0 unintended across 242) rather than `config push`. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_HOSTED_AUTH_READBACK.md`.
  - [ ] **The redirect allow list is correct BY CONSTRUCTION and unverified BY OBSERVATION.** Exercising it needs a delivered email, which needs SMTP that does not exist. Verification step is written down; do not record it as verified until executed.
  - [x] **APP_ORIGIN — set by the owner and VERIFIED AT THE PROVIDER, 2026-08-04.** Not accepted on report: a green recovery is consistent with a wrong origin, because `resolveConfiguredOrigin` accepts any https origin *and* localhost, and localhost is allow-listed for the online suite. Vercel's CLI here is unauthenticated, so the value was read where it lands — the hosted auth log records the deployment's own `/recover` with `redirect_to = https://my-brain-dusky.vercel.app/pt-BR/auth/callback?next=%2Fpt-BR%2Fauth%2Freset`. Recovery now returns `recovery-sent` with zero 5xx. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_ORIGIN_VERIFICATION.md`.
  - [x] **The redirect allow list is verified by observation — and it never needed SMTP.** `admin/generate_link` composes the link without sending it: an attacker host and a Vercel preview host were both offered and both rewritten to `site_url`. **Enforcement is silent rewriting, not refusal** — a disallowed `redirect_to` returns 200 — so `site_url` is the real backstop and a mis-set `APP_ORIGIN` fails invisibly. Re-run after any deployment environment change.
  - [~] **SH.5 — repository work COMPLETE; migration `202608040075` APPLIED to hosted 2026-08-05; stops at one owner action.** The throttle is wired to signup, recovery, sign-in and the new resend surface with admission before every provider call; ceilings hold under genuine concurrency against the deployed project (10 racers → exactly 3; 20 → exactly 5); Turnstile plumbing ships on four forms. **SH-CAPTCHA-002 is NOT claimed** — hosted CAPTCHA is off and the probes have not run. Evidence: `SIGNUP_HARDENING_SLICE_05_ACCEPTANCE.md`, `SIGNUP_HARDENING_MIGRATION_075_DEPLOYMENT.md`. Still open inside SH.5: the hosted password policy (SH-SIGNUP-007, owner dashboard), confirmation-required's behavioural half (needs SMTP), and SH-SIGNUP-011's **unmeasured timing residual**, carried to the rollout gate.
  - [x] ~~**SH.5 — migration SPENT (six of eight), mechanism BUILT, wiring NOT.**~~ *(superseded by the row above, 2026-08-05.)* `202608040075` delivers `auth_event_attempts` (hash-only, forced RLS, explicit deny-all policy, no table grants), `claim_auth_event_slot`/`finalize_auth_event_attempt` under advisory locks, the SQL ceiling cap, and the scheduler-only 30-day sweep; **ADR-080** records why `anon` holds `execute` (first in this database) and why ceilings are required parameters — which is what let the migration proceed while the SMTP-derived number stays pending. 46 pgTAP assertions; the policy layer, its two-directional SQL parity and SH-SIGNUP-010's guard-of-the-guard ship with it. **Still missing, and SH.5 cannot close without them:** the Server Actions do not call the throttle (so `SH-THROTTLE-003` and the enumeration-uniform refusal are not in force), no confirmation-resend surface (`SH-SIGNUP-008`), no Turnstile widget or token plumbing (`SH-CAPTCHA-003/004`), no session-fixation pins (`SH-SIGNUP-012`), `database.types.ts` not regenerated for the two new RPCs, genuine-concurrency script not written, no acceptance report.
  - [ ] **Owner: configure Resend custom SMTP.** Needs a sending domain, DNS and credentials that do not exist. Until then `rate_limit_email_sent = 2/hour` is a default-SMTP artefact, the `resend` ceiling stays marked `provisional` in `throttle-policy.ts`, and no delivered-link journey can run. **When it lands:** re-run `node scripts/hosted-auth-config.mjs --diff`, then revisit `AUTH_EVENT_CEILINGS.resend` and `.recovery` together and delete the provisional marker plus its assertion. No migration is needed — the ceilings are parameters.
  - [x] ~~**Owner: enable Turnstile**~~ — **DONE 2026-08-05, and SH-CAPTCHA-002 is SATISFIED.** Every direct-API bypass shape is refused `400 captcha_failed`; the refusal is identical for known and unknown addresses; the app surfaces `?error=captcha-failed` distinctly. Evidence: `scripts/sh5-captcha-enforcement-probes.mjs`, `SIGNUP_HARDENING_SLICE_05_ACCEPTANCE.md` §5.
  - [x] ~~**Owner: sign in ONCE**~~ — **DONE 2026-08-05.** Widget rendered and completed, credentials accepted, session created, product reached, no `captcha-failed`, no loop. Probes 3 and 5 recorded **executed and passed** interactively; the earlier SKIPPED record is preserved and appended to, not rewritten (`SIGNUP_HARDENING_SLICE_05_ACCEPTANCE.md` §5c).
  - [x] **SH-SIGNUP-007 — hosted password policy DONE 2026-08-05.** `6 → 12` plus four character classes by targeted PATCH; 242 fields compared, exactly 2 changed, 0 unintended, twelve postconditions held. Verified behaviourally 6/6 on a disposable account (`scripts/sh5-password-policy-probe.mjs`), which was then deleted and re-read `404`. Evidence: §5d.
  - [ ] ~~superseded~~ **Owner: sign in ONCE at `https://my-brain-dusky.vercel.app/pt-BR/auth/login` — the last unproven thing.** Probes 3 and 5 need a valid token and are recorded SKIPPED, because Turnstile refuses automated browsers by design. The auth log cannot settle it either: the three `grant_type=password → 200` at 15:59–16:00 UTC predate the earliest provably post-enablement refusal (16:11), and the audit-log endpoint is 404 on this plan. A successful sign-in producing a post-16:11 `grant_type=password → 200` closes it. **If sign-in fails, auth is refused for everyone — disable CAPTCHA protection in the Supabase dashboard immediately; that is an outage, not a control.**
  - [x] ~~**Owner: FIX THE TURNSTILE WIDGET'S HOSTNAME FIRST — the configured site key does not work on this domain.**~~ **WITHDRAWN — this diagnosis was wrong.** The control (Cloudflare's always-passes key) passes *regardless of client* and so never controlled for bot detection. The site key was fine; Turnstile was simply refusing an automated browser. Kept visible rather than deleted, because it was published as a finding and acted on. Proven by discriminating experiment on the live page, in one browser session, varying only the key: Cloudflare's always-passes key `1x00000000000000000000AA` returned a **real 21-char token**, the always-blocks key returned **`error-callback` code `600010`**, and the configured key `0x4AAAAAAEHELS4NcaQk8Whr` **timed out with zero iframes and no callback at all**. So the environment can complete a challenge and error callbacks do propagate — the real key simply never starts. **Action:** Cloudflare dashboard → Turnstile → this widget → confirm **Hostnames** contains `my-brain-dusky.vercel.app` and that the displayed site key matches `NEXT_PUBLIC_TURNSTILE_SITE_KEY`. The site key is public; the secret is not needed for this check. Then reload an auth page and confirm the widget visibly completes. **Do not enable hosted CAPTCHA until it does** — with no obtainable token, enforcement refuses every sign-in, signup, recovery and resend, and the owner account that would switch it back off is one of the locked-out ones.
  - [ ] **Owner: enable Turnstile in hosted Auth — the ONLY thing standing between SH.5 and its close, and strictly after the check above.** The application plumbing is built, merged and deployed; a deployed CSP defect that would have caused exactly that lockout was found and fixed first (PR #86). The exact configuration point: Supabase Dashboard → **Authentication → Attack Protection → CAPTCHA protection** — enable it, set **Provider = Turnstile** (it is currently `hcaptcha`, against ADR-076), and paste the Turnstile **secret key**; the matching **site key** is public and belongs in the deployment as `NEXT_PUBLIC_TURNSTILE_SITE_KEY`. Equivalent fields for a readback: `security_captcha_enabled`, `security_captcha_provider`, `security_captcha_secret`. Do not paste either value into a chat or a commit. **SH-CAPTCHA-002 stays unclaimed until the missing-token, invalid-token and valid-token probes have run against the deployment** — provider enforcement is not implied by the widget existing.
  - [x] **SH.6 — CLOSED AND DEPLOYED 2026-08-05** (2 migrations, `202608050076`/`202608050077`; the eight-migration budget is fully spent and was not exceeded). Merged at `cf540c7` and `6228a88`, merge-SHA CI green ×3 on both, branches preserved, hosted parity `202608050077`. Quota ceilings proven under genuine concurrency on the deployed database (60 racers, ceiling 50, exactly 50 stored); deployed acceptance lane 11/11 with zero residue; retention dry-run 0 eligible in all seven classes; `service_role` refused on both BYOK tables with rotation proven still working; `heartbeat` Edge Function undeployed and reading back 404. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_SH6_DEPLOYMENT.md`, `SIGNUP_HARDENING_SLICE_06_ACCEPTANCE.md`. **The SH.3 direct-PostgREST lifecycle residual on the six named RPCs is NOT closed here** — the matrix pins grants, and those six are granted deliberately rather than by drift. It stays open for SH.7's re-census.
  - [x] **SH.7 — CLOSED 2026-08-05, 0 migrations. The initiative is COMPLETE.** The fail-closed rollout gate (`npm run rollout:verify`) reads **25 pass · 3 fail · 2 owner-signature** against the deployed project and refuses to open signup. The traceability generator accounts for all **137** requirements (131 delivered, 3 deferred with a destination, 3 not delivered and named) and fails rather than print an unresolved claim. Re-census: zero user-owned tables missing forced RLS, zero `anon` table grants, six pinned zero-`service_role` tables; the one hosted delta is eight pre-existing trigger functions whose grants are unreachable through PostgREST, measured not assumed. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_FINAL_REPORT.md`.
  - [ ] **The four owner actions that stand between here and open signup.** (1) Configure custom SMTP — closes RG-DEP-1 and the three SMTP-deferred SH.5 requirements. (2) Restore a backup to a disposable project and record it — closes RG-DEP-3. (3) Sign the professional legal review — RG-LEG-4. (4) Sign monitoring/incident adequacy — RG-DEP-4. Then one green `rollout:verify`, the owner flip, and a second green run.
  - [ ] **SH.6's one optional owner action: enable the retention schedule.** `npm run sh6:retention-dry-run` then `npm run sh6:retention-schedule -- --enable`. **Enabling IS the authorization of the first live purge**; purged rows are unrecoverable. Nothing else about SH.6 waits on anybody.
  - [ ] **NOT AUTHORIZED / NOT EXECUTED: the first live retention purge against production.** Approving the schedule was not approving a purge (amendment P-1). The seven destructive sweeps are executable by no role at all, `service_role` included — **and after ADR-082 nothing schedules them either**. Applying `202608050077` had scheduled all seven; the five new ones plus a duplicate were removed the same day, leaving SH.5's and BYOK's authorized sweeps untouched. A non-zero dry-run count is information, never authorization. This is a deliberate stop, not unfinished implementation.
  - [~] **Deployment-tier owner gates.** SH-GD.3 is **CLOSED by construction** — the three acceptance journeys admin-create and remove their own disposable accounts, so it needs no owner action again. SH-GD.1 is **partly closed**: the public GoTrue settings were read back (`disable_signup: true`, `mailer_autoconfirm: false`, email the only provider), but the **rate limits**, redirect allowlist, `site_url` and password policy are Management-API-only and still open — and the rate limits are what block SH.5. SH-GD.2 restorable-backup proof is an owner **attestation**, unverified from this side, and it gates the orphan deletion. SH-GD.4 hosting/SMTP decision is untouched.
  - **`disable_signup` stays `true` throughout; opening signup is post-initiative, owner-only, gated by one green fail-closed rollout run (SH-ROLLOUT-005).**
- [x] **Phase 2G — AUTHORIZED 2026-08-05 (ADR-083), superseding this line's earlier UNAUTHORIZED state.** The definition study became the basis for the governing pair at `docs/initiatives/phase-2g/`; the active work is tracked in the Phase 2G section at the top of this file.
- [ ] **Public signup — BLOCKED.** Gated on BYOK implementation plus signup hardening. Three prerequisites do not exist at all: **account deletion**, **admin suspension**, **terms and privacy policy**.

The product UX/UI remediation ledger is unchanged by this entry: 35 findings, 30 RESOLVED, 4 RETAINED, 1 DEFERRED. Migration parity remains `202607310064`.

## Active milestone — Phase 2

- [x] Adopt permanent engineering standards in `ENGINEERING_STANDARDS.md`.
- [x] Reconcile existing capture, interpretation, tasks, questions, entity, dates, jobs, and AI capabilities in `PHASE_2_PLAN.md`.
- [x] Complete Phase 2A leased job reliability, recovery, observability, remote validation, documentation, and thematic commits.
- [x] Complete Phase 2B immutable interpretation revisions and the trust/entity-resolution foundation.
- [x] Complete Phase 2X — Product Convergence according to the approved architecture review, PRD, and implementation plan; official Slices 2X.1–2X.18 are complete.
- [x] Complete Phase 2C editable candidate tasks and transactional materialization according to `PHASE_2C_PRD.md` and `PHASE_2C_IMPLEMENTATION_PLAN.md` (Slices 2C.1–2C.6; split/merge sub-epic deferred as GitHub issue #8). See `docs/initiatives/phase-2c/PHASE_2C_REPORT.md`.
- [x] Complete Phase 2D conversational pending questions. Design approved (2026-07-23): `docs/initiatives/phase-2d/PHASE_2D_PRD.md`, `docs/initiatives/phase-2d/PHASE_2D_IMPLEMENTATION_PLAN.md` (six ordered slices 2D.1–2D.6), and ADR-033 (`Accepted`), with deterministic suggested answers (no AI schema change by default) and a single long-lived `resolve_pending_question_vN` RPC family. Slice 2D.1 (Traceable Answer Transition) is merged to `main` via PR #11 (migrations `202607230046`/`202607230047`, `resolve_pending_question_v1`; see `docs/reports/phase-2d/PHASE_2D_SLICE_01_REPORT.md`). Slice 2D.2 (Question Dispositions) is merged to `main` via PR #12 (additive migration `202607230048` adds `resolve_pending_question_v2` with the `deferred`/`dismissed`/`not_relevant` dispositions, deterministic snooze reactivation, and the content-free `question_resolved` event; see `docs/reports/phase-2d/PHASE_2D_SLICE_02_REPORT.md`). Slice 2D.3 (Suggested Answers and Source/Effect Preview) is implemented and gate-verified on branch `codex/phase-2d-slice-3` (base `400ce03`): a pure deterministic suggested-answer generator over the question's leading interrogative plus the entry's own owned domain context — no AI, extraction-schema, worker, provider, or Edge Function change and a safe empty fallback; a `server-only`, strictly non-mutating owner-scoped source projection and closed `none`/`reinterpret` effect preview that always states nothing has been applied yet; server-authenticated suggestion provenance recorded as the bounded `origin: typed|suggested` enum, with the closed `p_resolution` write shape and `resolve_pending_question_v1`/`_v2` untouched (ADR-033 reserves `_v3` for Slice 2D.4); and additive migration `202607230049` extending only the product-event allowlist (`question_effect_previewed`, the optional `origin` property, the `questions` surface), applied to the linked project with content-identical generated types. See `docs/reports/phase-2d/PHASE_2D_SLICE_03_REPORT.md` — not pushed, no PR, no app deployment. Slice 2D.4 (Confirmed Consequence / Reinterpretation) is implemented and gate-verified on branch `codex/phase-2d-slice-4` (base `d02477d`): additive migration `202607230050` adds `resolve_pending_question_v3` with the optional closed-enum `consequence` (`none`, `reinterpret`) on the `answer` kind, reusing the deployed `enqueue_entry_reprocessing`/worker path (idempotent per operation key, three distinct audit events, never applied implicitly), plus the `undo_operation` v3 branch that restores open and compensates the queued reprocess job — **and resolves the `2C-UNDO-004` hard gate by forward-fixing `undo_operation`'s own `40001` to `55P03`** (fail-closed `DO`-block assertion); the content-free boolean-by-existence `question_reinterpret_applied` event is allowlisted. Forward-only migration `202607230051` hardens `_v3` against a lock-ordering deadlock with a concurrent manual retry. The questions page gains an explicit **Answer and re-interpret** control (offered only when the read-only 2D.3 effect preview says reinterpretation is possible) that mutates nothing until an explicit **Confirm and re-interpret**. `_v1`/`_v2` untouched; applied to the linked project at parity through `202607230051` with content-identical types. See `docs/reports/phase-2d/PHASE_2D_SLICE_04_REPORT.md` — not pushed, no PR, no app deployment. Slice 2D.5 (Conversational Surfacing — Chat + Queue — and Cooldown) is implemented and gate-verified on branch `codex/phase-2d-slice-5` (base `218e56e`) with **no migration**: a pure deterministic `decideQuestionSurfacing` module (quiet hours/cap/rolling cooldown/override, mirroring the heartbeat and reusing the `notifications` ledger read-only as the shared nudge budget — pull-based, no new cron/channel/persistence, fails closed), a reusable `ConversationalQuestions` `region` panel that renders open questions as untrusted-data interactive elements resolvable through the unchanged `resolvePendingQuestion`/`undoQuestionResolution` contract (mounted proactive on Chat and pull on `?view=needs-you`, converging with `/questions` via the shared `actionablePendingQuestionFilter`), and content-free analytics reusing `needs_attention_viewed` on the `questions` surface. Verified: Vitest 902/902, ESLint/TS/build green, linked DB lint clean, authenticated Playwright desktop + Pixel 7 green (new Chat/queue convergence journey + 2D.1–2D.4 regressions). See `docs/reports/phase-2d/PHASE_2D_SLICE_05_REPORT.md`. Slice 2D.6 (Convergence and Closeout — no migration/RPC/product-source change) is implemented and gate-verified on branch `codex/phase-2d-slice-6` (base `62883af`): the convergence audit (single shared `actionablePendingQuestionFilter` predicate + `resolvePendingQuestion`/`undoQuestionResolution` contract, no product fix required), `scripts/generate-phase-2d-traceability.mjs` → the 69-row `docs/reports/phase-2d/PHASE_2D_TRACEABILITY_MATRIX.md` (fails closed on PRD drift), `scripts/verify-phase-2d-cleanup.mjs`, the deterministic fail-fast `test:remote:2d` aggregate, reconciled `SECURITY.md` (closing the `undo_operation` `40001` residual, resolved by migration `202607230050`), ADR-034, and the `PHASE_2D_SLICE_06_REPORT.md`/`PHASE_2D_REPORT.md` closeout reports. Phase 2D is closed. See `docs/initiatives/phase-2d/PHASE_2D_REPORT.md`.
- [x] Complete Phase 2E natural-language updates to existing tasks. Design approved (2026-07-25): `docs/initiatives/phase-2e/PHASE_2E_PRD.md` revision 2 - 122 requirements, 16 families, 8 epics, deterministic matching only (no embedding infrastructure; semantic retrieval deferred to Phase 2F with a whole-plan requirement in PRD SS22). **Slice 2E.1 (Epic 2E-A - bounded task command contract) is implemented, independently reviewed by five reviewers across nine dimensions, corrected and accepted** on branch `codex/phase-2e-natural-language-task-updates`: the action taxonomy as data, a declared bilingual temporal lexicon, a bilingual status/priority vocabulary, the closed command schema, the model contract in one importable module (ADR-039), `AIProvider.parseTaskCommand`, and migration `202607250055` (the `task_command` ledger operation literal). Contract only - no search, matching, mutation, RPC, UI, Server Action or product event. **Slices 2E.2 (Epic 2E-B, deterministic matching and margins) and 2E.3 (Epic 2E-C, disambiguation and read-only preview) are likewise implemented, independently reviewed, corrected and accepted.** **Slice 2E.4 (Epic 2E-D, reversible non-destructive updates) is implemented, reviewed across three rounds, corrected and accepted, CI-green on `bfa28a1` — the first RPC in this codebase that mutates an existing task (`public.apply_task_command`, migration `202607260058`), with thirteen of fifteen actions enabled and `cancel_task`/`restore_task` refused until Slice 2E.5. The third round — five lenses over the migration, the pgTAP suite and the TypeScript together, with per-finding refutation — found seven defects a migration-scoped round could not, including a Critical the slice report had recorded as a met requirement.** **Slice 2E.5 (Epic 2E-E, destructive actions and confirmation) is implemented, reviewed before and after coding, corrected and accepted: `cancel_task` and `restore_task` are enabled on the same RPC by `create or replace`, so all fifteen actions of PRD §11.2 now have one write path. `cancel_task` is gated on a server-issued single-use confirmation the database enforces (`public.task_command_confirmations`, writable by no client role); `restore_task` is gated on two collision guards. The design review, run before any code, found that enabling two actions required five hand-copied predicates to agree — two of which fail silently — and a second collision the PRD never named: a confirmed candidate writes no resolution-ledger row, so cancelling frees its candidate slot under `202607220040`'s partial indexes and a re-confirmed duplicate makes the later restore a bare `23505`. Both are now refused with declared codes. `2E_ACTION_NOT_ENABLED` is retired because nothing can raise it.** **Slice 2E.6 (Epic 2E-F, no-match activity creation) is implemented, reviewed, corrected and accepted:** a validated task-like no-match becomes one confirmed standalone inbox task through migration `202607270060`'s preview/confirmation/creation RPCs, which share the mutation contract's operation-key, fingerprint, audit and undo-registry primitives rather than reusing the entry-scoped materialization path. Created rows carry `created_by = 'agent'` and `status = 'inbox'`; undo cancels the exact created task and its live reminder without allowing later resurrection; exact replay returns the original identities, including after undo. A real two-session same-key PostgREST race runs in the CI database job. **Slice 2E.7 (Epic 2E-G, conversational and task-surface integration) is implemented, reviewed, corrected and accepted — and is the first slice of this phase with any user-visible behaviour.** A command console mounts on Chat (list and conversation) and on the task surface behind one Server-Action dispatcher; `/app/work/cancelled` is the cancelled-task recovery route, listed through `list_task_command_candidates` so 2E-DESTRUCTIVE-009's creation-undone guard is not bypassed; migration `202607280061` adds the `task_command` surface and four content-free event names to all three allowlists. The whole flow is deterministic because the clock is pinned once per command and every step re-derives from it (ADR-050); the destructive confirmation is issued by the round that renders the preview rather than the one that consumes it (ADR-051); and the dialog is hand-rolled because jsdom 29.1.1 has no `showModal` (verified by execution), which is what makes 2E-A11Y-004 assertable in the only accessibility gate CI runs. Nothing merged, nothing deployed; remote migration parity stays at `202607250054` while `202607250055`-`202607280061` are branch-only. See `docs/reports/phase-2e/PHASE_2E_SLICE_01_REPORT.md` through `PHASE_2E_SLICE_07_REPORT.md` and `docs/reports/phase-2e/PHASE_2E_PROGRESS.md`. **This sentence closed at Slice 2E.7 and is superseded by the paragraph below**, which records that Slice 2E.8 is accepted and that PR #18 is no longer a CI-evidence-only draft.

  **Slice 2E.8 (Epic 2E-H, convergence and closeout) is implemented and accepted, and Phase 2E is complete as a branch.** 118 of 122 requirements delivered; four are not, and `2E-MATCH-018` is delivered with a scope note. It adds no product behaviour and no migration; it delivers the fail-closed traceability generator and its 135-row matrix, the cleanup verifier, the aggregate remote smoke, this phase's final report with deployment/rollback/merge/release checklists, and the convergence audit. **The audit found a real defect seven slices had missed:** the 61-entry bilingual status/priority vocabulary sat outside the versioning regime, because the `policy-lock.test.ts` case named *"pins the status and priority vocabularies"* digested the closed **database literals** rather than the term mappings this codebase owns — so re-pointing `bloqueada` from `blocked` to `deferred` would have moved a user's task to a state they never named with the suite green. `TASK_VOCABULARY_VERSION` was orphaned and already a version stale; `vocabularyCoversEveryLiteral()` was exported and never called. All fixed. See `docs/reports/phase-2e/PHASE_2E_FINAL_REPORT.md`, `PHASE_2E_SLICE_08_REPORT.md` and `PHASE_2E_TRACEABILITY_MATRIX.md`. **Phase 2E is RELEASED.** PR #18 merged as `5d22400` (CI green on `96cf178`, run `30383879590`); the chain `202607250055`–`202607280061` is deployed and **remote parity is `202607280061`**; PR #20 merged as `0efcd82` to repair validation tooling the deployment exposed. **121 of 122 requirements delivered** — the three that were blocked on deployment authorization (`2E-OPERATIONS-003`, `2E-OPERATIONS-004`, `2E-OWNERSHIP-004`'s remote half) are now proven remotely, and only `2E-COMMAND-012` remains, reclassified to Phase 2F. Remote gates: aggregate smoke 23 assertions exit 0, cleanup exit 0 with zero residue, product-events exit 0, Phase 2E online journeys 12/12 on desktop and mobile in both locales. Tagged `phase-2e-complete`. This box is checked under the same rule Phase 2D's was: merged **and** deployed **and** released.

  Outstanding after Phase 2E, each with a named owner rather than argued away:
  - ~~**No deployment, and it gates everything else.**~~ **Discharged 2026-07-28.** The chain is deployed and remote parity is `202607280061`. 2E-OPERATIONS-003, 2E-OPERATIONS-004 and 2E-OWNERSHIP-004's remote half are delivered and proven by the aggregate smoke (23 assertions, exit 0) and the cleanup verifier (zero residue). The Phase 2E authenticated journeys pass 12/12 on desktop and mobile in both locales.
  - **New maintenance item, not a Phase 2E regression:** `e2e/intelligent-capture.spec.ts` does not pass deterministically online. It predates Phase 2E, depends on live OpenAI interpretation output, has no pre-deployment baseline (the suite is manual and CI never ran it), and its failures occur upstream of every Phase 2E mutation. Tracked in [issue #21](https://github.com/fabiokyrillos/my-brain/issues/21) for deterministic stabilization.
  - **`2E-COMMAND-012` is the one requirement Phase 2E did not deliver, and is reclassified to Phase 2F** by PRD revision 4 and is explicitly **not** delivered. Prompt and strategy versions are version-pinned build constants that travel on the command session, but no column persists them. Closing it means `drop function` plus a full re-declaration of `apply_task_command` (~1,460 lines), `create_task_command`, or `record_ai_usage` — the last being shared by every AI path in the product and pinned by two `::regprocedure` casts and a `has_function` array across two pgTAP files, hand-written types and the Deno worker. **Residual risk:** attributing a bad command to its prompt requires joining `ai_usage_events.created_at` to the deploy history instead of reading a column. ~~Phase 2F should close it alongside the `ai_usage_events` widening §22 already needs for task embeddings.~~ **Discharged 2026-07-30 by Slice 2F.6's convergence audit:** both halves of that sentence are now false. ADR-057 moved provenance **past** Phase 2F behind an executed-dry-run reopening gate, and the `ai_usage_events` widening for task embeddings is forbidden even at ADR-055's spike tier — so no future phase should read this as a Phase 2F obligation.
  - **`2E-MATCH-018`'s baseline is transcribed with its scope caveat attached** (one-step 0.429, ambiguous 0.214, no-match 0.214, confirmation-required 0.071, over 14 scenarios at policy `2026-07-25.3`). It measures the **scoring layer against declared SQL verdicts**, not end-to-end matching. ~~Any Phase 2F semantic comparison must use the same scope or re-measure; an end-to-end corpus needs a database.~~ **Discharged 2026-07-30:** `2F-MEASURE-007` re-measured end to end against the deployed contract through the real loader and the real scorer (ADR-059). Both baselines are published side by side and cross-scope comparison remains prohibited.
  - **The nineteen refuted PRD-round findings promised by PRD revision 2 are permanently unrecoverable.** They were never persisted to the repository. Revision 4 withdraws the promise rather than fabricating them.
  - **`src/features/tasks/task-candidate-form.test.tsx` is flaky under CI load.** A Phase 2C component test with no task-command references; it failed once on a docs-only commit and passes 3/3 locally. Not fixed in closeout because it has never reproduced locally, and a fix without a reproduction is a guess.
- [x] Complete Phase 2F — **One Write Path** — per the approved `docs/initiatives/phase-2f/PHASE_2F_PRD.md` Revision 4.3 (68 requirements, slices 2F.1–2F.6): Work-surface and manual-creation mutation convergence onto the deployed Phase 2E contracts, the scoped reminder-authoring exception, task grant revocation with the test-suite semantic migration, the measurement reader and semantic-retrieval evidence gate, and closeout. **This line previously described "retroactive history, mobile/localization/accessibility finish"** — that scope is displaced, not deleted, per ADR-056: retroactive history/date semantics/review invalidation/recurrence are unscheduled future work, gated on repairing review generation first (M11 below). **Slices 2F.1, 2F.2 and 2F.3 are accepted, merged and deployed.** 2F.1 via PR #23 → `8c59c1d` (no migration). 2F.2 (Work-surface mutation convergence) code-only, no migration. 2F.3 (manual creation convergence) via PR #26 → `48d6a83`, CI run `30467623925`, which applied `202607290062` and moved **remote parity `202607280061` → `202607290062`** with all 21 deployment-session gates passed. **Slice 2F.4 (task grant revocation and test-suite semantic migration) is accepted, merged and deployed** via PR #28 → `c174f8f`, CI run `30479818771`, which applied `202607300063` and moved **remote parity `202607290062` → `202607300063`** with all sixteen deployment-acceptance gates passed and no rollback required. Gate 9 needed a separate prerequisite correction of two stale Slice 2E.1 assertions in `scripts/remote-supabase-smoke.mjs` (PR #29 → `ba63204`), which changed no production semantics. See `docs/reports/phase-2f/PHASE_2F_SLICE_04_ACCEPTANCE.md`. The PRD is at **Revision 4.3**: Revision 4.2 corrected three facts found by 2F.4's pre-code inventory (owner decisions A1–A3), and Revision 4.3 corrected three §10 gate-ledger cells found by Slice 2F.6's convergence sweep — including a 2F.4 authenticated-journeys cell that claimed an execution its sixteen acceptance gates do not record. **None of the three revisions changed a requirement.** **Slice 2F.5 (measurement reader and evidence gate) is accepted and merged** via PR #31 → `2ae2606`, merge-SHA CI run `30506807423` green on all three jobs (this line cited `30506608871`, which is the branch-head run at `da1d2f3`; corrected by Slice 2F.6's audit), no migration, parity unchanged; ADR-055's expiry is dated **`2026-10-27`**. **Slice 2F.6 (convergence and closeout) is accepted and merged** via PR #33 → `7e3e5f0`, merge-SHA CI run `30520514810` green on all three jobs, no migration, parity unchanged — delivering the fail-closed traceability generator and its 68-row matrix, the phase-wide cleanup verifier, the executed census stop-gate (buckets 1 and 2 zero), the whole-phase convergence audit and the documentation reconciliation. Its audit filed six defects and one contradiction, none smoothed, and recorded `2F-OPERATIONS-002` as **partial**: one merge SHA in the phase — `6628b02`, a documentation-only acceptance merge — never had a green `application` job, on two different pre-existing flakes. **Phase 2F is complete.** See `docs/reports/phase-2f/PHASE_2F_REPORT.md` and `docs/reports/phase-2f/PHASE_2F_SLICE_06_ACCEPTANCE.md`.
- [ ] **AI provenance (`2E-COMMAND-012`) is deferred past Phase 2F** (ADR-057; PRD 2F-DECISION-004). Reopening gate: execute `scripts/phase-2f-gate1-record-ai-usage-dry-run.sql` against a disposable hosted project (it refuses any database not explicitly marked disposable), capture the transcript to the owning phase's directory under `docs/reports/` (never its root), and only then plan the `record_ai_usage` drop-and-recreate in a future phase. Residual risk unchanged from ADR-053: bad-command attribution requires joining `ai_usage_events.created_at` to deploy history.
- [ ] **Semantic-retrieval evidence-gate expiry (ADR-055, ADR-060; PRD 2F-MEASURE-006):** at 90 days after the 2F.5 command-funnel reader goes live with neither ADR-055 tier met, record an ADR removing semantic retrieval from the active roadmap until a new demand signal appears. Go-live is the Slice 2F.5 merge date (ADR-060) and the reader computes the date itself via `expiryDateFromGoLive`. **Expiry date: 2026-10-27** — go-live 2026-07-29 (the Slice 2F.5 merge date, ADR-060) plus 90 days, computed by `expiryDateFromGoLive` (2F-OPERATIONS-006 verifies this entry is dated at closeout).
- [ ] **`src/features/task-commands/sql-reachability.test.ts` fails two cases on a Windows checkout.** Not a logic defect and not CI-visible: the test searches migration text with `\n`-anchored literals (`migration.indexOf("\n  select\n    r.id,")`, a `\n\s*order by\n` regex) while `core.autocrlf=true` gives the working copy CRLF; the probes match after CRLF normalisation. Proven pre-existing by reproducing with every Slice 2F.5 change stashed, and re-reproduced at the Slice 2F.6 baseline (2423/2425, only these two failing). Fix by normalising line endings where that file reads migration text. Left out of Slices 2F.5 and 2F.6 as unrelated cleanup. **Its CI citation is corrected here:** the green-on-Linux evidence at `6628b02` is run **`30497118489`**, whose log shows `sql-reachability.test.ts … 46 tests` passing. Run `30496790432`, cited before, is at `9c5345c` — a different SHA. Run `30497118489`'s `application` job nonetheless concluded `failure`, for the unrelated reason in the next item.
- [ ] **`src/features/agent/question-answer-form.test.tsx` is flaky under CI load.** Found by Slice 2F.6's convergence audit while verifying the citation above: at `6628b02` (run `30497118489`) the case *"runs the undo flow and returns the question to an editable state"* failed at `:162` on `expect(element).toHaveTextContent()`, with 129 of 130 files passing. It passes locally and passed on the two subsequent `main` runs (`2ae2606`, `918ab23`), so it is a timing-sensitive component assertion rather than a defect — the same class as the `task-candidate-form.test.tsx` flake below. **Not fixed:** it has never reproduced on demand, and a fix without a reproduction is a guess. Fix by making the assertion wait on the state transition instead of the render.

### Phase 2X completed slices

- [x] Approve `PHASE_2_ARCHITECTURE_REVIEW.md`, `PHASE_2X_PRD.md`, and `PHASE_2X_IMPLEMENTATION_PLAN.md`.
- [x] Create the reusable `reports/SLICE_REPORT_TEMPLATE.md` for 2X execution evidence.
- [x] Complete Slice 2X.1 — daily-cycle product contracts, typed copy, deterministic fail-closed lifecycle matrix, and architecture guardrails.
- [x] Complete Slice 2X.2 — private product-events foundation: migration `024`, dedicated RPCs, generated types, server-only best-effort boundary, acknowledgement action, pgTAP contract, remote smoke, and retention documentation.
- [x] Preserve Product Projections prework from `9f0c1e6` — immutable, serializable DTOs and pure fail-closed mappers for CaptureReceipt, Inbox, Needs Attention, and Work; it is not the official Slice 2X.3 and has no consumer.
- [x] Complete Slice 2X.3 — atomic entry capture and input jobs: migration `025`, bounded `interpret_entry` payload, authenticated capture/reprocessing enqueue RPCs, service-only claims, generated types, pgTAP contract, and disposable remote smoke. No UI, route, Server Action, Edge Function, worker, or dispatch was added.
- [x] Complete Slice 2X.4 — entry-interpretation worker and automatic dispatch: migrations `026` (service-role interpretation access, `pg_net`, scheduled dispatch cron) and `027` (fixes a Slice 2X.3 privilege regression that broke authenticated attachment-job inserts — see `DECISIONS.md` ADR-022); `process-jobs` split into `index.ts`/`dispatch.ts`/`attachment.ts`/`entry.ts`; direct and unattended-drain invocation both remotely verified. No UI, route, or Server Action switched to the async capture path.
- [x] Complete Slice 2X.5 — asynchronous capture cutover: `captureEntry`/`reprocessEntry` call `capture_entry_async`/`enqueue_entry_reprocessing` and return immediately (no migration); a `next/server` `after()` callback nudges the deployed worker and records best-effort product events; `QuickCaptureForm` shows an inline receipt, clears/refocuses for consecutive captures, and rotates its idempotency key; `agent/actions.ts` gained a generalized `retryProcessingJob` for entry jobs (no UI consumer yet); the now-unreachable synchronous Node extraction orchestrator was removed (see `DECISIONS.md` ADR-023). Remote entry-processing/jobs/product-events/full smokes and online Playwright (desktop+mobile) all passed after the cutover.
- [x] Complete Slice 2X.6 — human processing states in Inbox and Home: `src/features/daily-cycle/inbox-projection.ts` owner-scoped query feeds the Slice 2X.1 lifecycle mapper per entry (no migration); `InboxItemRow` renders the resulting DTO; `/inbox` and a new "Atividade recente" Home panel share the same projection and row component, so both surfaces agree on an entry's state. `recordOnly`/`hasConsistencyIssue` remain `false` until Slice 2X.7's `is_record_only` column exists (documented known limitation, not a regression).
- [x] Complete Slice 2X.7 — candidate provenance and safe task confirmation: migration `028` persists `entry_interpretations.is_record_only`, adds `tasks.source_interpretation_id`/`operation_key`, replaces the entry-wide unique constraint with an interpretation-scoped one, and adds `confirm_entry_task_candidates` (only confirms candidates from the entry's actual current interpretation, rejects `record-only`, idempotent by operation key); `confirm_entry_tasks` kept for compatibility with only its `ON CONFLICT` target adjusted. Exercising both as a real `authenticated` role against the linked project (not just pgTAP/`service_role`) surfaced and fixed two pre-existing defects unrelated to this slice's own logic — see `DECISIONS.md` ADR-025: `confirm_entry_tasks` was `SECURITY INVOKER` and had never worked for a real user (`permission denied` on the `entry_interpretations` lock and the `undo_operations`/`audit_logs` inserts), and any RPC raising SQLSTATE `40001` — including the already-shipped `correct_entry_interpretation` — hangs until gateway timeout on the linked project. Both `confirm_*` functions are now `SECURITY DEFINER`; the new RPC signals its version conflict with `55P03` instead of `40001`. `scripts/remote-daily-cycle-smoke.mjs` (new) was executed, not just written, against the linked project and passed.
- [x] Complete Slice 2X.8 — separated review and technical-details projections: `src/features/daily-cycle/review-projection.ts` and `technical-details-projection.ts` (no migration) each pair a pure, tested mapper with a thin `server-only` loader; `loadInterpretationReview` is now internal infrastructure consumed only by these two modules. `src/app/[locale]/app/inbox/[entryId]/page.tsx` loads exclusively through the two new loaders, no longer reads `entries.status`/raw Supabase rows, and derives its status badge/banners/retry visibility from `productState`/`availableActions`; a new `page.architecture.test.ts` enforces that import boundary. The two loaders load independently so a technical-details failure never blocks or misreports the primary review. `InterpretationRevisionEditor`/`TaskCandidateForm` are unchanged, fed from the review projection's editable/candidate fields.
- [x] Complete Slice 2X.9 — decision-first progressive-disclosure entry review (no migration): new `src/features/daily-cycle/entry-review.tsx` (`EntryReview`/`ReviewUnderstanding`/`ReviewAttention`/`ReviewNextActions`/`OriginalRecord`) and `src/features/daily-cycle/technical-details.tsx` (`TechnicalDetails`) reorganize `src/app/[locale]/app/inbox/[entryId]/page.tsx` into four always-visible blocks (understanding — now also rendering the DTO's `humanFields` — attention, next actions gated exclusively by `view.availableActions`, and the original record) plus one collapsed technical block (trust panel, immutable version history/comparisons, and structured extraction), degrading gracefully when the technical-details load fails. `InterpretationRevisionEditor` gained an optional `showSummary` prop to avoid repeating the summary already shown in the understanding block; `TaskCandidateForm` needed no change. No lifecycle/candidate/RPC logic changed. Offline Playwright and the full gate (323 unit tests, lint, typecheck, build) passed; the online authenticated journey (`e2e/intelligent-capture.spec.ts`) was updated line-for-line for the new collapsed-technical-details selectors but could not be re-executed — no `ONLINE_SUPABASE_*` credentials are configured on this workstation.
- [x] Complete Slice 2X.10 — Needs Attention query and projection, no UI consumer yet: migration `030` adds `list_needs_attention` (owner-scoped, SQL, `SECURITY DEFINER`, keyset-paginated) reimplementing `resolveDailyCycleLifecycle`'s five-reason precedence in SQL for bounded-scan pagination (XG-025), plus a supporting partial index; `src/features/daily-cycle/attention-projection.ts` (`loadAttentionProjection`) hydrates the RPC's rows into `NeedsAttentionItemView` via the existing Slice 2X.1 mapper, reusing `review-projection.ts`'s `attentionActionId` (now exported) so the queue's primary action always matches the entry-review page's own. `scripts/remote-daily-cycle-smoke.mjs` was executed (not just written) against the linked project and found a real defect: a `generate_series` alias named identically to `tasks.candidate_index` made the correlated `has_unconfirmed_candidate` check tautological, so confirming one of two current candidates incorrectly removed the entry from the queue entirely. Migration `031` (`create or replace function`, identical signature/grants/index) fixes the alias collision; see `DECISIONS.md` ADR-027. 340 unit tests, lint, typecheck, and production build passed; offline Playwright unchanged at baseline (this slice adds no route/UI). `supabase/tests/needs_attention_projection.sql` (35 pgTAP assertions, committed, not locally executable — same Docker gap as every other pgTAP file here) includes a dedicated regression case for the migration-031 defect.
- [x] Complete Slice 2X.11 — Needs Attention on Home and Caixa, no migration: `src/features/daily-cycle/needs-attention-item.tsx` (`NeedsAttentionItemRow`) is shared by Home's new "Precisa de você" panel (bounded preview of 3, honest `{count}{+ if hasNext}` badge, empty state, "Ver tudo" link) and Caixa's new `?view=needs-you` canonical filter (two-tab `InboxViewTabs`, unchanged default `all` view). Caixa's "load more" is this codebase's first client-driven, Server-Action-backed pagination: `src/features/daily-cycle/needs-attention-list.tsx` (client component) accumulates pages fetched through the new `loadMoreNeedsAttention` action (`src/features/daily-cycle/attention-actions.ts`), preserving already-loaded items and showing a localized error without auto-retry on a failed subsequent page, and disabling its button while in flight. `needs_attention_viewed`/`needs_attention_item_opened` are deliberately not emitted (see `DECISIONS.md`/report — that instrumentation belongs to Slice 2X.15's own file list, and no client-side emitter exists yet anywhere in this codebase). 357 unit tests (13 new), lint, typecheck, and production build passed; offline Playwright unchanged at baseline. The authenticated online journey was extended with a Needs Attention detour but could not be executed (no `ONLINE_SUPABASE_*` credentials on this workstation).
- [x] Complete Slice 2X.12 — canonical Work route and task projection, no migration: new server-only `loadWorkProjection` is explicitly owner-scoped, resolves the profile timezone with a safe fallback, applies deterministic `today`/`all`/`waiting` filters and page-based lookahead, and reuses `toWorkItemView` with fail-closed invalid-row handling. New `/{locale}/app/work` renders only product DTOs through `WorkView`/adapted `TaskList`, with localized human states/origins/dates, manual task creation, existing complete/wait/resume/reopen actions, accessible tabs and filter-preserving pagination. `/today`, `/tasks`, and `/waiting` redirect to the equivalent localized view/page; relevant task Actions revalidate Work in both locales. Full gate: 68 files/375 tests, lint, typecheck, build, offline Playwright desktop/mobile (6 passing/10 online skips), migration sync through `031`; online authenticated assertions authored but not run because `ONLINE_SUPABASE_*` is absent.
- [x] Complete Slice 2X.13 — converged primary navigation and Mais/More grouping, no migration: new pure `src/features/shell/capabilities.ts` classifies every authenticated route, keeps Jobs context-only, maps canonical/nested/alias paths to deterministic active destinations, and builds localized canonical links. Desktop and mobile now share Início/Home, Caixa/Inbox, Trabalho/Work, and Brain as primary; capture remains distinct, notifications remain global, and secondary destinations use the same five ordered groups in the desktop tree/mobile Mais. Locale switching preserves path/query; mobile DOM and visual tab order agree; Escape closes Mais and restores focus; touch targets remain at least 44 px; unobservable "Brain atento/ativo" claims were removed. Strict RED/GREEN produced 9 focused tests; the full suite passed 69 files/382 tests, lint, typecheck, build, offline Playwright desktop/mobile (6 passed/10 credential-gated skips), targeted authenticated online Playwright desktop/mobile in PT-BR and English (2 passed), and linked migration sync through `031`. See ADR-028 and `docs/reports/phase-2x/PHASE_2X_SLICE_13_REPORT.md`.

- [x] Complete Slice 2X.14 — visible promises aligned with real behavior, no migration: a static capability registry classifies operational, informational, advanced, and future capabilities; Home derives observable status from existing projections; Settings hides unconsumed schedules/autonomy/privacy/identity/model routes, preserves their stored values server-side, and exposes proven model routing/cost transparency under accessible progressive disclosure; Reviews uses an owner-scoped localized product projection and on-demand language; lifecycle copy now distinguishes saved, queued, organizing, retry, and complete. Strict TDD, 75 files/404 tests, lint, typecheck, build, offline Playwright (6 passed/10 expected skips), authenticated online desktop/mobile PT-BR/English journeys (4 passed), and linked migration sync through `031` passed. See `docs/initiatives/phase-2x/PHASE_2X_REPORT.md` and `docs/reports/phase-2x/PHASE_2X_SLICE_14_REPORT.md`.

- [x] Complete Slice 2X.15 — instrument all 17 version-1 daily-funnel events over the existing private ledger, with exact payload allowlists, deterministic domain/worker idempotency, confirmed-visibility session deduplication, owner-scoped subjects, fail-open analytics, safe internal conversion/latency queries, full authenticated remote coverage and desktop/mobile Playwright. No migration, RPC, remote infrastructure action or analytics consumer/dashboard; the local worker source was not deployed in that slice and was later deployed by Slice 2X.18. See ADR-029 and `docs/reports/phase-2x/PHASE_2X_SLICE_15_REPORT.md`.
- [x] Complete Slice 2X.16 — close the projection boundary across Home/Caixa/Work/review, no migration: Home's priority panel now reads `loadWorkProjection(..., { view: "today" })` instead of a raw, divergent `tasks` query (removing both the raw-row access and a second, incorrect "due" rule that ignored `FLOW-003`); Home's waiting-count/open-question panels read the new `loadHomeSupplementalProjection` instead of raw `tasks`/`pending_questions` queries. `TaskCandidateForm` now renders `ActionableCandidateView[]` only — no AI-extraction confidence score, no client-side re-filtering of a validity rule `review-projection.ts` already applies once. `src/features/interpretations/data.ts` is now guarded `server-only`. New `src/features/daily-cycle/architecture.test.ts` locks the no-raw-row/no-raw-enum/no-raw-score boundary across all five touched surfaces. Full gate: 80 files/443 tests, lint, typecheck, build, offline Playwright desktop/mobile (3 passed/5 online-gated skips each), authenticated online Playwright (12/16 passed — 2 unrelated pre-existing signup/recovery-email failures, likely rate-limiting from the smoke runs just executed, in a file this slice never touched), remote daily-cycle and product-events smoke both passed, linked migration sync confirmed through `031`. See `docs/reports/phase-2x/PHASE_2X_SLICE_16_REPORT.md`.
- [x] Complete Slice 2X.17 — converged daily journey E2E, no migration: `e2e/intelligent-capture.spec.ts` reorganized into deterministic per-contract scenarios across two `test.describe` blocks (13 named tests reorganizing the existing real journey unchanged, plus 3 new tests for basic pending question, recoverable retry, and terminal retry via deterministic direct-RPC fixtures, previously entirely uncovered) with added keyboard/focus/live-region/touch-target assertions. `e2e/foundation.spec.ts`, `e2e/online-mobile-navigation.spec.ts`, and `scripts/online-playwright.mjs` reviewed and confirmed sufficient by running them, left unchanged. Real execution found and fixed one in-scope defect: a duplicate retry button on the entry-detail page for `recoverable_error`/`terminal_error` entries with no prior successful interpretation (`page.tsx`, one-line conditional, no capability removed). Its historical auth/provider observations remain in the slice report. Full gate: 80 files/443 tests (unchanged — E2E-only slice), lint, typecheck, build, offline Playwright 3/3 both projects, authenticated online `intelligent-capture.spec.ts` 18/18 both projects, `online-mobile-navigation.spec.ts` 1/1 both projects, remote daily-cycle and product-events smoke both passed, linked migration sync confirmed through `031`. See `docs/reports/phase-2x/PHASE_2X_SLICE_17_REPORT.md`.
- [x] Complete Slice 2X.18 — remote gate and permanent closeout, no migration/product-source change: preserved the deployed v12 rollback bundle; deployed only the complete committed `process-jobs` function as v13; downloaded v13 and proved exact local runtime parity; added the fail-fast `test:remote:2x` aggregate, fail-closed cleanup verifier, safe shared-queue assertions, and a reproducible 283-row PRD traceability annex; passed jobs, interpretations, product-events, entry-processing, daily-cycle, baseline, 80-file/443-test local suite, lint, typecheck, build, desktop/mobile core E2E, migration/type sync, linked lint, and cleanup. Auth logs established the provider's new `email_address_invalid` rejection for the reserved E2E domain; signup remains an explicit external skip, while sign-in/profile and the independently generated recovery-link/password-reset core pass on both projects. Deno/Docker remain explicit unavailable gates. See ADR-030, `docs/initiatives/phase-2x/PHASE_2X_REPORT.md`, `docs/reports/phase-2x/PHASE_2X_SLICE_18_REPORT.md`, `docs/reports/phase-2x/PHASE_2X_SLICE_18_EVIDENCE.md`, and `docs/reports/phase-2x/PHASE_2X_TRACEABILITY_MATRIX.md`.

### Phase 2C approved plan — implementation in progress

Current publication state is authoritative in the header above: Slices 2C.1–2C.3 are merged to `main`. The detailed completed bullets below preserve their point-in-time execution evidence, including branch status at the moment each slice originally closed.

- [x] Approve `PHASE_2C_PRD.md` and `PHASE_2C_IMPLEMENTATION_PLAN.md`, reconcile the current roadmap, and preserve Phase 2X historical evidence unchanged.
- [x] Complete Slice 2C.1 — Editable Core Confirmation: transient edits to title, description, and due date only; versioned transactional RPC (`confirm_entry_task_candidates_v2`, migrations `202607190032`/`202607190033`); generated types; audit/undo; canonical-fingerprint idempotency; correction-vs-confirmation race guard; 579/579 unit tests, 0 lint/typecheck errors, production build green, migration/db-lint parity, and a live authenticated desktop+mobile Playwright journey all passed on branch `codex/phase-2c-editable-candidate-tasks` (not yet pushed/merged/deployed). Known gap: the PRD §14/plan Task 5 `candidate_edit_started`/`candidate_edit_reset` events and `task_candidates_confirmed`'s `editedCandidateCount`/`editedFieldCount` properties were not implemented — tracked below. See `docs/reports/phase-2c/PHASE_2C_SLICE_01_FINAL_ACCEPTANCE.md` and the per-slice `PHASE_2C_SLICE_01_*` reports.
- [x] Complete Issue #3 end-to-end (application layer + database enablement): extend `task_candidates_confirmed` with `editedCandidateCount`/`editedFieldCount` and add the `candidate_edit_started`/`candidate_edit_reset` product events (PRD §14, implementation-plan Task 5), content-free and fail-open. `candidate_edit_started` fires once per entry/candidate/tab-session only from a real field change (title/description/dueAt edit or explicit clear) — never from expand/collapse, rerender, or React Strict Mode remount; `candidate_edit_reset` fires only from the explicit "Restaurar sugestão"/"Reset to suggestion" action (never from "Remover descrição"/"Remover prazo" clear) with `editedFieldCount` computed from the canonical normalized edit payload, not raw touched-field state. `editedCandidateCount`/`editedFieldCount` on `task_candidates_confirmed` are derived server-side in `confirmEntryTasks` from the same validated canonical edits array sent to the RPC (a candidate counts as edited only if its canonical `changes` object is non-empty). Application layer: commit `b2cd44a` on branch `codex/phase-2c-editable-candidate-analytics`. Database enablement: additive migration `202607210034` extends the `product_events.event_name` CHECK constraint with `candidate_edit_started`/`candidate_edit_reset`, extends `private.validate_product_event_properties` with their allowlisted-property validation (`candidateCount` fixed at exactly 1; `editedFieldCount` bounded 1–3, the number of editable candidate fields), and adds a new `private.require_task_candidates_confirmed_edit_counts` helper enforcing `0 ≤ editedCandidateCount ≤ candidateCount` and `0 ≤ editedFieldCount ≤ editedCandidateCount × 3` (both new properties optional together, preserving legacy `{candidateCount}`-only callers; supplying exactly one of the pair is rejected). No RPC signature, task-confirmation, undo, or idempotency change; `confirm_entry_task_candidates(_v2)` untouched; generated types unaffected (function signature and column types unchanged). Verified: local/remote migration parity through `202607210035`, `db lint --level error` clean, a dedicated pgTAP file (`supabase/tests/editable_candidate_analytics_events.sql`, 29 assertions covering both new events, the extended `task_candidates_confirmed` cross-field bounds, legacy-payload compatibility, unknown-property/unknown-event rejection, cross-owner subject denial, RLS, and security-definer/search-path preservation) executed for real online via `npx supabase db query --linked -f ...` (Docker unavailable on this workstation, and `supabase test db --linked` itself requires Docker for its `pg_prove` container even against a remote target — `pgtap` was temporarily installed into the linked project's `extensions` schema to run the file directly, then removed; not committed as a migration), plus a real disposable-fixture remote persistence smoke (`npm run test:remote:product-events`) that asserts actual rows exist in `product_events` for all three analytics changes (not just that the RPC didn't error) plus one deliberately invalid payload rejected — all passed against the linked development project. 594/594 application tests, 0 lint/typecheck errors, production build green. Commits: `feat(db): enable editable candidate analytics events` and a third forward-fix `fix(db): reject product event payloads missing a required allowlisted property` (migration `202607210035` — the first online pgTAP run caught a genuine pre-existing bug in `require_product_event_integer`/`require_product_event_enum` from migration `202607170024`, unrelated to Issue #3's own new code, where a `NULL`-valued PL/pgSQL `IF` condition let a payload missing its required key silently pass; fixed and re-verified 29/29 online). Branch not pushed, no PR opened, not merged or deployed.
- [x] Complete Slice 2C.2 — Planning, Priority and No-Due Semantics: new versioned RPC `confirm_entry_task_candidates_v3` (migration `202607210036`, additive per ADR-031's versioning precedent; `confirm_entry_task_candidates_v2` unchanged and callable) lets the user edit `plannedAt`, `manualPriority`, `intentionalNoDue`, and `noDueReason` per candidate before confirmation, reusing the `tasks.planned_at`/`manual_priority`/`intentional_no_due`/`no_due_reason` columns (present since early Phase 2X migrations but never read/written by any application code — v2 hardcoded them to `null`/`false`). No AI suggestion exists for these four fields, so their immutable baseline is always the neutral state; "reset" means "clear." Database enforces due/no-due mutual consistency inside the RPC and, as defense in depth, via a new `tasks_no_due_consistency_check` table constraint; the Slice 2C.1 correction-race guard trigger is extended to also cover v3-materialized tasks. `candidate-editor.tsx` gained a planned-date field, priority select, and a no-due checkbox (auto-clears/disables due-date, reveals an optional reason field). No new analytics events needed (existing `candidate_edit_started`/`candidate_edit_reset`/`task_candidates_confirmed` are already generic per-field-count events). `work-projection.ts`/`TaskList` now select and display planned date, priority, and an intentional-no-due indicator. Running the new online Playwright journey for real caught a genuine pre-existing bug this slice's own new fields exposed: Issue #3's `private.require_task_candidates_confirmed_edit_counts` bounded `editedFieldCount` at `editedCandidateCount * 3` (correct only for 2C.1's 3 fields) — with 2C.2's 4 additional fields (7 total), a valid edit was rejected by the database's own analytics allowlist. Fixed by migration `202607210037` (bound now `* 7`); the one Issue #3 pgTAP assertion that assumed the old bound was corrected to match. Verified: local/remote migration parity through `202607210037`, `db lint --level error` clean, generated types regenerated and byte-identical, a 25-assertion pgTAP file executed for real online (`phase_2c_slice_2_planning_priority_no_due.sql`) plus a clean online re-run of the existing Issue #3 29-assertion file after the bound fix, an 18-case disposable-fixture remote smoke (extended from 13), and a live authenticated Playwright run (desktop + Pixel 7) editing every new field and verifying persistence/undo/audit. 622/622 unit tests (up from 594), 0 lint/typecheck errors, production build green. Merged to `main` via PR #5.
- [x] Complete Slice 2C.3 — Owned Relations: new versioned RPC `confirm_entry_task_candidates_v4` (migration `202607220038`, additive per ADR-031's versioning precedent; `confirm_entry_task_candidates_v3` unchanged and callable) lets the user attach owned project, context, person, and waiting-on relations to a candidate before confirmation, reusing the pre-existing `task_projects`/`task_contexts`/`task_people` junction tables per PRD `2C-RELATIONS-002` — not the dormant `tasks.waiting_on_person_id` scalar column, which stays untouched. `task_people.role='involved'` covers the generic person relation; `role='waiting_on'` covers waiting-on, one mechanism for both. No AI suggestion exists for any relation, so the immutable baseline is always the empty set; "reset" means "clear." Every relation ID is validated against `(user_id, id)` ownership across all candidates at once, and one invalid or cross-owner ID aborts the entire multi-candidate materialization atomically; relation arrays are sorted/deduplicated before entering the replay fingerprint. A new server-only projection, `src/features/tasks/relation-options.ts`, loads the user's own bounded (200-row) projects/contexts/people as plain `{id,label}` pairs, threaded through `review-projection.ts` to the entry-detail page and `TaskCandidateForm`. `candidate-editor.tsx` gained four native `<select multiple>` listboxes (project/context/person/waiting-on) with matching clear buttons, disabled when the user owns nothing of that kind. `work-projection.ts`/`TaskList` hydrate and display the four relation kinds via a bounded two-step flat-select join (no Supabase embedded-resource select), matching the existing projects/people detail-page pattern. No new analytics events needed (existing `candidate_edit_started`/`candidate_edit_reset`/`task_candidates_confirmed` are already generic per-field-count events; `editedFieldCount`'s ceiling grows from 7 to 11). Writing this slice's own pgTAP coverage (not assumed from a read-through) found two genuine defects: an operator-precedence bug (`->` and `||` share precedence and associate left-to-right in PostgreSQL) silently defeated the person/waiting-on cross-owner check — the composite foreign key still correctly rejected the resulting insert with `23503`, so no cross-owner row was ever persisted, but the intended `2C_INVALID_RELATION` rejection never fired; and `candidate_edit_reset`'s own `editedFieldCount` bound was still `[1,3]` from Phase 2C.1, never updated when Slice 2C.2 raised the real ceiling to 7. Both fixed by migration `202607220039` (forward-fix; `038` itself was left unedited once applied, per this project's append-only convention) and re-verified. Verified: local/remote migration parity through `202607220039`, `db lint --level warning` clean, generated types regenerated and byte-identical, a 29-assertion pgTAP file executed for real online (`phase_2c_slice_3_owned_relations.sql`), a 23-case disposable-fixture remote smoke (extended from 18), and a live authenticated Playwright run (desktop + Pixel 7) selecting real owned relations through the actual listbox controls and verifying materialization/audit. 653/653 unit tests (up from 622), 0 lint/typecheck errors, production build green. The real-AI-capture Playwright journey in `intelligent-capture.spec.ts` could not be exercised this session — deployed worker/OpenAI dispatch latency exceeded the test's timeout, an external dependency this slice does not touch (confirmed via `git status`: no change anywhere in `supabase/functions/` or capture/job code); the deterministic-fixture describe block in the same file (basic question, recoverable retry, terminal retry) passed in full. Branch `codex/phase-2c-slice-3`, not pushed, no PR, not merged/deployed.
- [x] Complete Slice 2C.4 — Candidate Dispositions without a candidate-draft table: exact outcomes `confirmed`/`rejected`/`retained`/`dismissed`; narrow owner-scoped resolution ledger; atomic `confirm_entry_task_candidates_v5`; tasks only for `confirmed`; entry-local history; Needs Attention/Inbox convergence; existing undo restores pending; generic confirmed analytics only, with no category/content; migrations `040`–`043`; generated linked types; pgTAP 290/290; remote smoke 24/24 with cleanup/pre-existing counts preserved; Playwright 4/4 across PT-BR/en desktop/Pixel 7; Vitest 693/693; lint/typecheck/build green; independent final review approved. Branch remains local only, with no push/PR/merge/application deployment. See `docs/reports/phase-2c/PHASE_2C_SLICE_04_REPORT.md`.
- [~] Slice 2C.5 — Subtasks, Dependencies and isolated Split/Merge: the **subtasks + dependencies** half is implemented on branch `codex/phase-2c-slice-5` (not pushed, no PR, not merged/deployed). New RPC `confirm_entry_task_candidates_v6` (migration `202607220044`, additive; `v5` and earlier unchanged) lets a user attach a `parentRef` and a bounded `dependsOn` array to each confirmed candidate, where each target is another confirmed candidate in the same batch or an existing owned non-cancelled task (closed discriminated union, resolved to task ids, never by label). Rejects self-reference, non-confirmed targets, cross-owner/cancelled targets, duplicate dependency targets, and direct/indirect cycles (intra-batch subgraph only, with a proof in the migration header); materializes tasks and their `parent_task_id`/`task_dependencies` edges atomically in a second pass so forward references resolve. Reuses `tasks.parent_task_id`/`task_dependencies` (composite-owner-FK'd since migration `016`). No new analytics event; edited-field ceiling 11→13. Migration `202607220045` forward-fixes an `undo_operation` `pg_catalog.greatest` lookup failure reintroduced by `044`. Verified: new 34-assertion pgTAP 34/34 online plus all relevant earlier candidate suites green (two stale analytics-bound assertions corrected), 29-case remote smoke 29/29 with fatal cleanup/preserved pre-existing counts, disposition Playwright 4/4 (PT-BR/en × desktop/Pixel 7) on the v6 path, 714/714 unit tests, 0 lint/typecheck errors, production build green, byte-stable generated types, independent review with no Critical/Important findings. See `docs/reports/phase-2c/PHASE_2C_SLICE_05_REPORT.md`.
- [ ] Complete the deferred **split/merge** sub-epic of Slice 2C.5 (`2C-STRUCTURE-004`, GitHub issue #8): isolated, independently reversible candidate split/merge. Deferred because the PRD/implementation plan specify it only structurally (an isolated boundary that cannot block the rest of 2C.5) with no field-mapping, command shape, or UX; needs a product decision defining the exact command/UX before implementation. Non-blocking for 2C.5's core parent/dependency outcome.
- [x] Complete Slice 2C.6 — Product Convergence and Closeout: convergence audit of the four daily surfaces plus the candidate form (Slice 2X.16 projection-boundary guardrails and 714/714 unit tests green — no product source changed); `scripts/generate-phase-2c-traceability.mjs` → the 83-row `docs/reports/phase-2c/PHASE_2C_TRACEABILITY_MATRIX.md` (fails closed on PRD-inventory drift; never marks the deferred `2C-STRUCTURE-004` or residual-risk `2C-UNDO-004` green); `scripts/verify-phase-2c-cleanup.mjs` (fail-closed across 13 owner-scoped tables); the deterministic fail-fast `test:remote:2c` aggregate (confirmation v2–v6, product events, cleanup) plus `test:remote:2c:confirmation`; and the `PHASE_2C_SLICE_06_REPORT.md`/`PHASE_2C_REPORT.md` closeout reports. Fixed one remote-evidence drift: the Phase 2X `remote-daily-cycle-smoke.mjs` concurrency-race assertion now matches the Slice 2C.4 disposition contract (one winner, one `P0001` conflict, one task). No migration/RPC/product-UI change. Verified: Vitest 714/714, lint/typecheck/build green, offline Playwright 6/48-skip, `test:remote:2c` green (confirmation 29/29, cleanup 0 residual), independent review clean. Branch `codex/phase-2c-slice-6`, not pushed. See ADR-032 and `docs/reports/phase-2c/PHASE_2C_SLICE_06_REPORT.md`.

### Resolved — hotfix outside the slice sequence (2026-07-18)

- [x] ~~`correct_entry_interpretation` (Phase 2B, already shipped) raises SQLSTATE `40001` for its version-conflict check~~ — fixed by migration `202607180029`, which keeps the RPC's exact signature and every non-conflict behavior and switches only the conflict SQLSTATE to `55P03` (matching `confirm_entry_task_candidates` and `begin_entry_reprocessing`). `src/features/interpretations/actions.ts` now keys off `error.code === "55P03"`. Proven with the authenticated remote smoke: ~530ms bounded response, no gateway hang, no partial write, current interpretation not overwritten by the losing correction. See `DECISIONS.md` ADR-026 and `SECURITY.md`.
- [x] ~~`undo_operation` still raises SQLSTATE `40001` for its own, distinct conflict (`'Cannot undo after a newer interpretation revision'`)~~ — **resolved** by Slice 2D.4 migration `202607230050`, which forward-fixes it to `55P03` (mirroring ADR-026) with a fail-closed `DO`-block structural assertion. The `2C-UNDO-004` hard gate is closed. See `SECURITY.md` and ADR-033/ADR-034.

### Resolved — F1 candidate-lifecycle hotfix outside the slice sequence (2026-07-18)

- [x] ~~The architecture review of Slices 2X.5–2X.8 (`docs/reports/phase-2x/PHASE_2X_SLICES_2X5_2X8_ARCHITECTURE_REVIEW.md`, finding F1) found `hasMaterializedTaskForCandidates` computed entry-wide ("any non-cancelled task exists for this entry") instead of interpretation/candidate-scoped in both `inbox-projection.ts` and `review-projection.ts`, so `productState` could report `ready` on Inbox/Home/entry-detail while a genuinely unconfirmed candidate from the current interpretation was still shown by `TaskCandidateForm`~~ — fixed by deriving that lifecycle input from the same interpretation-scoped source `actionableCandidates` already used correctly (`computeUnavailableCandidateIndexes`), via a new shared pure helper `hasUnconfirmedTaskCandidates` (`src/features/interpretations/data.ts`). No migration, RPC, or `lifecycle.ts` change was needed. See `docs/reports/phase-2x/PHASE_2X_CANDIDATE_LIFECYCLE_HOTFIX_REPORT.md`.

### Phase 2B evidence

- [x] Apply and synchronize append-only migrations `020` through `023` for lifecycle states, current-version ownership, immutable revisions, aliases, reprocessing leases, runtime fixes, and truthful volatility.
- [x] Deploy correction, initial interpretation failure, leased reprocessing, idempotency, optimistic concurrency, audit, and compensating undo RPC contracts.
- [x] Add deterministic trust scoring/policies and bounded owned entity ranking with exact/alias/history/context/temporal/margin evidence and explicit missing-signal handling.
- [x] Add the typed review DAL, shared extraction pipeline, safe Server Actions, localized editor, per-element trust/evidence, current pointer, immutable history/comparison, undo, and recovery controls.
- [x] Pass 39 Vitest files/147 tests, lint, typecheck, production build, linked desktop/mobile Playwright, migration synchronization, focused interpretation smoke, and complete remote Supabase smoke.
- [x] Keep the 44-assertion pgTAP contract committed while Docker is unavailable; record the two pre-existing heartbeat lint warnings separately from a Phase 2B-clean database lint result.

### Phase 2A evidence

- [x] Apply and synchronize migration `019` with leased claims, stale-worker protection, retry/backoff, terminal exhaustion, metrics, and a per-minute reaper.
- [x] Deploy `process-jobs` version 9 with authenticated ownership, 300-second lease, 120-second timeout, persisted-result reuse, and lease-validated terminal transitions.
- [x] Expose failed/exhausted jobs, attempts, retry window, sanitized state, and a backoff-gated retry action to the owning user.
- [x] Generate the linked Supabase TypeScript schema and use its `jobs` contract in the Phase 2A page.
- [x] Pass lint, typecheck, 29 Vitest files/93 tests, production build, linked desktop/mobile Playwright 2/2, migration sync, db lint, remote job reliability smoke, and complete remote smoke.
- [x] Keep Docker-backed pgTAP execution explicit as an external limitation; structural pgTAP is committed and equivalent remote behavior passed.

## Completed milestone — Sprint 1.5

### 1. Permanent project state

- [x] Create `STATE.md` with the current implementation/deployment distinction.
- [x] Create `DECISIONS.md` with the accepted architecture and process decisions.
- [x] Create `CHANGELOG.md` with the Phase 1 baseline and Sprint 1.5 section.
- [x] Create `TODO.md` as the prioritized backlog.

### 2. Critical foundation fixes

- [x] Complete password recovery code: validated request, callback/code exchange, reset form, password update, safe redirect, localized errors, and E2E specification.
- [x] Validate signup server-side with Zod, strong password policy, password confirmation, normalized email, safe error mapping, and E2E specification.
- [x] Execute authenticated signup/recovery Playwright against the linked remote project; hosted email quota is isolated as an explicit external skip and safe UI state.
- [x] Add complete mobile access to every information-architecture destination without crowding the primary bottom navigation.
- [x] Remove direct user mutations from audit, undo, interpretation, embedding, message, summary, heartbeat, job, and other domain-controlled records.
- [x] Enforce ownership on relationships using composite foreign keys or validated security-definer RPCs; add cross-user denial tests.
- [x] Make heartbeat evaluation user-timezone-aware and locale-aware.
- [x] Isolate heartbeat failures per user and protect evaluation with idempotent/concurrency-safe execution.
- [x] Delay over-cap notifications instead of marking them dismissed; preserve important work and cooldown semantics.
- [x] Paginate potentially unbounded lists and avoid per-row signed URL calls where possible.
- [x] Check and surface every relevant Supabase error; prevent partial multi-write settings updates.
- [x] Hide Google OAuth until provider configuration, secrets, redirect URLs, and E2E validation exist.

### 3. Finish AI Routing and Cost Control

- [x] Complete behavioral pgTAP assertions for pricing, immutability, user isolation, and usage-write boundaries.
- [x] Record provider usage before downstream domain persistence so successful provider calls cannot disappear from cost history.
- [x] Replace the 5,000-row client aggregation ceiling with database-side complete aggregates.
- [x] Re-run targeted routing, cost calculator, summary, usage, and settings tests.
- [x] Expand pgTAP coverage for policies, ownership, pricing, immutability, and user isolation; Docker-backed execution is tracked as an external dependency.
- [x] Link and inspect the remote Supabase project and migration history.
- [x] Confirm migration `015` was already deployed and apply incremental migrations `016` through `018`.
- [x] Deploy/validate `process-jobs` configuration and required secrets with a real file-analysis call.
- [x] Smoke-test cost aggregation, ledger, and rendered dashboard with authenticated data.
- [x] Update architecture, database, AI, security, and implementation documentation where behavior changed.
- [x] Commit the completed phase with explicit migration/deployment notes.

### 4. Full quality gate

- [x] Run ESLint with zero errors.
- [x] Run TypeScript typecheck with zero errors.
- [x] Run the complete Vitest suite with zero failures.
- [x] Run coverage and record fresh scoped/repository limitations.
- [x] Run the production Next.js build successfully.
- [x] Run public and linked Playwright suites; distinguish real passes from explicit environment/scope skips.
- [x] Run linked Supabase database lint and remote behavioral smoke.
- [x] Validate the database through linked schema lint and equivalent disposable remote behavioral smoke; Docker-backed pgTAP execution is tracked below.
- [x] Resolve product regressions before closing the sprint.

### 5. Sprint closeout

- [x] Refresh `STATE.md` with final deployed/verified state.
- [x] Append decisions made during hardening to `DECISIONS.md`.
- [x] Move completed work into `CHANGELOG.md`.
- [x] Reorganize remaining work in this file.
- [x] Produce the Sprint 1.5 closing report and Phase 2 recommendation.

## Current backlog — Phase 2 candidates

Continue with the reconciled slices. Do not restart the architecture.

- [x] Harden the current jobs queue and attachment worker with leases, retries/backoff, stale-job recovery, exhaustion, metrics, and user visibility.
- [ ] Add a generic unattended due-job consumer for attachments only when a concrete background workflow requires it; current attachment retries remain explicit and user-initiated after persisted backoff. (`interpret_entry` jobs already have unattended dispatch as of Slice 2X.4.)
- [ ] Automatic daily/weekly review scheduling and verified delivery.
- [ ] Execute Phase 2C task editing and richer lifecycle controls in the approved 2C.1–2C.6 order; do not restart the architecture.
- [ ] Hybrid semantic/lexical search with measured relevance.
- [ ] Confidence-aware AI materialization and undo-first autonomy rules.
- [ ] Complete operational consumers for currently inert agent preference fields.

## Technical improvements

- [ ] Migrate the remaining Supabase clients to generated `Database` types domain by domain; validate existing preference literal unions and pgvector `number[]`/wire representations instead of casting them away.
- [ ] Add cached/typed data-access helpers to reduce repeated auth and query boilerplate.
- [ ] Use `getClaims()` where appropriate in request protection while retaining authoritative user checks for sensitive mutations.
- [ ] Split monolithic settings and action modules by domain responsibility.
- [ ] Consolidate duplicated project/person detail and task-list page patterns.
- [ ] Centralize localized action result/error messages and preserve locale when changing settings.
- [ ] Add route-specific metadata instead of the generic application title.
- [ ] Remove or adopt unused UI dependencies after verifying the intended design system.
- [ ] Strengthen CSP without `unsafe-eval`, add HSTS at the deployment layer, and document the final header policy.
- [ ] Add upload content validation, file-size/type enforcement, and malware scanning strategy.
- [x] ~~Add CI jobs for Playwright, database reset/lint/pgTAP~~ — done 2026-07-25 (pre-2E hardening, ADR-038): the `verify` workflow gained the `worker` job (`deno check` + `deno test`) and the `database` job (`supabase db reset` from zero, the full pgTAP suite, `db lint`, and `e2e/foundation.spec.ts` on the production build, desktop + Pixel 7). Dependency audit and coverage gates remain open below.
- [ ] Add a dependency-audit CI job.
- [ ] Add per-directory coverage thresholds where coverage is already high, and run `test:coverage` in CI. Thresholds must be measured, never guessed. (Architecture-review L10.)

## Pre-Phase-2E hardening — deferred follow-ups

Recorded by the consolidated hardening initiative (2026-07-25) as explicitly **not** implemented, with the reason. Full disposition in `docs/reports/pre-2e/PRE_2E_FOUNDATION_HARDENING_REPORT.md`; source findings in `docs/reviews/ARCHITECTURE_REVIEW_2026_07.md`.

- [ ] **C1 — rate limiting and per-user AI spend caps** behind open self-service signup. The only Critical finding in the review, and the one item that blocks *public production* rather than Phase 2E. Interim mitigation: invite-gate signup, which drops it to High. Needs per-user rolling-window limits on chat/capture, a daily budget checked against `get_ai_cost_summary`, and `max_output_tokens` per operation.
- [ ] **H7 — an error sink.** The authenticated error boundary no longer claims the problem was recorded and now surfaces `error.digest` with a structured `console.error`, but there is still no Sentry-equivalent and no `instrumentation.ts`/`onRequestError`. Deliberately out of scope: a real observability platform is its own decision, not a hardening quick win.
- [ ] **H8 / M21 — a cron dead-man's switch with a cost threshold.** Three `pg_cron` jobs are the operational backbone and every failure mode is silent.
- [ ] **M6 — retention purges.** The 180-day `product_events` limit is a table comment with no purge job; completed jobs and delivered notifications are never pruned either. Required before the first pilot.
- [ ] **M19 / M20 — deploy runbook and a verified backup/restore posture** for the Next.js app layer.
- [ ] **H4 — a runtime-neutral shared artifact for prompt text and version constants.** The extraction *schema* now has enforced parity (`src/lib/ai/extraction-parity.test.ts`) and the three hand-copied `_shared` modules have source-parity locks (`src/features/interpretations/deno-parity.test.ts`), but the *extraction* system prompt and its two version constants are still duplicated by hand between `openai-provider.ts` and `process-jobs/entry.ts`. **Partially paid by Phase 2E Slice 2E.1**, for the command path only: the command prompt, schema and both versions live in one importable module with no `server-only` import, asserted by repo-wide single-declaration counts plus a value-based check (ADR-039) - a shape the extraction prompt could adopt. The extraction duplication itself is untouched, and converting it needs the Deno worker to import from a runtime-neutral location, which is the real work.
- [ ] **H9 (partial) / M14 / M16 — remaining unit-test gaps.** `chat/actions.ts` now has tests for citation hydration and localized failures; `answerFromKnowledge` itself, the four untested `agent` actions (`createReminder`, `markNotification`, `uploadAttachment`, `generateReview`) and the auth boundary (`proxy.ts`, `auth/actions.ts`) remain uncovered.
- [ ] **M1 / M2 / M3 / M4 — structural work:** extract the pure shared layer out of `daily-cycle` (a hub slice with bidirectional imports to five slices), rename or consolidate the three task-domain slices, split the `agent` grab-bag, and adopt the projection-module rule for every new or touched page. All explicitly Phase-2E-or-later, folded into touched code.
- [ ] **M5 — product-event allowlist as a catalog table** instead of three duplicated places re-pasted per event addition. Do it when Phase 2E adds its first new event.
- [ ] **M7 / M8 — behavioural heartbeat tests and a cross-language lifecycle-precedence fixture.** `run_all_heartbeats` is still "tested" by `pg_get_functiondef LIKE` assertions, and `match_internal_knowledge` still has no pgTAP ownership test.
- [ ] **M9 / M10 — model routing and catalog.** `resolveAIRoutes` was deleted as dead rather than consolidated: five call sites still re-implement `preferences?.X ?? "<hardcoded>"`, and a single helper needs a Deno mirror to be truthful. Pricing still matches the provider model string exactly, so a rename silently flips cost events to `unpriced`.
- [ ] **M11 — review generation** still injects tasks as fake `memory` chat sources with fabricated similarity, computes period boundaries in server-local time, and prompts only in Portuguese.
- [ ] **M12 / M13 — the deterministic authenticated e2e tier and a six-route smoke** (memories, notifications, history, people, projects, reminders). CI now runs the unauthenticated foundation journey; the authenticated ones remain online-gated.
- [ ] **M17 — structured logging with correlation ids on the Next.js side.** The Deno worker already demonstrates the right shape.
- [ ] **M18 — one `product_events` reader.** 17 instrumented events, zero readers.
- [ ] **M22 / M25 — chat streaming and `loading.tsx` skeletons.** No `loading.tsx` exists anywhere; chat is a blocking nine-step serial action.
- [ ] **M23 — the pgvector decision.** `match_internal_knowledge`'s `UNION ALL` + computed-alias ordering defeats the HNSW indexes; `DATABASE.md` and `STATE.md` were corrected to say so. Decide explicitly: keep the exact scan and drop the indexes, or restructure to per-table `ORDER BY embedding <=> $q LIMIT k`.
- [ ] **M24 / L12 / L13 — query and index work:** the entry-review page loads the interpretation dataset twice serially; Home fetches a full 51-row inbox page to render four items; `entries (user_id, created_at desc)` is missing for inbox ordering. `undo_operations` JSONB containment lookups are better fixed by switching `interpretations/data.ts` to the existing `source_entry_id` index than by adding a GIN index — but that needs a backfill first, because the legacy `confirm_entry_tasks` path never populated `source_entry_id`, and without the backfill the undo button would silently disappear for old rows.
- [ ] **L11 — raw exception text on the Jobs page.** The worst leak is closed at the source (a `JSON.parse` `SyntaxError` message embeds an excerpt of the model output and reached `jobs.error`; the worker no longer propagates it), but `jobs.error` is still rendered verbatim and still *replaces* the localized attempts line.
- [ ] **L1 / L4 / L5 / O2 — contract-module naming convergence, a full `STATE.md` restructure, `DATABASE.md`'s remaining gaps, and formatter adoption.** `STATE.md` gained a current-truth section and three factual corrections in this pass; the full restructure is separate.
- [ ] **L2 — timezone validation implemented twice** with divergent acceptance rules (`review-projection.ts` vs `work-projection.ts`); hoist one helper into `src/lib`.
- [ ] **`error.tsx` should move from `reset()` to Next 16.2's `unstable_retry()`.** `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md` says "In most cases, you should use `unstable_retry()` instead", because `reset()` re-renders the boundary's children without re-fetching — so for a server-render failure "Try again" typically re-throws immediately. Deliberately not adopted in a hardening pass: it is still an `unstable_` API, and this branch's H7 scope was making the boundary truthful, not changing its retry semantics. Revisit when the API stabilizes or when the error sink lands.
- [ ] **`src/features/capture/schema.ts` writes its Zod messages in Portuguese only.** `capture/actions.ts` no longer surfaces them (it returns a locale-keyed message instead), so no user-facing defect remains, but the schema itself is still single-locale and any future consumer that echoes `issues[0].message` would reintroduce one. Migrate when that file is next substantially touched (ADR-036 incremental rule).
- [ ] **Watch item: Vitest forks-pool stability in CI.** An independent reviewer reproduced `[vitest-pool] Failed to start forks worker … Timeout waiting for worker to respond` locally under load, with jsdom `environment` setup aggregating 280–430s. `vitest.config.ts` sets no `poolOptions` or timeouts. Not tuned here: the observation came from a machine running several concurrent Vitest instances, and the suite has passed cleanly in three CI runs and repeated local runs. If the `app` or `database` job ever flakes on worker startup, pin `poolOptions.forks.maxForks` and raise the pool/test timeouts rather than retrying the job.
- [ ] **The Deno gate resolves `npm:@supabase/supabase-js@2` without a lockfile.** Deliberate — that is how the deployed function resolves it, so CI sees what production sees — but it means a provider release can red the `worker` job with no repository change. A committed Deno lockfile is not the fix: it would have to sit beside a function's `deno.json`, where `supabase functions deploy` would bundle it. If this becomes noisy, pin the exact version in each function's `deno.json` `imports` and mirror it in the deploy checklist.
- [ ] **`ci.yml` passes `process-jobs/deno.json` as the config for every function test directory.** Harmless while both `deno.json` files are `{"imports": {}}`; give each directory its own invocation the moment they diverge.
- [ ] **O1 — document the per-minute drain's throughput ceiling** (~3–10 interpretations/min under `DISPATCH_BUDGET_MS`) next to the constants, and expose oldest-pending-age as the operator signal.

### Discovered during the Phase 2E Gate 1 hardening cutover (2026-07-25)

Each of these was found by deploying the Pre-2E hardening to the linked project and verifying it. Fixes that were required to make Phase 2E buildable landed on the Phase 2E branch; the items below are the ones deliberately **not** absorbed. See `docs/reports/phase-2e/PHASE_2E_GATE_1_HARDENING_CUTOVER_REPORT.md`.

- [ ] **Four Phase 2C/2D smoke scripts race the unattended dispatch drain.** `remote-editable-candidate-confirmation`, `remote-question-preview`, `remote-question-reinterpretation` and `remote-question-resolution` each create an entry and then claim its `interpret_entry` job themselves as a simulated worker, while the per-minute `pg_cron` drain claims eligible jobs across *all* owners. When the drain wins, `claim_entry_interpretation_job` returns null and the smoke fails with `… interpretation job was not claimed` (observed once in three `test:remote:2d` runs). Pre-existing since the Slice 2X.5 drain; not a product defect. Fix by making the fixture job ineligible for the drain, or by tolerating a drain-claimed job as an equally valid outcome — but not by retrying blindly.
- [x] ~~**Write-path consolidation for `public.tasks`.** `authenticated` still holds `insert, update, delete` on `public.tasks` — the `202607170016` revoke list never included it — and `persistTaskStatus` is a plain client `UPDATE` with no undo row and no expected-pre-state guard.~~ **Closed by Phase 2F**, in the order this item predicted. Slice 2F.2 routed `applyWorkItemAction` onto `apply_task_command` and deleted `persistTaskStatus`/`updateTaskStatus`; Slice 2F.3 routed `createRecord`'s task branch onto the creation family; Slice 2F.4's migration `202607300063` then revoked `insert, update, delete` from `authenticated` — the entry `202607170016`'s revoke list had omitted. One correction to this item's own text, recorded rather than quietly dropped: it said the grants were absent from that revoke list, which was right, but the phase later found they were *granted* explicitly at `202607160003:195` inside a `DO` loop rather than inherited from Supabase's defaults (PRD Revision 4.2 §12).
- [ ] **`scripts/remote-product-events-smoke.mjs` hard-codes the event taxonomy.** It lists 19 names as "the canonical taxonomy" while `src/features/product-analytics/contracts.ts` and migration `202607230050` carry 22, and it does not import `contracts.ts`, so adding an event in TypeScript will not red it. Import the contract instead of restating it.
- [x] **`audit_task_change` stamped `actor = 'user'` unconditionally on UPDATE** (`202607160014:19`), mis-attributing any agent-assisted write. **Closed by Phase 2E Slice 2E.4** (migration `202607260058`, ADR-046): the UPDATE branch now derives its actor from a transaction-local `app.audit_actor`, read with `missing_ok` and defaulting to `'user'` so every existing caller is byte-identical, written with `is_local => true` so it cannot leak across a pooled connection, and normalizing an out-of-vocabulary value rather than raising `23514` from inside a trigger. The apply path sets `'user'`; the undo handlers set `'system'`. The same migration extended the watched columns to `title` and `description`, which the trigger previously ignored entirely. **Two residues remain, both deliberate:** the INSERT branch still derives from `new.created_by` (a different question — who authored the task, not who performed this write), and the extension is not retroactive, so pre-existing `task_updated` rows carry neither the new keys nor a derived actor and no backfill is attempted.
- [ ] **Whole-file generated-type parity has no CI check.** 2E-OPERATIONS-002 wants `database.types.ts` proven against the schema by content comparison. The direct implementation — `supabase gen types typescript` in the `database` job, then `diff` — was written, run and withdrawn during Slice 2E.2: the CLI (2.106.0) refuses to start without an access token even when given a local `--db-url` it never leaves, and the only way to satisfy that offline is to plant a credential-shaped string in the workflow, which GitHub push protection rejected (correctly). Slice 2E.2 instead proves the *function it adds* three ways — migration declaration, `src/features/task-commands/database-types-parity.test.ts`, and a `pg_proc.proargnames` assertion in pgTAP. Restore the whole-file check when the job can hold a real token, or when the CLI drops the precondition. Evidence: CI runs 30177038383 and 30177221622.
- [x] ~~**Slice 2E.2 owes a focused remote smoke (2E-OPERATIONS-003), blocked on deployment.**~~ **Discharged 2026-07-28, and the deployment claim is corrected here by Slice 2F.6's convergence audit.** `202607250056` is applied — the whole chain `202607250055`–`202607280061` reached the linked project on 2026-07-28 and parity is now `202607300063` — so the amendment window this item protected is closed by deployment, exactly as it said it would be. `2E-OPERATIONS-003` is delivered and proven remotely by the aggregate smoke (23 assertions, exit 0); see `docs/STATE.md` and `docs/reports/phase-2e/PHASE_2E_FINAL_REPORT.md`. The original text is preserved below as the point-in-time record. *(Original: `202607250056` is local-only and deployment is deliberately deferred until Slice 2E.3's preview proves the projection sufficient — `create or replace` cannot change a `RETURNS TABLE` shape (`42P13`), so deploying makes it permanent and buys a `_v2` in the next slice. Epic 2E-B's acceptance criteria do not name a remote smoke (Epic 2E-D's do), and PRD §19.3 makes it a phase-level Definition of Done item, so it is non-blocking for the slice verdict and owed at Epic 2E-H. **It did not run and is not claimed to have passed.** When it runs it must prove, against the real project: two-owner isolation, cross-owner non-disclosure (the other owner's identically-titled task *absent*, not outranked), `limit + 1` overflow, deterministic ordering across repeated executions, injection-pattern hints (`%`, `_`, embedded and trailing backslash) neither widening results nor raising `22025`, and complete fixture cleanup. All of these are already proven by pgTAP from zero; the smoke's distinct value is the real project's RLS, roles and PostgREST exposure.)*
- [ ] **The 2E-MATCH-018 baseline measures the scoring layer, not end-to-end matching.** The 14-scenario corpus supplies `prefilterTier`/`tokenOverlap`/`queryTokenCount` by hand. Every triple is now proven reachable (`describeUnreachableCandidates`, pinned against the migration by `sql-reachability.test.ts`), but an end-to-end corpus needs a database. Cite the scope whenever the rates are quoted, and consider a pgTAP-driven fixture set in Slice 2E.8 so §22's Phase 2F comparison has a like-for-like bar.
- [ ] **Stale permanent documentation.** `docs/DATABASE.md:29` and `docs/TODO.md` state 19/17 product events (actual: 22); `docs/initiatives/phase-2d/PHASE_2D_IMPLEMENTATION_PLAN.md:9` and `scripts/generate-phase-2d-traceability.mjs:86` still reference `next-intl`, removed by ADR-036; `docs/reports/shared/governance/SLICE_REPORT_TEMPLATE.md` is dead (every real slice report uses a different, richer structure). ~~the comment at `202607170028:33` claims `authenticated` has no INSERT on `audit_logs`, but only `update, delete` were revoked~~ — **corrected at source by Phase 2F Slice 2F.4 (2F-TESTMIG-007)**, in the only place a correction can honestly go: the migration itself was **not** edited, because the chain is append-only, and the `audit_logs` grant was **not** changed, because narrowing it is outside Phase 2F's scope. The vigent posture is now stated in `DATABASE.md`'s grant table and in `SECURITY.md`, both of which record that `authenticated` retains INSERT on `audit_logs` and why the migration comment disagrees. The remaining items in this bullet are still open.

### Discovered during Phase 2E Slice 2E.3 (2026-07-26)

Each of these is pre-existing or scope, not a Slice 2E.3 defect. Full disposition in `docs/reports/phase-2e/PHASE_2E_SLICE_03_REPORT.md`.

- [ ] **A pre-existing flaky test reds CI intermittently.** `src/features/tasks/task-candidate-form.test.tsx` → "keeps one idempotency key for a same-payload retry…" failed in run `30184955865` with `Unable to find an accessible element with the role "button" and name "Resolver 2 sugestões"`. It passes 3/3 locally, Slice 2E.3 never touched the file, and its most recent commit (`a2c263d`, Slice 2E.2) was itself a fix for the same async-read flake class — so the previous fix narrowed the window rather than closing it. **PRD §20 names flaky gates as a risk that erodes evidence**, and that is the real cost: a red `app` job that is *sometimes* meaningless trains everyone to re-run instead of read. Fix by making the assertion wait on the state transition rather than on the rendered button (the suggestion count in the accessible name is derived, so the button's name changes as the form settles), not by adding a retry to the job. Related watch item: the Vitest forks-pool stability note above, since both surface under load.
- [ ] **A task can hold a null `due_at` beside a live `scheduled` reminder, because nothing has ever cancelled a reminder.** The only writers of `public.reminders` are the `after insert` trigger (`202607160007:195-209`) and the heartbeat's two mark-sent updates (`202607160013:33`, `202607170016:552-560`); `authenticated` also retains direct `insert/update/delete` on the table. So clearing or completing a task has never cancelled its reminder, and a pre-Phase-2E row can sit in `completed`/`cancelled`, or with no due date at all, while still holding a `scheduled` row the heartbeat will happily fire. This also makes PRD §11.3's premise that `reopen_task` "re-creates the reminder the INSERT-only trigger cannot" **false for those rows**. Phase 2E's preview discloses observed state truthfully and Slice 2E.4's transitions will maintain consistency going forward, but **neither backfills the existing inconsistency** — and an adversarial reviewer's proposal to disclose a cancellation from a `no_change` preview was rejected, because 2E-UPDATE-009 makes `no_change` write nothing, so that disclosure would be an invented effect.

  **The decision this item asked for has been made, and this line is reconciled rather than left open.** Phase 2F's pre-code Gate 4 census measured **zero rows in every defect bucket** on the production project (one reminder in total, status `sent`), so the owner removed reconciliation from the phase: a migration correcting zero rows is scope stronger than its evidence (PRD §1, owner decision 2). What replaces it is not nothing — `scripts/phase-2f-reminder-census.mjs` is retained as the **2F.6 closeout stop-gate** (2F-OPERATIONS-005): a nonzero bucket 1 or 2 at closeout halts closeout and requires a **new** owner decision, and explicitly does not authorize an automatic migration even then. Bucket 7 (live independent reminders) is reported informationally, because a nonzero value there is the Option C exception working as intended. Two of this item's premises also changed under it: Slice 2F.2 made terminal transitions cancel the task's scheduled reminders through `apply_task_command`, and Slice 2F.4 revoked client UPDATE on `public.reminders`, so no client can create a new instance of this inconsistency.
- [ ] **Alias-driven relation resolution is unproven in pgTAP.** `public.resolve_owned_entity_exact` unions `entity_aliases` within the alias's validity window, and Slice 2E.3 routes the preview's `project`/`context`/`person` reference resolution through it (`202607250056`). The new assertions cover the **base-table path only** — no fixture creates an alias, and none exercises the validity window's boundaries or an alias that collapses two owned entities into the ambiguous-so-null case. **Slice 2E.4 does not depend on it** — its RPC receives already-resolved relation ids in the canonical patch and re-verifies ownership against the base tables directly, so `entity_aliases` is off the apply path — but the preview path still uses it unproven, and whoever first depends on alias resolution owes these fixtures.

### Discovered during Phase 2E Slice 2E.4 (2026-07-26)

Full disposition in `docs/reports/phase-2e/PHASE_2E_SLICE_04_REPORT.md`.

- [x] **OWED BEFORE SLICE 2E.4 COULD BE ACCEPTED: the second, whole-artifact adversarial round.** **CLOSED.** It ran in full — five lenses (SQL execution, concurrency/replay, undo truthfulness, ownership/security, test adequacy), every finding handed to a separate skeptic instructed to refute it: 19 findings, 10 survived, **7 distinct defects, one Critical**. The Critical was an undo that restored ten scalar columns while guarding on `status` alone, so rename → reschedule → undo-the-rename silently discarded the reschedule and stranded a live reminder against a null due date — and the slice report had recorded 2E-UPDATE-014 as met. All seven are fixed and CI-green (`bfa28a1`, pgTAP `Files=28, Tests=1059`). Its first attempt failed on a session limit and produced no findings; none were claimed from it.
- [ ] **No dedicated mutation round ran for Slice 2E.4.** Slice 2E.2's 36-mutation discipline was not repeated as its own pass. The test-adequacy lens is a narrower substitute — it identified three mutations that survived all 116 assertions and all three are now killed — and is **not** claimed as equivalent. Worth a real sweep before Epic 2E-H closeout.
- [ ] **Split `undeclared_failure` into a database-raised and a transport variant.** Deferred from Slice 2E.4. The single member was reworded so it no longer claims "nothing was changed" — untrue for a lost response, which `@supabase/postgrest-js` reports as a caught fetch failure with an empty `code` — but one member still has to cover both a raise that provably rolled back and a response that may have committed. Splitting gives each its own sentence at the cost of touching the declared vocabulary, both copy records, the mapper and four test files.
- [ ] **The plpgsql `if … case … then` trap has no automated guard.** Two of Slice 2E.4's defects were SQL constructs that look like function calls but are grammar: `coalesce`/`nullif`/`greatest`/`least` cannot be schema-qualified under `search_path = ''`, and a bare `case … when … then … end` cannot appear in an `if` condition because the condition scanner stops at the first `then` at paren-depth zero. The first is now caught by an extended post-deploy `pg_get_functiondef` grep; **the second cost a full CI round trip (`42601`, the migration would not apply at all) and is caught by nothing but a comment and a one-off audit.** Consider a lint script or a pgTAP assertion over `pg_get_functiondef` text before Slice 2E.5 writes more plpgsql.

- [ ] **Epic 2E-D's remote smoke and authenticated journeys are genuinely outstanding, unlike Epics 2E-B's and 2E-C's.** PRD §19.1's Epic 2E-D criteria **explicitly name** "a disposable remote smoke and authenticated desktop/mobile journeys", so the phase-level-Definition-of-Done argument that covered the two previous slices does not apply. Both are blocked on deployment (nothing calls `apply_task_command`, and it does not exist in the linked project) and on Slice 2E.7's surface. The smoke must additionally prove what pgTAP structurally cannot: a real **two-session** concurrency race (2E-UPDATE-008's loser path is unprovable inside one transaction), replay across two separate PostgREST requests rather than two calls in one transaction, and that the fingerprint TypeScript computes through `buildFingerprintPayload` equals the one the RPC derives — over the wire, for all thirteen enabled actions.
- [x] ~~**`public.reminders` still grants `insert, update, delete` to `authenticated`**, and Slice 2E.4 chose not to narrow it.~~ **Narrowed by Phase 2F Slice 2F.4, and deliberately not closed all the way.** `update` and `delete` are revoked by `202607300063` on the written 2F-REMINDER-003 determination (`SECURITY.md`): no production module issues either, every surviving reminder UPDATE runs in a `security definer` or definer-routed context, and no `delete` against the table exists anywhere in the repository. **`insert` is retained on purpose** — it is the Option C authoring exception (owner decision 3), whose sole caller is `createReminder` and whose reopening condition is a validated authoring contract. The `2E_REMINDER_INTEGRITY` guard remains valuable and unchanged: with UPDATE revoked its client-side trigger case is gone, but the postcondition still guards concurrent RPC and worker writes, which were always the more likely source.
- [ ] **`2E-UNDO-005`'s task-scoped undo listing is not built.** Slice 2E.4 *records* its operations task-scoped — `entity_type = 'task'`, `entity_ids = array[task_id]` — which is what such a listing needs, but the listing itself is a surface concern and no Phase 2E surface exists. Owed by Slice 2E.7, alongside 2E-UNDO-006's "the affordance is not rendered for an expired operation" (the RPC already returns `undo_expires_at`).
- [ ] **Two members of one defect family cost this slice two CI round trips, and only one is now guarded automatically.** SQL constructs that look like ordinary function calls but are grammar: `coalesce`/`nullif`/`greatest`/`least` cannot be schema-qualified under `search_path = ''`, and a bare `case … when … then … end` cannot appear in a plpgsql `if` condition because the condition scanner stops at the first `then` at paren-depth zero. The first is now caught by an extended post-deploy `pg_get_functiondef` grep; **the second is caught by nothing but a comment and a one-off audit.** Consider a repo-level check — a lint script or a pgTAP assertion over `pg_get_functiondef` text — before Slice 2E.5 writes more plpgsql.

## Known bugs and risks

- [x] ~~`correct_entry_interpretation`'s version-conflict path (SQLSTATE `40001`) hangs until gateway timeout~~ — fixed 2026-07-18 by migration `202607180029` (ADR-026); see the "Resolved" item under Slice 2X.7 above.
- [x] ~~`undo_operation`'s separate `40001` conflict raise is untouched by that fix and remains the same class of risk~~ — resolved by Slice 2D.4 migration `202607230050` (`40001`→`55P03`, fail-closed assertion); see the item above and `SECURITY.md`.
- [x] Worker-crash leases are bounded and expired `running` jobs are recovered or exhausted by migration `019`'s scheduled reaper; a scheduler outage remains an operational monitoring risk.
- [ ] Hosted Supabase email quota is unsuitable for production authentication delivery without custom SMTP.
- [ ] Three moderate transitive PostCSS advisories remain until a compatible Next.js dependency update is available.

## External dependencies

- [x] ~~Start Docker Desktop and execute the committed pgTAP suite through the Supabase CLI.~~ — superseded 2026-07-25: the suite now runs in CI on `ubuntu-latest`, where Docker is free, so it no longer depends on this workstation. Docker remains unavailable locally, which is why the `database` job's first real execution is on the pull request rather than locally; recorded as evidence debt, not claimed as local verification. Deno **is** now available locally (2.9.4) and the `worker` job was executed here.
- [x] Linked Supabase CLI credentials can run authenticated Playwright and disposable remote smoke without persisted secrets.
- [x] Verified OpenAI and Supabase Edge Function secrets through a disposable real worker call.
- [ ] Custom SMTP credentials, a provider-routable catch-all `ONLINE_AUTH_TEST_EMAIL_DOMAIN`, and verified real-inbox delivery before production launch.
- [ ] Google OAuth provider configuration if the integration is enabled in a later phase.

## Product UX/UI remediation — carried out of Slice G5 (2026-07-31)

Governed by `docs/reports/product-ux/PRODUCT_UX_FINDINGS.md`. Slice G5 closed UX-12 under DEC-6 and DEC-7 (ADR-060, ADR-061). What it did **not** close, and why:

- [ ] **`2F-REMINDER-004` — retire the dormant `reminders.status = 'snoozed'` CHECK member.** Still deferred, and now with more evidence than it had. Removing it requires a migration plus proof no row carries the value; the closeout census reads zero, and DEC-7 kept G5 from producing the first one. **Blocked on a prior decision:** `apply_task_command`'s close half and `list_task_command_candidates` see `status = 'scheduled'` only, while `private.undo_apply_task_command` and `private.undo_create_task_command` treat `status in ('scheduled','snoozed')` as the live set. Retiring the literal means reconciling that disagreement in four `SECURITY DEFINER` bodies, which is phase-scale work, not slice-scale.
- [ ] **Windows CRLF test fragility — repository hygiene, not UX.** `src/features/task-commands/sql-reachability.test.ts` fails two cases on a Windows checkout because it matches migration text with embedded `\n` literals (`indexOf("\n  scanned as (")`, `/\n\s*order by\n/`) against a file Git checks out with CRLF. It passes in CI on `ubuntu-latest`, and it was failing on a clean `main` before Slice G5 touched anything — verified by stashing. The fix is either normalising line endings when the test reads the file, or a `.gitattributes` rule for `supabase/migrations/*.sql`. Carried into Slice H as a separate item, deliberately **not** bundled with UX work.
- [ ] **`e2e/online-reminders.spec.ts` seeds one `sent` reminder with the service role.** Only the hourly heartbeat can produce that status, so the terminal-state case cannot reach it through the product. The seed is labelled at the call site rather than disguised as a product write; if a heartbeat trigger ever becomes callable from a test harness, the fixture should go.

## Product UX/UI remediation — COMPLETE at Slice H (2026-07-31)

**35 findings: 30 RESOLVED, 4 RETAINED with evidence, 1 DEFERRED. Zero OPEN, zero PARTIAL, zero BLOCKED. All 8 P0 findings RESOLVED.** Closed by **PR #50, merge SHA `864d39c`**, merge-SHA CI green on all three jobs. Accounting: `docs/reports/product-ux/PRODUCT_UX_CLOSEOUT.md` §10; durable ledger: `docs/reports/product-ux/PRODUCT_UX_FINDINGS.md`. What Slice H deliberately left for someone else, each with a destination:

- [ ] **Localization maintenance — UX-22's 266 inline locale ternaries across 34 non-test files.** The one DEFERRED finding, and it is **not Phase 2G product scope**: it changes no product behaviour. Method for re-measuring, so the number stays checkable: `git grep -oE '\bpt \?' -- src/`, excluding `*.test.ts(x)`. Trajectory 288 (pre-remediation base) → 266. The per-file inventory is §7 of the closeout report; `settings-form.tsx` (53) and `costs/page.tsx` (37) are 34 % of the total between them and are the suggested starting point. **Also write the guard the audit proposed and nobody built** — a test asserting the count cannot rise. Its absence is why four slices raised the count without anyone noticing.
- [ ] **`.gitattributes` with `*.sql text eol=lf` — repository maintenance, its own small PR.** Supersedes the CRLF item above with a proven mechanism rather than a description: `core.autocrlf = true` and no `.gitattributes` leave the working copy of `202607260059_phase_2e_destructive_confirmation.sql` with **3256 CRLF pairs and zero bare LF**, while the two failing assertions match multi-line literals anchored on bare `\n` (`indexOf("\n  select\n    r.id,")` and `/\n\s*order by\n/`). A lone `\n` still matches inside `\r\n`; a pattern needing two consecutive LF-anchored lines cannot. Linux CI checks out LF and is green, which is why Slice H's closeout evidence is trustworthy without this fix and why it was kept out of the UX diff.
- [ ] **Detail routes for organizations and contexts.** UX-04's outcome section degrades both to plain text because no route can receive them — it will not link what it cannot open. Both already have tables, ownership and relations; only the surface is missing. Read-only, no migration. Named as a **Phase 2G candidate** (2G-4), not authorized.
- [ ] **`audit_logs.operation_id`, closing UX-27's root.** A nullable `uuid` stamped by each RPC on its own row and, via a transaction-local setting the trigger reads (the mechanism `app.audit_actor` already establishes), on the trigger's row too. Additive, nullable, no backfill; every existing row renders exactly as it does now. UX-27 is **RETAINED** rather than deferred, because the two rows are two genuinely distinct events and History reads them as such — this would group them, not fix them. **Phase 2G candidate** (2G-3), not authorized.
