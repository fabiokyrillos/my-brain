# Project State

Last updated: 2026-08-09 (**PHASE 2K SLICE 2K.6 IS MERGED - one hard-coded example replaced by at most three deterministic suggestions.** Baseline `73e298a` (slice 2K.5), **CI green on that exact merge SHA across all three jobs**. **Determinism is a SECURITY property here, not a style preference.** T-2K-10: "contextual suggestions derived from current state" invites a **model call per page load**, spending the user own BYOK credential on something they did not ask for - and caching would reduce the cost without making an unrequested billed call legitimate. What is asserted is not that the suggestions are cheap but that the module is **incapable of costing anything**: no provider, no `recordAIUsage`, no rate-limit admission, and **no `async` at all**, because an async export would be the first sign it had started reaching for something. **The value is a `{category, name}` pair and NEVER a sentence.** OD-2K-4 permits naming a person or project the user can read - a suggestion that cannot say a name is not contextual - and forbids that name from ever entering telemetry. Keeping the two apart **in the type** is what makes that enforceable: a rendered sentence could be logged whole; a pair cannot. `suggestionTelemetryCategory` returns the category through a return type that has **no field a name could occupy**. **Sensitivity is stated rather than assumed**: `people` and `projects` carry **no sensitivity column**, so there is nothing to mask - and "no mask" and "mask not applied" look identical from the outside, which is why it is written down. **Three smaller decisions**: the cap is **interleaved** so a user with three people still sees a project; the suggestions are **text, not controls**, because a control would put words in their mouth with the composer one field away, and the accessibility fixture asserts that **absence** too; a blank name is dropped rather than rendered. **Zero is a legitimate answer** - a user with no people and no projects gets nothing, which is exactly what inventing an example used to hide. **A guard of mine retired for the SECOND time**: 2K.1 asserted "no continuity payload and no suggestion derivation **yet**", and both yets have now arrived. A guard whose premise expires is a guard that fails on correct work. It is replaced by statements about what each thing **is**, and now asserts that **both replacement guards exist** so the retirement cannot become "nobody checks either any more". **Executed:** tests first and red for the right reason, lint and typecheck zero-error, `npm test` **4878 passed / 0 failing tests** (3 files fail to load on Windows - known local baseline, green in CI), locale-ternary ceiling green with **one fewer** ternary, build green, Playwright 31 passed / 1 skipped at both viewports, `git diff --check` clean. **`2K-SUGG-005` is PARTIAL and recorded as such**: the bounded category and its narrowing exist; the **event** belongs to 2K.8, which owns the telemetry vocabulary and the single budgeted migration. **NOT PROVED:** screen reader. **Zero migrations, no provider call, no BYOK spend: budget stays `1 allocated - 0 spent`.** Rollout gate 25/3/2; signup closed. **Phase 2L not started.**)

Previously: 2026-08-09 (**PHASE 2K SLICE 2K.5 IS MERGED - the two exclusions the answer path already computed and threw away now reach the user.** Baseline `96be787` (slice 2K.4), **CI green on that exact merge SHA across all three jobs**. **Nothing new is computed**: everything below the similarity floor and every archived memory were already dropped and silently discarded, so the user could not distinguish "found nothing" from "found three things and rejected all of them". **The payload is two booleans, and that is the BOUNDARY, not the rendering.** T-2K-04: "do not show a count" is insufficient on its own because a **rate is a count over repeated queries** - so `buildAnswerExplanation` takes four counts and returns two booleans, and that function is the one place the numbers stop travelling. A count that never renders today is a count somebody can render tomorrow. **Proved by construction**: nineteen dropped matches and one dropped match serialize **identically**, so no count is reconstructible by any consumer present or future; and **no digit renders** anywhere in the panel, in either locale. **The two exclusions are treated differently and not arbitrarily**: the similarity floor is a property of THIS QUERY, not of the corpus; owner-archived memories are facts the audience created and retired, so telling them is not a disclosure. **Sensitivity is absent STRUCTURALLY**: 2K masks rather than excludes, and the builder reads four named counts of which none is a classification - planting one on its input changes nothing. **Progressive and never blocking**: a native `<details>`, closed, drawn **only** when there is something to say. Native rather than `useState` because it is keyboard-operable, announced and works before hydration. An answer written before this slice carries `null`, which draws nothing - "not recorded", deliberately not "nothing was excluded". **One honest PARTIAL, declared rather than implied** (`2K-EXPL-007`): correcting the **source** works and always has; correcting the **interpretation** has no table, no action and no consumer, so the panel SAYS so and explains why a button that records nothing would be worse. Remainder: an interpretation-correction domain. Destination: the roadmap successor own audit. **The accessibility fixture renders the panel OPEN while the product ships it CLOSED**, and the guard pins both halves - axe cannot scan what is not in the accessibility tree. **Executed:** tests first and red for the right reason, lint and typecheck zero-error, `npm test` **4849 passed / 0 failing tests** (3 files fail to load on Windows - known local baseline, green in CI), build green, Playwright 29 passed / 1 skipped at both viewports, `git diff --check` clean. **NOT PROVED:** screen reader; hydrated interactivity. **Zero migrations: budget stays `1 allocated - 0 spent`.** Retrieval, the floor, the lifecycle filter and `chat-schema.ts` untouched. Rollout gate 25/3/2; signup closed. **Phase 2L not started.**)

Previously: 2026-08-09 (**PHASE 2K SLICE 2K.4 IS MERGED - the persisted excerpt is GONE, and an answer can finally say it found nothing.** Baseline `a3a5837` (slice 2K.3), **CI green on that exact merge SHA across all three jobs**. **The live exposure the audit called the sharpest finding in the phase is closed.** `sendChatMessage` used to write a 220-character **copy** of every cited source into a second table whose rows do not carry the source classification - reclassify an entry or archive a memory and the quote stayed in the clear, forever. Carrying the classification beside the excerpt was **rejected**: two copies of one fact kept in sync by convention is the defect `202608080087` had to delete. Removing the copy removes the thing that can diverge. **No migration**: the jsonb column now holds two shapes and `parseCitations` normalizes both. The current shape has **nowhere to put content**; the legacy branch **drops the excerpt on the floor**, which is what keeps the named residual from becoming a live exposure again. A planted content field refuses the **whole** envelope, not just the field - proved against six field names. **A legacy row is `unknown`, never `no_qualifying_evidence`**: nobody recorded what retrieval found for those rows, so the surface says LESS rather than inventing either answer. **Insufficiency comes from RETRIEVAL, not from the count** - the ambiguity 2K.0 measured. The sharpest test in the slice: every cited id fabricated, zero references persisted, and the state is still `evidenced`, because retrieval did find something. Deriving it from the count would report the exact opposite of what happened. **An answer with no evidence is now visually distinct** from one with plenty; the block used to be drawn only when citations existed, which is why the honest case rendered **nothing at all**. **The reach is stated on both branches** - it matters most beside an answer that found nothing. **Support kind is decided SERVER-SIDE** (entry to direct_record, memory to product_state); `chat-schema.ts` is **untouched**, so the model still declares two fields and cannot widen its own authority. `inference` belongs to the **answer**, which stops it being a vocabulary member nothing produces. **Freshness reaches the user for the first time** - occurred_at, or valid_from else created_at, exactly what the RPC ranks on - as a machine-readable time element, and **absent rather than fabricated**. **The re-read is bounded and MEASURED**: two queries per message, one per table, asserted by counting `from` calls; zero for a message citing nothing. A source reclassified AFTER the answer was stored carries the new level; deleted and unreadable are byte-identical; an archived memory renders `unavailable` rather than in the clear, with an in-force memory as its positive control. **Executed:** tests first and red for the right reason, lint and typecheck zero-error, `npm test` **4809 passed / 0 failing tests** (3 files fail to load on Windows - known local baseline, green in CI), build green, Playwright 27 passed / 1 skipped with **both** evidence branches scanned at both viewports, `git diff --check` clean. **One NOT PROVED changed status rather than being discharged:** the prose a zero-source answer produces still needs a real OpenAI call, which ADR-101 does not authorize and this slice will not spend silently - but it **no longer gates the requirement**, because insufficiency is now a persisted structural fact and the model prose sits ABOVE a disclosure rather than being one. **Also NOT PROVED:** screen reader; hydrated interactivity. **Zero migrations, zero backfill: budget stays `1 allocated - 0 spent`.** Existing stored excerpts untouched and still the named residual, contained by a renderer that never reads one. Rollout gate 25/3/2; signup closed. **Phase 2L not started.**)

Previously: 2026-08-09 (**PHASE 2K SLICE 2K.3 IS MERGED — a pending action now survives looking at its own evidence.** Baseline `799191c` (slice 2K.2), **CI green on that exact merge SHA across all three jobs**. **The continuity handle is INCAPABLE of authorizing, not merely hard to forge**: five identifier fields, twelve names refused **by name** - `issuedAt`, `observedBefore`, `confirmationId`, `operationKey`, `requestFingerprint`, `fingerprint`, `patch`, `preview`, `mutation`, `authorization`, `session`, `expected` - each proved against a **planted instance**, and a `.strict()` schema that refuses a field nobody listed. Signing it would have made it authentic, not harmless. **`session` and `expected` are on that list deliberately**: the task-command envelope carries `issuedAt` and the staleness witness inside it, so transporting the envelope would transport the clock under another name - the shape R17 refuses. **The return mints its own clock**; no clock crosses the navigation and none can. **`requiresFreshConfirmation` is the literal `true` AS A TYPE**, mirroring `willMutate: false`, so no branch can set it false and compile - and it is proved at runtime across **all three** return shapes, because "always" is the requirement. **What re-derivation can honestly mean with an identifier-only payload**: the earlier preview is **not reconstructible by design**, so `2K-CONT-006` third branch is the only one the shape can express - the earlier preview no longer applies, here is the object now, state the action again. A payload rich enough to describe the difference would be rich enough to authorize the change. **And it reads as NORMAL, not as an error** (ADR-100). **Deleted, foreign and errored reads are byte-identical** - `unavailableCard` takes no cause parameter - and a **suspended account is refused one layer earlier and more strongly**, by the `requireUser` redirect, which is stated rather than folded into the identical-output claim. **A design point the slice forced, and the contract changed rather than the call site**: a memory reached as a REFERENCE is read-only even though a memory card may mutate, so `mayRenderMutatingControl` now reads the card own `mutability` field - the rule `2K-CARD-009` already states for reversibility. The type-level guarantee moved to the **builder**, and every 2K.1 assertion still holds. **Two deviations from the plan file list, both narrowing**: the entry point is a `server-only` module rather than a Server Action (a `"use server"` export becomes a client-callable endpoint, and the resume needs no client call), and `TASK_COMMAND_SESSION_VERSION` was **not** bumped because the resume never touches the envelope at all. **Three of my own guards were too coarse and were made precise**: a refusal LIST reads like a use, so the detector now matches identifiers rather than bare words, proved to fire on `buildTaskCommandPreview(input)` and not on `["requestFingerprint"]`. 2K.1 "no continuity payload YET" assertion had its yet arrive and is replaced by something stronger. **Executed:** tests first and red for the right reason, focused green (695), lint and typecheck zero-error, `npm test` **4760 passed / 0 failing tests** (3 files fail to load on Windows - known local baseline, green in CI), build green, Playwright 25 passed / 1 skipped at both viewports, `git diff --check` clean. **NOT PROVED:** screen reader; hydrated interactivity. **Deferred to 2K.8:** the authenticated answer to open-source to return to re-confirm journey. **Named limitation:** the citation link still renders the legacy stored excerpt as its label - 2K.4 replaces that. **Zero migrations, zero persistence, no TTL: budget stays `1 allocated - 0 spent`.** Rollout gate 25/3/2; signup closed. **Phase 2L not started.**)

Previously: 2026-08-09 (**PHASE 2K SLICE 2K.2 IS MERGED — edit before confirming, a discard that writes nothing, and a memory undo that ARCHIVES.** Baseline `0865a9a` (slice 2K.1), **CI green on that exact merge SHA across all three jobs**. **The editable set NARROWS the taxonomy and never widens it**: `editableParametersFor(action)` filters the action own `allowedPatchFields`, so a card can never offer a field the validator refuses, and the subset relation is asserted for every action. **Relation references are excluded by decision** (OD-2K-1: omit rather than widen) - editing one needs a name-to-entity resolution whose ambiguous, foreign and non-existent outcomes this phase does not model as card states - so `assign_project`, `assign_context`, `assign_person` and `set_waiting_on` expose **no** editable parameter, stated rather than hidden. **An edit re-derives rather than patches**: one validated value into the envelope, then `runCommandRound` re-runs the whole preview - the `withClarification` shape, which is why it cost no second pipeline. The canonical patch is a **fingerprint input**, so a confirmation minted against the pre-edit digest can no longer be consumed, mechanically rather than by memory. **The rule is asked on the server, from the RE-DERIVED action**, never from the form and never only at the control that drew the field. **`set_status` still cannot become a route to `cancel`** - the choices come from the policy own `allowedTargetValues`. **The discard takes NO Supabase client**, loads no context and carries no session forward, so "leaves no trace of intent" is checkable from a signature. **The memory undo DELEGATES to `setMemoryLifecycle`** with the `archive` transition: no new column, no new RPC, **no migration** - registering an `undo_operation` handler would have spent the ceiling and is refused. It is a **true** undo: the archived memory leaves the active window and `chat/actions.ts` filters retrieval through the same `isMemoryInForce`, so it stops answering questions rather than merely leaving a list. **The words are asserted NEGATIVELY in both locales** - no deletion wording anywhere in the undo copy, because the row survives and "deleted" is the word a contributor reaches for. **The undo of the undo IS offered**, because `restore` is the audited owner-scoped inverse that already ships; declaring absence would have been the inaccuracy. **A duplicate is `no_change`, not `accepted`** - this turn created nothing - which is also why the undo is withheld for it. The memory card is **honestly asymmetric**: a create has no pre-state, so no empty before/after table pretends to be a delta. **Positive controls throughout**: the edit refuses `priority: catastrophic` **and** accepts `urgent`; the pgTAP denial is preceded by owner A archiving its **own** memory in the same transaction, with a non-vacuity section proving both memories start **in force**. **Executed:** tests first and red for the right reason, focused green (2173), lint and typecheck zero-error, `npm test` **4724 passed / 0 failing tests** (3 files fail to load on Windows - known local baseline, green in CI), whole `task-commands` suite green with the two new intents, build green, Playwright 23 passed / 1 skipped at both viewports, `git diff --check` clean. **NOT PROVED:** screen reader; hydrated interactivity. **CI-only:** the new pgTAP suite, no local Docker. **Deferred to 2K.8 by the plan:** the authenticated confirm-and-undo journey. **Zero migrations: budget stays `1 allocated - 0 spent`.** No RLS, grant, secret, external service or write path added; retrieval and the outcome vocabulary untouched. Rollout gate 25/3/2; signup closed. **Phase 2L not started.**)

Previously: 2026-08-09 (**PHASE 2K IMPLEMENTATION IS AUTHORIZED THROUGH CLOSEOUT AND SLICE 2K.1 IS MERGED — ADR-101, an owner decision.** ADR-101 discharges gate G5, authorizes slices **2K.1-2K.8**, the traceability matrix and the closing report, and authorizes deploying the **single** telemetry migration if one is created - and nothing else. It also signs the last two open decisions, so **no Phase 2K requirement is blocked or gated on an unsigned decision**. **OD-2K-1:** editable parameters are a **closed per-action set** drawn from the existing typed command - never an arbitrary patch, never a caller-supplied field name, never free JSON, never a field the schema does not already recognise; an edit **re-derives and re-fingerprints**, which invalidates any earlier confirmation; a memory create may edit **only its content**, and `kind`, provenance, ownership and lifecycle are not editable; where a parameter is arguable it is **omitted rather than widened**. **OD-2K-4:** suggestions are **deterministic**, capped at **three**, **zero when none is honest**, cost **no extra model call**, carry **no mutation payload**, and may name a person or project only under current read access and the sensitivity policy - while suggestion text and names **never** enter telemetry, which carries a closed category only. **Slice 2K.1 delivers one closed card grammar**: ten server-decided states, seven object types partitioned into the two OD-2K-B lets mutate and the five it does not, and `mayRenderMutatingControl` as the **single** decision site - so `ConversationCardView` **drops** a control passed for a read-only type rather than trusting the call site. Reversibility is a **three-arm union resolved exhaustively**, not a boolean, so a memory card can never inherit the task card 24-hour window (OD-2K-3). `unavailableCard` takes **one argument** and can express **no cause**, which is `2K-CONT-008` groundwork built where a cause parameter would otherwise be added later. **Conversar is governed for sensitivity for the first time**: `chat` joins `GOVERNED_SURFACES`, masked in place with a local transient reveal, and `search` stays out so ADR-093 is not re-opened. An unclassified snippet resolves to the most protective level. A **real consumer** ships rather than a contract nobody uses: the `capture_intent` route bare link became a read-only entry card, and the now-dead `captureNextStep` copy was removed. **The accessibility lane was extended in the slice, not deferred to closeout** - the Conversar card states pass axe at desktop and Pixel 7, controls measure 44px from paint, and the mirror guard now re-derives the card class names, `data-state`, `aria-expanded`, its pt-BR copy and **every declared state**, so a new state cannot arrive unscanned. **Executed:** tests first and red for the right reason, focused green, lint and typecheck zero-error, `npm test` **4686 passed / 0 failing tests** (3 files fail to load on Windows - the known local baseline, green in CI), build green, Playwright 21 passed / 1 skipped at both viewports, `git diff --check` clean. **NOT PROVED and not rounded up:** a real screen-reader session; hydrated interactivity in a browser. **`2K-PRIVACY-003/004` are NOT claimed here** - the persistence change and the render-time re-read are 2K.4. **Zero migrations: budget stays `1 allocated - 0 spent`.** Hosted parity re-proved live at `202608080087`, 87 migrations, local = remote. No RLS, grant, policy, secret, external service or write path added. Rollout gate untouched at 25 pass / 3 fail / 2 owner-signature; signup closed; Phase 2K is not progress toward it. **A13 still targets the roadmap successor, which ADR-101 does not authorize.**)

Previously: 2026-08-08 (**PHASE 2K SLICE 2K.0 IS EXECUTED — measurement only, zero migrations, zero product code. The owner authorized SLICE 2K.0 ALONE; slices 2K.1–2K.8 are NOT authorized.** Three measurements, `2K-AUDIT-001..006`. **OD-2K-5 is closed by measurement and the answer is no**: `task_command_confirmations` has **no `expires_at`, no TTL, no age check** — the absence is **ADR-047** — and three *fact-based* refusals replace a clock (`55P03` from the twelve-column staleness gate, `2E_CONFIRMATION_REQUIRED` from the six-predicate consumption `UPDATE`, `2E_IDEMPOTENCY_MISMATCH` from a changed payload under the same key). `TASK_COMMAND_OUTCOMES` has **no `expired` member** and ADR-047 refuses to add one, so 2K's `expired` is a card-level presentation state mapping onto `rejected_stale`. **ADR-100 corrects the continuity consequence first drawn from that measurement, on the owner's direct review of the branch before merge.** The first draft said continuity *"must carry the original pinned `issuedAt` … or state plainly that a fresh confirmation is required"* — a contradiction of the same package's `2K-CONT-003` and of OD-2K-D, because `issuedAt` is a hashed fingerprint input and transporting it **to keep an earlier confirmation consumable is transporting an authorization**; offering the implementer a *choice* was the worse half. **The contract now has one reading: returning always re-derives with a NEW `issuedAt` and always requires a fresh confirmation**, the payload carries identifiers only, an unchanged object re-asks **as normal rather than as an error**, and an unreconstructible difference is **never invented**. Traceability gains **R17**; 2K.3's stopping condition was **inverted**, because the old one would have stopped the slice on correct behaviour. Old confirmation rows may persist as history and are a **separate residual** — no TTL is created. **M2 found what the audit had missed: the product has no structural representation of insufficiency at all** — `chatAnswerSchema` is `{answer, citedSourceIds}`, an empty source set sends the literal `None` into the prompt, and **`citations.length === 0` is ambiguous** between "nothing retrieved" and "retrieved but uncited", so `2K-SRC-005` now requires the signal to come from the **retrieval result**. **M3** confirmed `occurred_at` reaches the model prompt and never a `.tsx`, and is not even persisted. **The audit's §5.4 gap is closed by a test**: `phase_2k_knowledge_retrieval_ownership.sql` gives both owners the same vector so ownership is the only discriminator, reads posture from `pg_proc`/`pg_class`, and carries a **positive control**. **Hosted read-only probe:** the deployed RPC returns **zero rows even to `service_role`** — `security invoker`, `auth.uid()` null, so it is not a bypass door — and refuses `anon` at `42501` on the *table*; no write issued, **zero residue by construction**. **ADR-099 records the ADR-055 retirement OD-2K-6 signed**: the **widening** leaves the active roadmap, the retrieval that ships today over entries and memories is **not removed, disabled or degraded**, and **no renewal date is written**. **Reported NOT PROVED rather than as a pass:** the prose a zero-source answer produces, which needs an OpenAI credential this slice may not spend. **Budget stays `1 allocated · 0 spent`**; hosted parity unchanged at `202608080087`; rollout gate 25/3/2; signup closed. **Slice 2K.1 is not started.**)

Previously: 2026-08-08 (**PHASE 2K — CONVERSAR AS THE PRIMARY INTERFACE IS AUTHORIZED FOR PLANNING ONLY — ADR-097, an owner decision. Implementation is NOT authorized.** Scope is the owner's mobile-first PRD's **Etapa 3 only**; Etapa 4+ stays out, by ADR-092's decomposition rule that a phase which cannot close cannot be traced, budgeted or gated. **68 requirements across eleven families and eight slices planned for execution — none executed — migration ceiling `1 allocated · 0 spent`** — the ceiling is not an obligation and a zero-spend close is legitimate. **Four owner decisions are signed** (ADR-097): 2K.7 leaves implementation scope; mutating cards are limited to tasks and memories; the budget is one migration destined for content-free Conversar telemetry; and continuity works by reopening plus **server re-derivation**, persisting no pending confirmation. **The audit shrank the phase in four places and grew it in one.** Semantic retrieval **already ships** — `public.match_internal_knowledge` is pgvector over `entry_embeddings` and `memories`, `security invoker`, owner-scoped, called every turn after a BYOK gate and a **single** rate-limit admission covering what is two provider calls. Sources per answer **already ship** — citations computed, fabricated ids stripped by a `flatMap` that degrades to a missing citation rather than a `TypeError`, **persisted** to `conversation_messages.citations`, rendered as links. Claim-binding ships. The preview → confirm → apply → undo pipeline ships in depth **for tasks**: 66 files, a preview whose immutability is the literal type `willMutate: false`, a TOCTOU fingerprint, a server-issued single-use confirmation the caller deliberately cannot name, 24-hour undo and restore afterwards. **What grew is a live exposure**: Conversar is the only content surface with **no sensitivity policy** — `chat` is absent from `GOVERNED_SURFACES` — and the only one that **persists** a 220-character excerpt of every cited source into a second table where the source's classification no longer travels with it, so reclassifying or archiving the source leaves the excerpt in the clear (**OD-2K-2**, blocking). **Continuity is the one wholly-absent capability and needs no schema**: the whole card and proposal state lives in a client `useActionState`, so clicking a citation and returning destroys a pending confirmation silently. **Slice 2K.7 is REMOVED** — ADR-055 forbids `source_type` widening, backfill, pipelines, job types and indexes until a threshold is met, the funnel is **empty**, and it expires **2026-10-27, inside the phase's own 11–15 week estimate**, so `2K-AUDIT-004` makes the permanent resolution a closing requirement of the **first** slice. **A13 retargets from Phase 2K to the roadmap successor** in ADR-097's own commit, the sixth application, with the heading check now asserted across the whole series rather than only the newest ADR. **The guard was already red before this package existed and it was right**: `PHASE_2K_2O_MASTER_IMPLEMENTATION_PLAN.md` matches `^PHASE_2K_.*(IMPLEMENTATION_PLAN)`, so the roadmap document tripped signal 1 when it was written — the resolution is authorization, not a looser regex. **ADR-098 then signed the three decisions ADR-097 left BLOCKING**, so **no Phase 2K requirement is blocked on an unsigned decision** and the three that remain (OD-2K-1, OD-2K-4, OD-2K-5) only *gate*. **OD-2K-2:** new messages persist **no excerpt at all** — only a structured reference, with the source **re-read at render time** against its current classification, masked when `highly_sensitive`, unavailable when removed or out of validity, and a historical excerpt never reproduced in the clear; chosen over carrying the classification beside the excerpt because that keeps two copies of one fact in sync by convention, which is the defect `202608080087` had to delete. Excerpts already stored are a **named residual**; no backfill belongs to 2K. **OD-2K-3:** the memory undo **archives**, never physically removes — provenance preserved, the memory withdrawn from active use and from retrieval, and the UI saying explicitly archived, never deleted; it costs no migration because the audited lifecycle transition already exists and `memories` has no delete path by standing product decision. **OD-2K-6:** the semantic-retrieval **widening** retires at the ADR-055 expiry while today's retrieval over entries and memories continues **unchanged, not removed, disabled or degraded**; resumption needs a new measurable demand signal, a new audit, a new ADR, its own budget and explicit authorization, and **ADR-055 is not renewed artificially to keep a possibility on the roadmap**. All three were chosen partly because **none spends the one-migration ceiling**. A false "eight **executed** slices" claim was corrected in **four** documents, and the edit to the accepted ADR-097 is logged inside it rather than made silently. **Nothing is implemented, no migration is created, nothing is deployed**, hosted parity remains `202608080087`, and the signup rollout gate is untouched at **25 pass · 3 fail · 2 owner-signature**.)

Previously: 2026-08-08 (**PHASE 2J — TODAY, CAPTURE AND ATTENTION IS COMPLETE.** 74 requirements declared, **74 classified, 0 unclassified**: 59 built, 6 baseline, 5 evidenced negatives, 2 partial, 2 undelivered — every partial and every undelivered naming its destination. **Migration budget 2 allocated · 2 SPENT**, reconciled per slice: `202608080085` to 2J.4, `202608080086` to 2J.7. ADR-095's preferred `2 · 1` close was **not reachable, and the premise that promised it was ours**: ADR-096 records that `ai_usage_events.operation` has **no `other`** — that value belongs to `error_events.operation`, a different table — so no existing value could truthfully carry a transcription, and the conflation travelled from the audit into the PRD into the ADR without being re-derived against the constraint on the table it described. **That migration's own verification block then found a second gap**: `record_ai_usage` enumerates the vocabulary **in its body**, so widening only the table CHECK would have deployed clean and rejected every transcription at runtime, inside the path `usage.ts` swallows into a console line. **The accessibility lane found a real Phase 2I defect on its first execution** — `.library-search-link` at 16px on a Pixel 7, under WCAG 2.2 AA 2.5.8 — which is the whole argument for running it as slice **zero** rather than at closeout, where Phase 2I put it. **`Precisa de você` already existed** at `/app/inbox?view=needs-you`, so `2J-ATTN-001` is **baseline**; the phase's own audit predicted that class of error and it happened anyway. **Two of the five sensitivity surfaces had nothing to converge** — the capture receipt carries no content field at all, and there is **no push, service-worker or lock-screen payload anywhere** — both recorded as evidenced negatives rather than given a mask for content that does not exist. **Thirteen defects, ten of them in probes, guards, pins or tooling**, including a guard pinned to a **superseded** migration that passed for the wrong reason. **Phase 2I's accessibility residual is PARTIALLY REDUCED, not resolved**: axe, structure, accessible names, visible focus, focus order, rendered touch targets, reduced motion and dialog semantics are now proved in a real browser at two viewports; **hydrated interactivity and a real screen-reader session are not**. **Gate G-2J.4b is NOT discharged** — voice was never measured on real iOS Safari or Android Chrome. PRs #136 (`283380d`), #137 (`e2a9fc0`), #138 (`b91172a`), #139 (`88ddb9d`), #140 (`fa791f0`), **one green PR-head run and one green merge-SHA run each** under ADR-090. **Phase 2J added no RLS policy, no grant, no secret, no external service, no new provider and no second write path** — `T-2I-02`'s guard was proved red against a planted second caller *before* the unified capture surface existed. ~~**NOTHING IS DEPLOYED**: hosted parity remains **`202608070084`** while the chain head is `202608080086`.~~ **SUPERSEDED 2026-08-08 — BOTH MIGRATIONS ARE DEPLOYED. Hosted parity is now exactly `202608080086`, 86 migrations, local = remote**, read from hosted state rather than from a filename. `202608080085` and `202608080086` applied in chain order, each of their four embedded verification blocks passing, with **no grant, RLS, policy, cron, retention, storage or audio change** — every posture line is a post-deploy readback against a pre-deploy snapshot. `record_ai_usage` accepts `transcription` **end to end** (`cost_status=unpriced`, `pricing_id=null` — the accepted state, ADR-096), unknown operations still refuse `22023` and an unclaimed caller still refuses `42501`. **The deployment's own acceptance probe then found the phase's telemetry deliverable inert: `product_events` writes pass through THREE copies of the event vocabulary, and `private.record_product_event` holds a hardcoded 26-name gate that `202608080086` did not widen** — so `capture_mode_selected`, `voice_transcription_finished` and `attention_item_resolved` are all refused `22023 Unsupported product event` on the live project, while a control travelling the identical call path is accepted. **The same gate has silently refused `rate_limit_refused` since `202608070081`**, making this a pre-existing Phase 2H defect that Phase 2J's probe surfaced rather than a regression: the list froze at `202607280061` and no test writes a CHECK-declared name through the real writer. ~~**This needs a THIRD migration, which is a stop condition — nothing was fixed.**~~ **REPAIRED THE SAME DAY by `202608080087`, an owner-authorized POST-2J CORRECTION charged to NO phase budget — Phase 2J stays `2 allocated · 2 spent`, and the file is named `post_2j_*` precisely so the phase generator's `/phase_2j/i` filter cannot absorb it (the `202608070084` mechanism).** Option B was taken: the redundant gate is **deleted**, not synchronized, so the vocabulary now has exactly two enforcement points. **Fail-closed is preserved** — the CHECK still refuses `23514` and is untouched, and the validator's `else` arm raises the identical `'Unsupported product event'` / `22023`, so no caller can tell the difference; the migration additionally proves, **name-by-name and refusing to run vacuously**, that the CHECK and the validator are one vocabulary. **The missing test now exists**: `supabase/tests/post_2j_product_event_write_path.sql` derives the vocabulary from the CHECK and writes **every declared name through the writer production calls**, with non-vacuity proved by planting `202607280061`'s gate and requiring **exactly four** refusals. PR #143, PR-head `e9d6916` green attempt 1, merged `db5ee41`, exact merge-SHA green attempt 1, then deployed. **Hosted parity is now exactly `202608080087`, 87 migrations, local = remote.** All four events — the three Phase 2J ones and `rate_limit_refused` — are **ACCEPTED** through the real writer on the live project, negatives still refuse (`22023` undeclared/free-text, `42501` unclaimed and authenticated-to-service-role), and **producer → writer → `product_events` → consumer is proved end to end on a disposable account with zero residue** (68 before, 68 after; the cascade took the fixtures). Posture byte-identical: RLS, policies, grants, 5 cron jobs, retention, signup closed, CAPTCHA enforced, gate still **25 · 3 · 2**. Record: `docs/reports/phase-2j/POST_2J_PRODUCT_EVENT_WRITER_CORRECTION.md`. Probes left **zero residue** in both append-only ledgers (`ai_usage_events` 11 → 11, `product_events` 67 → 67). **ADR-055 remains open and unchanged, expiring 2026-10-27.** Rollout gate re-read at close and untouched at **25 pass · 3 fail · 2 owner-signature**. **Phase 2K is unstarted**, A13 green. The paragraph below is the prior state, retained.)

Previously: 2026-08-08 (**PHASE 2J — TODAY, CAPTURE AND ATTENTION IS AUTHORIZED FOR PLANNING ONLY — ADR-094, an owner decision. Implementation is NOT authorized.** Scope is the owner's mobile-first PRD's **Etapa 2 only**; Etapa 3+ stays out, by ADR-092's decomposition rule that a phase which cannot close cannot be traced, budgeted or gated. Governing pair: `docs/initiatives/phase-2j/PHASE_2J_PRD.md` and `..._IMPLEMENTATION_PLAN.md`; audit, threat model, traceability contract and gaps under `docs/reports/phase-2j/`. **74 requirements across nine families, eight slices.** **A13 retargeted from Phase 2J to the roadmap successor in the same commit as ADR-094**, the fifth application of that precedent — and ADR-094's heading says *"retargets to the roadmap successor"* rather than naming it, a property now **asserted by test** rather than left to memory. **The audit corrected the parent PRD in five load-bearing places, and every correction shrinks the phase except voice.** `/app/today` is a **redirect** to `/app/work?view=today`, while the cockpit the PRD asks for already exists at `/app` as `HomeDashboard`, composing four projections with capture at the top — so Hoje must be made **reachable under its own name**, not built. The attention queue **already exists**: `list_needs_attention`, `security definer`, `auth.uid()`-scoped, keyset-paginated, whose **five reasons cover four of the six** sources the PRD lists — task suggestions, ambiguities, questions and recoverable failures; what is missing is a surface and in-place actions, because every item's primary action is today a **link** to `/app/inbox/[entryId]`. **Memory conflicts do not exist in this schema** — there is nothing to compose. The **review domain already ships** with four periods; the gap is continuity from Hoje. And `configure_ai_credential` sits **deliberately outside** the queue because the analytics enum validating attention reasons is enforced *in the database*, so admitting it is a migration rather than a mapper change. **Voice is the only genuinely greenfield item** — a repository-wide search for transcription, `MediaRecorder` or audio returns **zero hits in code**, the sole mention being the parent PRD itself. **Its cost question resolves technically, not by owner decision:** `user_ai_credentials.provider` is `check (provider in ('openai'))`, one provider whose API transcribes, so **the user's existing BYOK credential already authorizes it** — no project-paid AI, no project key, no new secret, no new environment variable, no new vendor. Because the owner's signed flow **discards the original audio**, voice needs no bucket, no table, no retention class, no sweep and no deletion-cascade entry. **Migration budget MAXIMUM TWO**, allocated per slice: **M1 to 2J.7** because `product_events.event_name` is a **database check constraint** and no application path can record an unnamed event, and **M2 to 2J.4** for a transcription value on `ai_usage_events.operation` — **M2 is explicitly avoidable** (`'other'` is an allowed value), so **`2 allocated · 1 spent` is a legitimate close**. **Phase 2I's accessibility residual is discharged as slice 2J.0, a PRE-CODE gate** rather than a closeout item, because deferring it to the end is exactly what produced the partial; the repository has **no axe dependency today**, and the screen-reader session is recorded **as manual** or closes as an evidenced negative — never reported as automated. **Three exclusions, each with a destination:** *O Brain percebeu* is **out** (no deterministic source, and the alternative is unbounded proactive AI on the user's own credential, against the parent PRD's own *"silence is also a result"*), attention **snooze/defer** is **out** (the only attention action needing new state), and weekly/monthly review stays Etapa 4. **Three owner decisions remain open:** `highly_sensitive` behaviour on Hoje/attention/receipts/notifications/review summaries (**OD-2J-1** — Home, attention and Work apply **no** sensitivity predicate today while search excludes it by default, so two surfaces of one product currently disagree), voice with no valid AI credential (**OD-2J-2**), and whether daily priorities may be user-selected (**OD-2J-3**). Phase 2J adds **no RLS policy, no grant, no secret, no external service and no second write path** — `T-2I-02` is inherited and stressed hardest by 2J.3, whose gate requires the guard to **fail against a planted second path before the surface is written**. **ADR-055 remains open and unchanged, expiring 2026-10-27.** Rollout gate untouched at **25 pass · 3 fail · 2 owner-signature**; signup remains closed, and Phase 2J is not progress toward it. Hosted parity unchanged at **`202608070084`**. The paragraph below is the prior state, retained.)

Previously: 2026-08-07 (**PHASE 2I — FOUNDATION AND FINDABILITY IS COMPLETE.** 61 requirements declared, **61 classified, 0 undelivered**: 53 built, 4 baseline, 1 rename, 2 evidenced negatives, 1 partial with its destination named. **Migration budget 1 allocated · 0 SPENT** — gate G-2I.2 measured before any search code and the zero-migration shape met the budget, so the allocation lapsed unspent, which ADR-093 records as the preferred close. Hosted parity is **unchanged at `202608070084`** because the phase added no migration. PRs #131 (`e2779a5`), #132 (`1660bad`), #133 (`4295200`) and #134, **one green PR-head run and one green merge-SHA run each** under ADR-090 — no green ×3, no rerun used as acceptance. **The phase's own audit was wrong three times about what was already shipped** — `chat: "Conversar"` had shipped, `Mais` already rendered grouped destinations, and Library's membership already existed as `group: "context"` data — so `2I-SHELL-003` was reclassified **built → baseline**, and the matrix carries `baseline` as a distinct class precisely so a phase cannot report an assertion of existing behaviour as though it built something. **Two requirements are delivered by NOT being built**, both evidenced and both re-derived rather than asserted: `2I-LIB-004` (zero pin/favourite columns exist anywhere in the schema) and `2I-PALETTE-009` (recents would need state `2I-PALETTE-010` forbids). **Phase 2I added no write path, no model call, no RLS policy, no grant, no secret and no external service.** T-2I-01 is proved in pgTAP **with two accounts, in both directions**; T-2I-02 is closed three ways, the strongest being that **an action has no invocable field** and that a hostile query cannot invent one. **`2I-CLOSE-002` (accessibility) is PARTIAL and says so**: component behaviour is asserted by test, but no axe pass, screen-reader session or Playwright journey was executed — classifying it built would be the over-claim the `baseline` class exists to prevent. Locale ternaries **263 before · 263 after**, ceiling 266. **Fifteen defects, thirteen of them in probes, fixtures, guards or tooling**; the benchmark's first design would have left thousands of fixture rows in production had the Management API committed, and became a `DO` block whose only exit is a raise. The generator **refused on its first real run**, naming nineteen unevidenced requirements and writing nothing; mutation-proved by eleven fixtures. **ADR-055 remains open and unchanged, expiring 2026-10-27.** Rollout gate untouched at **25 pass · 3 fail · 2 owner-signature**. **Phase 2J is unstarted**, A13 green. The paragraph below is the prior state, retained.)

Previously: 2026-08-07 (**PHASE 2I — FOUNDATION AND FINDABILITY IS AUTHORIZED FOR PLANNING ONLY — ADR-092, an owner decision. Implementation is NOT authorized.** Scope is the owner's mobile-first PRD's **Etapa 0 + Etapa 1 only**; the parent PRD stays the roadmap and is decomposed into independently closable phases, because a seven-etapa phase cannot close and a phase that cannot close cannot be traced, budgeted or gated. Governing pair: `docs/initiatives/phase-2i/PHASE_2I_PRD.md` and `..._IMPLEMENTATION_PLAN.md`; audit, threat model, traceability contract and gaps under `docs/reports/phase-2i/`. **A13 retargeted from Phase 2I to Phase 2J in the same commit as ADR-092**, the fourth application of that precedent — and ADR-092's heading says *"retargets to the roadmap successor"* rather than naming 2J, because the detector treats an accepted ADR naming the next phase in its heading as a start signal; the first draft hit exactly that and was reworded. **Signed owner decisions:** public signup is **not** a prerequisite and instrumentation must separate the internal cohort from a future public one; **dark mode is out**, not partially implemented; **palette precedes search, search precedes Library**, because the problem is discovery and building Library first would group twelve items into five and then build the tools that make grouping unnecessary; **original audio is discarded** after confirmed transcription — recorded now, but **voice is not in 2I**. **Migration budget MAXIMUM ONE**, to slice 2I.5 only, **and `1 allocated · 0 spent` is the preferred close** — the decision is taken by measurement at gate G-2I.2 before any search code, and investigation established the input: **no `tsvector`, no GIN index, no `pg_trgm` exists anywhere in the 84-migration chain.** **The audit corrected the parent PRD in three load-bearing places:** the four primary destinations, capture-as-centre-action and the five-slot mobile bar are **already delivered**; the `chat: "Conversar"` rename is **already shipped**; and Library's six members **already share `group: "context"`** — so the navigation slice is one label (`home` → Hoje) plus one grouping, not a redesign. **Two owner decisions block slice 2I.5:** whether `private`/`highly_sensitive` records appear in search results, and whether `attachments.extracted_text` is searchable. Phase 2I adds **no write path, no model call, no RLS policy, no grant, no secret and no external service**. **ADR-055 expires 2026-10-27 and is neither satisfied nor superseded.** The rollout gate is untouched and still reads **25 pass · 3 fail · 2 owner-signature**. The paragraph below is the prior state, retained.)

Previously: 2026-08-07 (**Phase 2H is COMPLETE. A bounded POST-2H ROLLOUT READINESS effort is in execution, and it is NOT Phase 2I — no successor phase is authorized.** Two owner decisions opened it. **ADR-090 retires the green ×3 CI rule for all future work**: documentation/planning PRs need **one** complete green PR-head run and a clean full-diff review; code/migration/runtime PRs need one green PR-head run, a full-diff review, merge, **one** green exact-merge-SHA run, and only then the deploy. A flake is now a **defect with an owner**, never a cost absorbed by repetition — the first one was diagnosed and fixed the same day (`project-key-guard.test.ts` spent its 5 s budget importing the OpenAI SDK graph, not on its assertion; the import moved to `beforeAll`). **ADR-091 authorizes ONE post-2H migration, `202608070084`**, which unschedules the five user-content sweeps `202608050077` still created in any database built from the chain; it is **not** charged to Phase 2H's budget, which stays **5 allocated · 5 spent**. The regression proof is a pgTAP suite that runs after CI's `supabase db reset`, because a hosted readback cannot see a chain-only defect. Backup readiness, the SMTP path, monitoring adequacy and the legal packet are written at `docs/reports/post-2h-rollout/`; the governing artifact is `docs/initiatives/post-2h-rollout/POST_2H_ROLLOUT_READINESS.md`. **The rollout gate is unchanged at 25 pass · 3 fail · 2 owner-signature — "SIGNUP MUST NOT OPEN"** — because every remaining item needs an owner act. **Two things were deliberately not done:** `SIGNUP_HARDENING_BACKUP_RESTORE.md` was not created (its mere existence passes `RG-DEP-3`, and no restore has happened), and no alerting destination was invented for `RG-DEP-4`. Two findings: the shipped restore drill counted **`public.entities`, a table that does not exist** — it would have produced a false failure on a good restore, and is fixed at the source — and the **rate limiter has no operator read**, the one visibility gap that widens when signup opens. **MERGED AND DEPLOYED (PR #127, `a20505f`).** PR-head CI green on `50003c2` and **exact-merge-SHA CI green on `a20505f`, attempt 1, all three jobs** — one run each under ADR-090, with the merge-SHA run **preceding** the deploy. **Hosted parity is now exactly `202608070084`, local = remote**, the cron catalog is byte-identical at five jobs, and the deploy's own NOTICE is the idempotency proof on the live project: *"5 cron jobs before, 5 after, five user-content sweeps forbidden and absent"* — nothing removed because hosted was already clean, nothing added, and all four postconditions still executed. **A third defect surfaced from asking what else referenced those five job names, and it is the worst of the three:** `verify-signup-rollout.mjs` computes `retentionSweepsScheduled` as *"all five are in `cron.job`"*, so in any chain-built database **`RG-QUO-3` would have read PASS with nobody having authorized a purge** — the gate that exists to require an authorized retention activation, satisfied by the very defect ADR-082 was written about. Hosted was honest only by operator accident. No code change was needed: the gate logic was right; the chain was wrong. **CI also caught a second one**: `phase_2h_error_sink_and_deadman.sql` test 46 borrowed `sh-prune-notifications` as its never-reported fixture — a job that existed only because of the defect — so fixing the defect broke the test, correctly; it now schedules its own subject. Signup **disabled**, CAPTCHA **enforced**, SMTP **unconfigured**, reaper **unarmed**, **eight sweeps built and zero scheduled**, **no purge has ever run**. **Separately, PLANNING ONLY has begun on the next UX initiative** in the neutral workspace `docs/initiatives/next-experience/`, against the owner's PRD at `docs/initiatives/product-ux/MY_BRAIN_MOBILE_FIRST_EXPERIENCE_PRD.md`. **No phase is authorized, no ADR was written, and A13 remains green.** The audit read the code rather than recollection and found that **the PRD's Etapa 1 navigation is largely already shipped** — the five-slot mobile bar, capture as the global centre action, and `work` absorbing `today`/`tasks`/`waiting` all exist, and `Biblioteca`'s six members are already tagged `group: "context"` in `capabilities.ts`, a grouping that exists as data and is not rendered. The real problem is therefore **discoverability, not navigation size**, which makes the command palette and global search the highest-leverage work. **Voice is the largest unknown**, greenfield and landing on five contracts at once, and it is blocked on one owner decision: whether the original audio is kept. **Slice 3.6 collides with ADR-055, which expires 2026-10-27.** The proposal is **"Phase 2I — Foundation and Findability" covering Etapa 0+1 only**, with ONE migration allocated to the search slice and possibly unspent — a seven-etapa phase could not close, and a phase that cannot close cannot be traced or gated. The paragraph below is the prior state, retained.)

Previously: 2026-08-07 (**Phase 2H — Deploy and Operate is AUTHORIZED FOR EXECUTION and IN IMPLEMENTATION.** The owner authorized autonomous execution of slices 2H.1 through 2H.6 on 2026-08-07, including the five allocated migrations, merging on green gates, and applying each migration to the hosted project after exact merge-SHA CI is green ×3. **Slice 2H.1 — deletion recovery — is implemented on `codex/phase-2h-slice-1`;** budget **5 allocated · 1 spent**. Gate G-2H.1 is CLEARED: `508cf6c` is green ×3 on run `31116254874` attempt 6. The paragraph below is the prior planning-only state, retained.) (**Previously — Phase 2H AUTHORIZED and IN PLANNING — ADR-085, an owner decision.** Phase 2G is COMPLETE (2026-08-06, 29 requirements, 27 delivered, 2 partial on one named blocker, hosted parity `202608060078`). The authorization covers **planning only**: governing artifacts, repository-safe research, tests and generators. It does **not** authorize merging a Phase 2H implementation PR, deploying a Phase 2H migration, enabling retention scheduling, executing a purge, opening public signup, or deploying `process-jobs` — that last is its own separate decision, ADR-086, `Proposed` and awaiting the owner. The governing pair is `docs/initiatives/phase-2h/PHASE_2H_PRD.md` and `..._IMPLEMENTATION_PLAN.md`; the threat model is `docs/reports/phase-2h/PHASE_2H_THREAT_MODEL.md` and the traceability contract sits beside it. **44 requirements across nine families; migration budget FIVE, allocated per slice and non-transferable; 0 spent.** The account-deletion retry/reaper gap — *"`re-runnable` was implemented as a property, not a mechanism"* — is promoted from a closeout residual to the first-class family `2H-RECOVER`, and slice 2H.1 owns it. The A13 phase-start guard was retargeted from Phase 2H to Phase 2I in the same commit as ADR-085, so ADR-067's invariant never lapsed; ADR-068 names no Phase 2I, so that ADR also records where the real successor gate lives — the fail-closed signup rollout checklist, not A13. **Gate G-2H.1 is not met, and the run is now terminal:** on merge SHA `508cf6c`, `database and journey` ✅ and `edge worker` ✅, but the `application` job was **cancelled at 2026-08-06T16:10:47Z after ~15 minutes queued, having executed zero steps** — it never acquired a hosted runner and never reached `Set up job`, so it proved nothing about the code either way. Run `31116254874`'s overall conclusion is `failure`. This is a GitHub Actions runner-availability failure, not a code failure, and it is the fourth such occurrence on this SHA's lineage. Clearing G-2H.1 requires a fresh successful run of that job (`gh run rerun 31116254874 --failed`), which is an owner decision on timing given the ongoing incident. **No Phase 2H implementation may merge or deploy until all three jobs are green on this SHA.** Public signup remains disabled at both layers, retention remains unscheduled, no purge is authorized or executed, CAPTCHA remains enforced, SMTP remains unconfigured. The owner's rollout tasks are unchanged and remain in `TODO.md`.)

Previously: 2026-08-05 (**Phase 2G — Conversational Creation is AUTHORIZED and IN PLANNING — ADR-083, an owner decision.** Signup Hardening is COMPLETE (SH.0–SH.7); `main` is synchronized and clean; hosted migration parity is `202608050077`; the rollout gate was re-run live and reads **25 pass · 3 fail · 2 owner-signature**, refusing to open signup; public signup remains disabled at both layers; no production purge is authorized or executed; SMTP remains unconfigured. The Phase 2G governing pair exists at `docs/initiatives/phase-2g/` — PRD and implementation plan written against the definition study as amended by its §20–§22 — with the threat model at `docs/reports/phase-2g/PHASE_2G_THREAT_MODEL.md`. The migration budget is **ONE migration**, allocated to slice 2G.3 only. The A13 phase-start guard was retargeted from Phase 2G to Phase 2H in the same commit as ADR-083, so ADR-067's invariant — the next phase must not start before it is authorized — never lapsed. The study's M2 maintenance item (`.gitattributes`, `*.sql text eol=lf`) was executed first, as its §17 ordered. Phase 2H remains unauthorised. The owner's rollout tasks — retention activation, Resend SMTP, backup-restore drill, legal signature, monitoring signature, then the two-green-runs signup flip — remain open, recorded in `TODO.md` and the handoff, and none of them blocks repository-safe Phase 2G work.)

## Phase 2H — Deploy and Operate — **COMPLETE** (2026-08-07)

- **44 requirements declared, 44 delivered and cited, 0 partial, 0 undelivered.** The fail-closed generator (`npm run docs:phase-2h:traceability`) writes `docs/reports/phase-2h/PHASE_2H_TRACEABILITY_MATRIX.md` and **refuses rather than print an unresolved claim** — its first run against the real repository refused, naming exactly the five `2H-CLOSE-*` requirements 2H.6 had not yet evidenced. Final report: `docs/reports/phase-2h/PHASE_2H_REPORT.md`.
- **Migration budget: 5 allocated · 5 spent · 0 remaining**, each by the slice it was allocated to — reconciled **per slice**, not by count, because five migrations with two belonging to one slice and none to another spends the budget exactly and breaks the rule. **Hosted parity `202608070083`, 83 migrations, local = remote.**
- **Every merge SHA green ×3 per job**, PRs #112–#125, **with one recorded exception**: `4a7e9bd` (2H.6's closeout merge) was verified green **×1** on explicit owner instruction, given after its first run came back green on all three jobs. Written down rather than rounded up — a record that turned one green run into three would be the falsehood this phase built six slices of mechanism against. Every branch preserved.
- **Twelve defects found, six of them in probes rather than in the product.** The two with no resolution inside this phase are named with destinations: there is **no operator-restorable backup** of this project (`pitr_enabled: false`, `backups: []`, plan free — which also blocks `RG-DEP-3`), and **`202608050077` still schedules five user-content sweeps at apply time**, so any database built from the chain begins deleting user content at 04:11 UTC. Unscheduling them needs a **sixth** migration; the budget is five, non-transferable, so it is an owner amendment or the next phase's first migration.
- **Nothing was opened, armed, scheduled or purged.** Five cron jobs, none a sweep of this phase's or SH.6's; eight sweeps built and **zero scheduled**; deletion reaper **unarmed** (0/2 Vault secrets, 0 stalled rows); **no purge has ever run**; restore drill **not executed**; SMTP **unconfigured**; signup **disabled at both layers**; CAPTCHA **enforced**. Rollout gate re-read live: **25 pass · 3 fail · 2 owner-signature — "SIGNUP MUST NOT OPEN"**. No owner-signature gate was satisfied on the owner's behalf.
- **Phase 2I remains unstarted**, A13 re-verified green at close. **No successor phase is authorized.**

## Phase 2H — Deploy and Operate — **SLICE 2H.5 CLOSED AND DEPLOYED** (2026-08-07)

- **Merged at `40f2fac` (PR #124) with PR-head CI green ×3**, all three jobs on each attempt. **`202608070083` applied; hosted parity exactly `202608070083`, 83 migrations, local = remote.** Branch `codex/phase-2h-slice-5` preserved. No Edge Function deployed — the slice touches neither, and the runbook forbids redeploying to refresh a timestamp.
- **The count-only transcript is recorded and reads zero**: `error_events`, `scheduled_job_health` and `rate_limit_events`, all at the signed 90 days, **0 prunable each**. Cron catalog byte-identical before and after. **`--enable` was not run.**
- **Four defects repaired before merge**, three of them found by re-reading the diff rather than by a check: the migration managed its own transaction (which would have let its own self-assertion run *after* its DDL committed, and the pre-flight hid it by stripping exactly those lines); the registry seed was not authoritative; two pgTAP assertions were about the **environment** rather than the product; and SH.6's registry assertion was a whole-table equality.

## Phase 2H — Deploy and Operate — **SLICE 2H.5 IMPLEMENTED** (2026-08-07)

- **The migration budget is spent.** `202608070083` is the **fifth of five**, and 2H.6 holds none: **5 allocated · 5 spent · 0 remaining.** A sixth `phase_2h` migration is an owner amendment. `AUTHORIZED_MIGRATION_HEAD` moved in the same commit. `2H-DEPLOY-001…004`, `2H-DEPLOY-006/007`, `2H-RETENTION-001…004`, `2H-BACKUP-001/002` delivered and cited. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_05_ACCEPTANCE.md`.
- **The windows got a home, and no value was minted.** 2H.2 built sweeps and twins for `error_events` and `scheduled_job_health` but passed the window as a required argument with nowhere to live; three rows now sit in SH.6's `private.retention_windows`, all at **90 days** — PRD §14.1's signed figure, unchanged. `rate_limit_events`, the one class this phase created that genuinely accumulates for a live user, had **no sweep at all** and gets a predicate, a twin and a sweep; §14.1 does not name it, so it **reuses** the signed window rather than inventing a fourth number, stated loudly in the migration, the constants module and the guard. A new unsigned window is an owner stop.
- **A scheduled statement now carries no number.** The 2H.2 sweeps gain a zero-argument form that reads the window from the registry and the bound from `private.retention_batch_limit()`, so the cron catalog cannot drift from the registry. Thin wrappers, not a second copy of any predicate.
- **Nothing is scheduled, and the migration says so about itself.** It asserts against `cron.job` inside its own transaction, so ADR-082's rule travels with the DDL rather than only with the repository. Eight sweeps built, **zero scheduled**, no purge ever run; the two authorized attempt prunes are untouched.
- **31 boundary assertions, run against the real hosted database before the push** — `begin; create extension pgtap; <migration>; <suite>; rollback;`, `ran 31 / planned 31, failed 0`, readback proving nothing persisted. A row one second inside the window survives; one second outside is prunable; the twin's count equals the predicate's exactly; a **live** cron job's health row is never prunable at any age.
- **Finding: there is no operator-restorable backup of this project.** `pitr_enabled: false`, `backups: []`, plan **free**. The schema is recoverable from git; the rows are not, Storage is uncovered on any plan, and hosted Auth configuration is reconstructable but not restorable. This also blocks `RG-DEP-3` — a drill needs something to restore. **Owner action.**
- **The restore drill exists and refuses production, proven by execution** (exit 3 against the linked ref, with no override flag). **The drill itself has not been run.**
- **Three more findings, each with a destination**: `.env.example` was missing `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (fixed); the Edge env scan matched a variable name inside a *comment* about the key BYOK removed, and the "fix" would have re-declared the name the BYOK guard forbids (probe fixed before it produced a verdict); the application exposes no deployed-commit identifier over HTTP.

## Phase 2H — Deploy and Operate — **SLICE 2H.4 CLOSED AND DEPLOYED** (2026-08-07)

- **Merged at `70d26a5` (PR #121) with exact merge-SHA CI green ×3**, read per job. **`202608070082` applied; hosted parity is exactly `202608070082`, 82 migrations, local = remote.** Hosted acceptance **39 of 39**. Branch `codex/phase-2h-slice-4` preserved.
- **The wiring reports on the deployed system, and it reports honestly.** 49 seconds after the deploy, both per-minute jobs read `success_empty` — succeeding ticks, no useful work, which is the truth for an idle queue. Had `useful` been derived from "the call returned", both would already read `success_work` and the 2H.0 census defect would have been rebuilt inside the mechanism designed to catch it.
- **`my-brain-entry-dispatch` reporting at all proves the Edge Function deploy worked end to end** — `process-jobs` **v21 → v22**, deployed after the migration so the drain's report could not precede the RPC it calls. `verify:edge-parity` reads *every deployed function is at or ahead of its source*.
- **Three jobs still read `never_reported`, and that is correct**: the hourly heartbeat and the two 04:xx prunes had not ticked since the migration. No evidence of a successful run reads as *no evidence*, never as health. `npm run ops:health` exits **1** and names all three — the exit-code contract ADR-089 leaves for alerting, working on its first run.
- **`2H-SINK-005` proved live**: the consumer reads 2H.2's stored calibration row, not an empty set that would satisfy the requirement's letter while repeating ADR-084's defect.
- **The Management API is refused as a control**, not just noted: it runs as `postgres` with a null `auth.role()`, so a guarded read must refuse it — which proves the caller check is real rather than a grant, and is why every behavioural probe goes through PostgREST.
- **Finding: `database.types.ts` had been stale for five migrations** (SH.6's two, 2H.1, 2H.2, 2H.3). Nothing broke because none of those objects has a TypeScript caller — which is exactly why nobody noticed. Regenerated and adopted here; full suite 4179 passed over it.
- **Nothing scheduled, armed or purged.** Five cron jobs unchanged, reaper unarmed (0/2 Vault secrets), every retention sweep unscheduled, signup disabled, CAPTCHA enforced, SMTP unset. **Budget: 5 allocated · 4 spent · 1 remaining.**

## Phase 2H — Deploy and Operate — **SLICE 2H.4 IMPLEMENTED** (2026-08-07)

- **The operator surfaces exist, and they are the half that makes 2H.2 true.** Migration `202608070082` — the slice's whole allocation; budget now **5 allocated · 4 spent · 1 remaining**, the last one 2H.5's and 2H.6 holding none. `2H-OPS-001…005`, `2H-SINK-005` and `2H-DEADMAN-004` delivered and cited. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_04_ACCEPTANCE.md`.
- **`record_scheduled_job_run` has callers now.** All five scheduled paths report their own runs: the four in-database ones (`run_all_heartbeats`, `reap_expired_jobs`, and both pre-authorized attempt prunes) and the `mode=dispatch` drain in `process-jobs`. **`useful` is derived from the path's own result** — notifications created, leases recycled, rows pruned, jobs drained — never from the call having returned, which is precisely the 2H.0 census defect.
- **The mechanism had to move to `private`.** `pg_cron` executes as `postgres` with a **null** `auth.role()`, so the `service_role`-guarded RPC would have refused every in-database scheduled path with `42501` — the same trap the Management API sets. `private.apply_scheduled_job_run`/`apply_scheduled_job_failure` are executable by **no role**; the guarded RPC and the DEFINER paths are the only ways in.
- **A failure never advances the success clock**, and `record_scheduled_job_failure` is what makes `last_outcome = 'failed'` reachable at all — 2H.2's CHECK permitted it and nothing could write it.
- **Five operator reads, all `service_role`-only, all `stable`, all declaring columns.** A `returns jsonb` operator read is unauditable by construction, so the privacy census reads `pg_get_function_result` from the catalog rather than reviewing the source. No operator read touches `account_deletion_log`, and the sink's consumer returns classification and counts — never an id, a correlation id or an owner.
- **`2H-DEADMAN-004`: deployment parity is read in the same command as liveness.** `npm run ops:health` prints queue depth, expired leases, scheduled-job liveness, scheduled-job findings, Edge Function parity and error-sink volume, and exits non-zero when any of them needs attention. `heartbeat`'s deliberate absence classifies `undeployed_by_design`, which is **not** folded into `ok`.
- **`2H-OPS-005`: alerting is defined and deliberately not built** — ADR-089. The destination, the threshold and the receiving owner are all owner decisions, and `RG-DEP-4` is still unsigned. The exit code is the whole interface an integration needs.
- **ADR-075 holds and is mechanised.** `operator-surface-boundary.test.ts` walks `src/app` for an admin route, walks every route and page for the service-role key, and asserts the CLI names only operator reads. No product admin UI, no generic service-role HTTP endpoint.
- **Nothing scheduled, nothing armed, nothing purged.** The migration schedules nothing and asserts that about itself; both pre-authorized prunes keep their DELETE and predicate byte-identical.

## Phase 2H — Deploy and Operate — **SLICE 2H.3 CLOSED AND DEPLOYED; EDGE FUNCTION PARITY RESTORED** (2026-08-07)

- **Merged at `46f7244` (PR #119) with exact merge-SHA CI green ×3**, read per job. **`202608070081` applied; hosted parity is exactly `202608070081`, 81 migrations, local = remote.** Hosted acceptance **28 of 28**.
- **`2H-RATE-004` is executed, not argued.** The CI database job raced **80 connections against a ceiling of 60** through the worker door and **30 against 20** through real authenticated sessions: exactly 60 and exactly 20 admitted, the rest refused by name, and **the limiter stored exactly 60 and exactly 20** — read from the table, not counted from replies. pgTAP 63 of 63.
- **Both stale Edge Functions were deployed as separate, gated, recorded operations.** `delete-account` v2 → **v3** (ADR-088's reap door, closing 2H.1's gap; `executor.ts` byte-unchanged), verified **19 of 19**. `process-jobs` v20 → **v21** (ADR-086 executed; the pre-flight read 14 of 14 RPC signatures against hosted parity), verified **20 of 20**, including `SH-QUOTA-008`'s `413` byte bound proved **live**.
- **`npm run verify:edge-parity` now reads "every deployed function is at or ahead of its source"** — `delete-account` ok, `process-jobs` ok, `heartbeat` undeployed by design. **The phase's founding defect was a parity gap nobody measured; it is now measured and closed.**
- **The reaper is still unarmed, and that is proved rather than assumed:** an empty secret and a well-formed wrong secret both answer `401 reap_disabled`, 0/2 Vault secrets, no `my-brain-deletion-reaper` cron job, and `account_deletion_log`/`account_deletion_attempts`/`auth.users` unchanged across every probe.
- **Nothing else moved.** Cron catalog still exactly five jobs. No sweep scheduled. No purge. No provider call (`ai_usage_events` 10 → 10). No attempt burned (4 → 4). Signup **disabled**, CAPTCHA **enforced**, SMTP **unset**.
- **Budget: 5 allocated · 3 spent · 2 remaining** — one each for 2H.4 and 2H.5, none for 2H.6.

## Phase 2H — Deploy and Operate — **SLICE 2H.3 IMPLEMENTED** (2026-08-07)

- **The distributed rate limiter exists.** Migration `202608070081` — the slice's whole allocation; budget now **5 allocated · 3 spent · 2 remaining**, per-slice and non-transferable. All six `2H-RATE` requirements delivered and cited. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_03_ACCEPTANCE.md`.
- **It reuses SH.5's proven arrangement rather than a second design** (`2H-RATE-001`): advisory lock on `(bucket, owner)`, the count inside it, the slot reserved **by inserting** in the same locked transaction. The window is **rolling**, never a clock hour — a clock-hour counter admits twice the ceiling across its boundary.
- **The signed §14.2 values live in three places and are one value**, pinned in both directions by `rate-limits-parity.test.ts`: the PRD, `src/lib/rate-limits.ts` (passed as required arguments, no default anywhere in any signature) and `private.rate_limit_parameters` (read by the drain claim, whose signature can never grow — ADR-057).
- **Admission happens once, at the first claim** (`attempts = 0`), which is how V-4 and V-5 are implemented without a flag the queue does not carry. A refused drain claim returns `null`, **burns no attempt**, and records the refusal.
- **A refusal returns as data, not as an exception** — PostgREST runs one RPC in one transaction, so a raising refusal would roll back the row that recorded it (`2H-RATE-003`). The `rate_limited` literal is pinned to the database validator in both directions (ADR-084's discipline).
- **Fail-closed on both halves** (`2H-RATE-005`): the database refuses when its state is unreadable (proved by an injected fault, with the control run first), and the application refuses on transport failure, unexpected shapes and unknown refusal names.
- **`2H-RATE-004` is proved by 80 racers against a ceiling of 60 and 30 against 20, through both doors**, reading the **stored** count from the table rather than counting replies. The verdict function is mutation-proved against nine mutations.
- **Not deployed yet — discharged 2026-08-07.** ~~`202608070081` is not applied to the hosted project, and the hosted head does not move until the merge-SHA gate is green ×3 on all three jobs.~~ It was applied the same day, inside the merge window, and the section above records the readback. Kept rather than deleted because the ordering it describes is the one every later slice inherits.

## Phase 2H — Deploy and Operate — **SLICE 2H.2 CLOSED AND DEPLOYED** (2026-08-07)

- **Merged at `88c9e3b`, exact merge-SHA CI green ×3** (run `31158204968`, read per job). **Hosted parity is now `202608070080`** — 80 migrations, local = remote. Hosted acceptance **33/33**, including the deployed table reading `confdeltype = 'n'` (the cascade defect the SH.0 drill caught), all six sentinels refused `23514` with a calibration proving a declared failure still records, and UPDATE refused even for the `postgres` owner. **One permanent artifact by design:** the calibration row cannot be removed, because the sink is append-only and its sweep is executable by no role — the alternative was an uncalibrated probe, and six refusals alone are satisfied by a writer that refuses everything.
- **The error sink and the cron dead-man switch exist.** Migration `202608070080` — the slice's whole allocation; budget now **5 allocated · 2 spent · 3 remaining**. Seven requirements (`2H-SINK-001…004`, `2H-DEADMAN-001…003`) delivered and cited. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_02_ACCEPTANCE.md`.
- **The sink's privacy property is the absence of a column, not a careful writer.** `error_events` has exactly three text columns, each CHECK-bound to a closed set, and **no** json, jsonb, bytea or free-text column at all — asserted from `pg_catalog` by a migration postcondition, so a later `ALTER TABLE` fails the apply. Six content- and credential-shaped sentinels are pushed through the writer in pgTAP and each is refused `23514`, with a calibration proving a declared failure IS recorded.
- **Append-only has two independent locks.** The revoke, and a trigger that refuses UPDATE and DELETE **even from the table owner** — because a grant is what a future migration or a returning platform default restores in silence. The retention sweep is exempted through a narrow named flag rather than by holding a grant.
- **`record_scheduled_job_run` keeps "the tick fired" and "something was done" in separate columns.** The 2H.0 census read `my-brain-entry-dispatch` at 29 042 successes against four rows of work; a health claim built on tick counts measures silence.
- **`scheduled_job_liveness` enumerates from the `cron.job` catalog at run time** and classifies against a multiple of each job's own interval, taken as a required argument. A job that has never reported is `never_reported`, **not** healthy. Proven in both directions by an executed control: the same job, the same function, only the timestamp differs.
- **Nothing is scheduled and nothing is deployed yet.** Both sweeps are executable by no role including `service_role`; the migration asserts about itself that it scheduled neither.
- **Named as a known gap, not discovered at closeout:** the sink has no consumer until 2H.4 (`2H-SINK-005`, `2H-DEADMAN-004`), and no job reports yet, so every job classifies `never_reported` — which is the honest reading rather than a false `current`.

## Phase 2H — Deploy and Operate — **SLICE 2H.1 CLOSED AND DEPLOYED** (2026-08-07, ADR-088)

- **Merged at `d7d5091`, exact merge-SHA CI green ×3** (run `31148068318`, read per job). **Hosted parity is now `202608070079`** — 79 migrations, local = remote. Hosted acceptance: **29/30 on the first run plus 7/7 on a corrected control**; the one failure was a defective probe, not the mechanism, and it is written up rather than quietly re-run. Branch `codex/phase-2h-slice-1` preserved.
- **The stalled-deletion recovery mechanism exists.** Migration `202608070079` — the slice's whole allocation; phase budget now **5 allocated · 1 spent · 4 remaining**, per-slice and non-transferable. All six `2H-RECOVER` requirements delivered and cited. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_01_ACCEPTANCE.md`.
- **What it closes.** The 2026-08-04 stall sat for two days because `re-runnable` was a property of the executor and not a mechanism that re-ran it. An account entering `deleting` is now seeded into a bounded recovery state by the lifecycle trigger, claimed under an expiring lease, retried with exponential backoff (base 2, from 15 minutes, capped at 6 hours), and after a ceiling of 5 attempts classified terminally `stalled` — visible rather than silent.
- **The lifecycle status deliberately does not move.** Every SH.1 predicate that blocks writes tests for `deleting`; adding a `stalled` *status* would have made a stuck account writable again mid-deletion. `stalled` is a recovery classification in a separate table, and pgTAP asserts the lifecycle status is still `deleting` after a claim.
- **`account_deletion_log` was not widened.** It stays revoked from every role including `service_role`; the stop reason reaches an operator through `operator_stalled_deletions`, which reads the recovery table and the lifecycle table and — asserted from `pg_proc` — never mentions the log.
- **The reaper cannot delete.** No recovery function contains a `DELETE` at all, and none mentions `auth.users` or the storage catalog; both read from the catalog so an edit that adds one fails. It posts to the executor with a database-issued lease, and the executor's refusals are unchanged.
- **ADR-088 records the second door** on `delete-account` and why a lease rather than a target parameter is what bounds it. A caller holding the reap secret and an arbitrary account id gets `409` — an executed test, not a review claim.
- **Nothing is armed, and nothing is deployed.** The migration schedules no job and asserts that about itself; arming is `npm run ops:deletion-reaper-schedule -- --enable` plus two Vault secrets, neither set. **The deployed `delete-account` build now differs from this repository**, and closing that gap is a separate recorded deployment operation in ADR-086's shape — the phase whose founding defect was an undeployed worker does not get to create a second one silently.
- **Concurrency is proven where pgTAP cannot prove it.** `scripts/phase-2h-deletion-reaper-race.mjs` runs 6 real connections against 8 stuck accounts in the CI database job, with a control pass claiming 0 and a mutation-proven validator that rejects five kinds of false green.

## Phase 2H — Deploy and Operate — **SLICE 2H.0 CLOSED** (2026-08-06, ADR-085)

- **The planning package landed.** PR #112 merged at **`05e418d`**, exact merge-SHA CI **green ×3** (`application` 11 steps, `database and journey` 21, `edge worker` 9). Branch `codex/phase-2h-planning` preserved. The baseline `508cf6c` is also green ×3 (run `31116254874` attempt 6).
- **Slice 2H.0 — the six pre-code gates — is CLOSED, with zero migrations.** Phase total unchanged: **5 allocated · 0 spent**. Evidence: `docs/reports/phase-2h/PHASE_2H_SLICE_00_ACCEPTANCE.md`.
- **G-2H.2 deployment census:** migration parity is **exact** (78 local files, local head = remote head = `202608060078`). **One** parity gap: `process-jobs`, deployed `2026-08-02T02:42`, three deployable commits behind — governed by ADR-086 and **not deployed**. `heartbeat` undeployed by design. **New finding: a merge to `main` auto-deploys the application to Vercel Production** — `05e418d` shipped at `00:37:12Z` with no operator act.
- **G-2H.3 scheduled-job census:** **5 active `pg_cron` jobs, 0 failures, 0 stale, 0 duplicated**, read from the hosted catalog at run time; nothing enabled or disabled. **5 retention sweeps are built and deliberately unscheduled** (ADR-082). Two `prune_*` jobs *are* scheduled — they prune auth/credential **attempt** records, not user content, and conflating them with retention would misread the posture.
- **G-2H.4 deletion-stall reproduction:** the cause is reproduced **live, read-only, with a control**. `service_role` on `user_ai_credentials` answers **`403 / 42501 permission denied`** — the exact recorded error — while `admin_credential_status` answers `200 null` and a control read succeeds, so the refusal is attributable to the revocation rather than to a broken key. The consequence is pinned by an existing Deno test. **The end-to-end `409` was not re-elicited** (it would require deploying the old build) and that seam is stated rather than papered over. **Zero residue; no account created, stalled or mutated; the reaper was not built.**
- **G-2H.6:** **ADR-087** records the hosting platform and names the **deployment-ownership asymmetry** as the phase's central operational fact — the app deploys itself, the database, workers and cron do not. It migrates nothing.
- **Threat model reviewed; T-2H-25 added by the census** — a merge can ship the application while workers, schema and cron stay behind, and during the Actions outage Vercel built Previews for the two commits that had **no workflow run at all**. A green preview beside absent CI is the most misleading state the platform can present.

## Phase 2H — Deploy and Operate — authorization and package (2026-08-06, ADR-085)

- **What is authorized:** planning artifacts, repository-safe research, tests and generators. **What is not:** merging an implementation PR, deploying a migration, enabling retention scheduling, executing a purge, opening public signup, deploying `process-jobs`, or claiming the deletion closeout gate fully green.
- **Governing pair:** `docs/initiatives/phase-2h/PHASE_2H_PRD.md`, `..._IMPLEMENTATION_PLAN.md`. Threat model and traceability contract: `docs/reports/phase-2h/`.
- **44 requirements, nine families** — `2H-DEPLOY` (7), `2H-RECOVER` (6), `2H-SINK` (5), `2H-DEADMAN` (4), `2H-RATE` (6), `2H-RETENTION` (4), `2H-BACKUP` (2), `2H-OPS` (5), `2H-CLOSE` (5). **0 delivered.**
- **Migration budget FIVE**, allocated 2H.1/2H.2/2H.3/2H.4/2H.5 one each, 2H.0 and 2H.6 zero. Non-transferable; a sixth is an owner amendment. **0 of 5 spent.**
- **The phase's founding defect is the reason for its scope.** The 2026-08-04 deletion stall contained all of it at once: no retry, no error sink, no liveness check, no operator surface, no deployment parity contract. `2H-RECOVER` is therefore a first-class family, not a residual, and `2H-RECOVER-006` requires the historical failure be reproduced before it is called a regression test.
- **Gate G-2H.1 is NOT met, and the run is terminal.** Merge SHA `508cf6c`: `database and journey` ✅, `edge worker` ✅, **`application` cancelled** at `16:10:47Z` after ~15 minutes queued with **zero steps executed** — it never acquired a hosted runner and never reached `Set up job`. Run `31116254874` concluded `failure`. Nothing about the application code was tested, so this is neither evidence of a defect nor evidence of its absence. It was not cancelled by this work; the cancellation predates the planning branch's push by roughly twenty minutes. Clearing the gate needs a fresh successful run of that job, and its timing is the owner's call while the runner incident continues. No implementation merge or deployment may proceed until all three are green.
- **`process-jobs` remains undeployed, and the audit `2H-DEPLOY-007` is DONE** (`docs/reports/phase-2h/PHASE_2H_PROCESS_JOBS_AUDIT.md`). Deployed build is the tree at `7be25f0`; three deployable commits have landed since. **No migration changed a signature or grant on any RPC the deployed build calls**, so the `delete-account` failure mode is specifically absent. The worker is reachable and idle — 4 jobs, all completed, newest `2026-08-02T13:06:59Z`, *after* the deployment. The audit's recommendation is to **deploy as its own change after baseline CI is green, never bundled into a slice**; ADR-086 stays `Proposed` and nothing was deployed. Not deploying leaves three live gaps: no worker-side lifecycle re-verification, no request-body bound before an unauthenticated parse, no BYOK rotation-window support.
- **Gate G-2H.5 is CLEARED — the `2H-RATE` ceilings are SIGNED (2026-08-06, owner).** V-1 **60 AI operations/user/rolling hour**, V-2 **20 accepted uploads/user/rolling hour**, V-3 **rolling window**, V-4 **worker retries consume no slot / user retries do**, V-5 **background provider work consumes the owner's slot, without double-refusing drain-admitted work**, V-6 **no exemptions, including the owner**. Authoritative in `PHASE_2H_PRD.md` §14.2. The substantive finding behind them: SH.6's deployed quotas already bound daily volume, so **C1's real gap is burst rate, not volume** — which is why both ceilings are per rolling hour. **Clearing G-2H.5 does not authorize implementing, migrating, merging or deploying slice 2H.3**, and **G-2H.1 remains red**.
- **ADR-086 is ACCEPTED, and acceptance is not execution.** Four binding conditions: separate explicit operation; never bundled into a slice merge; only after `508cf6c` is green ×3 with the audit's verification lane and rollback ready; and not during the current GitHub Actions incident. Condition 3 is **unmet** and condition 4 is **active**; `process-jobs` remains at the `7be25f0` build and **no Phase 2H slice may depend on the lifecycle gate or the body bound being live in the worker**.
- **Traceability is deliberately unautomated during planning** and says so: the fail-closed generator is `2H-CLOSE-001`, built in 2H.6, because one run against a phase with zero acceptance records reports every requirement unresolved. The gap is recorded in `PHASE_2H_TRACEABILITY_CONTRACT.md` §4 rather than discovered at closeout.
- **Nothing destructive moved.** Signup closed at both layers, retention unscheduled (ADR-082), no purge authorized or executed, CAPTCHA enforced, SMTP unconfigured, hosted parity `202608060078` unchanged.

## Phase 2G — Conversational Creation — **COMPLETE** (2026-08-06)

- **All four slices are closed and every merge SHA is CI-green on all three jobs**: 2G.1 `ad0b56c`, 2G.2 `e63e103`, 2G.3 `e2c3718` (+ deployment record `4dcced9`), 2G.4 `3d35b84` and `a87d543`. Branches preserved.
- **29 requirements: 27 delivered, 2 not delivered and named with a destination.** The accounting is generated and fail-closed — `npm run docs:phase-2g:traceability` refuses to print an unresolved claim. **One migration of one budgeted, spent and deployed**; hosted parity is `202608060078`, verified behaviourally 5/5 with both controls refused.
- **What shipped:** the composer creates a task from the natural sentence through the deployed `create_task_command` family — preview, server-minted single-use confirmation, registered undo — and files an entry from an explicit imperative through `captureEntry` reused whole. It asks rather than choosing when a sentence names both a note and a task. The phase added **zero** RPCs and **zero** grants; `direct-write-guard.test.ts` is unchanged with the `tasks` allowlist still empty.
- **PARTIALLY delivered as of 2026-08-06, superseding the line below:** `2G-ROUTE-008` and `2G-CLOSE-003`. The online-session harness maintenance made the authenticated suite runnable again — **80 passed · 7 skipped · 0 failed** against hosted parity `202608060078`, where the number that could run before was **zero** — with zero fixture residue proven by `npm run verify:online-residue`. What remains open is the **conversational-creation** journey itself, on one named blocker: every turn is a provider call under BYOK and `BYOK_TEST_USER_A_OPENAI_API_KEY` is not provisioned. No BYOK provider behaviour is claimed and no credential is invented. Evidence: `docs/reports/phase-2g/PHASE_2G_ONLINE_HARNESS_ACCEPTANCE.md`.
- **NOT delivered — retained as what was true at Phase 2G's close:** `2G-ROUTE-008` and `2G-CLOSE-003` — the authenticated journeys are **written and not executed**, blocked by hosted CAPTCHA (which blocks **all 28** online specs, a control working as designed against a harness that has not caught up) and by an unprovisioned disposable BYOK credential.
- **Three blockers, not one, and the third is a product defect.** The harness work found that hosted CAPTCHA was only the first: SH.4's consent gate had been interposing on every admin-created fixture account since it shipped, independently of Turnstile; and `/auth/v1/token?grant_type=password` answers `400 captcha_failed` for **every** client, which means `requestAccountDeletion` — the one `signInWithPassword` call in the product that forwards no `captchaToken` — **refuses a correct password on the deployment, so an account cannot be deleted**. Recorded in `TODO.md` as a product change and deliberately not made inside a test-harness PR. Nothing about CAPTCHA, GoTrue or product authentication was weakened.
- **The funnel is empty, and that is the phase's honest result.** Zero qualifying commands. ADR-055's gate expires **2026-10-27**; the study's R9 named this exact outcome. If it expires empty, the honest reading is that nobody typed a command — not that semantic retrieval was measured and found wanting.
- **Four defects found, each with a lesson that generalises:** a producer with no consumer is invisible on both sides (ADR-084 — SH.6's quota refusals recorded nothing, and do not backfill); a test-harness fallback can make a whole assertion vacuous; a probe whose controls agree with its positives has measured nothing; and a document saying "this did not happen" must not satisfy the requirement it describes.
- **Nothing destructive moved.** Signup closed at both layers, retention unscheduled, no purge authorized or executed, SMTP unconfigured. **Phase 2H remains unauthorised.** Final report: `docs/reports/phase-2g/PHASE_2G_REPORT.md`.

## Phase 2G — authorization and planning (retained, 2026-08-05)

- **The authorization is ADR-083**, recording the owner's Phase 2G start directive of 2026-08-05 — the exact decision `AUTONOMOUS_LOOP_HANDOFF.md` §34 named as the missing precondition. This section supersedes every earlier claim below that "Phase 2G remains unauthorised", "has no governing artifact", or "no initiative is currently active"; those lines are retained as history.
- **Scope, from repository truth:** the create verb in the command taxonomy, routed to the deployed `preview_task_command_creation` → `issue_task_command_creation_confirmation` → `create_task_command` family — an addressability fix over infrastructure Phase 2E built and Phase 2F consolidated, stated as such. Creation surfaces are **tasks**, plus **entries** in the separately gated capture-routing slice. Reminders, projects, people, organizations, contexts, events and memories-as-commands **refuse deterministically** — reminders would reopen the Option C posture Phase 2F bounded, and the rest have no command contract. Preview-then-confirm is mandatory; undo rides the registered handler; provenance, idempotency and BYOK gating are consumed, not rebuilt.
- **Slices:** 2G.1 create-intent contract (0 migrations, zero re-established by an executed inventory gate) → 2G.2 creation from the composer (0) → 2G.3 capture routing (the phase's ONE migration: `captureSource` allowlist widening) → 2G.4 convergence and closeout (0). The withdrawn items stay withdrawn: no spend ceiling (BYOK made the owner not the payer; SH.6 owns quotas), and `max_output_tokens` is verified already shipped on all three provider operations.
- **Guards moved with the authorization, in one commit:** A13 now protects Phase 2H with the same four signals; the `product-ux-documentation.test.ts` prose guard now requires `STATE.md` to name ADR-083 beside any claim that Phase 2G started; `docs-taxonomy-guard` dropped its stale `phase-2g` exception the moment the governing pair existed.
- **Slice 2G.1 is CLOSED.** The planning PR #99 merged at `7516569` and the slice PR #100 at `ad0b56c`, both with exact merge-SHA CI green on all three jobs (the first runs were auto-cancelled by branch concurrency and re-run to completion — recorded because a cancelled merge-SHA run is not a green one); branches preserved. The wire contract carries the `create` classification; policy version `2026-08-05.1` with all four policy-lock digests unmoved and the fingerprint invalidation exercised; G-2G.1's zero-migration claim re-established by executed inventory. Evidence: `docs/reports/phase-2g/PHASE_2G_SLICE_01_ACCEPTANCE.md`.
- **Slice 2G.3 is CLOSED AND DEPLOYED.** PR #102 merged at `e2c3718`, exact merge-SHA CI green on all three jobs; **hosted parity is now `202608060078`** and the phase's migration budget is spent. The widened vocabulary is verified **behaviourally** on the deployed project, not merely by parity: 5/5 on a disposable account, with **both controls refused** (`400 / 22023`). Three earlier probe shapes measured nothing — wrong parameter names, a `service_role` caller the EXECUTE grant stops before the body, and a password sign-in that hosted CAPTCHA refuses by design — and each time every case returned identically, which is what the controls existed to reveal. The session that worked is SH.5's own `admin/generate_link` → `verify` exchange. Disposable account removed on every run, including the failures. Evidence: `docs/reports/phase-2g/PHASE_2G_MIGRATION_078_DEPLOYMENT.md`.
- **A finding recorded for 2G.4:** hosted CAPTCHA refuses automated password sign-in, and every `online-*.spec.ts` journey signs in through the login form — so the written-not-executed journeys may not be runnable headlessly as written. 2G.4 must establish this before planning around them; the fix, if needed, is a Playwright fixture change rather than a product change.
- **Slice 2G.3 repository record (retained).** `202608060078` is a `create or replace` of `private.validate_product_event_properties` that creates, drops and grants nothing; its body was diffed against `202607280061` **as sets of trimmed lines** (a positional diff of a 203-line re-declaration hides transcription errors) and exactly four lines changed. `AUTHORIZED_MIGRATION_HEAD` moves with it. **Any further DDL in Phase 2G is now an owner budget amendment.** The composer files an entry from an explicit imperative through `captureEntry` reused whole — lifecycle gate, SH.6 quotas, idempotency key, `awaiting_ai_configuration` and the worker nudge all inherited — and asks rather than choosing when a sentence names both a note and a task. ~~**`202608060078` has not yet been applied to the hosted project at the time of writing.** Hosted parity therefore remains one behind the chain head, at `202608050077`, and the app half degrades safely meanwhile because the event is best-effort and the capture itself still succeeds.~~ **Superseded** by the line above: `202608060078` was applied and verified behaviourally, and the sentence was true only until that deployment. Retained struck rather than deleted, because it is what the record said at the time. Evidence: `docs/reports/phase-2g/PHASE_2G_SLICE_03_ACCEPTANCE.md`.
- **The slice's inventory gate found a live defect, and ADR-084 records it.** SH.6 shipped `capture/actions.ts` emitting `failureKind: 'quota'` for a quota-refused capture; `'quota'` was in neither governing vocabulary, so `parseProductEventPayload` rejected the payload **before the RPC**, at a call site that wraps its emission in `.catch(() => {})`. **Every quota refusal has recorded no telemetry at all** while the code read as though it recorded one — the ceilings SH.6 proved under genuine concurrency have had invisible refusals ever since. Fixed in the same `create or replace` at zero additional migration cost; **the lost events do not backfill**. A regression test now pins the producer, `contracts.ts` and the migration against each other, and found its own behavioural half silently vacuous through an `importActual` shim that falls back to `{}`.
- **Slice 2G.2 is CLOSED.** PR #101 merged at `e63e103` with exact merge-SHA CI green on all three jobs; branch preserved.
- **Slice 2G.2 repository work (retained record).** The natural sentence routes to the deployed creation family — preview, server-minted single-use confirmation, `create_task_command`, registered undo — with zero migrations, zero RPC changes and the `tasks` direct-write allowlist still empty. The session envelope gains a `create` discriminator (`2026-08-05.1`); qualified creations re-enter `deriveTaskCommand` so one validator owns temporal, vocabulary and bounds; the `unsupported_action` refusal names the supported surfaces in both locales; an explicit create deliberately emits no `task_command_previewed` event (the no-match funnel ADR-055 measures stays unpolluted; the vocabulary question is dispositioned at 2G.4). **The authenticated journey is WRITTEN, NOT EXECUTED** — every conversational turn is a BYOK provider call, so `e2e/online-conversational-creation.spec.ts` runs in 2G.4's hosted verification. Evidence: `docs/reports/phase-2g/PHASE_2G_SLICE_02_ACCEPTANCE.md`.
- **FIRST LIVE PRODUCTION PURGE: NOT AUTHORIZED / NOT EXECUTED. Public signup remains closed. Phase 2H remains unauthorised.**

Previous update, retained: 2026-08-05 (**Signup Hardening is APPROVED and IN IMPLEMENTATION;** SH.0–SH.4 are merged and deploy-accepted; SH.5's repository work is COMPLETE, its migration `202608040075` is APPLIED to the hosted project, and the slice now stops at one owner-only action — enabling Turnstile in hosted Auth.** The throttle is wired to every auth path with admission before every provider call, its ceilings are demonstrated under genuine concurrency against the deployed project, and the Turnstile widget and token plumbing ship — but **SH-CAPTCHA-002 is not claimed**, because hosted CAPTCHA is off and the enforcement probes have not run. Custom SMTP is still absent, so two delivered-link journeys stay deployment-blocked and the resend ceiling stays provisional. Public signup remains disabled at both layers. The owner's `APP_ORIGIN` report was verified rather than accepted, at the provider, because a green recovery is consistent with a wrong origin; the redirect allow list is now verified by observation without SMTP, and its enforcement turns out to be silent rewriting rather than refusal. Custom SMTP is **not** configured and Turnstile is **not** enabled — both read from the hosted configuration, not asked. Public signup remains disabled at both layers. The hosted project is at migration parity `202608040074` and SH.2, SH.3 and SH.4 have been executed against it — every row those slices carried as NOT EXECUTED is closed, including SH-WORKER-004/005 and SH-ADMIN-005. Three re-runnable acceptance journeys and the deployed `delete-account` function are the artifacts; one production defect was found and fixed by running them. The storage-orphan manifest is **taken** (six objects, all `absent-owner`, no halting class) and deletion remains **unauthorized**. Five of the eight budgeted migrations are spent and **no migration was spent by the acceptance run**. The loop now stops before `202608040075`: SH.5's throttle ceilings must sit at or below the hosted GoTrue rate limits, and reading those needs the Management API — an owner action. Public signup remains disabled. The owner approved the full planning package — `ADR-077`; PRD `P-1`; plan `A-1`; ADR-073…ADR-076 now Accepted — including the eight-migration budget, the SH.0–SH.7 sequence, the §20 quota values and §7 retention schedule as proposed, the deletion-executor/admin-boundary/CAPTCHA architecture, and the v1 no-scanner storage posture. SH.0 is the active slice: the SH-DELETE-001 cascade drill and the SH-EXPOSURE-002 grant-census skeleton enter CI as permanent artifacts, zero migrations, head stays `202608010069`. Hosted signup remains disabled; nothing destructive is authorized; Phase 2G remains unauthorised.)

Previous update, retained: 2026-08-02 (**BYOK IS CLOSED.** Every gate is executed against the deployment: runtime parity `IN PARITY`, the owner's credential opens under both runtimes, the owner's asynchronous AI runs on it, two-user isolation with real credentials passes synchronously and asynchronously, genuine concurrent rotation resolves to one winner and one declared conflict, the Settings journeys pass 4/4 on desktop and Pixel 7 in both locales, and BYOK.6's last code deliverable -- the bounded two-key master-key rotation window -- is built, parity-locked across both runtimes and drilled. Zero migrations in the closeout; head stays `202608010069`. **Not closed and named rather than counted as delivered:** `BYOK-OPERATIONS` (6 requirements) is not built, the production master-key rotation has never been run (owner-authorised, no undo), six orphaned storage objects from 2026-07-16 predate this work, and 47 of 131 requirement ids are untraced -- all dispositioned in `docs/reports/byok/BYOK_TRACEABILITY_MATRIX.md`. **Signup Hardening is now authorised** per ADR-068; Phase 2G remains unauthorised.)

## Documentation is filed by initiative, and placement is guarded (2026-08-05)

- **`docs/` separates the living canon from per-initiative material.** The `docs/` root holds only the contract — `STATE.md`, `TODO.md`, `DECISIONS.md`, `CHANGELOG.md`, `ENGINEERING_STANDARDS.md`, `ARCHITECTURE.md`, `DATABASE.md`, `AI_AGENT.md`, `SECURITY.md`, `PRD.md`, `IMPLEMENTATION_PLAN.md`, `README.md`. Every PRD, implementation plan, proposal and phase-closing report now lives in `docs/initiatives/<name>/`. [`docs/README.md`](./README.md) is the index.
- **`initiatives/<name>/` and `reports/<name>/` use the same directory name**, so knowing one path gives the other: `initiatives/byok/` states the contract, `reports/byok/` holds the evidence.
- **`src/lib/closeout/docs-taxonomy-guard.test.ts`** fails the `application` job for a document added to the `docs/` root, a governing artifact left directly in `docs/initiatives/`, a non-kebab-case directory, an empty initiative directory, or an initiative that reported without a governing directory.
- **Phase 2G still has no governing artifact and must not acquire one without owner authorization.** The A13 detector now walks `docs/` recursively, so a Phase 2G PRD filed under `docs/initiatives/phase-2g/` is caught exactly as one at the root would be.

## Reports are filed by initiative, and placement is guarded (2026-08-05)

- **`docs/reports/` is a per-initiative taxonomy, not a flat directory.** Every report lives under the phase or initiative that governed it; [`docs/reports/README.md`](./reports/README.md) is the index and states which directories are closed and which initiative is active. **No initiative is currently active.**
- **A new phase creates its directory before its first report.** The rule is written in `ENGINEERING_STANDARDS.md` §"Reports and evidence placement" and mechanized by `src/lib/closeout/reports-taxonomy-guard.test.ts`, which fails the `application` job for a report placed at the reports root and names the path it should have used.
- **Durable handoff state is split across two paths, on purpose and now cross-linked.** `docs/reports/AUTONOMOUS_LOOP_HANDOFF.md` holds §1–§32; the repository-root `AUTONOMOUS_LOOP_HANDOFF.md` continues from §33. §33's claim that the file "did not exist before §33" was written by a loop that did not find the older one, and is corrected in place.
- **Nothing else changed.** No runtime behavior, migration, hosted configuration, rollout gate, signup posture or retention schedule was touched by the reorganization.

## Signup Hardening — **the initiative is COMPLETE (SH.0–SH.7)**; public signup remains CLOSED (`AUTONOMOUS_LOOP_HANDOFF.md` §33).

- **SH.7 delivered the initiative's actual output: the fail-closed rollout gate.** `npm run rollout:verify` against the deployed project reads **25 pass · 3 fail · 2 owner-signature**, and a failed or unsigned gate is a closed door — the script has no path that reports otherwise. The three failures are honest and named: retention sweeps deliberately unscheduled (ADR-082), SMTP unconfigured, and backup/restore never performed. The two signatures are professional legal review and monitoring adequacy. `signup-rollout-gate.test.ts` proves the fail-closed direction in 30 cases, the decisive one being that **no gate passes against an empty repository**.
- **Every requirement is accounted for.** `npm run docs:signup-hardening:traceability` generates the matrix and fails rather than print an unresolved claim: **137 requirements, 131 delivered, 3 deferred with a destination, 3 not delivered and named.** It found two defects in its own input on the first run.
- **Post-initiative re-census (SH-EXPOSURE-007):** 46 public base tables, **zero** user-owned tables missing forced RLS, **zero** `anon` table grants, and exactly the six pinned zero-`service_role` tables. One delta measured rather than assumed — the hosted catalog grants `anon` EXECUTE on eight pre-existing trigger functions CI does not show, and all eight were probed: seven answer `404 PGRST202` because PostgREST does not expose trigger-returning functions, the eighth refuses `0A000`. **A catalog difference, not a reachable one.** Recorded with a destination; revoking would need a migration this initiative has no budget for and would remove zero exposure.
- **Storage (SH-STORAGE-005):** bucket `public = false`, size limit and MIME count matching the constants, and all **four** own-prefix policies present. The first count read three because it filtered on `qual` and an INSERT policy carries `with_check` — recorded, because a probe reading the wrong column reports a shortfall that is not there.
- **Public signup remains closed and this initiative did not open it.** SH-ROLLOUT-005 is post-initiative and owner-only: one green gate run, an owner flip, then a second green run against the open state. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_FINAL_REPORT.md`.
- **FIRST LIVE PRODUCTION PURGE: NOT AUTHORIZED / NOT EXECUTED.**

## Signup Hardening — SH.6 close (retained).

- **SH.6 CLOSED 2026-08-05.** PR #93 merged at `cf540c7` and PR #94 at `6228a88`, both with exact merge-SHA CI green on all three jobs; branches preserved. **Hosted migration parity is `202608050077`** — both SH.6 migrations applied, in order, with a readback after each. **The eight-migration budget is fully spent and was not exceeded.**
- **Proven on the deployed database, not argued:** quota ceilings hold under genuine concurrency (60 simultaneous inserts, ceiling 50, exactly 50 admitted and 50 *stored* — the assertion is the stored count, because a count-then-insert design over-admits only there); the rest of the deployed lane is 11/11 with **zero** residual rows; `service_role` is now refused (403) on both BYOK tables, with master-key rotation proven still working through the narrow replacement; the `heartbeat` Edge Function is undeployed and reads back 404 while the in-database hourly path keeps running. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_SH6_DEPLOYMENT.md`.
- **Retention: built, deployed, and deliberately not running.** The dry-run measured **0 eligible rows in all seven classes**. The sweeps are executable by no role — `service_role` included — and **nothing schedules them**. Applying `202608050077` had scheduled all seven (its own `cron.schedule` block), which would have purged at 04:11 UTC the next morning with the dry-run transcript arriving afterwards to describe a deletion that had already happened; the five new schedules plus a duplicate of BYOK's sweep were removed the same day, leaving SH.5's and BYOK's authorized ones untouched. `scripts/sh6-retention-schedule.mjs --enable` is now the gate, and enabling **is** the authorization (ADR-082).
- **`sweepActive` stays `false` and the Privacy Policy keeps its honest-notice warning.** Deployed is not enforced: a window whose sweep is not scheduled is not being applied to anyone.
- **FIRST LIVE PRODUCTION PURGE: NOT AUTHORIZED / NOT EXECUTED.** A deliberate stop with a named owner action, not incomplete implementation.
- **Five defects found and recorded append-only** — three by review, one by CI, one by the deployment itself. The last is the one that mattered: a migration that schedules a destructive sweep has already authorized it.
- **Public signup remains disabled at both layers. SMTP remains unconfigured and no readiness is claimed. SH.7 is not started; Phase 2G and 2H remain unauthorised.**

## Signup Hardening — SH.6 repository phase (retained).

- **SH.6 repository work complete 2026-08-05, on two open PRs.** PR #93 (`codex/sh-slice-6-quotas`, migration `202608050076`) is green on all three jobs and **not merged** — merging is an action this loop is not permitted to take. PR #94 (`codex/sh-slice-6-retention`, migration `202608050077`) is stacked on it. **The eight-migration budget is now fully spent and was not exceeded.**
- **Quotas:** entries per UTC day, live jobs, storage bytes and object count, attachments per entry and per day, and per-owner drain fairness — all enforced on the TABLE rather than inside the RPC, because `authenticated` holds INSERT on all four tables and a ceiling inside `capture_entry_async` alone is one any client walks around with one POST. The triggers are `AFTER ... FOR EACH STATEMENT` over transition tables: a row trigger cannot see the earlier rows of its own statement, so a hundred rows in one array-bodied POST would each have counted zero. The ceilings are rows in `private.quota_ceilings`, so changing one is an UPDATE and not a migration (SH-QUOTA-010, ADR-057's constraint).
- **Two defects this slice's own review caught before CI:** the multi-row bypass above, and an exhausted-job lockout — `failed` with attempts spent has no `exhausted` status to move to, so counting it as live meant an owner with fifty dead jobs could never capture again while the copy told them the queue was full and nothing was running.
- **Retention:** seven sweeps, seven count-only twins, and ONE predicate per class shared by both, so a dry-run cannot report a small number for a large delete. Scheduler-only (executable by no role at all, `service_role` included), bounded per invocation, boundary-tested one second either side of every cutoff in both directions. Two named carve-outs: exhausted jobs are terminal, and an `undo_operations` row a candidate resolution points at is retained because the composite FK is `NO ACTION` and nulling the pointer would rewrite product history to suit a retention job.
- **Exposure:** `service_role` loses table DML on both BYOK tables. `SH-EXPOSURE-001` assumed nothing used that grant; the deletion executor and the master-key rotation both did, so they were routed through three narrow named functions instead of the closure being weakened (ADR-081). **T-26 is narrowed, not eliminated, and says so:** rotation still reads ciphertext, because the master key lives in the operator's environment and never in the database. The `authenticated` grant matrix is now pinned in full — norm plus 27 named exceptions, failing by table and by privilege. The `audit_logs` INSERT is retained by decision with the writer *surface* guarded. The proxy's fail-open is bounded to development.
- **NOT DONE, and deliberately: no production purge has run and none is authorized.** Amendment P-1 — approving the schedule was not approving a purge. The retention `sweepActive` flags stay `false`, so the Privacy Policy keeps its honest-notice warning; they flip after the deployment readback, not at merge. Neither migration is applied to the hosted project, so `npm run sh6:retention-dry-run` and `npm run sh6:quota-concurrency` have not run. The `heartbeat` Edge Function is dispositioned **undeploy** (nothing calls it; `pg_cron` calls `run_all_heartbeats()` inside the database) and the undeploy readback is an owner action. Evidence and the exact owner list: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_06_ACCEPTANCE.md`.
- **Public signup remains disabled at both layers. SH.7 is not started. Phase 2G and Phase 2H remain unauthorised.**

## Signup Hardening — SH.5 close (retained).

- **SH.5 CLOSED 2026-08-05.** PRs #85–#90 merged, all six merge SHAs green on all three jobs. **SH-CAPTCHA-002 is COMPLETE**: automated probes proved provider enforcement (every raw-request shape that skips the widget is refused `400 captcha_failed`, refusal identical for known vs unknown), and the owner's interactive sign-in closed the two valid-token probes that automation could not run. The SKIPPED record is **preserved and appended to**, not rewritten — and the three kinds of evidence stay distinct, because a green run of bypass probes is compatible with a product that refuses *everyone*. **SH-SIGNUP-007 is DONE**: hosted password policy raised `6 → 12` with four character classes by targeted PATCH (242 fields compared, exactly 2 changed, 0 unintended, twelve postconditions held), verified behaviourally 6/6 on a disposable account that was then deleted. The gap it closed was real — Zod guards the Server Actions, and `PUT /auth/v1/user` is validated by GoTrue alone.
- **Still open, all SMTP-blocked and carried to the rollout gate:** confirmation-required's behavioural half, both delivered-link journeys, SH-SIGNUP-011's unmeasured timing residual, and the final resend ceiling (`AUTH_EVENT_CEILINGS.resend` keeps its `provisional` marker). **No SMTP readiness is claimed.**
- **Migration budget at SH.5's close: six of eight spent.** SH.6 spent the remaining two; see the SH.6 block above.

- **`202608040075` is applied to the hosted project (2026-08-05).** Hosted head is now `202608040075`; five pre-flight checks and six post-apply verifications are recorded in `SIGNUP_HARDENING_MIGRATION_075_DEPLOYMENT.md`. It had to precede the wiring, not follow it: the throttle fails **closed** by design, so code calling a `claim_auth_event_slot` that did not exist would have taken production authentication down. `db push` changed schema and only schema — the full 239-field Auth configuration was read back before and after and diffed **byte-identical**, so `disable_signup` and the redirect allow list are provably untouched.
- **The throttle is wired to every auth path, and admission precedes every provider call.** Signup, recovery, sign-in and the new resend surface each claim a slot before touching GoTrue, so the refusal a caller sees is ours — with uniform copy — rather than the provider's, which differs between an address that exists and one that does not. Ordering is asserted textually per action. The signup gate still refuses *before* the throttle, so a closed door cannot be used to exhaust someone's allowance.
- **SH-THROTTLE-002's concurrency half is DEMONSTRATED, not argued.** `scripts/sh5-throttle-concurrency.mjs`, against the deployed project as `anon`: 10 simultaneous claims on a ceiling of 3 admitted exactly 3; 20 on a ceiling of 5 admitted exactly 5. A pgTAP file is one session and cannot show this — a sequential test passes whether the advisory locks exist or not.
- **An unreachable throttle refuses.** Considered and rejected: proceeding on error would make the control one an attacker can switch off by inducing errors, with nothing reporting it. Four refusals stay four facts — a ceiling, an unreachable throttle, a failed challenge and a wrong password each get their own code and copy.
- **The deployment probe found a defect no test here would have caught, and it was the lockout kind.** The CSP blocked `challenges.cloudflare.com`, so the widget rendered with the right site key and the right field name while Turnstile never injected its response input — **no token could exist**. Silent while hosted CAPTCHA is off; the moment it is switched on it becomes *every sign-in refused for a missing token*. Fixed route-scoped in PR #86 and verified live. The trap inside the fix: **two CSP headers on one response intersect**, so a looser `/auth` policy layered on the strict global one would have changed nothing while looking like a fix. **And then a second lockout-class defect, now proven rather than suspected:** the configured site key does not work on this hostname. On the live page, in one browser session, varying only the key — Cloudflare's always-passes key returned a real 21-char token, the always-blocks key fired `error-callback` `600010`, and the configured key timed out with zero iframes and no callback. So the environment *can* complete a challenge and error callbacks *do* propagate; the real key never starts. An earlier reading that blamed automation and local DNS was **wrong and is corrected in place**. **Owner action, and it comes before enabling anything:** Cloudflare dashboard → Turnstile → this widget → confirm **Hostnames** contains `my-brain-dusky.vercel.app` and the displayed site key matches `NEXT_PUBLIC_TURNSTILE_SITE_KEY`.
- **SH-CAPTCHA-002 is SATISFIED (2026-08-05).** The owner enabled hosted CAPTCHA with Turnstile; `security_captcha_enabled = true`, provider `turnstile`, secret present, read back before probing. Against the deployed project as `anon`, **every** direct-API shape that skips the widget is refused `400 captcha_failed` — no token, invalid token, empty string, `null`, token at the top level instead of inside `gotrue_meta_security`, meta object absent, and raw `/signup`. **UI-only enforcement is structurally impossible**, which is the requirement's exact words, and T-18 closes with it. The refusal is **identical for a known and an unknown address**, so enforcement did not open the enumeration channel SH-SIGNUP-011 closes. On the deployed application, a missing token lands on `?error=captcha-failed` — its own declared code, not `invalid-credentials` — so requirement 11 is verified on the deployment rather than only in unit tests. **Not proven, and recorded as such:** that a real client can currently solve the challenge. Probes 3 and 5 are **SKIPPED, not passed** — Turnstile declines automated browsers by design — and the auth log is ambiguous because the three `grant_type=password → 200` predate the earliest provably post-enablement refusal. One interactive sign-in settles it. **My earlier "the site key is broken" verdict is withdrawn:** the control was Cloudflare's always-passes key, which passes *regardless of client* and therefore never controlled for bot detection. A control exempt from the mechanism under test is not a control; two conclusions were published from it and both were wrong, in opposite directions.
- **Turnstile plumbing ships.** The widget rides four auth forms and the token reaches GoTrue. **The sign-in form carries it too**, though SH-CAPTCHA-003 names only three: Supabase's CAPTCHA protection is a per-project switch GoTrue applies to the password grant as well, so enabling it without a widget on sign-in would lock every existing account out, the owner's included. `security_captcha_enabled` is still `false` and **SH-CAPTCHA-002 stays unclaimed until the deployed missing/invalid/valid-token probes run.**
- **Still blocked on custom SMTP, and carried to the rollout gate:** no production email readiness is claimed, `rate_limit_email_sent = 2/hour` is a default-SMTP artefact and is never presented as a product ceiling, `AUTH_EVENT_CEILINGS.resend` keeps its `provisional` marker, and the two delivered-link journeys are recorded as deployment-blocked.
- **Not done, named rather than counted:** the hosted password policy (SH-SIGNUP-007, owner dashboard), confirmation-required's behavioural half (SH-SIGNUP-005, needs SMTP), and SH-SIGNUP-011's **timing** residual, which the requirement says to measure once and record — it has not been measured. Evidence: `SIGNUP_HARDENING_SLICE_05_ACCEPTANCE.md`. **SH.6 and SH.7 are not started.**

- **PR #84 is open and green at `d9c5ff9`, and is NOT merged.** All five checks pass, including the `database` job that applies the whole chain from empty and runs the new suite. `main` remains at `8ed69ed`; everything below labelled SH.5 lives on `codex/sh-slice-5-throttle` until that PR merges. The first CI run **failed** and the failure was the valuable kind: the new 46-assertion suite passed, while three **pre-existing** census assertions caught the migration — BYOK-SCHEMA-013's "`ip_hash` on exactly one table", SH.0's "no public function carries an explicit grant to `anon`", and SH.0's `service_role` revoke carve-out. All three are declared sets whose comments require a new member to join by name in its own slice; all three were widened by name citing ADR-080, **not loosened**, and the tempting fix for the first (rename the column) was refused because the value *is* an IP hash.

- **The owner's `APP_ORIGIN` report is VERIFIED, not accepted (2026-08-04).** All seven verification points hold against the running system: the deployment is healthy, recovery returns `recovery-sent` with **zero 5xx**, the `auth-misconfigured` refusal renders nowhere, hosted `site_url` and the 12-entry allow list match repository truth with no wildcard and no preview host, and signup is closed at both layers. The point that mattered — *which* origin the deployment sends — could not be settled by watching recovery succeed, because `resolveConfiguredOrigin` accepts any https origin **and** localhost, and localhost is allow-listed for the online suite. Vercel's CLI here is unauthenticated, so it was read at the provider instead: the hosted auth log records the deployment's own `/recover` with `redirect_to = https://my-brain-dusky.vercel.app/pt-BR/auth/callback?next=%2Fpt-BR%2Fauth%2Freset`. Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_ORIGIN_VERIFICATION.md`.
- **The redirect allow list is now verified BY OBSERVATION, and it did not need SMTP.** The readback report claimed a delivered email was the only way; that was wrong and is corrected in place. `admin/generate_link` composes the link GoTrue *would* send without sending it — an attacker host and a Vercel preview host were both offered and both **rewritten to `site_url`**. The enforcement shape is the finding: a disallowed `redirect_to` returns **200**, not an error, so `site_url` is the real backstop and **a mis-set `APP_ORIGIN` fails silently** — mail still arrives, pointing at the wrong host, with nothing in the application able to detect it. This check must be repeated after any change to the deployment's environment.
- **SMTP and Turnstile were established by reading the hosted configuration, not by asking.** The owner's brief left both as unfilled placeholders. Every `smtp_*` field is `null` — **custom SMTP is NOT configured and there is no verified sending domain**, so `rate_limit_email_sent = 2/hour` is a default-SMTP artefact. `security_captcha_enabled` is `false` and the provider is still `hcaptcha`, against ADR-076's Turnstile — **Turnstile is NOT configured**. No credential value was requested or displayed.
- **Migration `202608040075` is SPENT — six of eight. The mechanism is built; nothing calls it yet.** `auth_event_attempts` (hash-only, forced RLS, explicit deny-all policy, zero table grants), `claim_auth_event_slot` / `finalize_auth_event_attempt` under advisory locks, `private.auth_event_ceiling_cap`, and the scheduler-only 30-day sweep. **ADR-080** records the three decisions the plan left open — chiefly that `anon` holds `execute` (its first in this database), because every event here predates the session and the alternative was an RLS-bypassing service-role key in the web tier. The migration was **not** blocked by the pending SMTP number: ceilings are required parameters, so no provider-dependent value enters DDL. 46 pgTAP assertions cover shapes, grants, the lockout matrix, retention and the no-plaintext rule; **genuine concurrency is stated as NOT covered** — a pgTAP file is one session, so the advisory-lock claim is argued and not yet demonstrated.
- **A real defect was found by writing SH-SIGNUP-010's guard-of-the-guard.** `safeAuthNext` returned `/en/app/../../evil` verbatim; a browser resolves it to `/evil`. Not an open redirect — it cannot leave the origin — but it defeated the subtree pin the allowlist exists to enforce. Fixed in the guard, with backslash rejected alongside.
- **What SH.5 still does NOT have, stated plainly:** the Server Actions do not call the throttle yet, so `SH-THROTTLE-003` and the enumeration-uniform refusal are **not** in force; there is no confirmation-resend surface (`SH-SIGNUP-008`); there is no Turnstile widget or token plumbing (`SH-CAPTCHA-003/004`); session-fixation evidence (`SH-SIGNUP-012`) is not pinned; `database.types.ts` has not been regenerated for the two new RPCs; and no SH.5 acceptance report exists. **SH.5 cannot close yet. SH.6 and SH.7 are not started.**

- **SH-GD.1 is CLOSED and it found two live defects (2026-08-04, PR #83, merge `09cf1d3`).** With the owner-supplied PAT, the full 242-field hosted Auth configuration was read back. It showed the **production** project carrying `site_url = "http://localhost:3000"` with an **empty** redirect allow list — so every confirmation and recovery link the deployed product could send pointed at the recipient own machine. Separately, probing the deployment found **password recovery returning a bare HTTP 500**: PR #82 configured origin throws when unset and Vercel Production has no `APP_ORIGIN`. Both fixed; the 500 fix is **verified live** (declared `auth-misconfigured` refusal renders, no 5xx). Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_HOSTED_AUTH_READBACK.md`.
- **The hosted posture is now repository truth**, generated from `buildAuthCallbackUrl` so the allow list cannot disagree with the URLs the Server Actions send, and applied by a Management API **PATCH** rather than `supabase config push` — 242 fields returned means a push turns a two-field change into a change to everything. Result: **2 changed, 0 unintended**, proven by a full before/after diff. 12 exact redirect URLs, no wildcard, no preview host.
- **Provider ceilings are known**, which was §23 blocker: `rate_limit_email_sent = 2/hour` (binding, and the *default* Supabase SMTP value — it exists because no custom SMTP is configured), `rate_limit_verify` 30/5min, `rate_limit_otp` 30/5min, `rate_limit_token_refresh` 150/5min.
- **SH.5 is still incomplete and migration `202608040075` is unspent** (five of eight remain). Outstanding: `auth_event_attempts`, throttle claim/finalize, concurrency tests, retention/prune, confirmation resend, enumeration-uniform responses, session-fixation evidence, Turnstile plumbing, acceptance report. **SH.6 and SH.7 not started.** Owner actions: set `APP_ORIGIN` in Vercel (recovery is refusing until then; the Vercel CLI here needs an interactive OAuth login), configure Resend SMTP, enable Turnstile. `AUTONOMOUS_LOOP_HANDOFF.md` §24.

- **SH-DELETE-015 is CLOSED (2026-08-04).** Under explicit owner authorization, the six historical orphans were re-verified immediately before the act and removed; the scanner now reports **zero objects of every class**. Both protected accounts verified untouched. The removal tool is single-use by construction — re-running it refuses, because the manifest it reads describes objects that no longer exist. PR #81, merge `e9948a7`.
- **SH.5 is PARTIALLY delivered — the application half only.** Built with **no migration**: the app-level signup gate defaulting closed (strictly — only the exact string `true` opens it), honest `signup-disabled` copy in both locales, the configured application origin replacing request-`Origin` trust, and a `config.toml` that can no longer open signup. **Not built:** the database-locked throttles and migration `202608040075`, `auth_event_attempts` and its retention, concurrency boundary tests, confirmation resend, enumeration-uniform outcomes, Turnstile plumbing, session-fixation evidence, deployed probes. **SH.6 and SH.7 not started.**
- **A live footgun was closed:** `config.toml` carried `enable_signup = true`, and `supabase config push` sends that file wholesale to the linked project — so the standard command for any hosted Auth change would have **opened public signup** as a side effect of unrelated work. Now `false` in both tables with a guard test over the raw file.
- **The stop is hosted configuration, and it is precise.** Reading the GoTrue rate limits needs the Management API and a PAT that exists only in the Windows Credential Manager (extraction forbidden); the public settings endpoint does not expose them. Writing has one CLI path, `supabase config push`, which has no `pull`/`get`/`--dry-run` on either the installed or latest CLI and is all-or-nothing over the whole file — so rate limits cannot be set without overwriting `site_url`, whose correct value does not exist because **SH-GD.4 (hosting decision) is unresolved**. Smallest owner action: make a PAT available to the environment (never to chat), then decide SH-GD.4. `AUTONOMOUS_LOOP_HANDOFF.md` §23.

- **The hosted project reached migration parity `202608040074` (owner action), and SH.2, SH.3 and SH.4 were then executed against it.** Every deployment-gated row those three slices carried as NOT EXECUTED is now closed. Deployment ordering was verified *before* any journey ran: parity confirmed row-by-row, `account_lifecycle` present with **0 stateless accounts**, `policy_acceptances` present, the owner `active` and consented, and `disable_signup: true` read back from the deployed GoTrue. Three re-runnable journeys now live in the repository — `e2e/online-account-deletion.spec.ts`, `e2e/online-account-suspension.spec.ts`, `e2e/online-consent-interposition.spec.ts` (the last across **both locales**) — each provisioning and removing its own disposable account, so they are gates rather than one-shot transcripts. `delete-account` is deployed (v1, ACTIVE). Evidence: `docs/reports/signup-hardening/SIGNUP_HARDENING_DEPLOYED_ACCEPTANCE.md`.
- **The journeys found one production defect and two findings that review had not.** The consent decline path's deletion link pointed at `/{locale}/app/settings` — inside the consent gate being declined — so declining looped straight back to the interposition; SH.4 had built `/{locale}/account/delete` outside `app/` for exactly that caller and nothing pinned the link to it (fixed, with a regression test). The deletion **receipt copy is unreachable** on the deployed surface, because the lifecycle gate re-runs on revalidation and interposes the `deleting` screen before React paints the returned state — both gates correct, the copy dead. And the suspension journey's own teardown **created two new storage orphans** on its first run by deleting the auth row before the storage, the exact mechanism that produced the historical six; fixed, cleaned, and re-verified back to six.
- **The orphan manifest is TAKEN, and deletion is still not authorized.** Measured read-only against the deployed project: six objects, all `absent-owner`, all 2026-07-16, **zero `cross-owner`, zero `unparseable`** — so neither halting condition is present. Both deletion conditions were measured *independently per object* rather than inferred from the class (`attachments` holds zero rows project-wide, so no live row of any owner references any of the six). `docs/reports/signup-hardening/SH_DELETE_015_ORPHAN_MANIFEST.md` now carries the filled table. **Nothing was deleted**; the lane stops at owner authorization with SH-GD.2 before it.
- **The admin runbook is three-of-four DRILLED** — suspension, reactivation, and the provider-side ban and its lift, each with its real readback. Administrative deletion-start is left **undrilled deliberately** and says so: drilling it means an operator-initiated destruction of a real account, and the executor was already proven by the user-initiated path.
- **SH.5 is BLOCKED before its migration.** The checkpoint conditions spending `202608040075` on reading back the hosted GoTrue rate limits, because the application throttle ceilings must sit at or below them. Those values need the Management API; the CLI holds its personal access token in the Windows Credential Manager, and extracting it is not something this loop will do. The GoTrue **public** settings endpoint gave what it exposes (`disable_signup`, `mailer_autoconfirm`, enabled providers) and no more — the redirect allowlist, `site_url`, password policy and rate limits remain SH-GD.1 owner actions. **No migration was spent; five of eight remain.**

- **SH.4 closed:** PR #78, merge `66b2648`, merge-SHA CI `30941069995` green on all three jobs. Migration `202608040074` — head `202608040074`, five of eight spent. `policy_acceptances` with forced RLS and **exactly two policies** (select-own, insert-own): no UPDATE and no DELETE policy exists at all, so append-only is the absence of a policy rather than a revoke. The current version is stated in SQL as well as TypeScript and pinned in both directions (ADR-079), because SH-LEGAL-005 leaves a direct client INSERT open and a client able to write `version = '9999-12-31'` would pre-accept every future policy it has never seen; a BEFORE INSERT trigger refuses anything but the current version, and `record_policy_acceptance` takes **no version parameter** at all. Both drafts ship in both locales from repository truth, with the professional-review banner and four visibly unfilled owner placeholders. The retention section is **generated** from `retention.ts`, and renders an honest warning while any declared window has no active sweep — a test asserts the warning is present exactly while one is, so SH.6 removes it by flipping flags rather than by remembering. Public legal routes, a signup consent control enforced in the schema, one interposition covering both first-session and version-bump, and a real decline path. Evidence: 21 pgTAP assertions, 79 unit cases, credential-free e2e on all four legal routes. Acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_04_ACCEPTANCE.md`.
- **SH.4 closed two residuals it found rather than routing around them:** SH.2's deletion action had **no UI consumer**, so SH-LEGAL-010's decline path had nowhere to point — the surface is built here over the already-merged action; and SH.1's account-state surface used a CSS class (`auth-page`) that exists in no stylesheet.
- **SH.3 closed:** PR #77, merge `3a0f4f7`, merge-SHA CI `30936842221` green on all three jobs. Its adversarial review recorded one substantive residual with a destination: the lifecycle predicate covers the paths the PRD names (capture, reprocessing, the three claim paths, the heartbeat) but **not every `authenticated`-executable RPC**, so a suspended account holding a valid JWT could reach six of them directly through PostgREST. Destination: SH-EXPOSURE-007's re-census (SH.7) and SH-EXPOSURE-002's matrix (SH.6), recorded in `TODO.md`.

- **SH.3 (in PR):** migration `202608040073` — head `202608040073`, four of eight spent. The administrative boundary as `service_role`-only SQL (ADR-075): `suspend_account`, `reactivate_account`, `begin_account_deletion_admin` and the `admin_account_lifecycle_status` readback, each DEFINER with an empty `search_path`, a per-verb closed reason set, and the unconditional audit write of SH.1's machine; `defer_job_for_inactive_owner` returns a job to the queue when its owner stopped being active after the claim — not a failure, no `jobs.error`, no extra attempt burned, claimable again on reactivation with no operator re-enqueue. One shared worker gate wired into both handlers at their ownership reload closes SH-WORKER-001 and SH-WORKER-002 (a `deleting` or absent owner is terminal; an unreadable lifecycle defers). Reminders that came due during a suspension are never delivered retroactively and are never touched (SH-SUSPEND-005, ADR-078). The operator surface is `npm run account:lifecycle`: dry-run by default, explicit `--apply`, readback, no user content printed; a both-directions allowlist pins it as the only executable caller (T-10). The suspended surface shows the reason's public label from a closed set. Evidence: 42 pgTAP assertions, 14 new Deno cases (118 in the suite), 37 unit cases. Runbook: `docs/reports/signup-hardening/SIGNUP_HARDENING_ADMIN_RUNBOOK.md`, **written not drilled**. Acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_03_ACCEPTANCE.md`.
- **SH.3's deployment-gated evidence is recorded as NOT EXECUTED, not as satisfied:** suspending a real disposable account and observing the deployed drain skip and then complete its queued job (SH-WORKER-004), the heartbeat skip across an hourly tick (SH-WORKER-005), the provider-side sign-in ban and its `banned_until` readback (SH-ADMIN-005), and the authenticated suspended-surface journey. Blockers: SH-GD.3 and the four SH migrations not yet applied to the hosted project.
- **SH.2 closed:** PR #75, merge `947bb26`, merge-SHA CI green on all three jobs; handoff PR #76 merged as `497aa36`.
- **SH.1 closed:** PR #74, merge `3c227f6`, merge-SHA CI `30910079676` green on all three jobs.
- **SH.2 (in PR):** migration `202608040072` — head `202608040072`. `account_deletion_log` de-identified by construction (no user_id/email/name columns exist), forced RLS with no policy and no table grants, its DEFINER RPC the only writer; `request_account_deletion()` takes no parameter and runs through SH.1's audited machine; `account_owned_row_counts()` enumerates owned tables from the catalog at run time. The executor is an Edge Function outside `src/` (ADR-074), self-only by the shape of its input, stopping rather than forcing, removing storage by exact `<uid>/` prefix and verifying the prefix empty before the account row is touched. Request surface: provider-validated re-authentication plus a typed phrase, both server-side. Scanner reports and cannot destroy; the capability guard classifies all 20 `admin.deleteUser` holders with exactly one product site. Evidence: 15 Deno cases, 25 pgTAP assertions, 37 unit cases. Acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_02_ACCEPTANCE.md`.
- **SH.2's stop condition — the six 2026-07-16 orphaned storage objects.** `docs/reports/signup-hardening/SH_DELETE_015_ORPHAN_MANIFEST.md` holds the read-only procedure and an explicitly **unfilled** manifest. Taking the manifest needs the deployed project; deleting the objects is irreversible, owner-only, and gated on SH-GD.2's verified restorable backup. **Nothing has been deleted, and nothing will be without explicit owner authorization.**

- **SH.0 closed:** PR #73, merge `a05cfb5`, merge-SHA CI `30905402273` green on all three jobs. The cascade drill passed on its first execution — the bulk `auth.users` delete is **not** blocked by the 43 composite `NO ACTION` FKs — so gate SH-G0.2 is proven and the loop continued into SH.1 as the plan directs.
- **SH.1 (in PR):** migrations `202608040070`/`202608040071` — `public.account_lifecycle` (closed vocabularies, forced RLS, state-machine trigger with unconditional audit, `handle_new_user` seed + backfill, SH-EXPOSURE-004 executed) and the lifecycle predicate wired into capture, reprocessing, all three claim paths and the heartbeat. App side: `requireUser` + `assertActiveAccount` (19 inline sites) send every non-active account to the `/account-state` surface, fail-closed, sign-out preserved. Head moves to `202608040071`. Acceptance: `docs/reports/signup-hardening/SIGNUP_HARDENING_SLICE_01_ACCEPTANCE.md`. **Deployment ordering (recorded hazard):** apply both migrations to the hosted project before running this slice's app code against it — without the table the fail-closed gate closes the product for every account.

**This supersedes the "PLANNED, not started" section below for every claim they disagree on; that section is retained as the record of the planning stop.**

- **The owner approved the planning package in full** — `ADR-077`, PRD Amendment `P-1`, plan Amendment `A-1`. Approved: the five planning documents; ADR-073…ADR-076 (Proposed → **Accepted**, status lines updated with the history retained); the **eight-migration budget**; the **SH.0–SH.7 sequence**; the **PRD §20 quota values as proposed** (repository constants under the SH-QUOTA-010 parity contract, never migration content); the **plan §7 retention schedule as proposed** (approval is **not** purge authorization — every destructive sweep still needs a count-only dry-run, boundary tests, a transcript and explicit owner authorization before its first live production run); the **self-only resumable Edge Function deletion executor** (ADR-074); the **operator-CLI admin boundary, no product admin UI, no service-role HTTP endpoint** (ADR-075); **Cloudflare Turnstile** (ADR-076); the **v1 no-malware-scanner posture with its compensating controls** (SH-STORAGE-006); and the **Phase 2H destination for the operator surfaces**.
- **SH.0 delivers the all-implementation gates as permanent CI artifacts, zero migrations.** The **cascade drill** (`supabase/tests/signup_hardening_cascade_drill.sql`, SH-DELETE-001/SH-G0.2): a two-account fixture proven **row-complete** against the runtime-enumerated catalog — every user-owned table holds a row for both accounts, population failures fail by table name, an unpopulatable future table fails by name — then `delete from auth.users` through the 43 composite `NO ACTION` FKs, zero residue asserted schema-wide, and a negative control requiring the bystander to remain *row-complete*, not merely populated. The **grant-census skeleton** (`supabase/tests/signup_hardening_grant_census.sql`, SH-EXPOSURE-002/SH-G0.3): anon holds zero explicit table or function grants; **the `service_role` revoke carve-out is pinned in both directions** — exactly the two RPC-only ledgers carry zero `service_role` grants, asserted on explicit-grant presence because *which* privileges platform defaults grant differs by environment while the chain's revokes do not — plus the ledgers' effective-denial pins; forced RLS is censused over every user-owned table; and the two known exposures are pinned **by name** as facts awaiting their dispositioning slices — F-18 (`authenticated` INSERT on `audit_logs`, SH.6) and F-19 (`handle_new_user` PUBLIC EXECUTE, SH.1). The service_role pin took three cuts: CI refused the hosted-posture pin (no local table gives the full four-DML set) and then the zero-grants pin (40 of 42 tables carry grants locally) — a live measurement of the FINDINGS §12.3 local/hosted divergence, recorded in the acceptance report rather than smoothed over. The per-privilege matrix is SH.6's, beside SH-EXPOSURE-001's revoke and hosted readback.
- **Hosted signup remains DISABLED and this initiative never flips it.** `disable_signup` stays `true` (G-0.5 evidence unchanged); opening signup is post-initiative, owner-only, and gated by one green fail-closed rollout-gate run (SH-ROLLOUT-005).
- **Nothing destructive is authorized.** No production deletion, no orphan cleanup, no live retention purge. The six 2026-07-16 orphaned storage objects still require enumeration → classification → manifest → separate owner authorization (SH-DELETE-015, SH.2).
- **Deployment-tier gates stay open as owner actions** (SH-GD.1 hosted readback, SH-GD.2 restorable backup, SH-GD.3 disposable accounts, SH-GD.4 hosting/SMTP) and block only shared-environment execution at their slices' points of use.

## Signup Hardening — PLANNED, not started (2026-08-02)

**The Signup Hardening planning package exists and is approved-pending-owner; no implementation has begun and none is authorized by the planning artifacts.** `ADR-068` ordered this initiative after BYOK's close, and BYOK closed on 2026-08-02 at `b007ffa`, so the initiative is authorized to be **planned**. The package is `docs/initiatives/signup-hardening/SIGNUP_HARDENING_PRD.md`, `docs/initiatives/signup-hardening/SIGNUP_HARDENING_IMPLEMENTATION_PLAN.md`, `docs/reports/signup-hardening/SIGNUP_HARDENING_FINDINGS.md`, `docs/reports/signup-hardening/SIGNUP_HARDENING_THREAT_MODEL.md`, `docs/reports/signup-hardening/SIGNUP_ROLLOUT_GATE_DEFINITION.md`, and ADR-073…ADR-076 (Proposed).

- **Scope was measured, not assumed.** A four-agent read-only census (auth surfaces, database privileged boundaries, storage/workers/Edge Functions, product/ADR conventions) preceded scope. Findings that changed the shape: row-level cascade from `auth.users` is **already complete** (41/41 user-owned tables, zero exceptions), so deletion's hard part is storage (six live orphans from 2026-07-16, no cascade) and the **untested** 43 composite `NO ACTION` FKs in the delete path — not the tables; **no account-lifecycle state exists at all**, so deletion and suspension share one foundation; `service_role` retains platform-default DML on the two BYOK tables; retention is essentially absent except BYOK's 30-day prune.
- **Requirement namespace is `SH-*`**, deliberately not a phase prefix — a `2G-`-shaped declaration would trip `ADR-067`'s phase-start guard. Sixteen families, ~120 requirements, eight-migration budget across seven slices (SH.0–SH.7). Deletion, suspension and rollout never share a PR.
- **Pre-code gates are tiered** (`ADR-073`, applying `ADR-069`'s lesson): gates that block all implementation (census, the deletion-cascade drill green in CI, the boundary inventory, the owner-signed quota/retention values), gates that block only deployment (hosted-config readback, backup verified, disposable accounts, hosting/SMTP decision), and gates that block only public signup (the rollout gate — the initiative's output, blocking no slice).
- **Signup does not open in this initiative.** `disable_signup` stays `true`; the final slice delivers the fail-closed rollout gate script, not the flip. **Phase 2G stays unauthorised until Signup Hardening closes.**
- **BYOK-OPERATIONS operator surfaces are NOT absorbed here** (`ADR-073`, plan §8): Signup Hardening builds the administrative *boundary* (suspension, deletion, audit, operator CLI); a dashboard/alerting to *watch* it is recommended to a Phase 2H-aligned Operations initiative. Roadmap: Signup Hardening → Phase 2G → Phase 2H (absorbs operator surfaces) → open self-service signup.

## BYOK closed (2026-08-02) -- what the final gates proved

**This supersedes the "Post-remediation state" section below, which is retained as the record of the state between the cutover and the close.**

- **Every remaining gate is EXECUTED against the deployment**, with real provider calls on two disposable product credentials the owner provisioned: C11 Settings journeys **4/4** (desktop + Pixel 7 x pt-BR + English), the **two-user isolation matrix** synchronously and asynchronously, **genuine concurrent rotation**, the removal / queued-job / capture lifecycle, and bounded pending processing (25 of 26, partial message asserted).
- **Isolation is evidenced cryptographically, not by "both calls succeeded".** A provider-side distinguisher was looked for and **measured absent** -- both credentials return 200 from `/v1/models`, expose no organization or project header and see an identical model list. The stored fingerprint is `HMAC(pepper, plaintext)`, a function of the key alone, so A's row carrying A's fingerprint proves whose key is in whose row without reading either. The resolvers then prove each path reaches only its own row.
- **BYOK.6's last code deliverable ships:** the bounded two-key master-key rotation window. `key_version` selects **one** key -- a fallback would be a decryption oracle -- with a 30-day maximum refused rather than truncated, four environment names pinned by the guard in both runtimes, and a completion counter that will not say "done" while any row is unreadable. Drilled on disposable material; the production run is an owner action with **no undo**.
- **The acceptance lane found three product defects no unit test could reach**, each fixed with a regression test: `formatFingerprint`'s parse-don't-trust guard had **no production consumer**; the panel reported the previous action's outcome after a removal; and a save following a removal announced itself as a *replacement*.
- **Fixture cleanup passes with non-vacuous controls:** zero fixture users, zero orphaned rows across nine owned tables, the scanner proven able to see rows for a live account, and the owner's credential surviving untouched.
- **Named rather than counted as delivered:** `BYOK-OPERATIONS` (6 requirements) is not built; the production master-key rotation has never been run; six orphaned storage objects from 2026-07-16 predate this work and belong to Signup Hardening's account-deletion requirement; three OpenAI keys should be revoked; and 47 of 131 requirement ids are untraced. All dispositioned in `docs/reports/byok/BYOK_TRACEABILITY_MATRIX.md`.

## Post-remediation state (2026-08-02) — the cutover succeeded; BYOK is still NOT closed

**This supersedes the "Deployment state" section below for every claim they disagree on. That section is retained as the record of the failed cutover.**

- **Runtime parity is `IN PARITY`.** `npm run byok:verify-runtime` — 5 pass, 0 fail, 0 unverifiable, digest-algorithm control included, no value and no digest printed.
- **The owner's stored credential opens under the Node runtime's master key**, verified read-only against the row's own AAD with a passing round-trip control and a rejecting wrong-AAD control. The plaintext length was observed; the value was never read.
- **OWNER-ASYNC passes on the deployed worker.** Drained by the unattended `pg_cron` tick: job `completed`, attempts 1, one interpretation persisted, `ai_usage_events` 8 → 10. The probe entry was cleaned up; the ledger rows are genuine usage and stay. **The owner's asynchronous AI runs on the owner's own credential.**
- **The fail-closed properties still hold.** An account with no credential is refused with `credential_required`, one attempt, zero ledger rows; an account whose ciphertext cannot be opened is refused terminally with `credential_unreadable` and the entry returns to `awaiting_ai_configuration`. `.env.local` holds **no** `OPENAI_API_KEY` — there is nothing for a Node path to fall back to.
- **E5 passes and all five remote smokes are green.** Two of them were asserting **pre-BYOK** behaviour — that a deployed account without a credential still gets AI — and are **inverted, not weakened**: they now assert the refusal, the declared code, the single attempt and the absent ledger row. Do not restore the old assertions.
- **Zero residue.** Six readable product tables censused, no credential-shaped match; `jobs.error` and `entries.processing_error` hold zero distinct values. `product_events` is not readable by `service_role` — correct, and reported rather than silently skipped.
- **BYOK is still NOT closed, and the blocker is now exactly one thing:** a provider-accepted OpenAI key the loop is authorized to spend as a **product credential**. That blocks the two-user isolation matrix with real credentials, concurrent rotation (C10), the desktop/Pixel 7 Settings journeys in both locales (C11), and the end-to-end half of the entry-processing smoke. `BYOK_VALIDATION_OPENAI_API_KEY` is explicitly not one. **E3 remains refused on purpose.** The smallest owner action is `docs/reports/AUTONOMOUS_LOOP_HANDOFF.md` §15.

## Product defect closed (2026-08-02) — the service-worker response cache

- **`public/sw.js` cloned a `Response` it had already given away.** `caches.open` resolves on a later tick, so by the time its callback ran, `return response` had handed the body to the page and the browser had begun reading it — and a `Response` may only be cloned while its body is unread. Every cache miss logged `Failed to execute 'clone' on 'Response': Response body is already used`, and **the asset was never cached**: a caching layer that errored on every miss and cached nothing.
- **Corrected order: clone → return → cache**, the copy taken synchronously before the response is returned.
- **Application-owned, and the repository proves it** — `public/sw.js` is checked in and registered by `src/features/pwa/register-service-worker.tsx`. The `asynchronous listener` / `No Listener` messages reported alongside are **browser-extension noise** (`chrome.runtime` message channel); no source in `src` or `public` emits them and they are deliberately not chased.
- **Six regression cases, the first tests `public/sw.js` has ever had**, evaluating the shipped bytes against a stubbed `ServiceWorkerGlobalScope`. The regression case fails on the old file and passes on the new one, verified by reverting. The rest pin the boundary that keeps this worker from ever answering for an application route or a cross-origin URL.

## Product defect closed (2026-08-02) — the candidate due-date contract

- **The entry detail route no longer dies on an optional field.** `/…/app/inbox/bdf48c06-…` reached the app error boundary because `parseOffsetInstant` demanded seconds of literally `:00`, while the contract that produced the value — `z.string().datetime({ offset: true })`, in the extraction schema *and* the candidate edit contract — permits any seconds and optional fractional seconds. The reader was narrower than the contract on both sides of it. **Fixed at the reader. Zero migrations, no production data rewritten**; the stored value was valid throughout.
- **The same parser had already taken the reminders surfaces down twice**, each worked around locally. Those comments are corrected rather than left asserting a constraint that no longer exists.
- **A typed `absent | valid | unreadable` state replaces a throw** at the editor boundary, so a legacy or malformed value costs the owner that one field — rendered empty with a localized notice, candidate still editable, stored value untouched because an unedited field emits no change. Nothing invents an offset; a date-only value is refused rather than read as UTC midnight.
- **Two further crash sites were found by the integration test and fixed at the same boundary:** `formatDueDate` in `task-candidate-form.tsx` threw through `Intl` on a malformed offset and rendered dates in the *viewer's* timezone rather than the owner's, and `toEditSuggestion` carried an unreadable value into suggestion validation, which refused the **entire batch**.
- **Verified against the deployed environment:** 40 + 84 + 28 unit/integration cases, and an online journey seeding the exact affected shape that loads the real route in **pt-BR and English on desktop and Pixel 7, 4/4 green**, proving the offset is honoured (`23:59:59-03:00` arrives as `22:59` in `America/New_York`) rather than the digits copied.
## Deployment state (2026-08-02) — verified against the deployment, not reported

> **Superseded in part on 2026-08-02** by the *"Post-remediation state"* section above. The cutover failure described below **did happen** and was correct when written; the owner then performed the two remediation steps and the cutover was re-verified as passing. Retained unedited as the record of that failure.

- **BYOK is deployed and BYOK is NOT closed.** Migration parity is `202608010069` on both sides; `process-jobs` is version 20 and **16 of 16 files byte-identical** to `abef6e4`; the three BYOK secrets exist in the Edge Function store and **`OPENAI_API_KEY` does not**. The deployed bundle contains **zero** executable references to a project key, and the only env names it reads are the two BYOK secrets, the two Supabase ones, the dispatch secret and a model id. The full record is `docs/reports/byok/BYOK_DEPLOYED_ACCEPTANCE.md`.
- **THE OWNER CUTOVER FAILED, and the architecture is the reason it failed safely.** The owner's first asynchronous job reached the deployed worker and terminated `credential_unreadable` — attempts 1 of 5, no further attempt scheduled, `ai_usage_events` unchanged, entry returned to `awaiting_ai_configuration`. Under the pre-BYOK design that job would have **succeeded on the project key**. It failed closed instead, which proves *behaviourally* what BYOK.5 could previously only assert statically: **no deployed account receives a project-key fallback, including the owner's.**
- **The cause is a configuration mismatch, not a defect in any slice.** The runtime that saved the credential held a `BYOK_MASTER_KEY` that is neither the deployed value nor either value in the repository's env files — established three independent ways, each with its own control: digest comparison (positive control reproduces `SUPABASE_URL`), attempted decryption (positive control round-trips, negative control rejects a wrong AAD), and the deployed worker's own verdict. **There is no recovery.** The owner must re-enter the key.
- **Nothing in the product could have warned anybody, and that is correct.** `BYOK-CRYPTO-005` forbids a decryption failure from naming which of key, tag or AAD failed, so a runtime-key mismatch is structurally invisible from inside the product. `ADR-072` adds the outside check: `npm run byok:verify-runtime`, which refuses to return a verdict at all unless it can first reproduce a known digest, and prints no value and no digest. Thirteen unit cases pin it, including this exact defect.
- **There is no shared Next.js hosting environment**, and this is stated rather than glossed. No `vercel.json`, no `.vercel`, no `netlify.toml`, no `Dockerfile`, no deploy workflow; `README.md:20` and `ARCHITECTURE.md:91` record Vercel as deliberately deferred. The application runs locally against the linked project, so `.env.local` **is** this environment's Next.js runtime configuration — which is where the mismatch is.
- **`OPENAI_API_KEY` is gone from the environment contract** (`ADR-072`). A comment-stripped census over `src`, `supabase/functions`, `scripts`, `e2e` and `.github` found zero runtime consumers, so the name left `.env.example` and `README.md`; a guard now fails the build if either offers it again. The allowlist moved to four entries, and every one of them names the key **in order to assert its absence** — a stricter composition than the three it replaced.

## Authorization state (2026-08-01)

Bounded statements. Each is verified, and nothing here authorizes work beyond what it says.

- **Hosted signup is DISABLED and verified.** `disable_signup: true` on the linked project `ulvwzqlpsjyrnqzfxmck`, proven by three cache-busted reads plus a signup attempt that returned `422 signup_disabled` using a **provider-accepted** address as a positive control. Evidence: `docs/reports/byok/G05_HOSTED_SIGNUP_CLOSURE_EVIDENCE.md`. This closes gate **G-0.5**.
- **Entity Graph Completion is COMPLETE.** All three slices merged with green merge-SHA CI: EGC.1 as `840da99` (run `30672108083`), EGC.2 as `8305424` (run `30677225551`), EGC.3 as recorded in `docs/reports/entity-graph/EGC_REPORT.md`. **53 requirements, 53 delivered, 0 deferred**, disposed individually in `docs/reports/entity-graph/EGC_TRACEABILITY_MATRIX.md`. Zero migrations across the whole initiative; parity is still `202607310064`. Governed by `docs/initiatives/entity-graph/ENTITY_GRAPH_COMPLETION_PRD.md` and `docs/initiatives/entity-graph/ENTITY_GRAPH_COMPLETION_IMPLEMENTATION_PLAN.md`, on the evidence in `docs/reports/entity-graph/ENTITY_GRAPH_FINDINGS.md`. Three slices, **zero migrations**, zero grant or policy changes, no provider call, no `tasks`/`reminders` write-path change — all four held through EGC.1. Its acceptance records are `docs/reports/entity-graph/EGC_SLICE_01_ACCEPTANCE.md` (gates A1–A10), `docs/reports/entity-graph/EGC_SLICE_02_ACCEPTANCE.md` (gates B1–B13, including the Camila scenario end to end) and `docs/reports/entity-graph/EGC_REPORT.md` (gates C1–C7 and the whole-initiative accounting). Governed by `docs/initiatives/entity-graph/ENTITY_GRAPH_COMPLETION_PRD.md` and `docs/initiatives/entity-graph/ENTITY_GRAPH_COMPLETION_IMPLEMENTATION_PLAN.md`, on the evidence in `docs/reports/entity-graph/ENTITY_GRAPH_FINDINGS.md`.
- **BYOK architecture and design package are APPROVED; implementation has NOT started.** `docs/reports/byok/BYOK_SECURITY_DEFINITION.md`, `docs/initiatives/byok/BYOK_PRD.md`, `docs/initiatives/byok/BYOK_IMPLEMENTATION_PLAN.md`. No credential table, no resolver, no adapter and no guard exists yet.
- **BYOK pre-code gates: G-0.1, G-0.2, G-0.3 and G-0.5 are satisfied; G-0.4 is not.** G-0.1 is `docs/reports/byok/BYOK_G01_PROVIDER_CENSUS.md`, re-measured at `961feeb`. G-0.2 is executed Node 22 ↔ Deno 2 interop, 7/7 both directions (`npm run byok:interop`), evidence in `docs/reports/byok/BYOK_G02_CRYPTO_INTEROP_EVIDENCE.md`. G-0.3's procedure is written and `local`/`test` are provisioned and verified — present, valid base64, 32 bytes, 6/6 pairs distinct, 0 matches across 994 tracked files — in `BYOK_G03_MASTER_KEY_PROCEDURE.md` §7; `preview`/`production` are **deferred to the point of use**. **G-0.4 — the dedicated low-limit OpenAI validation key — is unprovisioned and remains a stop condition.**
- **The §0 pre-code rule is amended, by owner decision, from absolute to dependency-specific** — `ADR-069` and Amendment A-1 of `docs/initiatives/byok/BYOK_IMPLEMENTATION_PLAN.md`. **BYOK.1 and BYOK.2 are authorized.** G-0.4 gates **BYOK.3's live validation lane and BYOK.3's acceptance and merge**; the lane may not ship marked passed while unexercised. BYOK.3 onward remain unauthorized until G-0.4 is provisioned.
- **Phase 2G remains UNAUTHORIZED and UNSTARTED.** `docs/reports/phase-2g/PHASE_2G_DEFINITION.md` is a definition study: it declares no requirement, plans no work, and its own header says the phase is unauthorized. The A13 guard now measures phase start by **implementation-governing artifact, declared requirement family, accepted ADR or implementation-marked file** rather than by filename — see ADR-067.
- **Public signup remains BLOCKED** on BYOK implementation plus signup hardening, which includes account deletion, admin suspension, terms and privacy policy — none of which exists.
- **The closed product UX ledger is unchanged:** 35 findings, 30 RESOLVED, 4 RETAINED, 1 DEFERRED, 0 OPEN, 0 PARTIAL, 0 BLOCKED, all 8 P0 RESOLVED. No count and no disposition moved.
- **BYOK.1 is COMPLETE.** Migration `202608010065` — `user_ai_credentials` and `credential_validation_attempts`, forced RLS, own-row select/insert/update policies, **no `DELETE` grant or policy for any client role**, eight CHECK constraints, and a post-deploy assertion block that reads the resulting privileges back out of the catalog. Plus the crypto core in both runtimes, the fingerprint module, `BYOK-GUARD-005` and the chain scan. **No resolver, no adapter, no product surface, no provider call; `OPENAI_API_KEY` untouched.** Acceptance record with all nine gates: `docs/reports/byok/BYOK_SLICE_01_ACCEPTANCE.md`. Three PRD departures and one unresolved PRD conflict are recorded there rather than folded in.
- **BYOK.2 is COMPLETE.** Migration `202608010066` — `resolve_own_ai_credential()` and `resolve_job_ai_credential(p_job_id uuid)`, both `SECURITY DEFINER` with an empty `search_path`, **neither taking a `user_id` argument**, neither decrypting, both returning ciphertext only and only for `status = 'active'`. Grants are exact: the synchronous one to `authenticated`, the asynchronous one to `service_role` and nothing else. **No consumer yet.** Acceptance record with all seven gates: `docs/reports/byok/BYOK_SLICE_02_ACCEPTANCE.md`, including a `BYOK-RESOLVER-006`-versus-matrix-case-8 contradiction resolved and recorded rather than papered over.
- **G-0.4 is SATISFIED and BYOK.3 is AUTHORIZED to begin** (`ADR-070`). A dedicated OpenAI project and key exist as `BYOK_VALIDATION_OPENAI_API_KEY` — not `OPENAI_API_KEY` — with a USD 2 monthly budget **alert** (a soft alert, not a hard cap), restricted model access, the lowest practical rate limits, and use confined to the opt-in acceptance lane.
- **The reachability gap is CLOSED and the live lane has RUN.** The owner placed the value in `.env.local`; verified as present and non-empty with 1024 tracked files scanned and **0 matches**, and nothing about it recorded beyond that. `npm run test:byok:validation` — **4 passed**, three real round trips. `succeeded` is reachable only if a real authenticated call resolved, and `invalid_key` requires a genuine `401` carrying OpenAI's actual error shape, so the lane proves the half a mocked suite cannot.
- **BYOK.3 is COMPLETE, and it is the slice that removed the project-key fallback.** Migration `202608010067` — `ip_hash`, its partial index, 30-day bounded retention, a scheduler-only prune, and the two throttle RPCs. `options?.apiKey ?? process.env.OPENAI_API_KEY` is **deleted**, the credential is a required property of a required argument so an omission fails at `tsc`, all five Node provider sites route through the gate, plaintext travels in a branded `Secret` that throws on every serialization path, and Settings ships in the same slice — a fallback removed before the surface that configures a replacement would leave the product AI-less. Acceptance record: `docs/reports/byok/BYOK_SLICE_03_ACCEPTANCE.md`.
- **Three acceptance items are recorded as NOT EXECUTED, for one shared reason.** Matrix cases 1–3, concurrent rotation (C10) and the desktop/Pixel 7 Settings journeys (C11) all need a shared database carrying the BYOK tables, and **none of the three BYOK migrations has been applied to a shared environment**. They become available at the first BYOK deployment, which Amendment A-1.2 gates on preview and production secrets existing. `BYOK_SLICE_03_ACCEPTANCE.md` §4.
- **BYOK.4 is COMPLETE, and it is the slice that finished what BYOK.3 started.** Migrations `202608010068` (the `awaiting_ai_configuration` lifecycle, `entries.capture_idempotency_key`, a credential-aware `capture_entry_async`, and `mark_entry_awaiting_ai_configuration`) and `202608010069` (the credential-aware drain and `fail_job_terminal`). `Deno.env.get("OPENAI_API_KEY")`, its 503 branch, the `openaiKey` parameter through `dispatch.ts`, and the allowlist entry that permitted the read all went in **one commit** — the guard asserted the read's *presence* precisely so that deleting it would force the allowlist to shrink alongside it. **There is now no Deno path to a process-wide provider key**, and `jobs.error` receives a declared code and nothing else on both handlers, which closes the leak `SECURITY.md:34` records against the attachment path. Acceptance record with all fourteen gates: `docs/reports/byok/BYOK_SLICE_04_ACCEPTANCE.md`.
- **Capture without a key is a state, not an error.** The entry is stored, no job is enqueued, the status says `awaiting_ai_configuration`, and the Inbox and entry detail render it with a link to Settings. Nothing is bulk-processed when a key becomes active: a bounded, explicit action of 25 per invocation exists and reports the count it enqueued, because spending a user's money without an explicit act is the same class of error as an unconfirmed AI write.
- **One defect was found and fixed inside BYOK.4's own adversarial review**, before the PR: a reprocessing job whose credential vanished mid-flight left its entry in `reprocessing` forever, because `mark_entry_awaiting_ai_configuration` refuses an entry that already has an interpretation and the worker's first draft did not honour that answer. Both directions are now pinned by tests. Recorded in `BYOK_SLICE_04_ACCEPTANCE.md` §7, along with three smaller findings, because a review that finds nothing is a review that was not run.
- **BYOK.4's second migration carries a second function, and this is declared rather than folded in** (`ADR-071`). `fail_job_terminal` was needed because `fail_job` cannot express terminality — its only rule is `attempts >= max_attempts` — and PostgreSQL cannot extend an argument list with `create or replace`. **Two migration files; the head moves by exactly two.** `ADR-071` also narrows *"consumes no retry"* to what it can mean: the attempt is spent by the **claim**, so what is guaranteed is that no *further* attempt is scheduled.
- **BYOK.5 is STOPPED at its owner boundary, and that is a true stop rather than an incomplete slice.** Gate E4 is executed — the allowlist is pinned at exactly three entries with a classification per entry compared in both directions, every exception is proven unreachable from a deployed user bundle, and no deployed module in either runtime reads an `API_KEY`-shaped environment name. Gates E1, E2 and E3 need platform-secret access and a credential typed into an authenticated product surface; E5 is deliberately not run, because its purpose is to confirm the remote scripts still work **after** the cutover. Acceptance record with the four exact owner actions: `docs/reports/byok/BYOK_SLICE_05_ACCEPTANCE.md`.
- **The owner is not privileged in the credential contract, proven as an absence.** The resolution chain carries no identity comparison, no identity read from configuration, and no hardcoded uuid — so the mechanism by which the owner *could* be exempted does not exist. This does not discharge E3, which still needs the journey run; it removes the way E3 could fail.
- **A plan correction, measured rather than absorbed.** Task 5.3 names `scripts/remote-*.mjs` as the third classified allowlist exception. **It is empty in fact** — no script references `OPENAI_API_KEY`, and `remote-supabase-smoke.mjs` carries the literal `"openai"` only as a provider *name*. The allowlist's count agrees with the plan while its composition does not, and the guard now asserts that in both directions.
- **BYOK.6 is PARTIAL and cannot close.** Executed: a zero-secret residue verifier that runs in CI and **enumerates every base table in `public` carrying a `user_id`** rather than checking a fixed list — so a future BYOK table without a cascade fails by name — with five calibration assertions proving the scanner finds rows before the delete and four negative-control assertions proving a second account's rows survive. Plus an incident runbook for master-key rotation, loss and compromise, **marked "written, not drilled"** at the top of the file and of every section.
- **What BYOK.6 cannot do, and does not claim:** the cross-user isolation matrix, concurrent rotation, queued-job removal and rotation execution, the Settings journeys, the master-key loss drill, validation-key revocation evidence, the remote smoke, and the final BYOK report — all blocked on the same four owner actions. Its one remaining code deliverable, the bounded two-key rotation window, is deliberately not attempted against no environment. **BYOK is NOT closed**, and `ADR-069`'s rule governs: a lane marked "passed" while unexercised is worse than one marked "unexercised", because the first is believed.
- **The per-IP throttle conflict BYOK.1 raised is RESOLVED by owner decision** (`ADR-070`). `credential_validation_attempts` gains one column, `ip_hash` — `HMAC-SHA256` over a canonicalized IP, never the raw address — under a **third independent secret**, `BYOK_RATE_LIMIT_PEPPER`, which is never the master key and never the fingerprint pepper. **The initiative migration budget rises from four to five**, with BYOK.3's allocation moving from 0 to 1. Requirements `BYOK-SCHEMA-010…015`, PRD Amendment P-1; plan Amendment A-2.
- **Six secrets are now provisioned for `local` and `test`** — three per environment. 6/6 present, valid base64, 32 bytes; **15/15 pairs distinct**; 1007 tracked files scanned, **0 matches**. `preview` and `production` remain deferred to the point of use for all three, per Amendment A-1.2.
- **The migration chain head is now `202608010069`** — five migrations across four slices, each moving it by exactly the allocation its slice is budgeted: BYOK.1 one, BYOK.2 one, BYOK.3 one (raised from zero by `ADR-070`), BYOK.4 two.
- **No BYOK migration has been applied to any shared environment yet.** All five are validated on every CI run by `supabase db reset` from an empty database, plus 57, 31, 58 and 47 pgTAP assertions; local/remote parity will be re-verified at the first BYOK deployment, which Amendment A-1.2 gates on preview and production secrets existing. **This is the shared reason BYOK.4's gate D2 and half of D10 are recorded as not claimed** — there is no deployed worker that can decrypt a credential to run them against.
- **One production account was removed** as abandoned test residue, with owner authorization and full cascade verification: `docs/reports/byok/GENERATED_ACCOUNT_CLEANUP_EVIDENCE.md`. Two real accounts remain.

## Slice EGC.1 — Organizations and Contexts (complete)

**`public.organizations` and `public.contexts` now have their first application write path.** Both were created by `202607160003` with forced RLS, four own-row policies and full `authenticated` CRUD, and for fifteen days nothing in the application wrote either — the only writer of any kind was `persist_entry_interpretation`. So a company could exist because a capture mentioned it, while the Company selector reported emptiness with no way to fix it (`EG-04`).

Delivered: four routes (`/app/organizations`, `/app/organizations/[organizationId]`, `/app/contexts`, `/app/contexts/[contextId]`), five Server Actions, two list loaders and one bounded options loader, both destinations in the `context` navigation group, both entity types in the History vocabulary and linkable from a history row, and create-and-select on the Company selector of the Person and Project forms.

**Zero migrations, zero grant changes, zero policy changes, zero new privileged boundaries, zero AI calls, no `tasks`/`reminders` write-path change, no deletion path.** Parity stays `202607310064`. The locale-ternary count stays at **266**, the G-0.4 baseline exactly.

**The adversarial review found thirteen defects and all thirteen were fixed, not argued down.** Three were BLOCKERs in the pgTAP suite that would have made the `database` CI job red on its first run — Docker is unavailable locally, so the suite could not be executed before CI would have. One was a real audit defect: the create-and-assign path overwrote an existing company with no `before_state`. One was **older than this slice** — Cancel-after-a-refusal permanently killed the reopen control on the Project and Person edit forms shipped in Phase 2F, leaving a dead Edit button until a page reload. Full accounting in `docs/reports/entity-graph/EGC_SLICE_01_ACCEPTANCE.md` §4.

**Two tests fail locally and are not this slice's.** `sql-reachability.test.ts` loses 2 of 46 assertions on this Windows checkout and loses the identical two on `main` at `4071a2f`, whose CI was green. The cause is the CRLF fragility Slice H diagnosed; it is recorded as repository maintenance and deliberately not fixed inside a feature branch. See §5 of the acceptance record.

## Slice EGC.3 — Convergence and closeout (complete)

**Four permanent instruments, and one of them changed shape because a production incident said it had to.**

`egc-reachability.test.ts` turns `EGC-INVARIANT-004` into a mechanical check over the **whole** authenticated route tree — every `page.tsx` must appear in the inventory with a verdict, so a new route cannot slip in unenumerated. It needed **three** verdicts rather than two, and the third was forced by the code: the Context detail page renders the people in a context and offers no control, because `EGC-ASSOC-003` puts the single writer on the Person side. That is not `EG-09` — `EG-09` was no path *anywhere* — so "writable from a named other route" is recorded as its own fact and then checked. It also resolves the **export name** rather than the module, which caught a real inventory error on its first run.

`locale-ternary-guard.test.ts` is the permanent non-increase guard the UX closeout proposed and nobody built. Proven by executing the mutation over a real temporary tree rather than over `src/`, because two sibling closeout tests walk `src/` concurrently.

`scripts/generate-egc-traceability.mjs` parses the inventory, resolves every artifact on disk, fails closed on drift, and regenerates content-identically. **It shipped with the same CRLF fragility that costs this repository two red assertions elsewhere** — it found zero of the three owner decisions on a Windows checkout, because their declaration ends `(.+)$` and `.` does not match `\r` while the table rows kept working on `\s*$`. Fixed, with a test that parses one fixture under both line endings.

`scripts/verify-egc-cleanup.mjs` **replaces the fixed-prefix rule with combined evidence**, because the prefix rule failed in production: the account removed on 2026-07-31 was classified as a real user by two acceptance artifacts while the hourly heartbeat ran on its behalf for three weeks. Six independent signals, two of them behavioural; `likely-fixture` requires a behavioural signal plus one more, so a naming heuristic alone can never reach it. **It reports and never deletes, and holds no delete path** — asserted by a test that reads its own source. Live run at closeout: 2 accounts, 0 likely fixtures, 0 residue.

## Slice EGC.2 — Person Relationships and Associations (complete)

**`public.person_relationships` now has a writer. It had never had one — in either layer.** The table was created by `202607160009` with forced RLS, four own-row policies and full `authenticated` CRUD; not the interpretation RPC, not a trigger and not the application had ever written it. `person_contexts` and `person_projects` had a trigger writer and nothing the owner could reach. So the owner could not record that Camila is their wife, and the only field on a Person page that looked like it described her was the company she works at (`EG-01`…`EG-04`).

Delivered: a typed, localized, **version-locked** relationship vocabulary of fourteen members; single-writer modules for all three tables; relationship add/correct/soft-end on the Person page; person↔context and person↔project association from the Person page and person↔project from the Project page — **all three through one contract**; role editing; eight new History action types and three entity types in both locales.

**Zero migrations, zero grant changes, zero policy changes, zero new privileged boundaries, zero AI calls, no `tasks`/`reminders` write-path change, and no hard delete anywhere on the three tables.** Parity stays `202607310064`. Locale ternaries fall from 266 to **262** — the read-only blocks these panels replace carried inline ternaries and their replacements do not.

**Two factual corrections to the PRD are recorded rather than folded in.** `person_relationships` has **no unique index beyond its primary key**, so EGC-REL-007's "the partial unique index" applies to the two association tables and not to it — which makes the duplicate refusal application-only, with no `23505` behind it, and the module says so. And three bounds in `schema.ts` are product ceilings rather than column mirrors, because `relationship_type`, `description` and `role` are all unconstrained `text`. Both corrections are asserted in `egc_relationship_lifecycle.sql` so they stay measured facts.

**The journey run found a real product defect, not only spec defects.** Linking a person to the owner's only context left the panel printing *create a context first* — advice to make something they already had — over the confirmation it had just replaced. `options.length === 0` was standing for two facts with different next steps. Same shape as `EG-04`, in a new place; fixed, with a regression test.

**The adversarial review found thirteen more, and all thirteen were fixed.** One was a BLOCKER in the pgTAP suite, again unreachable locally for want of Docker: `person_contexts` and `person_projects` each carry **two** unique constraints, and `now()` is constant across a transaction, so the gate-B3 re-add collided with the base `(person_id, context_id, valid_from)` key — while the duplicate assertions passed for the *wrong* constraint and would have kept passing with the partial index dropped. Two were real application defects: the relationship **edit** path had no duplicate check where the create path did, so a correction could deterministically produce two live rows of one type, which then made every later create report an outage and discard the owner's typed note. Full accounting in `docs/reports/entity-graph/EGC_SLICE_02_ACCEPTANCE.md` §5.

## Current truth

**Phase 2F — One Write Path is COMPLETE**, governed by the approved `docs/initiatives/phase-2f/PHASE_2F_PRD.md` Revision 4.3 (68 requirements, 12 families, slices 2F.1–2F.6, all six accepted and merged). Slice 2F.6 merged as **PR #33 → `7e3e5f0`** on 2026-07-30, with **merge-SHA CI run `30520514810` green on all three jobs on the first attempt**, and every closeout gate re-executed from merged `main` content: census stop-gate clear, cleanup verifier CLEAN with zero residue, funnel proof 32/32, end-to-end baseline 9/9, full remote suite exit 0, traceability matrix regenerating content-identically. Parity `202607300063` before and after. **Both of the phase's two expected migrations are applied** — 2F.3's creation contract and 2F.4's revocation, and **the phase added no third**: Slices 2F.5 and 2F.6 are read-only. The final accounting is `docs/reports/phase-2f/PHASE_2F_REPORT.md`, and every requirement is mapped individually in `docs/reports/phase-2f/PHASE_2F_TRACEABILITY_MATRIX.md`.

**No successor phase is authorized or started.** *(Amended 2026-07-31; the original sentence read "Phase 2G is named as a recommendation in `docs/initiatives/phase-2f/PHASE_2F_PROPOSAL.md` and nowhere else: no PRD, no plan, no ADR, no requirement ID, no artifact." Its first clause stopped being true when the owner commissioned a definition study; its **substance** did not, and the amendment states which.)* Phase 2G is recommended in `docs/initiatives/phase-2f/PHASE_2F_PROPOSAL.md` and evaluated in the definition study `docs/reports/phase-2g/PHASE_2G_DEFINITION.md`. **There is still no PRD, no implementation plan, no accepted ADR, no declared requirement ID and no implementation artifact of any kind.** The closeout asserts this rather than assuming it — now through the corrected A13 guard, which measures the four signals that mean a phase has started instead of banning a filename (ADR-067) — and the traceability generator still fails closed if a non-`2F-` requirement is ever declared as Phase 2F work.

**Remote migration parity is `202607310064`.** Slice 2F.4's deployment moved it to `202607300063` on 2026-07-29, and that was the Phase 2F head; the UX remediation's **Slice G5** moved it to `202607310064` on 2026-07-31 by adding the reminder lifecycle command boundary (UX-12, DEC-6, DEC-7 — ADR-060 and ADR-061). Local head is identical and there is no drift. Phase 2F's own claim that the phase applied exactly two migrations is unaffected: `202607310064` is **not** Phase 2F work, and the traceability generator's "no third Phase 2F migration" rule reads the phase's own inventory rather than the directory's length.

**The product UX/UI remediation is COMPLETE, and it was not a phase.** It was governed by `docs/reports/product-ux/PRODUCT_UX_FINDINGS.md`, ran in sixteen lettered slices (A, B, C, D1–D3, B2, E, F1, F2, G1–G5, H), and merged into `main` between 2026-07-29 and 2026-07-31. **Slice H closed it as PR #50, merge SHA `864d39c`, with merge-SHA CI run `30648854282` green on all three jobs.** Final `main` HEAD: **`864d39c`**. Its accounting is `docs/reports/product-ux/PRODUCT_UX_CLOSEOUT.md` §10.

**35 findings, every one with exactly one final disposition: 30 RESOLVED, 4 RETAINED with evidence, 1 DEFERRED to a named destination. Zero BLOCKED, zero OPEN, zero PARTIAL. All 8 P0 findings are RESOLVED.** The four retained limitations are decisions with recorded evidence rather than deferrals (UX-24 does not reproduce; UX-25 is Home's height, accepted in Slice C; UX-27's two audit rows are two genuinely distinct events; UX-33 is user-agent date-placeholder behaviour the page cannot influence). The single deferred item is **UX-22** — 266 inline locale ternaries across 34 files — with a destination named (**localization maintenance**), an exact count and a per-file list in `docs/TODO.md` and the closeout report.

**Slice H adds no migration, no SQL, no RPC and no grant change.** Parity is `202607310064` on both sides, unchanged by H and verified with `npx supabase migration list --linked`. `authenticated` still holds `SELECT` and `INSERT` only on `public.reminders`, and `SELECT` only on `public.tasks` — verified behaviourally against the deployed project, not asserted.

**Phase 2G is not authorized and has not started.** Slice H's job was to *recommend* its scope, and it does so in §8 of the closeout report — ranked, with the evidence for each item. Nothing there is authorized. Three items are explicitly **not** product scope and are named as repository maintenance instead: the UX-22 localization sweep; a `.gitattributes` fix for the Windows-only CRLF test fragility diagnosed in H (`core.autocrlf = true`, no `.gitattributes`, against test patterns anchored on bare `\n`; Linux CI is green and is the proof); and a scheduled whole-authenticated-set journey run, which is what would have caught UX-35 three slices earlier.

**`public.tasks` now has exactly one validated write path in both the application and the database.** The application half closed at Slice 2F.3; the database half closed here. `authenticated` holds `SELECT` only on `public.tasks`, and `SELECT` + `INSERT` on `public.reminders` — the latter being the documented Option C authoring exception. `anon` holds nothing on either table. See `docs/reports/phase-2f/PHASE_2F_SLICE_04_ACCEPTANCE.md`.

### Slice 2F.6 — Convergence and closeout (accepted, merged)

**No migration, no deployment, no production write of any kind.** Remote parity is `202607300063` before and after, verified both times with `npx supabase migration list --linked`. The slice delivers `2F-OPERATIONS-003` through `006`, the whole-phase convergence audit and the phase's acceptance record, governed by `docs/reports/phase-2f/PHASE_2F_SLICE_06_PRD.md`.

**The closeout's own instruments are stronger than the four before them, and deliberately so.** `scripts/generate-phase-2f-traceability.mjs` **derives** what it can and **resolves** what it declares: the 68-requirement inventory is parsed from PRD §6 by a declaration-anchored rule; slice ownership is derived from §7's own table as two relations, because the "Owns" column covers 61 of 68 and seven requirements are cross-cutting-only by design; every artifact path is resolved on disk and every `npm run` gate in `package.json`; migrations are read out of the directory; ADR coverage out of `DECISIONS.md`; and CI gates resolve two ways — a job-level gate to a workflow job *and step*, a suite-level gate to a file that sits inside a path the workflow actually executes. That last leg makes ADR-059's hazard mechanical: a suite swept out of `vitest.config.ts` now fails the generator instead of sitting committed and unexecuted.

**The census stop-gate executed clear, and the census needed a correction first.** It paged with `.range()` and no `order` — an `OFFSET` over an unordered relation may return a row twice or never, and `taskById` is what the two *blocking* buckets join against, so a skipped task page turned a real bucket-1 row into `TERMINAL_TASK_STATUSES.has(undefined)` and the gate failed **open**. Slice 2F.5 had already fixed the same defect class in the funnel reader. Corrected to a total order on the primary key with exhaustion asserted, the bucket predicates extracted as pure functions with CI cases in both directions, and the read-atomicity limitation printed by the census itself: two independent reads joined in memory are not a snapshot, so a nonzero blocking bucket requires a second confirming run before it escalates. Executed 2026-07-30T02:39:48Z against `ulvwzqlpsjyrnqzfxmck`: **buckets 1 and 2 zero**, bucket 7 zero and informational, 1 reminder total at status `sent`, 0 `snoozed`.

**The cleanup verifier says what it can and cannot prove, which is the point.** Every table it scans has `user_id … references auth.users(id) on delete cascade`, so an orphan row is *structurally impossible* and a zero orphan count is not a measurement — the Phase 2E verifier already made that argument to *exclude* a table. So the scan is kept for the one regression it can detect (a dropped or `NOT VALID` key, now also asserted in CI pgTAP), and the load-bearing detectors are named: zero surviving fixture-prefix users, zero fixture objects in `user-files`, and per-table read reachability. The storage scan the draft had dropped is back, because storage is the one residue class no foreign key removes. Fixture prefixes are read off the scripts that mint them — including `phase2f-gate3-`, which has no hyphen after `phase2f` and which a naive `phase-2f-` prefix would have missed entirely.

**Deferrals are proven to have held, not assumed.** Through PostgREST's own OpenAPI definition — a GET that reaches no function and writes nothing — `create_reminder` is absent, `create_task_command_v2` is absent, and `ai_usage_events` carries no provenance column, checked as a pattern over the **live column set** rather than against a guessed name, because no provenance column name was ever declared. ADR-057's reopening gate is intact: the dry-run script is present and no transcript of it exists.

**The convergence audit found what five slices had not.** PRD §10 — the phase's gate ledger — carried three inaccurate cells, and Revision 4.3 records all three: 2F.5's `database` cell understated a slice that added three pgTAP assertions; the parity row's stated rule contradicted its own 2F.2 cell; and **2F.4's authenticated-journeys cell claimed `● regression` for a gate that appears in none of its sixteen acceptance gates**, which is exactly what `2F-PRECOND-003` forbids. The audit also corrected a misattributed CI citation — the green-on-Linux proof for the CRLF defect is run `30497118489` at `6628b02`, not `30496790432` at `9c5345c` — and, in verifying it, found a third flaky component test (`question-answer-form.test.tsx`) that had never been recorded. Both are in `docs/TODO.md`; neither is fixed here.

**`--posture` was designed, reviewed and deleted.** The initial closeout draft proposed minting one fixture user in production to re-measure the `authenticated` denial. The review established that the property is already proven three ways, that the fixture reminder would move the census buckets the stop-gate reads, and that minting a user writes `profiles`, `agent_preferences` and an hourly `heartbeat_runs` row rather than one reminder. It is the phase's only removed-rather-than-added scope, and the posture is cited from the three executed sources instead.

### Slice 2F.5 — Measurement reader and evidence gate (accepted, merged)

**Merged as PR #31 → `2ae2606` on 2026-07-29; merge-SHA CI run `30506807423` green on all three jobs.** *(This line cited `30506608871` until Slice 2F.6's audit checked it: that run is at `da1d2f3`, the branch head, not the merge commit — the same misattribution class the audit corrected two paragraphs down.)* No migration; nothing deployed; remote parity unchanged at `202607300063`, verified before and after.** Acceptance record: `docs/reports/phase-2f/PHASE_2F_SLICE_05_ACCEPTANCE.md`.

**ADR-055's 90-day expiry is now dated: go-live 2026-07-29 (the merge date, ADR-060), expiry `2026-10-27`**, computed by `expiryDateFromGoLive` and carried in `docs/TODO.md` for `2F-OPERATIONS-006` to verify at closeout. Governed by `docs/reports/phase-2f/PHASE_2F_SLICE_05_PRD.md`; decisions ADR-058, ADR-059, ADR-060; implementation report `docs/reports/phase-2f/PHASE_2F_SLICE_05_REPORT.md`.

**ADR-055's evidence gate is now computable.** `scripts/phase-2f-command-funnel.mjs` aggregates the already-emitted `task_command_*` events into owner-scoped, content-free measures and evaluates both tiers. The reader also reports, per tier, whether the window it was run at could satisfy that tier at all: a 14-day window admits at most 15 distinct local dates, so the planning tier's 20 active days is unreachable there for a reason that has nothing to do with the data, and each tier must be read at its own window. The spike tier (50 qualifying commands / 10 active days / a 14-day window) can return `met`; the planning tier **structurally cannot** — `distinctUsers` is out of an owner-scoped reader's range, reported as `null` with `privilegedReadRequired`, so its best verdict is `met_pending_privileged_read`. `windowDays` is a **ceiling**, not a floor: fifty commands spread over a year is weaker evidence than fifty over a fortnight, and the gate refuses it. The rate gates are decided by integer multiplication against declared fractions, so no rounded rate stands between the evidence and the gate and a zero denominator can never satisfy one.

**`no_match` follows ADR-055's *final-outcome* definition, not the preview disposition.** `creation_offered` is emitted at preview time and a command that goes on to create ends `applied`; with no per-row join key the split is still exact in aggregate through `min(creationOffered, created)`, and creations whose offer predates the window are reported as skew rather than allowed to push a rate above 100%. Replayed creations are excluded and counted separately — a double-submit must not move a gate.

**The reader reports what it cannot measure.** Three allowlisted preview categories (`unsupported`, `applied`, `rejected_conflict`) are emitted by no deployed code path, so **`2F-MEASURE-005`'s unsupported-refusal volume is not measurable this phase** — a named partial, not a zero counter passed off as an observation. One intent emits one to three preview rounds and some emit none, so the counting unit is `qualifying_preview_round`. A CI case reads `actions.ts` and derives the unreachable set from the emitters, so a future emitter breaks the build rather than the claim.

**Exclusion mechanisms proven, with the residual named.** ADR-058 adds `is_synthetic` as a fourth mechanism and classifies it as hygiene rather than a trust boundary, since the field is client-supplied. The remote proof executed 32 assertions at exit 0 — owner-scoping, cross-owner isolation asserted after each owner's own positive count, anonymous denial, **service-role select denial**, synthetic and unsupported exclusion, replay exclusion, policy-version attribution, tier evaluation, expiry, cascade deletion and fail-closed cleanup — with every fixture proven gone. `revoke all … from service_role` is real and asserted, so **no credential this repository holds can read an event row once its owner is gone**: the cascade is proven as schema truth in CI pgTAP plus an asserted owner deletion, and the orphan-event residual stays pointed at 2F-OPERATIONS-004's verifier in Slice 2F.6.

**The end-to-end match baseline is published, pinned and scope-labelled** (2F-MEASURE-007, discharging 2E-MATCH-018's caveat by measurement). Eleven scenarios at policy `2026-07-25.3` through the real `loadTaskCandidates` against the deployed `list_task_command_candidates` and the real `rankTaskCandidates`: one-step **0.455**, needs-deliberateness **0**, confirmation-required **0.091**, ambiguous **0.273**, no-match **0.182**. The corpus reaches all three title tiers (six scenarios at tier 0, two at tier 1, one at tier 2), plus a status-excluded and an unconnected case; the coverage case asserts the tiers **SQL assigned**, not the corpus's own annotations. The retained scoring-layer numbers (0.429 / 0.071 / 0.071 / 0.214 / 0.214 over 14 scenarios) stay beside them and **cross-scope comparison remains prohibited** — the near-agreement is a coincidence of two differently-composed corpora, not a validation of either.

Three independent adversarial review cycles are recorded in the PRD (§23, §23.1, §23.2): 23 design findings, 21 implementation findings and 10 final pre-merge findings, of which three were rejected or narrowed on executed evidence. The recurring lesson is that **a guard which reads its own input proves nothing**: the first end-to-end corpus was degenerate (every hint missed the top two tiers, so the destructive and duplicate-title assertions passed because nothing matched), and the case meant to catch that grouped on the hand-written tier label rather than the tier SQL assigned. Both were fixed by making each check read the thing it is about, and a genuine tier-2 scenario was added rather than the claim narrowed.

**Pre-existing defect recorded, not fixed here:** `src/features/task-commands/sql-reachability.test.ts` fails two cases on a Windows checkout because it searches migration text with `\n`-anchored literals while `core.autocrlf=true` gives the working copy CRLF. Proven by reproducing with every 2F.5 change stashed and by matching after normalisation; CI checks out LF and the test passed 46/46 there at `6628b02`. Left to a separate change — see `docs/TODO.md`. *(Citation corrected by Slice 2F.6's audit: the green-on-Linux run at `6628b02` is **`30497118489`**, not `30496790432`, which is at `9c5345c`. That run's `application` job nonetheless concluded `failure`, on an unrelated and previously unrecorded flake in `question-answer-form.test.tsx` — now in `docs/TODO.md`.)*

### Slice 2F.4 — Task grant revocation and test-suite semantic migration (accepted, merged, deployed)

**Deployed 2026-07-29 18:38:20Z–18:38:28Z** via `npx supabase db push --linked`, applying exactly one migration with no manual intervention; all nine post-deploy assertion groups held. Merged as PR #28 → `c174f8f`, CI run `30479818771` green on all three jobs on the exact merge SHA.

All sixteen deployment-acceptance gates passed, including live `42501` denials for task INSERT/UPDATE/DELETE on **both a stale pre-migration session and a fresh one** (PostgREST reflected the revocation immediately), the retained Option C reminder INSERT, the cross-owner `23503` proof, read-side task RLS in both directions, and 14/14 production-flow checks — among them `create_due_task_reminder` still firing inside a definer path, and the same caller that creates through the validated contract being refused `42501` on a direct INSERT. **No rollback was required or executed, and the slice has no data residual of any kind.**

Gate 9 (full remote suite) required a **separate prerequisite correction**: two assertions in `scripts/remote-supabase-smoke.mjs` were written by `5099f81` for a one-event AI-usage fixture and never updated when `6c4a907` (Slice 2E.1) added a second legitimate event. **That defect predates Slice 2F.4**, which merely made the previously unreachable downstream checks matter for acceptance. Corrected in PR #29 → `ba63204` with no production semantics changed; `npm run test:remote` then passed exit 0 from merged main content.

**`authenticated` no longer holds any write privilege on `public.tasks`.** Migration `202607300063` revokes `insert, update, delete` there and `update, delete` on `public.reminders`. **No application source file changed** — since Slice 2F.3 no module issued those writes, so the revocation removes a permission, not a caller. That is also why this is the only Phase 2F slice with **no data residual**: a revocation writes no rows.

- **Final posture.** `public.tasks`: `authenticated` holds `SELECT` only. `public.reminders`: `SELECT` + `INSERT`, the latter being the Option C authoring exception whose sole caller is `createReminder`. `anon` holds nothing on either table. No grant widened anywhere (2F-REVOKE-006). The `tasks` direct-write allowlist stays **empty** and is compared by exact equality in both directions (2F-REVOKE-008).
- **The 2F-REMINDER-003 determination is written and is a revocation** (owner decision A4, recorded in `SECURITY.md`): no production module issues a reminder UPDATE or DELETE; every surviving UPDATE runs inside `apply_task_command`, `run_user_heartbeat` or a definer-routed `private.undo_*` handler; and **no `delete` against `public.reminders` exists anywhere in the repository**, so rows are removed only by cascade.
- **13 pgTAP statements dispositioned, not 11** (PRD Revision 4.2, owner decision A2). Slice 2F.3 had added two origin-drift stagings after Revision 4 fixed §9's table; Slice 2F.4's pre-code inventory found them. **10 change vehicle to the owning role; 3 — the reminder INSERTs — stay `authenticated` and double as living proof the retained Option C grant works.** No statement was mechanically re-roled and none was deleted: the interference proofs keep their exact behavioural pins, now writer-agnostic in fact rather than only in intent.
- **The one claim that died is inverted rather than reworded.** `apply.sql`'s "a plain client-side task UPDATE still works" is false by construction now; the denial moved to `supabase/tests/phase_2f_task_write_grants.sql`, and the property that write was actually carrying — the audit trigger tolerating an unset `app.audit_actor` — is restaged privileged. Because a `postgres` vehicle proves nothing about production, `phase_2c_slice_5_task_graph.sql` gains the actor-default assertion for a real SECURITY DEFINER writer that sets no actor (2F-TESTMIG-003).
- **Non-vacuity is now exact.** `phase_2f_task_write_grants.sql` re-issues, inside its own transaction, the grants `202607160003:195`/`202607160007:162` issued, proves the refused writes succeed, re-revokes and proves they refuse again — so the privilege is demonstrably the only variable.
- **PRD Revision 4.2 corrects three facts** (owner decisions A1–A3), none of which changes a requirement: §12's privilege provenance (the grants are versioned in the chain, issued dynamically inside `DO` loops — not platform defaults, except for `service_role`, where the original claim holds), §9's row count, and five drifted `creation.sql` anchors.
- **CI gains a re-grant rehearsal** as the `database` job's last step, with three explicit posture boundaries so no earlier assertion can have run under the wrong privileges. Its scope is stated everywhere it appears: it proves the committed rollback SQL applies and restores the versioned privileges; it does **not** prove PostgREST schema-cache convergence, in-flight session behaviour, or an operational production rollback.
- **All deployment-session gates are executed and recorded.** The migration applied 2026-07-29 18:38:20Z–18:38:28Z; the full remote suite passed at exit 0 from merged content after PR #29; and the live schema-cache residual — the one the CI rehearsal explicitly excludes — was closed by measurement, with `42501` observed on a session minted **before** the migration as well as a fresh one. *(This bullet previously stated the negation of all three, as three unmet pre-deployment gates: a paragraph written before the deployment and left standing beside this same section's deployment record above it. Slice 2F.6's convergence audit found the contradiction, and the traceability generator now fails closed on that class rather than relying on a reader noticing. Corrected, not deleted — the correction is recorded here as a correction.)*

### Slice 2F.3 — Manual task-creation convergence (accepted, merged, deployed)

Merged as PR #26 → `48d6a83`, CI run `30467623925` green on all three jobs. **Migration parity moved `202607280061` → `202607290062`** in the deployment session of 2026-07-29 (2F-OPERATIONS-001), applied on the first attempt with every post-deploy `DO` assertion holding. All 21 deployment-session gates executed and passed, including manual creation through the rendered form persisting `created_by = 'user'` with audit actor `'user'` and an executed undo, the two-owner creation probe, and zero fixture residue. Exactly one `pg_proc` row for `create_task_command`; legacy six-argument callers remain byte-identical. See `docs/reports/phase-2f/PHASE_2F_SLICE_03_ACCEPTANCE.md`.

### Slice 2F.2 — Work-surface mutation convergence (accepted, merged, deployed)

**The four Work buttons now route through `public.apply_task_command`.** A click resolves at click time through the deployed `public.list_task_command_candidates` — the rendered title as `p_title_query`, the mapped taxonomy action, the caller's own `auth.uid()` scope — selects the row whose `task_id` equals the clicked id, and applies. **No migration, no new RPC, no grant change, no change to `list_task_command_candidates`.**

- **`persistTaskStatus` and `updateTaskStatus` are deleted** (2F-SURFACE-012/013). The architecture gate's `tasks` allowlist dropped to **one entry** at this slice — `createRecord`'s insert — which Slice 2F.3 then took, leaving it **empty**; the gate fails on an allowlist entry whose writer no longer exists, so each reduction is mechanical rather than asserted.
- **Three disclosed behaviour changes, all owner-approved in PRD §4:** an unresolvable or ineligible click now **refuses with a localized refresh affordance** instead of blind-overwriting; **completing a task now cancels that task's scheduled reminders** (`apply_task_command` reconciles them; the deleted writer cancelled nothing) while `wait_task`/`resume_task` leave every reminder untouched; and **progressive enhancement without JavaScript is intentionally lost** for the four buttons, because rendering declared outcomes requires `useActionState`.
- **Title drift is permissive** (owner decision 4): the clicked `task_id` is authoritative, a changed title never refuses on its own, and the outcome renders the title resolution returned rather than the stale rendered one.
- **The ranker is deliberately not on this path.** `rankTaskCandidates`/`buildTaskCommandPreview` select through `result.candidates`, which is floored at `minCandidateScore` and capped at `TASK_MATCH_LIMITS.ranked`; routing a click through them would refuse legitimate clicks in exactly the drift and same-title cases 2F-SURFACE-004 forbids. Selection is by id out of the whole resolution result, which is what that requirement says. Everything else is reused: `validateTaskCommand`, `loadTaskCandidates`, `toTaskPreState`, `buildCanonicalPatch`, `buildApplyPayload`, `applyTaskCommand`.
- **Analytics use the existing vocabulary** — `task_command_applied` with `commandOrigin: 'work'` (already allowlisted at `202607280061:434`) and `task_status_changed` unchanged in shape. **No analytics migration exists.**
- **CI green on the exact head SHA `836ced3`** (run `30425313872`): `application`, `edge worker` and `database and journey` all successful — including the empty-DB migration chain, the full pgTAP suite, `db lint` and the foundation e2e on desktop and Pixel 7. The `application` job's green confirms the two local `sql-reachability.test.ts` failures are this workstation's (CRLF checkout), not the slice's; `git diff main` over that test and `supabase/` is empty. Local: lint 0/0, typecheck clean, build green, 2354/2356 unit tests.
- **Its deployment-session gates were subsequently executed** and the slice is accepted, merged and live. This slice carried no migration, so parity was unchanged by it.

### Slice 2F.1 — Guardrails, decisions and preconditions (accepted, merged)

Accepted on `c7e03b3` with CI run `30422655547` green on all three jobs; merged as PR #23 → `8c59c1d`. No migration, no deployment, no behaviour change.

- **Two CI guards exist and were proven red-first** (`src/lib/supabase/sql-grammar-guard.test.ts`, `direct-write-guard.test.ts`): the plpgsql grammar-trap guard (the `pg_catalog.coalesce/nullif/greatest/least` lookup that cannot resolve under `search_path = ''`, and the `if … case … then` `42601` trap — each detected in a deliberately defective fixture before the fixture was removed), and the architecture regression gate holding the direct-writer inventory of `public.tasks`/`public.reminders` to exact allowlists (tasks: the two legacy writers pending 2F.2/2F.3, empty at 2F.4; reminders: the Option C exception, permanent). The chain scan found exactly the two known superseded historical `pg_catalog.greatest(` sites (`202607220041:1524`, `202607220044:1506`) and nothing else.
- **Four ADRs recorded** (ADR-054 activity stays a task; ADR-055 the two-tier semantic-retrieval evidence standard with its 90-day expiry; ADR-056 phase-letter reconciliation — `PHASE_2_PLAN.md` §2F and `TODO.md` re-pointed; ADR-057 provenance deferred past 2F behind the executed-dry-run reopening gate).
- **`effective_limit` row semantics pinned** (`supabase/tests/phase_2f_effective_limit.sql`, 2F-PRECOND-002): truncation returns exactly `effective_limit + 1` rows, a population at the limit returns no phantom probe row, a negative limit clamps up to one, and `effective_limit` is a query-scalar — executable only in CI's `database` job, since Docker remains unavailable on this workstation.
- **The pre-code gate package is preserved and tracked** (2F-PRECOND-001): the writer inventory, reminder census, Gate 3 probe, the refuse-unless-disposable Gate 1 dry-run SQL, and `src/features/task-commands/work-surface-reuse.test.ts`, which now runs in every CI `app` job.

**Phase 2E — Natural-Language Task Updates is RELEASED.** All eight slices — 2E.1 (Epic 2E-A) through 2E.8 (Epic 2E-H) — are implemented, reviewed, corrected, accepted, merged, deployed to the linked project and validated against it.

**Phase 2E took remote migration parity to `202607280061`.** The whole chain `202607250055`–`202607280061` is applied; every post-deploy assertion held on the first attempt and no rollback was required. *(Current parity is `202607290062`, moved there by Slice 2F.3 — see the top of this section.)*

| Fact | Value |
|---|---|
| Phase merge | PR #18 → `5d22400` (merge commit, all 64 commit messages preserved), CI green on the exact merge SHA `96cf178` (run `30383879590`) |
| Corrective merge | PR #20 → `0efcd82`, CI green on `08d3940` (run `30391388573`) |
| Requirements | **121 of 122 delivered** |
| Still deferred | **`2E-COMMAND-012` only** — Phase 2F, by recorded decision (ADR-053, PRD revision 4) |
| Remote gates | aggregate smoke 23 assertions exit 0 · cleanup exit 0, zero residue · product-events exit 0 · Phase 2E online journeys 12/12 desktop+mobile, pt-BR+en |

**The three requirements Phase 2E closed as blocked on deployment are now delivered:** `2E-OPERATIONS-003`, `2E-OPERATIONS-004` and `2E-OWNERSHIP-004`'s remote half. They were never blocked on code.

**Deployment found three broken validation scripts, and none was a fault in the deployed system.** Eight defects across `remote-phase-2e-smoke.mjs`, `verify-phase-2e-cleanup.mjs` and `remote-product-events-smoke.mjs` — seven of them the same mistake, tooling restating a contract instead of reading it. One assertion had been passing while testing nothing (`undefined !== "undone"` is permanently true). All three had never executed against a deployed database: the smoke had only ever run its own preflight, and the verifier had only ever taken its absent-table branch. `PHASE_2E_SLICE_08_REPORT.md` §4.2 had already named this exact failure mode after finding one instance at closeout. Fixed in PR #20; no migration, RPC, privilege or production contract changed.

**Known non-blocking issue:** `e2e/intelligent-capture.spec.ts` does not pass deterministically online. It is model-dependent, predates Phase 2E, has no pre-deployment baseline, and its failures occur upstream of every Phase 2E mutation. Tracked in [issue #21](https://github.com/fabiokyrillos/my-brain/issues/21). **It is not a Phase 2E regression** and must not be recorded as one.

**121 of the phase's 122 requirements are delivered. One is not.** `2E-COMMAND-012` is reclassified to Phase 2F by recorded decision (ADR-053, PRD revision 4) — the versions exist as build constants and travel on the command session, but no column persists them, and adding one means `drop function` plus a full re-declaration of a ~1,460-line RPC or of `record_ai_usage`, which every AI path in the product shares. Residual risk: attributing a bad command to its prompt requires joining `ai_usage_events.created_at` to the deploy history rather than reading a column.

`2E-OPERATIONS-003`, `2E-OPERATIONS-004` and `2E-OWNERSHIP-004`'s remote half **were** blocked on deployment authorization and are now delivered and proven remotely. `2E-MATCH-018` is **delivered with a scope note** — the measured baseline covers the scoring layer against declared SQL verdicts rather than end-to-end matching, so any Phase 2F comparison must use that scope or re-measure. The full accounting is `docs/reports/phase-2e/PHASE_2E_FINAL_REPORT.md`, and every requirement is individually mapped in `docs/reports/phase-2e/PHASE_2E_TRACEABILITY_MATRIX.md`.

### Phase 2E slice narrative — point-in-time records, superseded by the Phase 2E summary above

> Each paragraph below describes its slice **at the moment that slice closed**, and several were written before Phase 2E was deployed. They are deliberately not rewritten: they are the record of how the phase was built, and this repository's convention is to re-label superseded narrative rather than delete it. Read the summary above — and `## Current truth` at the top of this file — for what is true now. In particular, every "local only", "not applied to the linked project", "did not run", "blocked on deployment" and "has not been read" below was true when written and is false today: the whole chain `202607250055`–`202607280061` is applied, parity is `202607300063`, and the remote gates those paragraphs owed were executed and recorded in the Phase 2E summary above.
>
> Slice 2F.6's convergence audit added this label. It found that `## Current truth` contained five present-tense claims that deployed migrations were undeployed — the same class as the Slice 2F.4 leftover corrected in this same slice, one section higher. Both scans in `scripts/generate-phase-2f-traceability.mjs` now treat a section carrying a supersession marker as history and everything unmarked as a current claim, which is the fail-closed direction.

**Slice 2E.8 closed the phase and its convergence audit found a real defect the seven slices before it had not.** `src/features/task-commands/vocabulary.ts` — the 61-entry bilingual status/priority term table — sat outside the versioning regime every other policy module lives inside. The `policy-lock.test.ts` case named *"pins the status and priority vocabularies"* actually digested `TASK_STATUSES`/`TASK_PRIORITIES`, the **closed database literals this module neither owns nor can change**, while the mappings it does own were digested by nothing. Re-pointing one entry — `bloqueada` from `blocked` to `deferred` — would have moved a user's task to a state they never named with the whole suite green. `TASK_VOCABULARY_VERSION` was orphaned and had already drifted a version behind while promising in its own docstring that it had not, and `vocabularyCoversEveryLiteral()` was exported and never called. All three are fixed, and the digest is sorted by code unit rather than `localeCompare` so an ICU update cannot move a pinned gate. See `docs/reports/phase-2e/PHASE_2E_SLICE_08_REPORT.md`.

**The closeout tooling was executed, not merely written.** `npm run test:remote:2e:cleanup` passes against the live linked project; `npm run test:remote:2e` correctly exits 2 with `BLOCKED ON DEPLOYMENT`; the traceability generator's fail-closed behaviour was proven by four tamper runs. Running the cleanup verifier is what found its own defect — it detected an absent table by SQLSTATE `42P01`, but PostgREST answers from its schema cache and returns `PGRST205` without reaching Postgres, so it died on the first Phase 2E table.

**All three of Phase 2E's authorizations are given and discharged: merged, deployed, released.** The checklists in `PHASE_2E_FINAL_REPORT.md` §10–§13 are executed. The rollback posture in §11 remains valid and unchanged — no applied migration is ever reverted; rollback means unmounting the command console. **That statement was true through Phase 2E and stopped being true in Slice 2F.2:** the command console is no longer the only entry point to the Phase 2E RPCs — `applyWorkItemAction` now reaches `list_task_command_candidates` and `apply_task_command` too, and `persistTaskStatus` no longer exists. Rolling back Phase 2E's surface would now also have to revert 2F.2, whose own rollback is a mechanical code revert with one recorded data residual (reminders cancelled while the new path was live stay cancelled).

**Slice 2E.7 gives the phase its first user-visible behaviour.** Every contract Slices 2E.1–2E.6 built had no production caller; this slice is that caller. A command console mounts on Chat (both the list page and a conversation) and on the task surface, with one Server-Action dispatcher behind it; `/app/work/cancelled` is the cancelled-task recovery route 2E-DESTRUCTIVE-006 requires, linked from the Work page because `capabilities.ts`'s `nested: true` drives active-state highlighting only. Migration `202607280061` adds the `task_command` surface and four event names to all three allowlists. See `docs/reports/phase-2e/PHASE_2E_SLICE_07_REPORT.md`, ADR-050, ADR-051 and ADR-052.

- **The clock is pinned once per command, and that is the mechanism rather than a detail.** `loadTaskCandidates` derives `p_observed_before` from the instant it is given and the RPC echoes it into the preview, which `buildApplyPayload` then hashes — so a fresh instant per step would move the fingerprint on every step of one operation key. Replay would return `2E_IDEMPOTENCY_MISMATCH` instead of the original outcome, a double-submit would be refused rather than replayed, and a destructive retry could **never** succeed, because the surviving confirmation is bound to the first attempt's digest. The design draft justified not pinning it on a claim about the twelve-column staleness gate that is simply false. See **ADR-050**.
- **The destructive confirmation is issued by the round that *renders* the preview, not by the one that consumes it.** Issuing at Confirm time binds the token to whatever the envelope says then rather than to what the user saw, which leaves 2E-DESTRUCTIVE-003 with no prior confirmation to invalidate. The same placement applies to the creation confirmation. See **ADR-051**.
- **The confirmation dialog is hand-rolled, on executed evidence.** jsdom 29.1.1 has **no** `showModal`, `show` or `close` on `HTMLDialogElement.prototype` — `showModal()` throws — and Vitest with jsdom is the only accessibility gate CI runs, so a native `<dialog>` would leave 2E-A11Y-004 asserted by nobody. The focus trap that replaces it found a real defect on its first run: `input:not([disabled])` matches `type="hidden"`, so "the first focusable element" resolved to a hidden form field and the dialog opened with focus still on the page behind it.
- **A writing path requires the staleness witness, and refuses without it.** Every render records `{taskId, updatedAt}`; every write passes it back as the preview's `expected`. An envelope that reached a write without one is an envelope no server render produced, and honouring it would make 2E-PREVIEW-006 structurally unreachable and turn the apply into the silent recompute-and-apply PRD §12.6 forbids by name.
- **Nothing throws out of a Server Action.** `creation.ts`, `confirmation.ts`, `apply.ts`, `candidates.ts`, `matching.ts` and `preview.ts` all signal precondition faults by throwing; an uncaught one escapes the action and drops the user's pending command, which 2E-UX-002 forbids. Every one is mapped onto a rendered, retryable state.
- **Retry is keyed on the declared failure policy, never on the outcome.** `undeclared_failure` is `refused` **and** `retryable: true` (`errors.ts:335-340`) — the lost-response case, safe to retry because the operation key is unique in the database. Keying off the outcome would render it as a dead end.
- **`task_command` is its own analytics surface, and the remote smoke now reads its vocabulary from `contracts.ts`** instead of restating it (2E-ANALYTICS-006). Making the vocabulary authoritative immediately exposed three event names the smoke had never exercised. See **ADR-052**.
- **A `"use server"` module may export only async functions.** The build — not lint, not typecheck — caught the state vocabulary and the idle value being exported beside the actions; they now live in `console-state.ts`, which is also what keeps the client component from importing the server graph for a type.

**Documentation catch-up owed by this slice, now paid.** Commit `291cc75` accepted Slice 2E.6 while touching only the progress file and that slice's report, so `STATE.md`, `CHANGELOG.md`, `TODO.md` and `DECISIONS.md` were never updated for it — a Definition-of-Done §13 gap on an already-accepted slice, recorded rather than silently repaired. Slice 2E.6's entry is below.

**Slice 2E.6 shipped no-match activity creation (Epic 2E-F) and was accepted on 2026-07-27.** A validated task-like no-match becomes one confirmed standalone inbox task. Migration `202607270060` adds the canonical creation payload, owned relation resolution, the preview/confirmation/creation RPCs, a compensating undo and shared creation-family guards; `creation.ts` carries the capability-bound one-clarification continuation and its terminal outcome. Created rows have `created_by = 'agent'`, `status = 'inbox'` and null `source_entry_id`/`source_interpretation_id`/`candidate_index`; the existing task-insert trigger remains the only due-reminder creator. Exact replay returns the original identities, including after undo, and never recreates or restores the task. A real two-session same-key PostgREST race runs in the CI database job. See `docs/reports/phase-2e/PHASE_2E_SLICE_06_REPORT.md`.

**Slice 2E.5 enables the two verbs Slice 2E.4 refused, and closes two ways a cancelled task could be resurrected illegitimately.** Migration `202607260059` extends `public.apply_task_command` by `create or replace`, keeping its seven arguments so neither verb acquires a second write path. `cancel_task` is gated on a new `public.task_command_confirmations` row that **no client role may write** — both writers are definer functions — bound to the owner, the normalized operation key and a fingerprint the RPC derives itself, and consumed by one guarded UPDATE placed after the replay branch and inside the mutating transaction. **All fifteen actions of PRD §11.2 are now enabled.** `2E_ACTION_NOT_ENABLED` is retired, because nothing can raise it any more. **There is still no UI, route, Server Action, product event or model call**; the consumer is Slice 2E.7. See `docs/reports/phase-2e/PHASE_2E_SLICE_05_REPORT.md`, ADR-047, ADR-048 and ADR-049.

- **2E-DESTRUCTIVE-008's creation-undo collision is real and is closed in both orderings**, with one declared `55P03` token read from one shared `private` predicate by all three doors. See **ADR-048**.
- **A second collision was claimed, implemented, and then withdrawn as unreachable — before acceptance.** A review of the *design* argued that cancelling a task frees its candidate slot under `202607220040`'s partial indexes, so a re-confirmed duplicate would make the later restore a bare `23505`. A five-lens review of the *shipped code* disproved it, four lenses independently: `public.record_entry_task_candidate_confirmation` (`202607220041:299-364`) is an `after insert or update` trigger that writes the `'confirmed'` resolution row `confirm_entry_task_candidates_v6` deliberately does not, it survives the cancellation, and `2C_TERMINAL_DISPOSITION` reads it — so the duplicate cannot be created, and the only path that frees the slot is a creation-undo, which is already refused earlier. The token, the predicate, both guards, both `unique_violation` traps and the pgTAP section were removed rather than kept as defence in depth, because ADR-049 — written in this same slice — says an unprovokable member of a closed vocabulary is the same defect as a missing raise. **The correction cost more than the feature**, and the lesson is recorded in ADR-048's withdrawal note: a reachability argument built from one function's source is a claim about that function, not about the system, and triggers are where the difference hides.
- **Enabling two actions required five hand-copied predicates to agree, and two of them fail silently.** Missing the domain-write list sends `cancel_task` into the *relation insert* branch; missing the recorded-`applied_state` list makes the ten-column undo guard refuse **every** cancel-undo forever. Slice 2E.4's own comment had named the hazard. The five copies are now one declared constant.
- **2E-DESTRUCTIVE-005 and 2E-DESTRUCTIVE-007 were both partially unmet and are now met.** The preview disclosed three of the four things the requirement lists — the missing one being what happens after the undo window closes, which is the whole reason `restore_task` exists. And `private.undo_confirm_entry_tasks` cancels tasks without setting `app.audit_actor`, so a system-executed compensating cancellation was recorded as a user act; it is re-pasted with one line, using the mechanism ADR-046 already defines rather than a second one.
- **Every test that greps an RPC's text had to move.** `202607260059` supersedes `202607260058`'s declaration of `apply_task_command` and `202607250056`'s of `list_task_command_candidates`, so six assertions pointed at the old files were reporting on bodies the database no longer runs — red for a token correctly retired, green for two subjects that no longer exist. This is a standing hazard of `create or replace` in an append-only migration chain, and is recorded as such in the progress handoff.

**Slice 2E.4 ships the first RPC in this codebase that mutates an existing task.** Before it, the only live task-mutation path was a plain client `UPDATE` (`src/features/operations/actions.ts:148-152`) with no undo record, no expected-pre-state check, and an audit trigger that stamped every change as a user action. Migration `202607260058` adds `public.apply_task_command` — owner-scoped, expected-pre-state gated, operation-key idempotent, fingerprint replay-safe, audited with a real actor, and undoable through two registered private handlers, with reminder consistency for every transition that invalidates one. **Thirteen of the fifteen actions are enabled**; `cancel_task` and `restore_task` are refused with the declared code `2E_ACTION_NOT_ENABLED` because cancellation is destructive and PRD §23.4 ships `restore_task` with it — Slice 2E.5 admits both by `create or replace` on the same function rather than a second write path. **There is still no UI, route, Server Action, product event or model call**, and nothing in the product calls the RPC; its consumer is Slice 2E.7. See `docs/reports/phase-2e/PHASE_2E_SLICE_04_REPORT.md`, ADR-044, ADR-045 and ADR-046.

- **Staleness is a typed twelve-column `is distinct from` comparison against the `for update`-locked row, and the fingerprint is not the gate.** The claimed instants are cast to `timestamptz` *for comparison* while the same `observedBefore` text is hashed *verbatim* for the fingerprint — conflating them would force the RPC to re-render the locked row's timestamps byte-identically to what PostgREST emitted, which is unwinnable. The `55P03` raise deliberately carries **no detail**, because `src/features/agent/actions.ts` branches on `error.code === '55P03'` alone before any details check. Relation arrays and reminder state are deliberately excluded: comparing relations would refuse a `complete_task` because an unrelated `assign_project` landed, and the heartbeat flips `scheduled → sent` hourly with no user act, so gating on reminders would manufacture a stale refusal on a cron tick.
- **`no_change` writes nothing at all, which required deciding it before the reservation.** 2E-UPDATE-009 demands no task update, no audit row **and no undo row**, but 2E-UPDATE-007 makes the reservation the first write — so the canonical delta is computed against the *claimed* pre-state and returns immediately, with no lock taken. An unlocked ownership probe still runs first, so a non-owned and a nonexistent task both yield `P0002 'Task not found'` (2E-OWNERSHIP-002). See **ADR-045**.
- **The shared audit trigger now derives its actor from a transaction-local `app.audit_actor`**, read with `missing_ok` and defaulting to `'user'` so `persistTaskStatus` stays byte-identical, written with `is_local => true` so it cannot leak across a pooled PgBouncer session. The apply path sets `'user'`; the undo handlers set `'system'`, which is what makes 2E-DESTRUCTIVE-007 provable at the trigger layer in Slice 2E.5 without reopening the trigger. Its watched columns extend to `title` and `description`, closing the blind spot 2E-MATCH-006 declared — going forward, not retroactively. **This is the first Phase 2E migration that is not inert on a dormant surface:** `audit_task_change` fires on every existing `public.tasks` write. See **ADR-046**.
- **The RPC is deliberately unversioned.** ADR-037 §1 warrants a version only when a closed input shape changes incompatibly, and there is no predecessor; the contract version lives in the hashed `TASK_COMMAND_POLICY_VERSION` and the `'taskcmd-v1:'` operation-key prefix. It is nonetheless added to `rpc_version_retirement.sql`'s posture array, which is hardcoded and would otherwise never notice a new definer function. See **ADR-044**.
- **Three defects were found, and two are one family: SQL constructs that look like function calls but are grammar.** An adversarial round found `pg_catalog.coalesce(...)` at eleven sites — COALESCE has no `pg_proc` entry, like GREATEST/LEAST, so it cannot be schema-qualified under `search_path = ''`; two of the eleven sat in `audit_task_change`, so the first `public.tasks` UPDATE would have broken `persistTaskStatus`, `undo_confirm_entry_tasks` and two existing pgTAP suites. The same round found three reminder-integrity guards comparing a value against the write that produced it, so declared members of a closed vocabulary could never be raised. Then **CI found what no local gate could**: `42601 syntax error at end of input` — plpgsql reads an `if` condition by scanning for the first `then` at paren-depth zero, so a bare `case … when … then … end` there ends the condition at the `case`'s own `then`, and the migration would not apply at all. One review finding was **refuted with verified reasoning**: the terminal timestamps are written unconditionally, mirroring `persistTaskStatus` as PRD §11.2 requires by name, because `changedFields` is a disclosure list and not a write manifest — which `updated_at` proves by being written on every UPDATE and named in no action's list.
- **The pgTAP arithmetic is the evidence, not the pass.** CI reports `Files=28, Tests=1059, Result: PASS` — exactly `27 + 1` files and `927 + 132` tests against the Slice 2E.3 baseline, with `phase_2e_task_command_apply.sql ... ok` in the log, so every assertion this slice added executed rather than being skipped. All thirteen declared `2E_*` codes are provoked (2E-UPDATE-017); three of them by transaction-scoped fault injection, because one pgTAP transaction cannot stage a concurrent writer, and that is stated rather than hidden. `apply_task_command`'s signature is pinned **three ways** (2E-OPERATIONS-002) — migration text, content comparison against the hand-written types, and `proargnames`/`pronargdefaults` from the real catalog.
- **A second, whole-artifact adversarial round found seven more defects — including a Critical this slice's own report had recorded as a met requirement.** Five lenses, every finding handed to a separate skeptic: 19 findings, 10 survived, 7 distinct. The Critical: `undo_apply_task_command_fields` restored ten scalar columns while guarding on `status` alone, so rename → reschedule → undo-the-rename silently discarded the reschedule, returned `{"undone": true}`, and left a live `scheduled` reminder against a null due date. 2E-UPDATE-014 and 2E-PROVENANCE-001 were both violated and both recorded as MET; the report now marks those rows as corrected rather than silently rewritten. Also fixed: an unraisable `2E_UNDO_REMINDER_INTEGRITY`, two evidence gates that used `<>` where `jsonb_typeof` makes it NULL-blind, `append_note` unappliable past 2000 characters, and copy promising "nothing was changed" for a transport failure where the write may have committed. **Three guards were unfalsifiable by the 116-assertion suite** and now have cases; `plan(116)` → `plan(132)`. The lesson: the first round reviewed the migration alone and could not have found five of the seven, because they live in code it never read or require reasoning across two artifacts at once.
- **2E-OPERATIONS-003's remote smoke and the authenticated journeys are genuinely outstanding for this epic, unlike the two before it.** Epic 2E-D's acceptance criteria (PRD §19.1) explicitly name "a disposable remote smoke and authenticated desktop/mobile journeys", so they cannot be deferred to phase level as Epics 2E-B and 2E-C's were. They are blocked on deployment and on Slice 2E.7's surface, and are recorded as gaps rather than argued away. The smoke must additionally prove a real two-session concurrency race (2E-UPDATE-008's loser path is unprovable inside one pgTAP transaction) and that the fingerprint TypeScript computes equals the one the RPC derives, over the wire, for all thirteen actions.

**Slice 2E.3 ships the projection half of Epic 2E-C, and still no mutation.** `src/features/task-commands/preview.ts` (`buildTaskCommandPreview`) and `disambiguation.ts` (`buildTaskDisambiguation`) are pure read-only projections over an already-observed candidate set: five declared dispositions, `willMutate: false` as a literal type, relation- and instant-aware `no_change`, linked reminder effects computed from observed state, ranked order preserved untouched, distinguishing evidence marked, and `confirm_one` never rendered as a list of one. `outcomes.ts` declares 2E-UX-001's twelve outcomes for the first time as an iterable `as const`; `copy.ts` covers all of them in pt-BR and English with no locale fallback. **No UI, no route, no Server Action, no product event, no model call, and no user-visible behaviour change** — nothing calls any of it. See `docs/reports/phase-2e/PHASE_2E_SLICE_03_REPORT.md`, ADR-042 and ADR-043.

- **The projection sufficiency question was answered by execution, and the answer was "no" — twice.** Slice 2E.2 deliberately left `202607250056`'s amendment window open so its first consumer could prove the projection sufficient. It was not: `patch.projectRef`/`contextRef`/`personRef` are the user's own words while the pre-state carries relation *ids*, and `reminders` was the only member of the taxonomy's `changedFields` with no observed state (`due_at is not null` is not a proxy — `create_due_task_reminder` is `after insert` only). **Neither gap was fixable in TypeScript**: `normalizer-divergence.test.ts` enumerates the whole feature directory and forbids `normalizeEntityName(` in every non-test module in it, including ones not yet written. So `202607250056` was **amended in place, twice** — +3 appended `default null` relation-ref arguments and +8 result columns (`project/context/person_ref_id`, `project/context/person_ref_name`, `scheduled_reminder_count`, `next_reminder_at`) — with resolution delegated to `public.resolve_owned_entity_exact` rather than a second copy of entity resolution (ADR-021). **The amendment window is now closed by exhaustion, not by policy:** any further change to its result columns or arguments costs a `_v2` (`42P13`).
- **Migration `202607250057` adds `public.task_command_fingerprint`, and TypeScript never hashes.** `immutable`, `strict`, **not** `security definer`, `set search_path = ''`, with `jsonb_build_object` as the canonicalizer so 2E-IDEMPOTENCY-003 holds structurally. Instants cross the boundary as text, because `to_jsonb(timestamptz)` renders through the session `TimeZone` GUC that an empty `search_path` does not pin. **The fingerprint is identity, never authorization** — its inputs are all client-held, so 2E-DESTRUCTIVE-002's server-issued single-use token remains a separate mechanism. See **ADR-042**.
- **Nothing in this repository has ever cancelled a reminder, and that contradicts the PRD in one place while confirming it in another.** The only writers of `public.reminders` are the `after insert` trigger (`202607160007:195-209`) and the heartbeat's two mark-sent updates (`202607160013:33`, `202607170016:552-560`). So §11.3's premise that `reopen_task` "re-creates the reminder the INSERT-only trigger cannot" is **false for rows written before Phase 2E** — such a task may still hold a live `scheduled` row, and the preview discloses close-and-insert for it. Conversely `snoozed` is **not** a gap: every heartbeat path selects `status = 'scheduled'` and no other (`202607160007:274`, `202607160013:31`, `202607170016:510`), so a snoozed reminder can never fire and §11.3's wording is complete.
- **One adversarial review returned NOT READY on two Criticals, both reproduced by execution.** The preview could never name the entity it would add — `relationAfter` looked the resolved id up in the relations the task *already holds*, so the real addition rendered a bare `"+1"` (a raw literal bypassing the copy module) while the already-held case rendered "Acme, Acme" beside copy saying nothing would change. And the reminder rule required a full hour of lead (`due - 1h > now`), misreading `greatest(now(), due_at - interval '1 hour')`: "move it to 5pm" typed at 4:30pm disclosed "no reminders are affected" while the mechanism would have created one. Both fixed, along with duplicate-reminder promises under `reopen_task`/`restore_task`, `completed_at`/`cancelled_at` reporting `changed: false` while rendering a change, the stale/refused shells reading candidate content before ownership was established, two different instants acting as "now", and a fabricated empty-string `observedBefore` that would have produced a *valid* digest over an instant that never happened. **One finding was rejected with reasoning recorded:** 2E-UPDATE-009 makes `no_change` write nothing, so disclosing a reminder cancellation there would be the invented effect.
- **Epic 2E-C's "surfaces are keyboard operable and localized" clause is deliberately NOT met.** No Phase 2E UI, route or Server Action exists; 2E.4/2E.5/2E.6 each change the surface's shape; and with no mutation there is no async action, so 2E-A11Y-002/003 cannot be tested against anything. Components are deferred to **Slice 2E.7**, which must own 2E-A11Y-001/002/003 in the traceability matrix. Shipping them now would repeat the consumer-less `retryProcessingJob` pattern the pre-2E hardening pass removed. See **ADR-043**.
- **The acceptance gate has not been read.** `lint` 0/0, `typecheck` 0, `npm test` 1875 passed / 113 files, focused `src/features/task-commands` 786 passed / 15 files, and `build` clean — all local. pgTAP is **authored but not executed locally** (`plan(73)` and `plan(19)`); Docker is unavailable here, so `supabase db reset`, `supabase test db` and `supabase db lint --local` cannot run and **no pgTAP or `db lint` result is claimed as a local execution**. CI's `database` job passed on `c50960b`, proving the *first* amendment and `202607250057` apply from an empty database with the full pgTAP suite green; the *second* amendment (the three `*_ref_name` columns) and the updated `plan(73)` **have not been executed anywhere**. A green `database` job on `4f9aff8` makes the verdict unconditional.
- **2E-OPERATIONS-003's focused remote smoke still did not run and is still not claimed to have passed**, now owed for this slice too. Nothing calls the RPCs and neither exists in the linked project. The specification it must satisfy — Slice 2E.2's, plus ref resolution including cross-owner and ambiguity, reminder counts, and the fingerprint's stability and grant posture — is in §11 of the slice report.

**Slice 2E.2 ships the read half of matching, and no mutation.** Migration `202607250056` adds `public.list_task_command_candidates` — owner-scoped, filtered on the action's declared eligible statuses, ordered totally and deterministically **before** it truncates, returning one row beyond the limit so truncation is detectable, and projecting the full observed pre-state of every field §11.2 can change. `src/features/task-commands/matching.ts` scores, orders and classifies; it is pure — no AI, no network, no clock read, no Supabase client. Nothing calls the RPC: its first consumer is Slice 2E.3's preview.

- **It is `security definer`, and that was forced rather than chosen.** `202607170020:314` revokes EXECUTE on `normalize_entity_alias` from `authenticated`, and its non-null `proconfig` stops the planner inlining it, so `security invoker` — which is what 2E-MATCH-001's "and by RLS" clause literally wants — would raise `42501` for the only role that calls it. Granting EXECUTE would also have worked and was **rejected**: 2E-OWNERSHIP-003 says this phase widens no grant. The cost is stated rather than papered over: inside the function the `auth.uid()` predicate is the only ownership control, and cross-owner denial is *proven by execution* in pgTAP — symmetrically, plus a null-caller case — rather than argued from role attributes. See **ADR-040** and the new `SECURITY.md` section.
- **One generated type is hand-maintained.** `supabase gen types typescript` cannot run in either available environment, so the RPC's `database.types.ts` entry is hand-written and **no claim of regeneration is made anywhere**. Parity is proven three ways instead — the migration declares the signature, `database-types-parity.test.ts` compares by content, and pgTAP pins both against `pg_proc.proargnames` from the real catalog. See **ADR-041**.
- **The measured 2E-MATCH-018 baseline** at policy version `2026-07-25.3`: one-step 0.429, matched-needs-deliberateness 0.071, confirmation-required 0.071, ambiguous 0.214, no-match 0.214. **State the scope when citing these:** the 14-scenario corpus supplies the SQL verdicts by hand, so the rates measure the *scoring layer*, not end-to-end matching. Every hand-written triple is now proven to be one SQL can actually emit.
- **Six independent reviews.** Five found five Criticals, all fixed — the RPC could never have run (`42501`); an expression index would have broken task creation on a live path; a bad clock manufactured a one-step apply; a relation hint rewarded the task that already held the relation, which for `set_waiting_on` wrote a real row on the wrong task; and two NUL bytes silently voided the entire pgTAP file. A sixth review found the suite green under **fifteen** mutations that change what the matcher decides. Three further acceptance reviewers then found that four of this slice's *own* correction-pass fixes were themselves untested — including the guard stopping a relation hint from rewarding the task that already held the relation — plus one live defect: the status hint was tautological for `reopen_task` and `restore_task`, reaching a one-step apply on a word that excluded no candidate. All are fixed and covered; **36 mutations applied across three rounds, 36 killed.** Full disposition in `docs/reports/phase-2e/PHASE_2E_SLICE_02_REPORT.md`.
- **Migration `202607250056` is local only, and deployment is deliberately deferred.** Not because it is unready, but because `create or replace` cannot change a `RETURNS TABLE` shape (`42P13`): while the migration has only ever been applied to ephemeral CI databases, the projection can still be corrected in place if Slice 2E.3's preview proves it insufficient. Deploying now would buy a `_v2` in the very next slice — the sprawl ADR-037 exists to contain. The amendment window closes at merge (2E.8) or deployment, whichever comes first.
- **2E-OPERATIONS-003's focused remote smoke did not run and is not claimed to have passed.** It is blocked on deployment. Epic 2E-B's acceptance criteria do not name a remote smoke — Epic 2E-D's explicitly do — and PRD §19.3 makes it a phase-level Definition of Done item, so it is classified as a non-blocking deployment dependency owed at Epic 2E-H. Slice 2E.1 set the same precedent.

The normative contract is `docs/initiatives/phase-2e/PHASE_2E_PRD.md` — approved at revision 2 and **now at revision 4**, with the requirement inventory unchanged at 122 across 16 families and 8 epics. PR #18's `database` job resets the migration chain from zero and runs the full pgTAP suite, a gate that cannot run on the development workstation because Docker is unavailable; that is why the PR was opened as a draft and held that way through Slice 2E.7. **The draft-only framing in this paragraph is historical and is superseded by the Current-truth section above:** Slice 2E.8 is accepted and PR #18 is ready for review.

**Slice 2E.1 ships a contract and nothing else.** There is no task search, no matching, no mutation, no RPC, no UI, no Server Action, no product event, and no user-visible behaviour change. `AIProvider.parseTaskCommand` exists and has **no production caller yet** — by design; its first consumer is the Server Action of Slice 2E.7.

- **The action taxonomy is data** (`src/features/task-commands/taxonomy.ts`): PRD §11.2's fifteen actions with eligible source statuses, allowed target values, the patch-field contract, changed fields, destructiveness, one-step eligibility, confirmation requirement and undo strategy. The *allowed target values* column is load-bearing, not documentation: without it `set_status` carrying `cancelled` would be a non-destructive, one-step, unconfirmed route to the exact transition `cancel_task` exists to guard. `src/features/task-commands/policy-lock.test.ts` transcribes §11.2 row for row and pins the table to its policy version by digest, so neither the taxonomy nor the prompt can change without the version moving in the same commit (§10.4).
- **Temporal phrases resolve deterministically** (`temporal.ts`): a declared, enumerable bilingual lexicon whose entries state their own resolution rules, against an explicitly injected instant and IANA timezone, converted by the already-proven `resolveLocal` and failing closed on a non-existent wall time, a non-calendar date, an unknown zone, or a phrase outside the lexicon. The model never produces an instant — a complete timestamp is refused, not trusted.
- **Status and priority are bilingual** (`vocabulary.ts`): a declared closed table maps the user's own pt-BR or English word onto the database literal, and distinguishes "names no value we have" from "names one this action may not reach".
- **The model contract is one importable module** (`src/lib/ai/task-command-schema.ts`): prompt, response schema, both version constants, the bounded-input classifier and the closed content-free error vocabulary. See **ADR-039** for why 2E-COMMAND-010's parity requirement is discharged as single declaration rather than cross-runtime comparison.
- **Migration `202607250055`** adds the `task_command` literal to the `ai_usage_events` operation CHECK *and* to the `record_ai_usage` guard, with a fail-closed assertion that the constraint swap took effect. `source_type` is deliberately unchanged — at parse time no task is selected, so a null source is truthful and `'task'` remains a Phase 2F decision (PRD §22). Generated types are unaffected: `operation` is `text`, so CHECK literals never appear in `database.types.ts`. **Local only — not applied to the linked project.**

Five independent reviewers found two Critical defects, both proven by executing the code: `lastDayOfMonth` returned **1 February** in every non-leap year, so "fim do mês" produced a deadline up to 27 days in the past reported as resolved; and a model-supplied instant was accepted verbatim, so `2026-13-45T99:99:99Z` and `2026-02-30T12:00:00Z` both passed as valid. Both are fixed, with regression coverage. Full disposition in `docs/reports/phase-2e/PHASE_2E_SLICE_01_REPORT.md`.

Previously, between Phase 2D and Phase 2E, one consolidated hardening initiative ran against `docs/reviews/ARCHITECTURE_REVIEW_2026_07.md` on branch `codex/pre-2e-foundation-hardening`. It delivered the minimum architectural hardening the review names as pre-2E, and nothing else:

- **CI now verifies the database and the worker.** `.github/workflows/verify` gained two jobs beside the existing application one: `worker` (`deno check` on both deployed entrypoints, `deno test supabase/functions/`) and `database` (local Supabase stack → `supabase db reset` applying the whole migration chain from an empty database → the complete pgTAP suite → `supabase db lint` → the deterministic `e2e/foundation.spec.ts` journey on the production build, desktop and Pixel 7). `supabase/seed.sql` installs pgTAP for local/CI only. This closes the `SECURITY.md` "Teste pgTAP integral em CI com banco limpo" item and the review's H1/H10. Running the suite for the first time found and fixed one real drift: `resolve_pending_question_v3.sql` declared `plan(34)` for 37 assertions.
- **`undo_operation` is a thin router.** Migration `202607250052` moves each operation family's compensation into a private handler registered in `private.undo_operation_handlers`, keeping the RPC's signature, security posture, grants, preamble order, lock order, error codes, and per-branch return shapes byte-compatible. A trigger refuses to record an operation whose `action_type` is not registered, so an operation cannot exist without a handler to reverse it. Adding a Phase 2E undoable operation is one handler plus one registry row — never again a ~470-line re-paste (review H2, the only true pre-2E blocker).
- **The production extraction path validates model output.** `supabase/functions/_shared/extraction-validation.ts` replaces the worker's bare `as Extraction` cast, and `src/lib/ai/extraction-parity.test.ts` fails the build whenever it and the Zod source of truth disagree on any of 114 corpus cases. Migration `202607250053` adds a defense-in-depth trigger bounding AI-origin interpretations (review H5).
- **One canonical localization mechanism.** ADR-036 adopts typed feature copy modules, removes the never-imported `next-intl` dependency, and fixes 30 Portuguese-only strings that reached English users across four Server Action files.
- **RPC versions have a lifecycle.** ADR-037 and the `docs/DATABASE.md` policy, applied once: migration `202607250054` revokes `authenticated` execute on `confirm_entry_tasks`, the single evidence-backed case. The `resolve_pending_question_v1`/`v2`/`v3` deferral (PRD §21.7, ADR-034) stands untouched.
- Selected quick wins: a truthful error boundary that surfaces `error.digest`, explicit OpenAI timeout/retries, embedding request-id idempotency, defensive chat citation hydration with the chat slice's first tests, deleted dead cost/routing mirrors, `retryProcessingJob` removed, and Node/Deno source-parity locks.

See `docs/reports/pre-2e/PRE_2E_FOUNDATION_HARDENING_REPORT.md` for the full finding-by-finding disposition, including what was rejected and deferred, and ADR-035 through ADR-038.

## Phase history

> **This section and `## Status summary` below are append-only point-in-time records, superseded by `## Current truth` at the top of this file.** Their paragraphs describe the state at the moment each was written and are deliberately not rewritten — they are the evidence of how the project got here. Where one of them describes a phase as in flight, read `## Current truth` for what is actually true now. Slice 2F.6's convergence audit added this preamble because the first line below opened with "Current phase" and contradicted the top of the same document; the sentence is re-labelled rather than deleted.

Phase 2E checkpoint (superseded — see `## Current truth`): **Phase 2E — Natural-Language Task Updates, Slices 2E.1 and 2E.2 accepted; Slice 2E.3 implemented, corrected and CI-green (run `30203421883`, all three jobs; pgTAP `Files=27, Tests=927, Result: PASS`).** Base `2e2acfd`; slice base `52f8db6`; branch HEAD `4f9aff8` (`97c633a`, `129fb46`, `c50960b`, `4f9aff8`); draft PR #18 (CI evidence only, not for merge). Migrations `202607250055`, `202607250056` (amended in place twice by Slice 2E.3) and `202607250057` are local-only; remote migration parity is unchanged at `202607250054`, and the deployed workers are unchanged at `process-jobs` v15 and `heartbeat` v4 — Slices 2E.1–2E.3 touch no worker code. Slices 2E.4 (reversible non-destructive updates) through 2E.8 (convergence and closeout) have not started, and each proceeds under the approved PRD without further authorization. See `docs/reports/phase-2e/PHASE_2E_SLICE_01_REPORT.md`, `docs/reports/phase-2e/PHASE_2E_SLICE_02_REPORT.md`, `docs/reports/phase-2e/PHASE_2E_SLICE_03_REPORT.md`, `docs/reports/phase-2e/PHASE_2E_PROGRESS.md`, and ADR-039 through ADR-043.

Previously: Phase 2D — Conversational Pending Questions is **complete through Slice 2D.6**. Slice 2D.6 (Convergence and Closeout, Epic 2D-F) is implemented on branch `codex/phase-2d-slice-6` (base `62883af`, Slice 2D.5 merged via PR #15). It adds **no migration, no RPC, and no product/UI source change**: it delivers the Phase 2D convergence audit, the closeout tooling, the phase reports, and the permanent-documentation reconciliation. The convergence audit confirmed every actionable-question reader already shares the single `actionablePendingQuestionFilter` predicate (`/questions`, the `conversational-questions` Chat/queue panel, the surfacing budget, and the Home/Inbox/review projections), that `list_needs_attention` mirrors that predicate in SQL, and that `resolvePendingQuestion`/`undoQuestionResolution` are the single resolution/undo path — so the questions page, Chat, "Precisa de você"/Needs Attention, and entry review converge with no product fix required (`question-preview-projection` reads specific questions by id, a legitimately different query). Closeout tooling: `scripts/generate-phase-2d-traceability.mjs` → the 69-row `docs/reports/phase-2d/PHASE_2D_TRACEABILITY_MATRIX.md` (fails closed on PRD-inventory drift; no requirement is left non-green — the deterministic suggested-answer path satisfies its requirement and the `2C-UNDO-004` hard gate is resolved); `scripts/verify-phase-2d-cleanup.mjs` (fail-closed across Auth users and 14 owner-scoped tables); the deterministic fail-fast `test:remote:2d` aggregate (question-resolution, suggested-answer/preview, reinterpretation, product events, cleanup); and the `PHASE_2D_SLICE_06_REPORT.md`/`PHASE_2D_REPORT.md` closeout reports. `SECURITY.md` was reconciled to record the `undo_operation` `40001`→`55P03` fix (migration `202607230050`) and the Phase 2C/2D resolution security controls. The legacy `answerPendingQuestion` wrapper and `resolve_pending_question_v1`/`v2`/`v3` RPCs are preserved intact; their retirement stays deferred per PRD §21.7. Local/remote parity is preserved through `202607230051`; generated types are unchanged. See ADR-034 and `docs/reports/phase-2d/PHASE_2D_SLICE_06_REPORT.md`. The split/merge sub-epic (`2C-STRUCTURE-004`, GitHub issue #8) remains deferred and non-blocking; Phase 2E (natural-language task updates) is the next official scope and awaits separate authorization.

Previously (Slice 2D.5): Phase 2D — Conversational Pending Questions, Slice 2D.5 implemented. Slice 2D.5 (Conversational Surfacing — Chat + Queue — and Cooldown) is implemented, gate-verified, linked-verified, and independently reviewed on branch `codex/phase-2d-slice-5` (base `218e56e`, Slice 2D.4 merged via PR #14). It ships **no migration**: it is a UX/surfacing slice over already-shipped contracts, reusing the resolution/reinterpretation/undo/analytics contracts of 2D.1–2D.4 byte-for-byte, so generated types are unchanged and local/remote parity is preserved through `202607230051`. The deterministic core is `src/features/agent/question-surfacing.ts` — a pure, LLM-free `decideQuestionSurfacing` mirroring the heartbeat discipline (`run_user_heartbeat`): quiet hours in the user's own timezone (reusing `isWithinQuietHours` verbatim), a per-local-day `max_followups_per_day` cap, a rolling 24h cooldown, and an `important_reminder_override` bypass that only an *important* item may use, with gates in a stable order (quiet hours → cap → cooldown). Its `server-only` loader `question-surfacing-data.ts` derives inputs from owner-scoped data and **reuses the heartbeat's existing `notifications` ledger read-only** as the shared nudge budget (delivered-today count + last-nudge cooldown anchor) — no new cron, channel, or persisted surfacing state, so surfacing stays pull-based per ADR-033 decision 5 — and fails **closed** (no nudge) on any read error. `src/features/agent/conversational-questions.tsx` (`server-only`) is a reusable `<section>` (role `region`) panel rendering open actionable questions as interactive, untrusted-data elements resolvable through the **unchanged** `resolvePendingQuestion`/`undoQuestionResolution` contract and the existing `QuestionAnswerForm` (answer/defer/dismiss/not-relevant, 2D.3 suggestions/previews, 2D.4 confirm-to-reinterpret) — no resolution logic is duplicated, and conversation state derives entirely from `pending_questions` via the shared `actionablePendingQuestionFilter` that `list_needs_attention` and `/questions` also use, so the surfaces converge. It has two modes: **proactive** (Chat — shown with attention only when the surfacing module allows, otherwise collapsed to one quiet, reachable link to `/questions` so nothing is ever permanently hidden) and **pull** (the "Precisa de você" queue — always shown; the decision only sets header emphasis). Every projected string is React-escaped owner content and is never injected into the chat prompt; the panel is fully failure-isolated (a read error degrades to "no panel", never crashing its host page). It is mounted above the chat form (`chat/page.tsx`, proactive) and above the needs-attention list on `?view=needs-you` (`inbox/page.tsx`, pull). Analytics is content-free and migration-free: `ConversationalQuestionsViewed` reuses the allowlisted `needs_attention_viewed` event with the allowlisted `questions` surface and a bounded item count only, while resolution outcomes keep flowing through the shared Server Action's existing events. Verified: ESLint/TypeScript clean, Vitest 902/902 across 93 files, production build green, linked DB lint (`--level error`) clean, and authenticated Playwright desktop + Pixel 7 green — a new journey resolving the same question from Chat and the queue through the identical contract with cross-surface convergence, plus the full 2D.1–2D.4 question regression set. Independent review found and fixed two Important issues on this branch (an `<aside>`→`<section>` landmark-semantics fix so the panel is a named `region`, and a failure-isolation fix so a pending-question read error degrades to "no panel" instead of crashing Chat/Inbox). The branch is not pushed, has no PR, and no application code is deployed. Slice 2D.6 (convergence/closeout) awaits separate authorization; the split/merge sub-epic (`2C-STRUCTURE-004`, GitHub issue #8) remains deferred and non-blocking.

Previously (Slice 2D.4): Phase 2D — Conversational Pending Questions, Slice 2D.4 implemented. Slice 2D.4 (Confirmed Consequence / Reinterpretation) is implemented and gate-verified on branch `codex/phase-2d-slice-4` (base `d02477d`, Slice 2D.3 merged via PR #13). It introduces the first real consequence after answering: additive migration `202607230050` adds `resolve_pending_question_v3` — the third version of the single long-lived resolution RPC family — whose closed discriminated `p_resolution` keeps every 2D.1/2D.2 kind byte-compatible and adds exactly one approved extension, an **optional closed-enum `consequence` (`none`, `reinterpret`) on the `answer` kind**. A consequence on any non-answer kind, or an unknown value, rejects with `22023` before any mutation; an absent consequence canonicalizes to `none` and hashes identically, so replay stays deterministic. `reinterpret` reuses the deployed owner-scoped path (`enqueue_entry_reprocessing` → one `interpret_entry` reprocess job → the deployed `process-jobs` worker appends a new immutable interpretation revision) — no new engine, queue, worker, scheduler, secret, or Edge Function; the reprocess key is derived from the resolution's own canonical fingerprint so the consequence is idempotent per operation key and never double-enqueues on replay or concurrency. The consequence is **never** applied implicitly: only an explicit `"consequence": "reinterpret"` triggers it, and answering vs. applying remain separate decisions. Audit distinguishes three independently replay-safe events (`resolve_pending_question_v3`, `question_consequence_confirmed`, `entry_reprocessing_enqueued`) with no duplication. The `undo_operation` v3 branch restores the question to exactly `open` (guarded by the evidence's status so it can never clobber a newer resolution) and compensates the queued reprocess job — an un-claimed job is cancelled (`reprocessing_cancelled`), a claimed/running one is left intact (`reprocessing_in_progress`); undo restores pointers, never history, never deleting or resurrecting an interpretation revision, and is idempotent. Migration `202607230050` also **resolves the `2C-UNDO-004` hard gate**: `undo_operation`'s own "Cannot undo after a newer interpretation revision" conflict, which raised the gateway-hanging SQLSTATE `40001`, is forward-fixed to `55P03` (mirroring ADR-026), asserted by a fail-closed `DO` block. Forward-only migration `202607230051` hardens `resolve_pending_question_v3` against a lock-ordering deadlock with a concurrent manual retry by taking `enqueue_entry_reprocessing`'s advisory lock before the entry row lock for the reinterpret consequence. The content-free `question_reinterpret_applied` product-event (boolean-by-existence, no properties) is added to the allowlist. The application cuts the questions-page consumer over to `resolve_pending_question_v3` via `resolvePendingQuestion` (with `answerPendingQuestion` retained as the answer-kind wrapper); the answer form gains an **Answer and re-interpret** control that appears only when the read-only 2D.3 effect preview says reinterpretation is possible (interpretation current), opens a pure disclosure stating "nothing has been applied yet", and applies the consequence only on an explicit **Confirm and re-interpret** (or lets the user **Skip consequence**) — with focus, live region, ≥44 px targets, no horizontal overflow at Pixel 7, and PT-BR/English copy. Migrations are applied to the linked development project at local/remote parity through `202607230051`; `resolve_pending_question_v1`/`_v2` stay byte-identical and callable for rollback, and the `resolve-v1:`/`resolve-v2:`/`resolve-v3:` namespaces never collide; generated types are content-identical. The branch is not pushed, has no PR, and no application code is deployed. Slices 2D.5–2D.6 (chat/queue surfacing + cooldown, convergence/closeout) each await separate authorization; the split/merge sub-epic (`2C-STRUCTURE-004`, GitHub issue #8) remains deferred and non-blocking.

Previously (Slice 2D.3): Phase 2D — Conversational Pending Questions, Slice 2D.3 implemented. Slice 2D.3 (Suggested Answers and Source/Effect Preview) is implemented and gate-verified on branch `codex/phase-2d-slice-3` (base `400ce03`, Slice 2D.2 merged via PR #12). It is a read-only slice: no resolution RPC version was added, `resolve_pending_question_v1`/`_v2` are untouched and byte-identical, and the closed `p_resolution` write shape is unchanged (ADR-033 reserves `_v3` for Slice 2D.4's consequence). A new pure `src/features/agent/question-suggestions.ts` derives bounded, closed, deduplicated suggested answers from the question's own leading interrogative plus the entry's own owned domain context — with no AI call, extraction-schema field, worker, provider, or Edge Function change. Because the extraction contract stores only `question`/`reason`/`confidence` (there is no persisted question type), the discovered taxonomy is `yes_no` (polar → the closed `{Sim,Não}`/`{Yes,No}` set), `person`, `project`, `organization`, and `context` (each drawn from the interpretation's own extracted entity names); `quando`/`when`, `por que`/`why`, and `quanto`/`how much` have no truthful deterministic answer in the current schema and correctly yield no chips, as does any supported kind whose owned context is empty — values are never fabricated. Options are bounded (≤6, ≤160 chars), deduplicated, dropped rather than truncated, ordered deterministically, and identified by semantics (`person:ana-prado`, `yes_no:yes`) so ids stay locale-independent while yes/no values localize. A new `server-only` `question-preview-projection.ts` returns a closed source DTO (question, reason, candidate index, bounded entry excerpt, entry dates, interpretation version/date/summary, truthful current/superseded indicator) after verifying question/entry/interpretation ownership and interpretation-to-entry provenance — no raw row, raw interpretation JSON, provider output, embedding, operation key, or audit internal reaches the client, and a cross-owner question is indistinguishable from a missing one. Its deterministic `toQuestionEffectPreview` returns the closed `{kind: "none" | "reinterpret", title, description, notice, willMutate: false}`, describing reinterpretation only as what a later confirmation could apply and always stating that nothing has been applied yet; nothing is selectable, enqueued, or mutated. Selecting a suggestion fills the existing editable answer field, moves focus there, and never submits; the browser may submit only a bounded suggestion id, which the Server Action authenticates by regenerating the deterministic options server-side and requiring the id to have been presented for that question and to carry exactly the submitted answer — a forged, stale, or edited-away id records `typed`, so attribution can never be forged and a UI hint never fails a resolution. The authenticated result is recorded as the bounded `origin: typed|suggested` enum on the persisted-outcome `question_answered_basic` event (the granularity PRD §18 sanctions), and additive migration `202607230049` extends only the product-event allowlist: the new property-free `question_effect_previewed`, the optional bounded `origin` property (optional so the pre-cutover application's `{}` payload keeps recording, making the migration independently deployable and the application commit rollback-safe), and a new truthful `questions` surface. The migration is applied to the linked development project at local/remote parity through `202607230049`; generated types are content-identical and unmodified. The branch is not pushed, has no PR, and no application code is deployed. Phase 2C is complete through Slice 2C.6 (merged via PR #10); the Phase 2D design is approved on `main`.

Previously: Slice 2D.2 implemented. Phase 2C is complete through Slice 2C.6 (merged via PR #10); the Phase 2D design is approved on `main` (`docs/initiatives/phase-2d/PHASE_2D_PRD.md`, `docs/initiatives/phase-2d/PHASE_2D_IMPLEMENTATION_PLAN.md`, ADR-033). Slice 2D.1 (Traceable Answer Transition) is merged to `main` via PR #11 (migrations `202607230046`/`202607230047`, `resolve_pending_question_v1`). Slice 2D.2 (Question Dispositions) is implemented and gate-verified on branch `codex/phase-2d-slice-2` (base `cb33bd6`): additive migration `202607230048` introduces `resolve_pending_question_v2` — the second version of the single long-lived resolution RPC family — adding the approved `deferred`/`dismissed`/`not_relevant` dispositions to the closed discriminated command (a bounded, strictly-future, explicit-offset deferral instant canonicalized to UTC; `not_relevant` reuses the `dismissed` status with a distinct evidence kind per ADR-033), plus a guarded `resolve_pending_question_v2` branch of `undo_operation` that can never clobber a newer resolution, the widened deterministic snooze-reactivation predicate in `list_needs_attention`, and the content-free `question_resolved` product-event allowlist (bounded `kind` only). The migration is applied to the linked development project at local/remote parity through `202607230048`; `resolve_pending_question_v1` stays byte-identical and callable for rollback, and the `resolve-v1:`/`resolve-v2:` operation-key namespaces never collide. The questions-page consumer cuts over to `resolve_pending_question_v2` via `resolvePendingQuestion` (with `answerPendingQuestion` retained as the answer-kind wrapper): the page now offers Answer/Defer/Dismiss/Not Relevant, the Defer flow using a profile-timezone `datetime-local` picker (reusing the Phase 2C wall-time conversion), each with distinct states, field-associated errors, live regions, predictable focus, and ≥44px controls in PT-BR/English. A shared `actionablePendingQuestionFilter` treats a snoozed question past its deadline as open, applied across the questions page and the Home/Inbox/entry-review projections so read-time reactivation converges every surface with `list_needs_attention`. The branch is not pushed, has no PR, and no application code is deployed. Slices 2D.4–2D.6 (permitted reinterpretation consequence, chat/queue surfacing + cooldown, convergence/closeout) each await separate authorization; the `undo_operation` `40001` residual (`2C-UNDO-004`) remains a hard gate for Slice 2D.4, and the split/merge sub-epic (`2C-STRUCTURE-004`, GitHub issue #8) remains deferred and non-blocking
Source of truth order: current code; linked remote database and migrations; `STATE.md`; `TODO.md`; `DECISIONS.md`; `CHANGELOG.md`; `SPRINT_1_5_REPORT.md`; implementation plans; remaining documentation

## Status summary

Phase 1 is implemented as a hardened pre-MVP foundation. Sprint 1.5 remains closed. Phase 2A operational reliability remains deployed. Phase 2B is implemented and deployed through migration `023`: captures use the persisted lifecycle, interpretations are immutable snapshots selected by an owned current pointer, corrections and undo append versions, trust/entity evidence is deterministic and persisted per element, and synchronous reprocessing is protected by an expiring database lease. Desktop/mobile and remote behavior are verified. Phase 2X — Product Convergence has an approved architecture review, PRD, and implementation plan. Slice 2X.1 supplies pure daily-cycle contracts and guardrails. Slice 2X.2 is complete and deployed through migration `024`: a private, allowlisted, idempotent product-events ledger now has distinct user/server RPCs, generated schema types, a server-only best-effort boundary, a thin acknowledgement action, pgTAP coverage, and a disposable remote smoke. The historical product-projection commit `9f0c1e6` is preserved and reclassified as reusable prework. Slice 2X.3 is deployed through migration `025`: authenticated capture/reprocessing enqueue RPCs atomically persist only an entry plus a bounded `interpret_entry` job, while service-role claims use the existing leased queue. Slice 2X.4 is complete and deployed through migrations `026` and `027`: `interpret_entry` jobs are now actually processed end to end by a deployed worker (`supabase/functions/process-jobs/entry.ts`), reachable both by direct authenticated invocation and by an unattended per-minute `pg_cron`/`pg_net` dispatch drain authenticated by a Vault-backed secret; `process-jobs` was split into `index.ts` (auth/routing), `dispatch.ts` (fail-closed type router and drain loop), `attachment.ts` (unchanged attachment behavior, extracted), and `entry.ts` (new). Migration `027` additionally fixed a Slice 2X.3 regression that had been silently breaking every real file upload since migration `025` shipped (see ADR-022). The official Slice 2X.5 is complete with no new migration: `captureEntry` and `reprocessEntry` now call `capture_entry_async`/`enqueue_entry_reprocessing` directly and return as soon as that atomic persistence settles — no redirect, no synchronous AI call. A non-blocking `after()`-scheduled callback nudges the deployed worker and records best-effort product events; the automatic `pg_cron` drain remains the correctness backstop if the nudge fails. `agent/actions.ts` gained a generalized `retryProcessingJob` for entry jobs (no UI consumer yet — lands with the Needs-Attention slices). **Superseded 2026-07-25:** those slices shipped a different Action (`reprocessEntry`, wired on the entry-review page) and `retryProcessingJob` was removed as consumer-less in the pre-2E hardening pass. The now-unreachable synchronous Node extraction orchestrator (`src/features/interpretations/interpret-entry.ts`) was removed; the Deno worker remains the only production extraction path (see ADR-023). The official Slice 2X.6 is complete with no new migration: `src/features/daily-cycle/inbox-projection.ts` (`loadInboxProjection`) is the first production consumer of the Slice 2X.1 lifecycle mapper — it reads a page of owner-scoped `entries`, each entry's latest `interpret_entry` job, the current interpretation's `task_candidates`, open pending questions, and materialized tasks, then resolves each entry's `ProductState`/`AttentionReason` through `resolveDailyCycleLifecycle`. When that fail-closed mapper refuses an unrecognized internal combination, the loader still surfaces the entry as an explicit `could_not_organize`/`resolve_consistency` item instead of silently dropping it. `/inbox` and a new "Atividade recente" panel on Home both call this same projection and render the same new `InboxItemRow` component, so the two surfaces are guaranteed to agree on an entry's human-readable state; neither page computes lifecycle or reads `entries.status` directly anymore. `recordOnly`/`hasConsistencyIssue` are conservatively passed as `false` until Slice 2X.7 adds persisted candidate provenance — a documented, non-regression limitation, not a defect (see `CHANGELOG.md`). The official Slice 2X.7 is complete and deployed through migration `028`: `entry_interpretations.is_record_only` is now persisted (not just a transient input to the computed lifecycle status), `tasks` gained `source_interpretation_id`/`operation_key`, and a new `confirm_entry_task_candidates` RPC only ever materializes candidates belonging to the entry's actual current interpretation, refuses `record-only` interpretations outright, and is idempotent per operation key — replacing the old entry-wide `(source_entry_id, candidate_index)` uniqueness (which silently blocked a legitimate same-index candidate in a newer interpretation) with an interpretation-scoped one. `src/features/tasks/actions.ts` and `task-candidate-form.tsx` now bind every confirmation to the interpretation the UI actually displays; `src/features/interpretations/data.ts` exposes `isRecordOnly` per revision and a pure, tested `computeUnavailableCandidateIndexes` that conservatively treats provenance-less legacy tasks as unavailable. Exercising the new and existing confirmation RPCs as a real authenticated user against the linked project (not just via pgTAP or `service_role`) — done by actually running the new `scripts/remote-daily-cycle-smoke.mjs`, not only writing it — found and fixed two defects unrelated to this slice's own design: `confirm_entry_tasks` was `SECURITY INVOKER` and had never worked for a real user (missing `UPDATE`/`INSERT` grants on `entry_interpretations`/`undo_operations`/`audit_logs`; both `confirm_*` RPCs are now `SECURITY DEFINER`, matching every other RPC in this schema that writes to those tables), and any RPC raising SQLSTATE `40001` — including the already-shipped Phase 2B `correct_entry_interpretation` — hangs until gateway timeout on the linked project, a platform behavior confirmed via a raw `fetch()` with no application code involved. The new RPC signals its conflict with `55P03` instead; `correct_entry_interpretation`'s own equivalent `40001` path was deliberately left unchanged (out of scope for this slice's file list) and was tracked as an urgent, separate follow-up (see ADR-025, `TODO.md`, `SECURITY.md`). A standalone hotfix outside the slice sequence (migration `202607180029`, 2026-07-18) closed that follow-up: `correct_entry_interpretation` keeps its exact signature and every non-conflict behavior, now raising `55P03` instead of `40001` for its own version conflict, proven by an extended authenticated remote smoke to return in a bounded ~530ms with no gateway hang and no partial write (see ADR-026). The official Slice 2X.8 is complete with no new migration: `src/features/daily-cycle/review-projection.ts` and `src/features/daily-cycle/technical-details-projection.ts` are the two new server-only projection modules the entry-detail page now exclusively loads through — each pairs a pure, independently tested mapper (`toEntryReviewProjection`/`toEntryTechnicalDetailsView`) with a thin loader that reuses `loadInterpretationReview` (now internal infrastructure, no longer imported by any page). `src/app/[locale]/app/inbox/[entryId]/page.tsx` no longer reads `entries.status`, `entry.processing_error`, or any other raw Supabase row; its status badge, error/organizing banners, and retry-button visibility are driven entirely by the Slice 2X.1 `productState`/`availableActions` computed through the same `resolveDailyCycleLifecycle` mapper Caixa/Início already use (Slice 2X.6). A new architectural test (`page.architecture.test.ts`) enforces that boundary by forbidding `database.types`/`Database["public"]`/`@/lib/supabase/server`/`entry.status` in the page file. The two loaders run independently (each performs its own `loadInterpretationReview` call) specifically so a technical-details failure — malformed trust JSON, an unexpected query error — can never block or misreport the main review as ready; the page renders without the trust panel/comparisons section in that case, nothing else changes. `InterpretationRevisionEditor` and `TaskCandidateForm` are unchanged this slice and keep receiving the same prop shapes as before, now sourced from the review projection's non-frozen `editableCurrent`/`taskCandidates` fields rather than a page-level Supabase call. A standalone hotfix outside the slice sequence (2026-07-18, no new migration) resolved architecture-review finding F1: `hasMaterializedTaskForCandidates` was computed entry-wide ("any non-cancelled task exists for this entry") in both `inbox-projection.ts` and `review-projection.ts` instead of interpretation/candidate-scoped, so confirming only some of a multi-candidate interpretation's suggestions could make `productState` read `ready` while `TaskCandidateForm` still showed an unconfirmed candidate. Both loaders now derive that input from the same interpretation-scoped source `actionableCandidates` already used correctly (`computeUnavailableCandidateIndexes`), via a new shared pure helper `hasUnconfirmedTaskCandidates` (`src/features/interpretations/data.ts`); `lifecycle.ts` itself needed no change. See `docs/reports/phase-2x/PHASE_2X_CANDIDATE_LIFECYCLE_HOTFIX_REPORT.md`. The official Slice 2X.9 is complete with no new migration: the entry-detail page is now the decision-first, progressive-disclosure layout the PRD specifies, built entirely on the two Slice 2X.8 projections. `src/features/daily-cycle/entry-review.tsx` (`EntryReview`, `ReviewUnderstanding`, `ReviewAttention`, `ReviewNextActions`, `OriginalRecord`) renders, in order, what happened (`view.understanding` plus the `humanFields` DTO entries, rendered for the first time), what needs the user's decision (`view.attentionItems`, with the retry button and the specific error/pending-question text injected by the page as slot content rather than read by the component), the next action (candidate confirmation or the correction editor, now gated exclusively by `view.availableActions` instead of raw array lengths), and the always-available original record. `src/features/daily-cycle/technical-details.tsx` (`TechnicalDetails`) collapses the former two-column grid — trust/scores/policy/evidence per element, the immutable version history with field-by-field comparisons, and the structured extraction (concepts, dates, entity links, mentions) — behind a single native `<details>`, open on user request, unaffected by and independent from a technical-details load failure (still rendered inert with a fallback message, matching the Slice 2X.8 guarantee). `InterpretationRevisionEditor` gained an optional `showSummary` prop (default `true`) so the collapsed correction-editor state does not repeat the summary already shown in the understanding block; `TaskCandidateForm` needed no behavior change. No lifecycle, candidate-confirmation, or RPC logic changed — this slice is presentation/composition only over already-existing DTOs. The official Slice 2X.10 is complete, deployed through migrations `030` and `031` (the second applied in the same working session, before either was committed — see ADR-027): new RPC `list_needs_attention` (`SECURITY DEFINER`, owner-scoped via `auth.uid()`, keyset-paginated by `(occurred_at, entry_id)`) reimplements the exact five-reason precedence already codified in `src/features/daily-cycle/lifecycle.ts` directly in SQL — the only way to keep the "Precisa de você" queue paginable without an unbounded per-user scan (XG-025), since the canonical TypeScript mapper cannot itself run inside Postgres and Inbox's fixed-page approach does not generalize to "every entry that currently qualifies." `src/features/daily-cycle/attention-projection.ts` (`loadAttentionProjection`) is the TypeScript consumer: it never recomputes lifecycle, only hydrates the RPC's already-decided rows (minimal additional owner-scoped queries bounded to the page actually returned, not a second full scan) into `NeedsAttentionItemView` through the existing Slice 2X.1 mapper `toNeedsAttentionItemView`, and reuses `review-projection.ts`'s `attentionActionId` (now exported) so a queue item's primary action always agrees with the entry-review page's own action for the same reason. Actually exercising this against the linked project with `scripts/remote-daily-cycle-smoke.mjs` (not just pgTAP or mocked Vitest) found a real defect in migration `030`'s first body: `has_unconfirmed_candidate` named its `generate_series` output `candidate_index` — identical to `tasks.candidate_index` — so the unqualified reference inside the correlated `tasks` subquery silently resolved against `tasks` itself instead of the outer loop value, making the comparison always-true; confirming one of two current candidates incorrectly removed the entry from the queue entirely instead of leaving it listed until the second candidate resolved. Migration `031` (`create or replace function`, identical signature/grants/index) fixes this with an unambiguous two-part alias (`candidate_slot(idx)`); `supabase/tests/needs_attention_projection.sql` gained a dedicated regression case (confirm one of two candidates, assert still listed; confirm the second, assert resolved) exercised through the real `confirm_entry_task_candidates` RPC, not injected booleans. The official Slice 2X.11 is complete with no new migration: `src/features/daily-cycle/needs-attention-item.tsx` (`NeedsAttentionItemRow`) is a shared presentational row consumed by both surfaces this slice wires — Home's new "Precisa de você" panel (`loadAttentionProjection` with a bounded limit of 3, an honest `{count}{+ if hasNext}` badge that never claims an unverified exact total, an empty state, and a "Ver tudo"/"View all" link) and Caixa's new `?view=needs-you` canonical filter, reached through a new two-tab `InboxViewTabs` nav alongside the unchanged default (`all`) offset-paginated view. The Caixa queue's "load more" is the first client-driven, Server-Action-backed pagination in this codebase: `src/features/daily-cycle/needs-attention-list.tsx` (client component) holds the accumulated `NeedsAttentionItemView[]` and cursor in local state and calls a new thin action, `loadMoreNeedsAttention` (`src/features/daily-cycle/attention-actions.ts`), which wraps `loadAttentionProjection` in an authenticated, discriminated `{ ok, ... }` result so a Supabase error or expired session never rejects uncaught in the browser; a failed subsequent page preserves already-loaded items and shows a localized, non-auto-retrying error, and the button disables itself while a request is in flight to prevent duplicate calls. The `?view=needs-you` URL itself carries no cursor and stays stable/bookmarkable — only the first page is server-rendered, matching XG-027 (a refresh reloads page one of a live queue by design). `needs_attention_viewed`/`needs_attention_item_opened` (already-defined Slice 2X.2 events) are deliberately not emitted by this slice — no client-side product-event emitter exists anywhere yet in this codebase, and the implementation plan's own Slice 2X.15 file list already claims "attention" component emitters as its scope; building that pattern now for a single call site would be a premature abstraction. See `docs/reports/phase-2x/PHASE_2X_SLICE_11_REPORT.md`.

The official Slice 2X.12 is complete with no migration: `/{locale}/app/work` is now the canonical Work surface with `today`/`all`/`waiting` product views, while localized `/today`, `/tasks`, and `/waiting` routes redirect safely with equivalent view and page. The server-only, explicitly owner-scoped `loadWorkProjection` is the canonical page's only task-table reader, uses the authenticated profile timezone (safe fallback on invalid/missing values), applies deterministic ordering and page-based lookahead, and reuses `toWorkItemView` so invalid rows fail closed. `WorkView` and the adapted `TaskList` consume product DTOs only, localize human state/origin/date, preserve manual creation and complete/wait/resume/reopen actions, and preserve `view` in pagination URLs. Relevant task Actions revalidate canonical Work in both locales. See `docs/reports/phase-2x/PHASE_2X_SLICE_12_REPORT.md`.

The official Slice 2X.13 is complete with no migration or backend change: `src/features/shell/capabilities.ts` is the pure route/product navigation contract for every authenticated destination. Início/Home, Caixa/Inbox, Trabalho/Work, and Brain are the shared desktop/mobile primary set; capture is a distinct global action; notifications remain global; Contexto, Reflexão, Organização, Transparência, and Preferências share one order between the desktop tree and mobile Mais; Jobs remains directly reachable only for explicit technical support and never appears in common navigation. Active state maps nested Inbox/review and Brain routes plus canonical Work and `/today`/`tasks`/`waiting` aliases to exactly one primary destination, independently of supported query views. Locale switching preserves pathname and query, mobile DOM order matches visual/tab order, Mais closes on Escape with restored focus, visible targets remain at least 44 px, and the shell no longer displays the unobservable static claims "Brain atento"/"Brain ativo". See ADR-028 and `docs/reports/phase-2x/PHASE_2X_SLICE_13_REPORT.md`.

The official Slice 2X.14 is complete with no migration or infrastructure change: the static capability registry now records operational, informational, advanced, and future product promises with a visible surface and consumer evidence. Home derives an honest all-saved/organizing/needs-attention state from the existing Inbox and Needs Attention projections; Settings exposes only proven controls, places model routing/cost transparency under accessible progressive disclosure, and preserves hidden legacy values through owner-scoped server reads before calling the existing full-payload RPC; Reviews loads owner-scoped summaries through a server-only localized product DTO and describes generation as manual/on demand. Capture, enqueue, organizing, retry, and completion copy are distinct in PT-BR and English. See `docs/initiatives/phase-2x/PHASE_2X_REPORT.md` and `docs/reports/phase-2x/PHASE_2X_SLICE_14_REPORT.md`.

The official Slice 2X.15 is complete with no migration, RPC or remote infrastructure change in that slice: all 17 version-1 daily-funnel events are connected to one meaningful trigger over the existing private ledger. Domain outcomes emit only after successful Actions; worker completion/failure/retry requires the corresponding persisted job outcome; browser intent/view/open events use a closed authenticated boundary with confirmed visibility and session deduplication. Deterministic UUID keys make operation replay idempotent, meaningful new interactions remain countable, payloads stay content-free, and analytics failure cannot change product state or UI success. The authenticated remote smoke proved allowlist/privacy, ownership/RLS, service-role restrictions, idempotency/repeat behavior, bounded internal queries and cleanup; desktop/mobile online journeys passed. Local worker source changed but was not deployed at that checkpoint; Slice 2X.18 later deployed the complete accumulated function as v13. See ADR-029 and `docs/reports/phase-2x/PHASE_2X_SLICE_15_REPORT.md`.

The official Slice 2X.16 is complete with no migration: an audit of the four daily-cycle product surfaces against `PROJ-001`–`PROJ-020` found two residual violations. `src/features/shell/home-dashboard.tsx` still queried `tasks`/`pending_questions` directly for three panels, and its "priority" panel reimplemented a second, looser definition of "due" (any open task, top 5 by `due_at`) instead of reusing the "today/overdue" rule `work-projection.ts` already exposes for Work — a duplicated rule `FLOW-003`/`PROJ-016` forbid. The priority panel now calls `loadWorkProjection(..., { view: "today" })` directly (same profile-timezone/fallback/ordering rule Work uses), sliced to five items and linking to `/{locale}/app/work?view=today`; this also removes a raw-enum fallback (`task.status.replaceAll(...)`), since every `today` item carries a `due_at`. The two remaining panels (waiting count, newest open question) had no existing rule to reuse, so they moved into a new minimal `server-only` module, `src/features/daily-cycle/home-projection.ts` (`loadHomeSupplementalProjection`), rather than a heavier abstraction with a single consumer. Separately, `TaskCandidateForm` rendered the raw AI-extraction candidate list — including a numeric `confidence` score in the primary flow (`PROJ-005`) — and re-filtered `unavailableIndexes` on the client, duplicating a validity rule `review-projection.ts` already computes once into `actionableCandidates` (`COH-010`/`PROJ-017`). The form now takes `ActionableCandidateView[]` only (no score) and uses each candidate's own `key` as the submitted index, so the raw `taskCandidates`/`unavailableCandidateIndexes` fields left `EntryReviewProjection`'s public output entirely. `src/features/interpretations/data.ts` gained `import "server-only"` (previously unreachable from a Client Component only by convention, not enforced). A new `src/features/daily-cycle/architecture.test.ts` enforces the boundary across Home, Caixa, Work, the entry review, and the candidate form by the same source-text pattern the two existing page-scoped architecture tests already use. No product-event contract changed. See `docs/reports/phase-2x/PHASE_2X_SLICE_16_REPORT.md`.

The official Slice 2X.17 is complete with no migration: `e2e/intelligent-capture.spec.ts` is reorganized from one 379-line serial test into deterministic, independently-attributable scenarios by contract inside two `test.describe` blocks — the existing real capture→correction→confirmation→chat→reviews→files→costs→settings→heartbeat→undo→product-events journey (unchanged behavior, one disposable user, 13 named tests), plus a new, previously entirely absent describe covering basic pending question, recoverable retry, and terminal retry through deterministic direct-RPC fixtures (`begin_entry_interpretation`/`fail_entry_interpretation`/`persist_entry_interpretation`/`begin_entry_reprocessing`/`persist_reprocessed_entry_interpretation`, all already-granted `authenticated` RPCs from Slices 2X.3/2X.4/2X.7) rather than unreliable real-AI-failure induction. Keyboard/focus/live-region/touch-target assertions were added for the entry-review page's progressive disclosure. `e2e/foundation.spec.ts`, `e2e/online-mobile-navigation.spec.ts`, and `scripts/online-playwright.mjs` were reviewed and confirmed — by running them, not by inspection — to already satisfy the plan's Home/Caixa/Trabalho/Brain/Mais/legacy-redirect and desktop+Pixel-7×pt-BR/en-matrix requirements; none needed a change. Real execution (not just authoring) of the new retry scenarios found and fixed one genuine in-scope defect: `src/app/[locale]/app/inbox/[entryId]/page.tsx` rendered two identical "Reinterpretar entrada"/"Reinterpret entry" retry buttons whenever an entry had never had a successful interpretation and its latest state was `recoverable_error`/`terminal_error` (the attention-notice slot and the "no interpretation yet" fallback block both offered it); the fallback's own button is now conditioned on `!canRetry`, removing the duplicate with no capability lost. `e2e/online-auth.spec.ts` was not modified in that slice; its historical provider result remains recorded in the Slice 2X.17 report.

The official Slice 2X.18 closes Phase 2X with no migration or product-source change. The authorized complete local `process-jobs` function was deployed from v12 to v13 after preserving the v12 rollback bundle and confirming the only accumulated runtime delta was `entry.ts` plus new `product-events.ts`; a fresh v13 download matches every local runtime/dependency file and excludes the local-only Deno test. The full fail-fast `test:remote:2x` aggregate, local 80-file/443-test suite, lint, typecheck, production build, desktop/mobile core browser matrix, migration/type synchronization, linked DB lint, worker outcome/idempotency checks, and fail-closed disposable cleanup all passed. Provider-delivered signup remains an explicit external skip: current redacted Auth logs return `email_address_invalid` for the reserved test domain, while sign-in/profile and the independently generated recovery-link/password-reset core passed on desktop/mobile. Deno and Docker remain unavailable and are not claimed. See ADR-030, `docs/initiatives/phase-2x/PHASE_2X_REPORT.md`, `docs/reports/phase-2x/PHASE_2X_SLICE_18_REPORT.md`, the 283-row `docs/reports/phase-2x/PHASE_2X_TRACEABILITY_MATRIX.md`, and the sanitized `docs/reports/phase-2x/PHASE_2X_SLICE_18_EVIDENCE.md`.

Phase 2C — Editable Candidate Tasks and Transactional Materialization has an approved canonical PRD and implementation plan (`docs/initiatives/phase-2c/PHASE_2C_PRD.md`, `docs/initiatives/phase-2c/PHASE_2C_IMPLEMENTATION_PLAN.md`, ADR-031). Slice 2C.1 — Editable Core Confirmation is now implemented on branch `codex/phase-2c-editable-candidate-tasks` (7 commits atop `main`@`7d550ca`, not pushed): migrations `202607190032`/`202607190033` add `confirm_entry_task_candidates_v2` (`SECURITY DEFINER`, `set search_path = ''`, `auth.uid()`-only identity, closed/bounded JSON edit allowlist for `title`/`description`/`dueAt`, duplicate/out-of-range/unselected-edit rejection, canonical request-fingerprint idempotency stored on `undo_operations`, atomic multi-candidate materialization with a `2C_ALREADY_MATERIALIZED` guard) and a correction-vs-confirmation race guard trigger on `entry_interpretations`; the pre-existing `confirm_entry_task_candidates(uuid, uuid, integer[], text)` RPC is unchanged and still callable. `src/features/tasks/candidate-edit-contract.ts` and `candidate-due-date.ts` provide the client-side Zod validation and local-wall-time↔offset-instant conversion (explicit nonexistent/ambiguous-DST rejection via a bounded ±24h-minute scan) that `candidate-editor.tsx` (per-candidate inline edit/reset/clear UI) and the rewritten `task-candidate-form.tsx` (edit-map keyed by candidate index, retained across deselect/reselect, payload-signature-driven operation-key rotation) consume; `confirmEntryTasks` (`src/features/tasks/actions.ts`) re-validates everything server-side, never forwards client-supplied ownership/task IDs, and maps every RPC error to a stable localized code with no raw SQL/PostgREST text reaching the UI. `review-projection.ts`/`page.tsx` now thread the authenticated profile timezone (safe-validated, defaulting to `America/Sao_Paulo`) into the editor. Verified: 579/579 unit/component tests, 0 lint/typecheck errors, production build green, migrations 032/033 at local/remote parity, `db lint --level error` clean, and a live authenticated Playwright run (desktop + Pixel 7, real Server Action, real linked database, disposable fixture torn down) editing/confirming/auditing/undoing candidates. Known gap: the PRD §14/implementation-plan Task 5 analytics extension (`candidate_edit_started`, `candidate_edit_reset` events, and `editedCandidateCount`/`editedFieldCount` on `task_candidates_confirmed`) was not implemented in this slice — see `docs/reports/phase-2c/PHASE_2C_SLICE_01_FINAL_ACCEPTANCE.md`. Not yet pushed, no PR opened, not deployed.

## Implemented functionality

- Localized public and authenticated Next.js application shell (`pt-BR` and `en`).
- Supabase email/password authentication, session refresh, protected application routes, sign-out, explicit PKCE callback continuations, validated signup, and complete password reset UI/actions.
- User profile and agent preference management.
- Intelligent capture with AI interpretation, confirmations, pending questions, and task materialization. Capture and reprocessing return immediately after durable persistence (Slice 2X.5); interpretation happens in the deployed worker, not in the request.
- Inbox, canonical Work (Today/All/Waiting views, with legacy Today/Tasks/Waiting redirects), Projects, People, Reminders, Reviews, Agent chat, Memories, Files, Notifications, Change history, Settings, and entity detail routes.
- OpenAI-backed extraction, chat, summarization, embeddings, and background job processing.
- Semantic memory infrastructure with `pgvector`. HNSW cosine indexes exist on `entry_embeddings.embedding` and `memories.embedding`, but `match_internal_knowledge` cannot use them: it computes `1 - (embedding <=> query) as similarity` inside a `UNION ALL` and orders by that alias, which forces an exact per-user scan. Correct and fast enough at the current data volume; the explicit keep-or-restructure decision is an open item in `TODO.md` (architecture-review M23).
- Undo/audit infrastructure and entity timelines.
- Scheduled heartbeat generation with user-local dates/locale, quiet hours, rolling cooldown, lossless daily caps, per-user locks, and failure isolation.
- Paginated list routes, batched signed URLs, shared Supabase result contracts, and an authenticated application error boundary.
- AI route selection, pricing catalog, append-only usage ledger, database-side complete cost aggregation, settings, and cost dashboard.
- Remote `process-jobs` version 9 with authenticated ownership, 300-second leases, 120-second external timeout, idempotent reuse of persisted interpretations, lease-validated completion/failure, bounded backoff/exhaustion, and real file-analysis smoke coverage.
- Scheduled per-minute abandoned-job reaper, service-only queue metrics, user-safe recoverable/terminal failure visibility, and a backoff-gated retry Server Action.
- Linked Supabase-generated TypeScript schema with the Phase 2A `jobs` contract used by the Files route.
- Eight-state entry lifecycle with immutable interpretation origin/parent/correction metadata and `entries.current_interpretation_id` as the authoritative current pointer.
- Transactional correction with optimistic concurrency, operation-key idempotency, owned complete-link validation, audit, and compensating append-only undo.
- Deterministic per-element trust weights/policies/evidence plus bounded owned entity ranking across normalized names, aliases, history, organization context, temporal validity, optional semantic input, and candidate margin.
- Typed interpretation review DAL and localized accessible inbox UI for correction, dates, concepts, entity links, classifications, pending-question retention, record-only mode, trust evidence, history/comparison, undo, and reprocessing.
- Daily-cycle product contracts for five public states, five attention reasons, product DTOs, stable Action results, PT-BR/English copy, and a deterministic lifecycle projection that fails closed on unknown states.
- Private `product_events` funnel ledger with 17 closed event names, event-specific property allowlists, no personal-content fields, owner RLS, per-user idempotency, synthetic-test marking, and dedicated authenticated/service-role RPCs. The 180-day retention limit is a **declared requirement recorded as a table comment, not an implemented purge** — no `pg_cron` purge job exists for `product_events`, completed jobs, or delivered notifications. Required before the first pilot (`SECURITY.md`, `TODO.md`, architecture-review M6).
- Durable entry-processing job contracts: `capture_entry_async` atomically persists a saved entry and a minimal `interpret_entry` job; `enqueue_entry_reprocessing` creates an idempotent job without changing the current interpretation; service-only claims use leases, retry eligibility, `SKIP LOCKED`, and ownership guards. Payloads contain only `entry_id`, `mode`, and a reprocess operation key when applicable.
- Deployed entry-interpretation worker and automatic dispatch: `interpret_entry` jobs (both `initial` and `reprocess` modes) are processed end to end by `supabase/functions/process-jobs/entry.ts` through a single pipeline that reloads the entry, reuses the same deterministic entity-resolution/trust modules the Node side is built from, calls the OpenAI extraction/embedding APIs, and persists through RPCs extended to be service-role-callable via an explicit, gated `p_service_user_id`. `process-jobs` now routes by job type through a fail-closed dispatcher; unknown types and invalid payloads fail immediately without a claim attempt. Direct authenticated invocation (`{ jobId }`, unchanged contract) and an unattended `pg_cron`/`pg_net` scheduled drain (entry jobs only, Vault-backed secret) are both live and remotely verified. AI usage and a best-effort `capture_processing_completed`/`capture_processing_failed` product event are recorded as effects independent of the job's own completion. As of Slice 2X.5 this worker is the only production path that ever calls the AI provider for entry interpretation — the prior synchronous Node orchestrator was removed.
- The pure product-projection mappers for Inbox, Needs Attention and Work are **all in production**, each behind a `server-only` loader: `inbox-projection.ts` (Home "Atividade recente" and `/inbox`, Slice 2X.6), `attention-projection.ts` (Home "Precisa de você" and `/inbox?view=needs-you`, Slices 2X.10–2X.11), `work-projection.ts` (`/work` and Home's priority panel, Slices 2X.12/2X.16), plus `review-projection.ts`/`technical-details-projection.ts` on the entry-detail page (Slice 2X.8) and `toCaptureReceipt` in `captureEntry` (Slice 2X.5). The earlier "preserved as prework, still without a consumer" description was left behind by Slice 2X.6 and is corrected here. All of them remain immutable, serializable-DTO-only, and free of React/Supabase/database-type/RPC/table dependencies, enforced by `daily-cycle/architecture.test.ts` and the page-scoped architecture tests.
- Official Slice 2X.5 — asynchronous capture cutover: `captureEntry` and `reprocessEntry` call `capture_entry_async`/`enqueue_entry_reprocessing` and return as soon as that persists, with no redirect and no synchronous AI call. `QuickCaptureForm` shows "Salvando…"/"Saving…" while pending, then an inline `CaptureReceiptView` receipt; the field clears and refocuses immediately so consecutive captures do not wait on interpretation, and a rotating client-generated idempotency key protects retries from creating duplicates. A `next/server` `after()` callback nudges the deployed worker (`kickEntryInterpretationWorker`, `src/lib/jobs/entry-worker.ts`) and records best-effort `capture_save_succeeded`/`capture_save_failed`/`capture_processing_enqueued` product events without adding latency to the response; the existing `pg_cron` drain remains the correctness backstop if the nudge itself fails. `agent/actions.ts` gained `retryProcessingJob`, generalizing manual retry to `interpret_entry` jobs (kick when `failed` and eligible; a fresh `enqueue_entry_reprocessing` when `exhausted`) without changing `retryAttachmentJob`. No UI consumed `retryProcessingJob`, and it was removed in the pre-2E hardening pass (2026-07-25); `retryAttachmentJob` remains live on the Files page.
- Official Slice 2X.6 — human processing states in Inbox and Home: `src/features/daily-cycle/inbox-projection.ts` (`loadInboxProjection`) is the first production consumer of the Slice 2X.1 daily-cycle lifecycle mapper. `/inbox` renders `InboxItemView`s through the new `InboxItemRow` component instead of reading `entries.status`/`lifecycleLabels` directly; a new "Atividade recente" panel on Home calls the same projection and renders the same component so both surfaces share one source of truth for an entry's human-readable state. The loader fails closed without dropping data: an entry whose internal state the pure mapper does not recognize still appears, as an explicit `could_not_organize`/`resolve_consistency` item, because the original is always preserved. `.status-badge` styling for the Caixa list now keys off the five product states instead of the eight internal `entries.status` values.
- Official Slice 2X.7 — candidate provenance and safe task confirmation: `entry_interpretations.is_record_only` is now a persisted column, and `tasks.source_interpretation_id`/`operation_key` give every confirmed task explicit, FK-composite-proven provenance. New RPC `confirm_entry_task_candidates` confirms only candidates belonging to the entry's actual current interpretation (checked inside the same transaction that locks the entry row), refuses to confirm anything for a `record-only` interpretation, and is idempotent by operation key; the old entry-wide `(source_entry_id, candidate_index)` uniqueness — which silently blocked a legitimate same-index candidate in a newer interpretation — is now interpretation-scoped, with the pre-existing entry-wide behavior preserved only for provenance-less legacy rows. `confirmEntryTasks` (`src/features/tasks/actions.ts`) and `TaskCandidateForm` bind every confirmation to the interpretation the current page actually displays; `loadInterpretationReview` (`src/features/interpretations/data.ts`) exposes `isRecordOnly` per revision and a pure, tested `computeUnavailableCandidateIndexes` that hides an already-confirmed or unprovable-provenance candidate from the form rather than letting the component compare IDs itself. Both `confirm_entry_tasks` (compatibility RPC, no new consumer) and the new RPC are now `SECURITY DEFINER` — exercising them as a real authenticated user against the linked project (not just pgTAP/`service_role`) found that `confirm_entry_tasks` had never actually worked for a real user before this slice, and that raising SQLSTATE `40001` from any RPC (including the already-shipped `correct_entry_interpretation`) hangs the request until gateway timeout on this platform; see ADR-025 for both findings and the urgent, explicitly-out-of-scope-for-this-slice follow-up they leave for `correct_entry_interpretation` itself.
- Official Slice 2X.8 — separated review and technical-details projections: `src/features/daily-cycle/review-projection.ts` and `src/features/daily-cycle/technical-details-projection.ts` each pair a pure mapper (`toEntryReviewProjection` producing the Slice 2X.1 `InterpretationReviewView`, with no score/policy/evidence field; `toEntryTechnicalDetailsView` producing the complete `InterpretationTechnicalDetailsView`, with per-element scores/policies/signals/evidence/overrides, version comparisons, and per-task candidate provenance) with a thin `server-only` loader. `loadInterpretationReview` (`src/features/interpretations/data.ts`) is now internal infrastructure consumed only by these two modules, never by a page. `src/app/[locale]/app/inbox/[entryId]/page.tsx` loads exclusively through the two new loaders and no longer reads `entries.status`, `entry.processing_error`, or any raw Supabase row; its status badge, error/organizing banners, and retry-button visibility come from `productState`/`availableActions`, computed through the same `resolveDailyCycleLifecycle` mapper Caixa/Início already share (Slice 2X.6). The two loaders each perform an independent `loadInterpretationReview` call so a technical-details failure can never block or misreport the primary review as ready; `InterpretationRevisionEditor`/`TaskCandidateForm` are unchanged and keep receiving the same prop shapes, now sourced from the review projection's non-frozen `editableCurrent`/`taskCandidates` fields. A new `page.architecture.test.ts` enforces the import boundary (no `database.types`, `Database["public"]`, `@/lib/supabase/server`, or `entry.status` in the page file). Two small, deliberate UX consequences of this centralization: `recoverable_error` and `terminal_error` now both offer a retry button (previously only `recoverable_error` did), and the `reprocessing`-only "reinterpretation in progress" banner is now the same shared `organizing` banner Caixa/Início already show, also surfaced for a first-ever interpretation still in flight.
- Official Slice 2X.9 — decision-first progressive-disclosure entry review: `src/features/daily-cycle/entry-review.tsx` and `src/features/daily-cycle/technical-details.tsx` (no migration) replace the former single-flow entry-detail markup with four always-visible blocks (understanding, attention, next actions, original) plus one collapsed technical block, built entirely from the Slice 2X.8 projections. `ReviewUnderstanding` renders `view.understanding` and, for the first time, the DTO's `humanFields`; `ReviewAttention` renders `view.attentionItems` with page-supplied slot content (retry button, error/pending-question detail) so the component itself never branches on internal state; `ReviewNextActions` wraps the existing candidate-confirmation/correction UI, now shown only when `view.availableActions` actually lists the corresponding action id; `TechnicalDetails` consolidates the trust panel, immutable version history/comparisons, and structured extraction (concepts/dates/entity links/mentions) behind one native `<details>`, degrading to a fallback message (not to a blocked page) when the technical-details load fails. `InterpretationRevisionEditor` gained an optional `showSummary` prop (default `true`, `false` here) to avoid repeating the summary already shown in the understanding block; `TaskCandidateForm` needed no change. No lifecycle, candidate, RPC, or migration logic changed.
- Official Slice 2X.10 — Needs Attention query and projection (no UI consumer yet): migration `030` adds `list_needs_attention(p_limit, p_cursor_occurred_at, p_cursor_entry_id)` (`SECURITY DEFINER`, owner-scoped via `auth.uid()`, `set search_path = ''`) and a supporting partial index `jobs_interpret_entry_status_idx`; the RPC reimplements `resolveDailyCycleLifecycle`'s exact five-reason precedence in SQL, restricted to a bounded candidate set (entries whose status alone already implies possible attention, plus `completed` entries with actionable current-interpretation candidates or an open question, plus the narrow `saved`+settled-or-unrecognized-job fallback) so the queue stays paginable without an unbounded per-user scan (XG-025). `src/features/daily-cycle/attention-projection.ts` (`loadAttentionProjection`) consumes the RPC's already-decided reason (never recomputes lifecycle), hydrates only the page actually returned (title from the current interpretation's summary, falling back to the original preview), and maps through the existing Slice 2X.1 `toNeedsAttentionItemView`; its primary action reuses `review-projection.ts`'s `attentionActionId` (now exported) so a queue item's action always matches the entry-review page's own action for the same reason. Running `scripts/remote-daily-cycle-smoke.mjs` (extended this slice) against the linked project — not just pgTAP or mocked Vitest — found and fixed a real defect in migration `030`'s first body: a `generate_series` alias sharing `tasks.candidate_index`'s name made the correlated unconfirmed-candidate check tautological, so confirming one of two current candidates incorrectly emptied the entry out of the queue entirely. Migration `031` (`create or replace function`, identical signature/grants/index) fixes the alias collision; see `DECISIONS.md` ADR-027.
- Official Slice 2X.11 — Needs Attention on Home and Caixa (no migration): `src/features/daily-cycle/needs-attention-item.tsx` (`NeedsAttentionItemRow`) is a shared, pure row consumed by both surfaces. Home's new panel calls `loadAttentionProjection` with a bounded preview limit (3) and shows an honest count (`items.length`, `+` only when `hasNext`, never an unverified exact total) plus a "Ver tudo"/"View all" link to `/{locale}/app/inbox?view=needs-you`. Caixa gained a two-tab filter (`InboxViewTabs`: "Todos"/"All", "Precisa de você"/"Needs you", `aria-current="page"` on the active tab); the `needs-you` branch server-renders the first page (stable, cursor-less URL) and hands "load more" to a new client component, `src/features/daily-cycle/needs-attention-list.tsx` — this codebase's first client-driven, Server-Action-backed pagination. It holds accumulated items/cursor in local state, calls the new `loadMoreNeedsAttention` action (`src/features/daily-cycle/attention-actions.ts`, an authenticated wrapper around `loadAttentionProjection` returning a discriminated `{ ok, ... }` result instead of an uncaught rejection), preserves already-loaded items and shows a localized non-auto-retrying error on a failed subsequent page, and disables its button while a request is in flight to prevent duplicate calls. `needs_attention_viewed`/`needs_attention_item_opened` (already-defined Slice 2X.2 events) are deliberately not emitted here — no client-side product-event emitter exists anywhere yet, and Slice 2X.15's own file list already claims attention-component emitters as its scope.
- Official Slice 2X.12 — canonical Work route and task projection (no migration): `src/features/daily-cycle/work-projection.ts` is the server-only, owner-scoped task reader for `/{locale}/app/work`, resolves the profile timezone safely, filters/orders/page-slices `today`/`all`/`waiting`, and maps through the existing `toWorkItemView` contract with fail-closed invalid-row handling. `WorkView` and the adapted `TaskList` receive product DTOs only, render PT-BR/English human states/origins/dates and the existing mutation actions, preserve manual creation, and retain `view` in page pagination. `/today`, `/tasks`, `/waiting` redirect to their localized equivalent Work view with `page`; task creation/mutation/candidate-confirmation/undo revalidate Work in both locales. Navigation organization remains unchanged for Slice 2X.13.
- Official Slice 2X.16 — projection-boundary closeout (no migration): Home's priority panel now reads `loadWorkProjection(..., { view: "today" })` directly instead of a raw, divergent `tasks` query, so it shares Work's exact due-today/overdue rule and timezone handling; Home's waiting-count and open-question panels read the new `loadHomeSupplementalProjection` instead of raw `tasks`/`pending_questions` queries. `TaskCandidateForm` now renders only `ActionableCandidateView` (no AI-extraction confidence score, no client-side re-filtering of already-server-filtered candidates). `src/features/interpretations/data.ts` is now guarded `server-only`. `src/features/daily-cycle/architecture.test.ts` enforces the no-raw-row/no-raw-enum/no-raw-score boundary across Home, Caixa, Work, the entry review, and the candidate form.
- Official Slice 2X.17 — converged daily journey E2E, no migration: `e2e/intelligent-capture.spec.ts` is reorganized into deterministic per-contract scenarios (two `test.describe` blocks — the existing real journey unchanged, plus a new describe covering basic pending question, recoverable retry, and terminal retry through deterministic direct-RPC fixtures) with added keyboard/focus/live-region/touch-target assertions. `e2e/foundation.spec.ts`, `e2e/online-mobile-navigation.spec.ts`, and `scripts/online-playwright.mjs` were confirmed sufficient and left unchanged. A duplicate retry-button defect on the entry-detail page (`recoverable_error`/`terminal_error` with no prior successful interpretation) was found by real execution and fixed with a one-line conditional; no capability was removed.

## Pending or incomplete functionality

- Google OAuth is hidden until provider configuration and end-to-end validation exist.
- Phase 2X — Product Convergence is complete through Slice 2X.18. Phase 2C is complete through Slice 2C.6: Slices 2C.1–2C.5 are merged to `main` (2C.5 via PR #9), and Slice 2C.6 (closeout — no migration/RPC/product-source change) is implemented and gate-verified on branch `codex/phase-2c-slice-6` (not pushed, no PR, not deployed). The split/merge sub-epic (`2C-STRUCTURE-004`, GitHub issue #8) is deferred/non-blocking; Phases 2D–2F remain pending.
- ~~Issue #3 (PRD §14/implementation-plan Task 5 analytics fast-follow) needed a follow-up migration before its events could persist~~ — complete end-to-end and merged to `main` via PR #4 (`codex/phase-2c-editable-candidate-analytics`). Application layer (commit `b2cd44a`): `candidate_edit_started` (once per entry/candidate/tab-session, only from a real field edit or explicit clear, never expand/rerender/Strict Mode), `candidate_edit_reset` (only from the explicit reset action, never clear, with `editedFieldCount` from the canonical edit payload), and `task_candidates_confirmed`'s `editedCandidateCount`/`editedFieldCount` (derived server-side from the same canonical edits array sent to the RPC). Database layer (commit `feat(db): enable editable candidate analytics events`, migration `202607210034`, additive): extends the `product_events.event_name` CHECK constraint and `private.record_product_event`'s allowlist with both new event names, extends `private.validate_product_event_properties` with their property validation (`candidate_edit_started.candidateCount` fixed at exactly 1; `candidate_edit_reset.editedFieldCount` bounded 1–3), and adds `private.require_task_candidates_confirmed_edit_counts` enforcing the cross-field bound between `candidateCount`/`editedCandidateCount`/`editedFieldCount` while preserving legacy `{candidateCount}`-only payloads. No RPC signature, task-confirmation, undo, or idempotency change. Verified: local/remote migration parity through `202607210035`, `db lint --level error` clean, and a real disposable-fixture remote persistence smoke (`npm run test:remote:product-events`) proving actual `product_events` rows exist for all three analytics changes plus a rejected invalid payload, all passed against the linked development project. The 29-assertion pgTAP file (`editable_candidate_analytics_events.sql`) is not locally executable via `supabase test db` (Docker unavailable on this workstation, and that command requires Docker to run `pg_prove` even against a `--linked` target) — it was instead executed for real online via `npx supabase db query --linked -f ...` after temporarily installing `pgtap` into the linked project's `extensions` schema (removed again afterward; not committed as a migration). That live run caught a genuine pre-existing bug, unrelated to Issue #3's own new code: `private.require_product_event_integer`/`private.require_product_event_enum` (migration `202607170024`) silently accepted a payload missing its required property, due to PL/pgSQL treating a `NULL`-valued `IF` condition as false when a missing jsonb key propagated `NULL` through the type/bounds check. Fixed additively by migration `202607210035` (both functions now check key presence first); re-running the same online pgTAP file afterward showed no failure diagnostic (29/29 pass). 594/594 application tests, 0 lint/typecheck errors, production build green.
- ~~The architecture review of Slices 2X.5–2X.8 found `hasMaterializedTaskForCandidates` computed entry-wide instead of interpretation/candidate-scoped in `inbox-projection.ts` and `review-projection.ts` (F1), letting `productState` report `ready` while a genuinely unconfirmed candidate from the current interpretation remained visible~~ — fixed 2026-07-18 by a standalone hotfix, outside the slice sequence. Both loaders now derive that input from the same interpretation-scoped source (`computeUnavailableCandidateIndexes`) `actionableCandidates` already used correctly, via a new shared pure helper `hasUnconfirmedTaskCandidates` in `src/features/interpretations/data.ts`. No migration, RPC, or `lifecycle.ts` change was needed. See `docs/reports/phase-2x/PHASE_2X_CANDIDATE_LIFECYCLE_HOTFIX_REPORT.md`.
- ~~`correct_entry_interpretation`'s version-conflict path raises SQLSTATE `40001`~~ — fixed 2026-07-18 by the standalone hotfix migration `202607180029` (ADR-026), outside the Slice 2X.8 sequence. The RPC now raises `55P03` for that conflict with its signature, ownership checks, idempotent replay, and every other behavior otherwise byte-for-byte unchanged; the authenticated remote smoke proved a ~530ms bounded response with no gateway hang and no partial write. `undo_operation`'s own separate `40001` raise (a different action, not exercised by this hotfix) remains an explicit, untouched risk — see `SECURITY.md`.
- Slice 2C.2 (Planning, Priority, and No-Due Semantics) is implemented on branch `codex/phase-2c-slice-2` (not pushed, no PR, not merged/deployed). New RPC `confirm_entry_task_candidates_v3` (migration `202607210036`, additive per ADR-031's versioning precedent — `confirm_entry_task_candidates_v2` remains unchanged and callable) lets the user edit `plannedAt`, `manualPriority`, `intentionalNoDue`, and `noDueReason` per candidate before confirmation, reusing `tasks.planned_at`/`manual_priority`/`intentional_no_due`/`no_due_reason` (columns that existed since early Phase 2X migrations but were never read/written by any application code until now — v2's RPC hardcoded them to `null`/`false`). The AI never suggests these four fields, so their immutable "suggestion" baseline is always the neutral state (`null`/`null`/`false`/`null`); "reset" for them means "clear," not "restore an AI value." The database enforces due/no-due mutual consistency both inside the RPC and, as defense in depth, via a new `tasks_no_due_consistency_check` table constraint (`intentional_no_due` true requires `due_at` null; a non-null `no_due_reason` requires `intentional_no_due` true). The correction-race guard trigger from Slice 2C.1 (`guard_v2_confirmed_interpretation_correction`) is extended to also protect v3-materialized tasks. `candidate-editor.tsx` gained a planned-date field, a priority select, and a no-due checkbox (which auto-clears/disables the due-date field and reveals an optional reason field) using the exact edit/reset/clear pattern established for title/description/dueAt; no new analytics events were needed since the existing `candidate_edit_started`/`candidate_edit_reset`/`task_candidates_confirmed` events are already generic per-field-count events. `work-projection.ts`/`projection-mappers.ts`/`TaskList` now select and display planned date, priority, and an intentional-no-due indicator (previously selected and displayed nowhere). Running the new Playwright journey for real online caught a genuine pre-existing bug this slice's own new fields exposed: `private.require_task_candidates_confirmed_edit_counts` (Issue #3, migration `202607210034`) bounded `editedFieldCount` at `editedCandidateCount * 3`, correct only for Phase 2C.1's 3 editable fields — with Slice 2C.2's 4 additional fields (7 total), a genuinely valid edit was rejected by the database's own analytics allowlist. Fixed by migration `202607210037` (bound now `* 7`); the one Issue #3 pgTAP assertion whose test value assumed the old bound was corrected to match. Verified: local/remote migration parity through `202607210037`, `db lint --level error` clean, `database.types.ts` regenerated and byte-identical to the committed file, a 25-assertion pgTAP file executed for real online (`phase_2c_slice_2_planning_priority_no_due.sql`, plus a clean re-run of the existing Issue #3 29-assertion file after the bound fix), an 18-case disposable-fixture remote smoke (`scripts/remote-editable-candidate-confirmation-smoke.mjs`, extended from 13), and a live authenticated Playwright run (desktop + Pixel 7, real Server Action, real linked database) editing every new field and verifying persistence, undo-operation naming, and audit evidence. 622/622 unit/component tests (up from 594), 0 lint/typecheck errors, production build green.
- Slice 2C.3 (Owned Relations) is implemented on branch `codex/phase-2c-slice-3` (not pushed, no PR, not merged/deployed). New RPC `confirm_entry_task_candidates_v4` (migration `202607220038`, additive per ADR-031's versioning precedent — `confirm_entry_task_candidates_v3` remains unchanged and callable) lets the user attach owned project, context, person, and waiting-on relations to a candidate before confirmation, reusing the pre-existing `task_projects`/`task_contexts`/`task_people` junction tables per PRD `2C-RELATIONS-002` rather than the dormant `tasks.waiting_on_person_id` scalar column (added in migration `202607160009`, never read/written by any application code, left untouched). `task_people.role='involved'` covers the generic person relation and `role='waiting_on'` covers waiting-on — one mechanism, no redundant column. The AI never suggests relations, so the immutable baseline is always the empty set; "reset" means "clear." The database validates every relation ID's `(user_id, id)` ownership across all candidates in one pass and aborts the entire multi-candidate materialization atomically on any invalid or cross-owner ID; relation arrays are sorted/deduplicated before entering the replay fingerprint. A new server-only projection, `src/features/tasks/relation-options.ts`, loads the user's own bounded (200-row) owned projects/contexts/people as plain `{id,label}` pairs, threaded through `review-projection.ts` to the entry-detail page and `TaskCandidateForm`; `candidate-editor.tsx` gained four native `<select multiple>` listboxes with matching clear buttons, disabled when the user owns nothing of that kind. `work-projection.ts`/`TaskList` hydrate and display all four relation kinds via a bounded two-step flat-select join (no Supabase embedded-resource select), matching the existing projects/people detail-page pattern. No new analytics events were needed (existing generic per-field-count events; `editedFieldCount`'s ceiling grows from 7 to 11). Writing this slice's own pgTAP coverage (not assumed from a read-through) found two genuine defects: an operator-precedence bug (`->`/`||` share precedence and associate left-to-right in PostgreSQL) silently defeated the person/waiting-on cross-owner check — the composite foreign key still correctly rejected the resulting insert with `23503`, so no cross-owner row was ever persisted, but the intended `2C_INVALID_RELATION` rejection never fired — and `candidate_edit_reset`'s own `editedFieldCount` bound was still `[1,3]` from Phase 2C.1, never updated when Slice 2C.2 raised the real ceiling to 7. Both fixed by migration `202607220039` (forward-fix; `038` itself left unedited once applied, per this project's append-only convention) and re-verified. Verified: local/remote migration parity through `202607220039`, `db lint --level warning` clean, `database.types.ts` regenerated and byte-identical to the committed file both before and after the forward-fix, a 29-assertion pgTAP file executed for real online (`phase_2c_slice_3_owned_relations.sql`), a 23-case disposable-fixture remote smoke (`scripts/remote-editable-candidate-confirmation-smoke.mjs`, extended from 18), and a live authenticated Playwright run (desktop + Pixel 7, real Server Action, real linked database) selecting real owned relations through the actual listbox controls and verifying materialization/audit. 653/653 unit/component tests (up from 622), 0 lint/typecheck errors, production build green. The real-AI-capture journey in `intelligent-capture.spec.ts` could not be exercised this session (deployed worker/OpenAI dispatch latency exceeded the test's timeout, an external dependency untouched by this slice); the deterministic-fixture describe block in the same file passed in full.
- Slice 2C.4 (Candidate Dispositions) is implemented on `codex/phase-2c-slice-4`. The owner-scoped `entry_task_candidate_resolutions` ledger stores only candidate identity, one of `confirmed`/`rejected`/`retained`/`dismissed`, task/undo provenance, and timestamps; no candidate content is duplicated. `confirm_entry_task_candidates_v5` resolves a mixed multi-candidate batch atomically, creates tasks only for `confirmed`, fingerprints canonical resolutions plus edits, preserves v2–v4, and reuses the existing undo operation to restore pending state. Entry review shows local outcome history; Needs Attention/Inbox stop treating terminal candidates as actionable; Work remains task-only. Migrations `040`–`043` are linked and aligned, including forward-only fixes for reconfirmation after undo, undo affected counts, and safe legacy provenance enrichment. Verified: linked pgTAP 290/290 across seven relevant suites, remote smoke 24/24 with fatal cleanup and pre-existing counts preserved, Playwright 4/4 (PT-BR/en × desktop/Pixel 7), Vitest 693/693, lint/typecheck/build green, generated types byte-stable, and independent final review approved with no Critical/Important findings. Merged to `main` via PR #7.
- Slice 2C.5 (Subtasks and Dependencies) is implemented on `codex/phase-2c-slice-5`. New RPC `confirm_entry_task_candidates_v6` (migration `202607220044`, additive; `v5` and earlier unchanged and callable) extends the v5 disposition contract so each confirmed candidate's edit `changes` may carry a `parentRef` and a bounded `dependsOn` array. References are a closed discriminated union — another confirmed candidate in the same batch (`candidateIndex`) or an existing owned non-cancelled task (`taskId`) — resolved to task ids inside the transaction, never by label. The RPC rejects self-reference, non-confirmed targets, cross-owner/cancelled task targets, duplicate dependency targets, and direct/indirect cycles (cycle detection is proven to only need the intra-batch `candidateIndex` subgraph), then materializes tasks and their `parent_task_id`/`task_dependencies` edges atomically in a second pass so forward references resolve. Reuses the pre-existing `tasks.parent_task_id` column and `task_dependencies` table (both already composite-owner-FK'd from migration `016`). `relation-options.ts` loads the user's own bounded active tasks; `candidate-editor.tsx` gained a parent select and a dependency listbox; `work-projection.ts`/`task-list.tsx` hydrate and display parent/dependency targets. No new analytics event; the edited-field ceiling grows 11→13. Migration `202607220045` forward-fixes an `undo_operation` `pg_catalog.greatest` lookup failure reintroduced by `044` (`044` left unedited). Verified: new pgTAP 34/34 online plus all relevant earlier candidate suites green (two stale analytics-bound assertions corrected), remote smoke 29/29 with fatal cleanup and pre-existing counts preserved, disposition Playwright 4/4 (PT-BR/en × desktop/Pixel 7) now on the v6 path, Vitest 714/714, lint/typecheck/build green, generated types byte-stable, and independent review with no Critical/Important findings. The **split/merge** sub-epic (`2C-STRUCTURE-004`) is deferred — it has no field/command/UX specification in the current docs and the plan frames it as an isolated, independently reversible boundary. Merged to `main` via PR #9. See `docs/reports/phase-2c/PHASE_2C_SLICE_05_REPORT.md`.
- Slice 2C.6 (Product Convergence and Closeout) is implemented on branch `codex/phase-2c-slice-6` (not pushed, no PR, not merged/deployed) — a closeout slice with no migration, no RPC, and no product/UI source change. It delivers: a convergence audit of the four daily surfaces plus the candidate form (the Slice 2X.16 projection-boundary guardrails and 714/714 unit tests are green — no product source needed to change); `scripts/generate-phase-2c-traceability.mjs` (`npm run docs:phase-2c:traceability`) producing the 83-row `docs/reports/phase-2c/PHASE_2C_TRACEABILITY_MATRIX.md` (fails closed on PRD-inventory drift; never marks the deferred `2C-STRUCTURE-004` or the residual-risk `2C-UNDO-004` green); `scripts/verify-phase-2c-cleanup.mjs` (`npm run test:remote:2c:cleanup`, fail-closed across 13 owner-scoped tables); the deterministic fail-fast `test:remote:2c` aggregate (`remote-supabase-smoke.mjs --phase-2c`: confirmation v2–v6, product events, cleanup) plus the `test:remote:2c:confirmation` alias; and the `docs/reports/phase-2c/PHASE_2C_SLICE_06_REPORT.md` / `docs/initiatives/phase-2c/PHASE_2C_REPORT.md` closeout reports. One remote-evidence drift was found and fixed: the Phase 2X `remote-daily-cycle-smoke.mjs` concurrency-race assertion predated the Slice 2C.4 disposition contract and expected two different-key racing confirmations to both succeed; it now asserts one winner, one `P0001` terminal-disposition conflict, and still exactly one task (no product behavior changed). Verified: Vitest 714/714, lint/typecheck/build green, offline Playwright 6 passed/48 online-gated skips, `test:remote:2c` aggregate green (confirmation 29/29, product-events controls, cleanup 0 residual), daily-cycle standalone green, and independent review with no Critical/Important findings. See ADR-032 and `docs/reports/phase-2c/PHASE_2C_SLICE_06_REPORT.md`.
- A generic unattended due-job consumer is not deployed because no current flow requires one. Failed attachment retries are explicit, user-initiated, and blocked until persisted `next_attempt_at`; add an unattended consumer only with a concrete background workflow.
- Automatic weekly reviews, the Phase 2C.5 split/merge sub-epic (GitHub issue #8), Phase 2D–2F, hybrid search, and broader NLP completion remain future implementation work.
- Some preference fields are stored but do not yet have an operational consumer; they must not be presented as effective behavior until wired.
- Docker-backed `supabase test db --linked` remains unavailable on this workstation. The linked Management API fallback executed the canonical Phase 2C.5 pgTAP assertions (34/34) and relevant earlier candidate suites transactionally, with temporary pgTAP installation removed afterward; the disposable remote smoke independently passed 29/29.
- Provider-delivered signup remains unverified: the linked Supabase Auth service currently rejects the reserved `example.com` E2E domain with `email_address_invalid`. Production delivery requires custom SMTP and a provider-routable test address before launch.
- Three moderate `npm audit` advisories remain transitively inside the current Next.js/PostCSS dependency graph; the npm-proposed forced fix is an incompatible downgrade and was not applied.

## Next priorities

1. Keep Phase 2X closed; use `docs/initiatives/phase-2x/PHASE_2X_REPORT.md`, the Slice 2X.18 report, its evidence manifest, and the per-ID traceability annex as the deployed handoff.
2. Phase 2C is complete through Slice 2C.6. Use `docs/initiatives/phase-2c/PHASE_2C_REPORT.md` and `docs/reports/phase-2c/PHASE_2C_TRACEABILITY_MATRIX.md` as the phase handoff. Keep Slice 2C.6 local until separately authorized to push/open a PR. Do not begin the deferred split/merge sub-epic (GitHub issue #8) or Phase 2D without separate implementation authorization.
3. Adopt generated Supabase client types incrementally as each legacy preference/vector contract is validated.
4. Add custom SMTP and re-run the non-throttled signup delivery smoke before production launch.
5. Execute pgTAP locally/CI when Docker is available and add the database gate to CI.
6. Consider whether `undo_operation`'s separate `40001` raise (`'Cannot undo after a newer interpretation revision'`) needs the same `55P03` substitution — not confirmed to hang, but the same class of platform risk; see `SECURITY.md`.

## Existing structure

- `src/app`: App Router pages, layouts, styles, auth callback, and localized application routes.
- `src/features`: domain server actions and UI for auth, capture, agent, profile, shell, and operations.
- `src/lib`: Supabase clients, AI provider/routing/usage helpers, validation, i18n, and shared utilities.
- `src/lib/supabase/database.types.ts`: linked Supabase-generated `public` schema used incrementally by typed data boundaries.
- `supabase/migrations`: append-only schema history (`001` through `031`).
- `supabase/functions/process-jobs`: authenticated Edge Function worker for queued AI jobs, split into `index.ts` (auth/routing), `dispatch.ts` (type router and unattended drain loop), `attachment.ts`, and `entry.ts`; `supabase/functions/_shared`: cross-function Deno modules, including Deno-runtime copies of the entity-resolution/trust-scoring domain logic.
- `supabase/tests`: pgTAP coverage for Phase 1 RLS, intelligent capture, and AI usage.
- `e2e`: public foundation, online auth, and intelligent capture Playwright suites.
- `.github/workflows`: CI quality checks.
- `docs`: product, architecture, database, AI agent, security, implementation, state, decisions, changelog, and backlog documentation.

## Current database

- PostgreSQL on Supabase with `pgcrypto`, `vector`, RLS, triggers, RPCs, and scheduled-job support.
- Identity: profiles and agent preferences.
- Knowledge: entries, interpretations, pending questions, memories, embeddings, summaries, attachments, and entity relationships.
- Work management: tasks, projects, people, reminders, reviews, and waiting-related records.
- Agent operations: conversations, messages, operations, undo records, audit logs, jobs, notifications, heartbeat runs, and delivery state.
- Cost control: AI model pricing, AI usage events, `record_ai_usage`, and `get_ai_cost_summary`.
- Product observability: private `product_events`, `record_product_event`, and service-only `record_product_event_for_user`.
- Entry-processing contract: `capture_entry_async`, `enqueue_entry_reprocessing`, `claim_entry_interpretation_job`, and `claim_next_entry_interpretation_job`; the interpretation persistence RPCs are now service-role-callable via an explicit `p_service_user_id`.
- `pg_net` is enabled; `my-brain-entry-dispatch` is a per-minute `pg_cron` job that calls the deployed `process-jobs` function in dispatch mode via Vault-backed URL/secret.
- Candidate confirmation contract (migration `028`): `confirm_entry_task_candidates` is `SECURITY DEFINER`, binds to `entries.current_interpretation_id`, and is idempotent by `operation_key`; `tasks.source_interpretation_id`/`operation_key` and `entry_interpretations.is_record_only` are persisted columns. `confirm_entry_tasks` (compatibility, no new consumer) is also now `SECURITY DEFINER` with explicit `authenticated`-only grants — both were previously `SECURITY INVOKER` and `confirm_entry_tasks` could not complete for a real authenticated user (see ADR-025).
- All 31 migrations are applied to the linked `my-brain` project; local/remote migration history is synchronized and linked schema lint at level `warning` shows only one pre-existing, unrelated finding (`run_user_heartbeat`). Migration `029` (2026-07-18 hotfix, outside the slice sequence) redefines `correct_entry_interpretation` with the identical signature, replacing only its version-conflict SQLSTATE (`40001` → `55P03`); see ADR-026.
- Needs-attention query contract (migrations `030`/`031`, Slice 2X.10): `list_needs_attention` is `SECURITY DEFINER`, owner-scoped via `auth.uid()`, keyset-paginated by `(occurred_at, entry_id)`, and returns only ids/reason codes/timestamps/keys — no copy, no trust. Migration `031` fixed a `generate_series`/`tasks.candidate_index` alias collision found by the real remote smoke (see ADR-027); `030` itself was left unedited once applied, per this project's append-only migration convention.

## Existing integrations

- Supabase Auth, PostgreSQL, Storage, RPC, Edge Functions, and RLS.
- OpenAI Responses/embedding APIs through the project AI provider abstraction.
- `pgvector` for semantic retrieval.
- Browser service worker for application shell support.
- GitHub Actions for CI.

## Existing jobs

- `jobs` queue with idempotency keys, atomic leased claims, worker identity, expiry, bounded attempts/backoff, recoverable `failed`, terminal `exhausted`, sanitized last error, and service metrics.
- `interpret_entry` is a bounded queue type. Initial capture payloads contain only `{ entry_id, mode: "initial" }`; reprocessing adds a validated operation key. It now has a deployed worker and automatic dispatch (Slice 2X.4): direct authenticated invocation and an unattended per-minute drain, both remotely verified end to end.
- `process-jobs` Edge Function for authenticated attachment execution (unchanged behavior, extracted into `attachment.ts`) and entry interpretation (`entry.ts`), routed by a fail-closed dispatcher (`dispatch.ts`) that rejects unknown job types before attempting any claim.
- `my-brain-job-reaper` pg_cron entry runs every minute and recovers expired leases or makes exhausted work terminal; `my-brain-entry-dispatch` runs every minute and drains eligible `interpret_entry` jobs across all owners via `claim_next_entry_interpretation_job`.
- The Files route exposes only the current user's failed jobs, attempt state, retry window, terminal state, and authenticated retry action without rendering internal errors.
- Database heartbeat functions and scheduled heartbeat entry points.
- There is intentionally no generic unattended due-job consumer for attachments; the current attachment retry path remains explicit and user-driven after database backoff. Unattended dispatch exists only for `interpret_entry`, which now has a real user-facing producer: every capture and reprocess request enqueues through it (Slice 2X.5).

## Existing heartbeats

- Event-based notification generation for overdue/stale tasks and due reminders.
- Per-user quiet hours, locale/timezone day boundaries, daily caps, rolling cooldown, importance override, advisory locks, and failure isolation are active.
- Over-cap candidates remain pending and were verified remotely by withholding then delivering the same reminder after the cap changed.

## Existing tests

- Vitest unit/component tests for auth UI, profile/settings, capture (now covering the atomic-enqueue Action, receipt-driven form reset/refocus/idempotency rotation, and the shared entry-worker kick helper), AI parsing/routing/cost math/usage (including AI-usage-ordering guardrails for both the attachment and entry Edge Function workers — narrowed to the Deno paths now that Node no longer runs extraction directly), daily-cycle contracts/lifecycle/projection-mapper guardrails, product-analytics contracts/server/action behavior, the new owner-scoped `inbox-projection` loader (every reachable product-state/attention-reason combination, the fail-closed fallback, pagination, and locale-scoped hrefs), the new `InboxItemRow` component, `HomeDashboard`'s first test coverage (recent-activity wiring, empty state, and a no-internal-string-leak guardrail), the new `confirmEntryTasks`/`undoAgentAction` Action tests (interpretation binding, dedupe, `55P03`/`55000`/generic error mapping, auth guard), `TaskCandidateForm`'s new interpretation-binding/unavailable-index/empty-state cases, `computeUnavailableCandidateIndexes`/`isRecordOnly` propagation in `interpretations/data.test.ts`, the new `review-projection`/`technical-details-projection` pure-mapper and loader tests (human contract free of trust fields, lifecycle-driven `productState`, record-only/unavailable-candidate exclusion, current-interpretation-scoped materialized tasks, retry gating, serializability, per-element score/policy/signal/evidence/override extraction, version comparisons, per-task provenance), and the new `page.architecture.test.ts` import-boundary guardrail for the entry-detail page, and the new `attention-projection` loader tests (title/explanation hydration from the RPC's already-decided reason, locale-scoped primary action, keyset lookahead/cursor derivation, and fail-closed dropping of a row that cannot be hydrated or carries an unrecognized reason), the new `needs-attention-item` row tests, the new `needs-attention-list` client-component tests (accumulation across load-more, duplicate-click prevention, error preservation of already-loaded items, locale-scoped labels), the new `attention-actions` tests (unauthenticated/success/thrown-error branches of `loadMoreNeedsAttention`), and `HomeDashboard`'s extended coverage for the Needs Attention panel (count with/without the `+` suffix, empty state, preview items, and the "view all" link href).
- Playwright public foundation, online auth, and intelligent capture suites; the capture step now asserts the immediate receipt, cleared/refocused field, and enabled submit button before polling the entry detail route until the worker finishes. `intelligent-capture.spec.ts` is reorganized (Slice 2X.17) into deterministic named scenarios by contract across two `test.describe` blocks, with new deterministic-fixture coverage for basic pending question, recoverable retry, and terminal retry, and keyboard/focus/live-region/touch-target assertions on the entry-review progressive disclosure.
- Vitest/Testing Library now cleans up the rendered DOM between tests in every file (`src/test/setup.ts`) — a pre-existing gap that let one test's render leak into the next within the same file, caught while adding the `CaptureReceiptView` test.
- pgTAP tests for foundational RLS, capture RLS, AI usage schema/RLS, product-events schema/RLS/RPC contracts, entry-processing queue contracts, the Slice 2X.4 service-role interpretation access surface (`entry_interpretation_worker.sql`), the Slice 2X.7 candidate-consistency contract (`candidate_action_consistency.sql`, 33 assertions), and the new Slice 2X.10 needs-attention contract (`needs_attention_projection.sql`, 35 assertions, including a dedicated regression for the migration-031 candidate-index alias collision) — all committed but not locally executed (see Known limitation below).
- A Deno test file (`supabase/functions/process-jobs/dispatch.test.ts`) specifies the dispatcher's fail-closed type-routing guard; it could not be executed on this workstation (no Deno runtime installed) — deployment plus the remote smoke served as the equivalent verification.
- `scripts/remote-daily-cycle-smoke.mjs` (new, Slice 2X.7): actually executed against the linked project with disposable users, not just written. This is what found the two pre-existing/newly-introduced defects fixed in this slice (see ADR-025) — a pgTAP-only or mocked-Vitest-only verification would not have caught either, since both require real `authenticated`-role grants and a real gateway round trip.
- CI currently runs lint, typecheck, unit tests, and production build.

## Known coverage

Verified on 2026-07-18:

- Vitest: 54 files and 266 tests passing after official Slice 2X.7.
- Statements: 93.66% (266/284).
- Branches: 61.61% (305/495).
- Functions: 90.62% (87/96).
- Lines: 95.88% (233/243).
- Playwright without online credentials: 4 public tests passing and 10 expected online skips.
- Playwright with linked remote credentials: 11 tests passing and 3 explicit skips (desktop-only mobile-nav exclusion, mobile duplicate signup exclusion, and hosted email quota exhaustion).
- Final targeted recovery journey after the harness hardening: 1/1 passing.
- Phase 2A linked intelligent-capture/file journey: 2/2 passing across desktop and mobile.
- Phase 2A remote job smoke: exclusive lease, stale-worker denial, recovery, exhaustion, sanitization, metrics, and RLS passing.
- Phase 2B linked intelligent-capture/revision journey: passing separately on desktop and Pixel 7 mobile, including correction, dates, classifications, record-only mode, immutable history, undo, `pt-BR`/English, and cleanup.
- Phase 2B remote interpretation smoke: immutability, append-only correction, idempotency, concurrency, ownership, rollback, audit, undo, aliases, reprocessing, sanitization, RLS, and cleanup passing.
- Complete remote Supabase regression smoke: auth, atomic settings, RLS, ownership, heartbeat, AI ledger/aggregation, and deployed file worker passing after Phase 2B.
- Slice 2X.2 remote product-events smoke: allowlist, forbidden payloads, idempotency, RLS, service-role worker control, and disposable-user cleanup passing.
- Slice 2X.3 remote entry-processing smoke: atomic capture/replay, bounded payloads, owner isolation, service-only claims, exclusive lease, retry eligibility, stale-worker denial, reaper recovery, reprocess isolation, and cleanup passing.
- Slice 2X.4 remote entry-processing smoke (extended): the deployed worker completes both `initial` and `reprocess` direct invocations end to end (real OpenAI extraction, real entity-resolution/trust computation, real persistence via the service-role-extended RPCs), rejects an incorrect dispatch secret, and the unattended dispatch drain processes a pending fixture job with no `jobId` supplied — passing.
- Slice 2X.4 remote job-reliability smoke (attachment regression, re-verified after the migration `027` fix): exclusive lease, stale-worker denial, recovery, exhaustion, sanitization, metrics, and RLS passing.
- Slice 2X.4 remote full regression (`test:remote`): auth, atomic settings, RLS, ownership, heartbeat, AI ledger/aggregation, and the deployed file worker (now routed through `dispatch.ts`/`attachment.ts`) passing.
- Slice 2X.4 ad hoc verification: the worker's best-effort `capture_processing_completed` product event was confirmed persisted with the expected properties for a disposable test user before cleanup.
- Slice 2X.5 remote entry-processing, job-reliability (attachment regression), product-events, and full (`test:remote`) smokes were all re-run against the linked project after the cutover and passed unchanged — the async Actions call the exact same RPCs these smokes already covered.
- Slice 2X.5 online Playwright (`intelligent-capture.spec.ts`) passed on both desktop (~1.1 min) and mobile (~1.0 min) against the linked project: immediate receipt with no redirect, cleared/refocused field and enabled submit button proving interactivity before the worker finishes, the safe "Ver registro" link, and the full downstream journey (correction, undo, task confirmation, chat, reviews, files, settings, heartbeat, final undo) all passing against the now-asynchronous entry.
- Slice 2X.6, verified on 2026-07-17: Vitest 53 files/248 tests passing (12 new for `inbox-projection`, 4 for `InboxItemRow`, 4 for `HomeDashboard`'s first coverage); lint, typecheck, and production build clean. Offline Playwright (public foundation only, both `desktop` and `mobile` projects): 2/2 passing, 5 expected online skips — this workstation has no `ONLINE_SUPABASE_*` credentials, so the online intelligent-capture/auth/mobile-navigation journeys could not be re-run here; nothing in this slice's own unit coverage depends on that journey, since it exercises the new query/mapping/rendering path directly against realistic fixture data. This is the same environment constraint documented for Docker-backed pgTAP elsewhere in this file, not a regression.
- Slice 2X.7, verified on 2026-07-18: Vitest 54 files/266 tests passing (18 new: 9 `tasks/actions.test.ts`, 5 `task-candidate-form.test.tsx`, 4 `interpretations/data.test.ts`); lint, typecheck, and production build clean. `supabase db push` applied migration `028` cleanly to the linked project (caught and required fixing a `min(uuid)` aggregate error before it would apply); `supabase db lint --linked --level warning` shows only the one pre-existing, unrelated `run_user_heartbeat` finding. `supabase gen types typescript --linked` was regenerated from the live schema. `scripts/remote-daily-cycle-smoke.mjs` was executed (not just written) against the linked project with disposable users and passed after fixing three real defects it found: a `min(uuid)` aggregate bug in the migration's backfill, `confirm_entry_tasks`/`confirm_entry_task_candidates` needing `SECURITY DEFINER` instead of `SECURITY INVOKER` (the former had never completed for a real authenticated user before this slice), and `confirm_entry_task_candidates`'s conflict code needing to be `55P03` instead of `40001` (which was independently confirmed, via a raw `fetch()` against the REST endpoint with no application code involved, to also hang the already-shipped `correct_entry_interpretation` — see ADR-025 and the urgent `TODO.md` follow-up). `supabase/tests/candidate_action_consistency.sql` (33 assertions) is committed but could not execute locally (Docker unavailable, same pre-existing environment gap as every other pgTAP file in this project) — the remote smoke's real execution is the equivalent verification, and is in fact how the SECURITY DEFINER and `40001` defects were actually found, which a Docker-backed pgTAP run (typically superuser-equivalent) would likely not have caught either. Offline Playwright: 2/2 passing, 5 expected online skips, unchanged from Slice 2X.6 — this slice did not add new online Playwright coverage for the candidate-confirmation UI itself (real AI-driven candidate content makes a deterministic journey nontrivial to script, and the full progressive-disclosure UI those journeys would exercise is Slice 2X.9's scope); the RPC-level invariants this slice is actually about were instead verified precisely, at the database layer, by the remote smoke script.

Slice 2X.8, verified on 2026-07-18: Vitest 57 files/286 tests passing (19 new: 10 `review-projection.test.ts`, 7 `technical-details-projection.test.ts`, 2 `page.architecture.test.ts`); lint, typecheck, and production build clean. No migration in this slice; local/remote migrations remain synchronized through `029` from the prior hotfix. Offline Playwright (`desktop`+`mobile`, public foundation only): 4/4 passing, 10 expected online skips — this workstation has no `ONLINE_SUPABASE_*` credentials, so `intelligent-capture.spec.ts` (the load-bearing regression for this exact page — the "Confiança por elemento"/"Trust by element" heading, `.revision-timeline` version/origin text, the original-record disclosure, and the record-only/undo journey) could not be re-run live here. The rewrite preserved every one of that spec's selectors/copy by design rather than leaving them unverified; the two pure projection mappers now carry direct unit coverage that the original inline page logic never had. `git diff --check` clean; `git status` shows only the files this slice touched.

Hotfix verified on 2026-07-18 (migration `029`, outside the slice sequence): Vitest 54 files/267 tests passing (1 new — the `55P03` conflict-mapping case in `interpretations/actions.test.ts`); lint and typecheck clean; production build passing. `supabase db push` applied migration `029` cleanly (a first draft failed its own post-deploy verification harmlessly — a false positive from an inline comment containing the digits `40001`, fixed and re-pushed without any partial write, confirmed by `supabase migration list --linked` showing the migration unapplied after the failed attempt); `supabase db lint --linked --level warning` remains at the single pre-existing `run_user_heartbeat` finding; regenerated types showed no diff beyond a BOM artifact, confirming the RPC signature was fully preserved. `scripts/remote-interpretation-revisions-smoke.mjs`, extended with a bounded elapsed-time and SQLSTATE assertion around the pre-existing concurrent-correction race, was executed (not just written) against the linked project and passed: the losing concurrent correction returned in ~530ms with `55P03` (not the old `40001`), the interpretation-row count advanced by exactly one (no partial write from the rejected side), and the current-interpretation pointer was not overwritten by the losing correction.

Hotfix verified on 2026-07-18 (F1 candidate-lifecycle scoping, outside the slice sequence): Vitest 58 files/302 tests passing (35 new: 6 `interpretations/data.test.ts` for the new `hasUnconfirmedTaskCandidates` helper, 4 `inbox-projection.test.ts`, 7 `review-projection.test.ts`, 1 new `lifecycle-consistency.test.ts` file cross-checking Inbox and Review against the same fixture); `npm run lint` and `npx tsc --noEmit` clean; `npm run build` production build passing. No migration, RPC, or schema change — `tasks.source_interpretation_id`/`candidate_index` already existed and were already read by `interpretations/data.ts`; local/remote migrations remain synchronized through `029`. Offline Playwright (`desktop`+`mobile`): 4/4 passing, 10 expected online skips, unchanged from the Slice 2X.8 baseline. `git diff --check` clean (only pre-existing LF/CRLF advisories). Regression reproduced first (failing tests confirmed against the pre-fix code for both loaders), then fixed. See `docs/reports/phase-2x/PHASE_2X_CANDIDATE_LIFECYCLE_HOTFIX_REPORT.md`.

Slice 2X.10, verified on 2026-07-18: Vitest 61 files/340 tests passing (13 new: 13 `attention-projection.test.ts`); `npm run lint`, `npx tsc --noEmit`, and `npm run build` clean. `supabase db push` applied migration `030` (after fixing a `min(uuid)` aggregate error the first attempt caught — Postgres has no `min()`/`max()` aggregate for `uuid`, same class of defect as migration `028`'s backfill query — resolved with an order-by/limit-1 subquery before anything committed remotely). `scripts/remote-daily-cycle-smoke.mjs`, extended with needs-attention fixtures, was executed (not just written) against the linked project and found a second, more subtle real defect: `has_unconfirmed_candidate`'s `generate_series` alias collided by name with `tasks.candidate_index`, making the correlated check tautological once any task existed for the entry, so confirming one of two current candidates incorrectly emptied the entry out of the queue. Migration `031` (`create or replace function`, identical signature/grants/index) fixed the alias collision; see ADR-027. After the fix, the full remote smoke passed: a qualifying entry with unconfirmed candidates is listed; partial confirmation keeps it listed; full confirmation resolves it out of the queue; another owner's entries never leak either direction; keyset pagination across three pages has no overlap/duplication; and the RPC's own response was bounded (well under the 5-second assertion ceiling). `npm run test:remote:entry-processing` and `npm run test:remote:jobs` were re-run unchanged and passed, confirming no regression to the attachment/entry-processing paths this slice did not touch. `supabase db lint --linked --level warning` remained at the single pre-existing `run_user_heartbeat` finding after both migrations. `supabase gen types typescript --linked` produced no diff after migration `031` (same signature as `030`); after `030` alone, the only diff was three fields the CLI's table-returning-function heuristic marks non-nullable despite being genuinely nullable in this schema (`current_interpretation_id`/`job_id`/`open_question_id`) — the actual generated output was adopted verbatim rather than hand-corrected, to avoid perpetual drift against future regenerations. Offline Playwright (`desktop`+`mobile`): 4/4 passing, 10 expected online skips, unchanged from the Slice 2X.9 baseline — this slice adds no route or UI. `git diff --check` clean (only the pre-existing LF/CRLF advisories). `supabase/tests/needs_attention_projection.sql` (35 pgTAP assertions, committed, not locally executable — same Docker gap as every other pgTAP file here) includes a dedicated regression case for the migration-031 defect, exercised through the real `confirm_entry_task_candidates` RPC rather than injected booleans.

Slice 2X.11, verified on 2026-07-18: Vitest 64 files/357 tests passing (13 new: 4 `needs-attention-item.test.tsx`, 5 `needs-attention-list.test.tsx`, 3 `attention-actions.test.ts`, plus 5 new/adjusted cases in `home-dashboard.test.tsx`); `npm run lint`, `npx tsc --noEmit`, and `npm run build` all clean. No migration in this slice; local/remote migrations remain synchronized through `031`. Offline Playwright (`desktop`+`mobile`): 4/4 passing, 10 expected online skips, unchanged from the Slice 2X.10 baseline. `git diff --check`: clean (pre-existing LF/CRLF advisories only). The authenticated online journey (`e2e/intelligent-capture.spec.ts`) was extended with a Needs Attention detour (Home panel lists the entry, "Ver tudo" reaches the Caixa `?view=needs-you` filter with the tab marked active, the row navigates back to the same entry, and the English tab label renders on a direct localized visit) but could not be executed — this workstation has no `ONLINE_SUPABASE_*` credentials configured.

Slice 2X.12, verified on 2026-07-18: focused RED captured 18 expected missing-slice failures before production edits, plus one focused RED for moving the product-action→persisted-status translation out of JSX; final focused GREEN passed 6 files/29 tests. The full suite passes 68 files/375 tests (18 new), lint/typecheck/build are clean, and the production route manifest contains `/[locale]/app/work` plus the three legacy routes. Offline Playwright desktop/mobile passes 6 tests with 10 credential-gated skips; exact alias targets and the confirmed-task/undo Work journey are also present in the online spec but were not executed because all three `ONLINE_SUPABASE_*` variables are absent. `supabase migration list --linked` confirms local/remote synchronization through `031`; no migration/RPC/Edge Function changed. `git diff --check` is clean apart from Git's pre-existing Windows LF/CRLF advisories.

Slice 2X.17, verified on 2026-07-19: Vitest unchanged at 80 files/443 tests (E2E-only slice plus one UI conditional with no dedicated render harness — see report); `npm run lint`, `npx tsc --noEmit`, and `npm run build` all clean. No migration; local/remote migrations remain synchronized through `031`. Offline Playwright (`foundation.spec.ts`, `desktop`+`mobile`): 3/3 passed each, unaffected. `git diff --check`: clean (pre-existing LF/CRLF advisories only). Authenticated online: `intelligent-capture.spec.ts` 18/18 passed on both `desktop` and `mobile` (two `test.describe` blocks — the existing real journey reorganized into 13 named tests, plus 3 new tests for basic question/recoverable retry/terminal retry using deterministic direct-RPC fixtures); `online-mobile-navigation.spec.ts` unchanged, re-run 1/1 passed on both projects to confirm no regression from the page.tsx fix; `online-auth.spec.ts` re-run once — sign-in 2/2, recovery 2/2 (now genuinely green, resolving the Slice 2X.16 `recovery-failed` observation as transient rate limiting), signup explicitly skipped both projects (`error=email-rate-limited`, confirmed ongoing hosted-email quota exhaustion, not a code defect). `npm run test:remote:daily-cycle` and `npm run test:remote:product-events` both passed, confirming no regression to surfaces this slice did not touch. Real execution of the new retry scenarios found and fixed one genuine defect: a duplicate "Reinterpretar entrada"/"Reinterpret entry" retry button on the entry-detail page when an entry had never had a successful interpretation and its latest state was `recoverable_error`/`terminal_error`; fixed with a one-line conditional in `src/app/[locale]/app/inbox/[entryId]/page.tsx`, no capability removed. See `docs/reports/phase-2x/PHASE_2X_SLICE_17_REPORT.md`.

Coverage percentages are the last explicit coverage measurement from the Phase 2B baseline and were not recomputed for Slice 2X.5 onward. They apply only to modules imported by Vitest; they are not repository-wide coverage. Remote smoke and Playwright complement, but do not numerically contribute to these percentages.

## Important recent commits

- `80bb233` — `fix(interpretations): include correction history trust signal` (2026-07-17).
- `00eabe5` — `test(e2e): stabilize interpretation revision journey` (2026-07-17).
- `9e894de` — `fix(db): align trust fallback volatility` (2026-07-17).
- `8fbd615` — `feat(inbox): add immutable interpretation review` (2026-07-17).
- `8331e68` — `feat(interpretations): add correction and reprocessing actions` (2026-07-17).
- `91c1722` — `chore(supabase): generate interpretation revision contract` (2026-07-17).
- `ae0be18` — `feat(db): add immutable interpretation revision operations` (2026-07-17).
- `9a87c54` — `feat(interpretations): add deterministic trust and entity contracts` (2026-07-17).
- `ac9f08e` — `chore(supabase): generate typed job queue contract` (2026-07-17).
- `86fa041` — `feat(files): expose recoverable and terminal job failures` (2026-07-17).
- `ab902e9` — `feat(worker): enforce lease ownership and bounded retries` (2026-07-17).
- `fe2f464` — `feat(jobs): add leased claims and abandoned job recovery` (2026-07-17).
- `c8365b8` — `test(jobs): specify lease and recovery invariants` (2026-07-17).
- `437b626` — `docs(phase-2): adopt engineering standards and reconciled plan` (2026-07-17).
- `a89210a` — `test: close remote foundation quality gate` (2026-07-17).
- `5099f81` — `feat: harden foundation and complete AI cost control` (2026-07-17).
- `40272ba` — `fix: expose complete mobile navigation` (2026-07-17).
- `0201963` — `fix: complete secure authentication journeys` (2026-07-17).
- `3aa0946` — `docs: establish permanent project state` (2026-07-17).
- `107a65f` — `feat: deliver intelligent brain pre-mvp` (2026-07-16).

## Technical pending items

- Generated Supabase types are active for the Phase 2A job boundary and Phase 2B interpretation DAL; legacy global client typing still exposes preference-literal and pgvector representation mismatches and must be migrated domain by domain.
- Unattended dispatch exists only for `interpret_entry` jobs (Slice 2X.4); attachments intentionally remain without a generic unattended consumer until a concrete background flow requires due-job pickup.
- Generic page metadata and partially localized operational copy.
- CI does not yet enforce Playwright, pgTAP, database lint, audit, or a meaningful coverage threshold.
- Upload malware scanning and stronger content validation remain pre-production work.
- Some server/data-access and page patterns remain duplicated and should be refactored incrementally.

## External pending items

- Start Docker Desktop and provide `SUPABASE_DB_PASSWORD` to execute the expanded pgTAP files through the Supabase CLI.
- Configure custom SMTP and verify real inbox delivery before production launch.
- Wait for a compatible Next.js/PostCSS dependency release that resolves the moderate audit advisories without a forced downgrade.
- Keep Google OAuth disabled until a provider, redirect URLs, and secrets are configured and verified.

## Sprint 1.5 checklist

- [x] Establish permanent state, decision, changelog, and backlog documents.
- [x] Fix password recovery and signup validation locally; online proof remains in the deployment gate.
- [x] Complete mobile navigation with an accessible overflow menu.
- [x] Harden RLS and relationship ownership.
- [x] Correct heartbeat behavior.
- [x] Add pagination and consistent Supabase error handling.
- [x] Hide the unconfigured Google OAuth entry point.
- [x] Finish AI Routing and Cost Control tests and migrations.
- [x] Validate the remote database and deployed worker.
- [x] Smoke-test cost aggregation, rendered dashboard, and the deployed AI worker.
- [x] Run lint, typecheck, unit tests, coverage, build, and Playwright.
- [x] Update all four permanent project documents with final evidence.
- [x] Commit the completed sprint in reviewable units.

## Phase 2 entry checklist

- [x] Foundation quality gate is green.
- [x] Remote schema, migration history, worker, RLS, ownership, heartbeat, and AI cost paths are verified.
- [x] Remaining risks are explicit and do not require an architectural restart.
- [x] Write the Phase 2 vertical-slice plan before implementation.
- [x] Include and implement job recovery/observability criteria before expanding background workflows.
- [x] Approve the canonical Phase 2C PRD and implementation plan without starting implementation.
